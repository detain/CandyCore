# P7.S3 premise — skills-into-the-prompt (channel audit)

> Consolidated from delegated premise-checks (whispering-coffee-urial = path analysis, close-red-moth = blast radius), 2026-09-06. Line cites against master tip 6444ddc26; re-derive at staffing.

## PART A — path analysis (verdict: PROCEED-WITH-CORRECTIONS; plan premise PREMISE-WRONG on item 3)

- `withSkills` definition: EngineBackend.php:318 (plan cite :221 STALE). Ctor-promoted property :228; registry param :232. Sole consumer: complete() :600 `->withEnabledSkills($this->skills)` on the per-turn App rebuild :598-611.
- ALL withSkills callers are test-only: EngineBackendTest:1069 (generic setter provider), MemoryPromptWiringTest:281/:322 (both empty arrays). ZERO production callers CONFIRMED.
- Bootstrap withSkillRegistry sites: :2217 (backend()) and :2281 (backendFor()) — plan cites :2160/:2224 stale by ~+57. Registry resolved :2204/:2272 via self::skillRegistry($root) (def :2907). Third App-side wiring: withAvailableSkills at Bootstrap.php:1995 (app() TUI-model path, bypasses backend).
- Registry path lands Runtime.php:2698-2702 `listForPrompt($app->availableSkills)` = METADATA LISTING ONLY (name + description; comment :2691-2695 explicitly NOT bodies).
- withSkills path lands Runtime.php:2675-2689 (foreach; section Stability::PerTurn; full bodies via systemPromptContribution).
- Both splices live inside Runtime::buildSystemPrompt (:2485); reach the wire via Runtime::run() CompleteRequest(systemPrompt:) (Runtime.php:1105-1111).
- PICKER CHAIN: Ctrl+S KeyboardHandler.php:705 SourceSkillCmd -> App.php:890 OpenSkillPickerMsg -> handleOpenSkill options App.php:1196-1199 -> Enter KeyboardHandler.php:458-460 SelectSkillMsg -> App.php:1219-1265 handleSelectSkill appends enabledSkills (dedupe :1233-1240).
- THE BREAK: App::applySkillsToSystemPrompt() (App.php:398-415) has ZERO src callers (admits it at :1076-1080). Runtime::run's single production caller EngineBackend:656 builds a FRESH App whose enabledSkills = permanently []. Chat hands the backend history only (Chat.php:7776-7777; NonInteractive.php:246; BackgroundSessionRunner.php:314).
- TODAY bodies reach the model ONLY via SkillTool.php:102 (tool-result "## Skill: name / body" into HISTORY) and AgentManager.php:439 (subagents).
- Shared emit Skill::systemPromptContribution() Skill.php:107-110; NO dedupe at the withEnabledSkills seam (:600 REPLACES the array).
- The Skill-section marker literal appears at Skill.php:109 and SkillTool.php:102.
- Exactly-once substr_count cannot attribute origin (same array/renderer) but CAN distinguish listing-only vs body-once vs body-twice.
- De-dup precedent cites drifted: RuntimeTest.php:1591/:1610 are now idle-clock assertions; the substr_count-1 pattern lives at RuntimeTest.php:1652 + cluster :1674-1677.
- VERDICT: the plan premise "skill bodies enter the main prompt via the Ctrl+S picker" is PREMISE-WRONG — the picker terminates in UI state production never reads (dead seam). The real question: make the enabledSkills channel reachable from production at all.

## PART B — blast radius (verdict: all answers verified)

- BootstrapSkillSkipsTest.php (10 tests) pins skill-FILE skip diagnostics (Bootstrap::skillSkips(), stderr "could not be read"), NOT prompt wiring — will not flip.
- SystemPromptWiringTest.php (11 tests): L545 testDiscoveredSkillsAreListedInTheProviderSystemPrompt asserts the Level-1 listing reaches the wire via withSkillRegistry L550 (contains "Available skills (invoke via Skill tool):"). Zero-skill absence pins L580/L727 drive locally-built backend() (L778-785)/PromptFixture with NO registry — they pin the EngineBackend DEFAULT, not Bootstrap wiring, so they survive unchanged.
- FeatWiringReachabilityTest.php launchedSystemPrompt() :1336; reseatProvider :1367 already passes privateValue($backend,'skills') positionally — a populated array flows without edits.
- NO test asserts the launched backend skills array is empty (only App-unit defaults AppSkillTest:116, AppBuilderTest:99/393/495 — unaffected).
- RuntimeTest.php:1383 notContains of the Skill marker is testBuildSystemPromptIgnoresNonSkillEnabledSkills (L1373) feeding a non-Skill string — not a zero-skills pin.
- 19 Skill-test files exist in total.
- new EngineBackend in src: exactly 3 — Bootstrap.php:2206 (echo; chains withSkillRegistry :2217), :2274 (chains :2281), :6195 (hooksDisabled probe, NO skills wiring).
- withSkills call sites in src/ and bin/: ZERO.
- Only skill config key: disabledSkills denylist (LayeredSettings.php:304 LAYERED_KEYS, :517; read Bootstrap.php:2917 — disabled equals readUserConfig()['disabledSkills'] default empty). No skills.enabled toggle exists; gating happens upstream of the registry.
- Skill body field: Skill.php:25 public string $content; frontmatter split :62-64 then trim :82; contribution :109. SkillRegistry keyed by name (:12; register :201).
- GOLDENS: the base golden ALREADY contains the fixture-helper Skill section (golden-system-prompt.txt:115), so it moves only if assembly ORDER changes. PromptStabilityTest byte tables tests/Providers/PromptStabilityTest.php:851,870 are the byte-count drift candidates.
