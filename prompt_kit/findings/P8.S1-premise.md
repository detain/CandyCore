# P8.S1 premise — structured summary sections for the compaction prompt (v1, 2026-09-08)

**Step**: prompt_plan.md :2722-2739 — replace the one-line-per-exchange compaction
summary format with a structured template; change ONLY prompt text + parse; no method
restructure.

**Dated 2026-09-08.** All anchors measured on master `be01660a0`; sources: verification
scout + orchestrator direct reads, re-verified in the step worktree at staffing time.

## Verified state of master (be01660a0) — all anchors re-derived 2026-09-08

- `Chat::COMPACT_SUMMARY_PROMPT` — private const, heredoc,
  `sugar-crush/src/Chat.php:9020-9032` (docblock :9015-9019). PLAN DRIFT: plan says
  :8606-8618 (~+414). Content claims verified: "ONE line" per exchange :9022;
  "under 200 characters" :9030.
- Sole consumer: `Message::system(self::COMPACT_SUMMARY_PROMPT)` at `:9187` inside
  `Chat::buildSummarizationRequest()` `:9169-9229` (NOTE: lives in Chat.php, NOT
  ContextCompactor.php — plan :2769 file list drift for P8.S3).
- Result parse: `Chat::parseExchangeSummaries()` `:9426-9445` — positional
  `/^\s*(\d+)\s*[.)]\s*(.+)$/u` → `$keys[N-1]`; degrades-not-misattributes
  (out-of-range/dup/missing → heuristic fallback, docblock :9407-9425).
- Line bound: `Chat::SUMMARY_LINE_MAX_CHARS = 200` `:9454`, docblock `:9447-9453`
  claims it MATCHES the prompt's ask — any P8.S1 edit that drops the prompt's 200 ask
  MUST move or re-document this const or its docblock becomes false.
- Sanitizer: `Chat::sanitizeSummaryLine()` `:9462-9476` (whitespace-flatten +
  200-clip with `…`). A structured multi-line record dies HERE as much as in the regex.
- Input serialisation (unchanged by this step): `Chat::renderExchangesForSummary()`
  `:9396-9405` (`### Exchange N\nUser: …\nAssistant: …`).
- Downstream transport: `HistoryCompactedMsg->summaries` is `array<string,string>`
  keyed by `ContextCompactor::exchangeKey()` (sha256 :106-109), consumed
  `compactionChanges :8964` → `withExchangeSummaries :83-86` →
  `generateExchangeSummary :1208-1213`: DIRECT READ CONFIRMED the model-supplied
  string returns VERBATIM (no cap on the compactor side).
- Compaction-triggering "busiest method" = `Chat::submit()` `:6056-6316` —
  UNTOUCHABLE per step constraint.
- E21 is CLOSED (2026-08-19, backlog
  `docs/plans/crush_code_hardening_backlog.md:1410`); its "restructure of the busiest
  method" already produced scheduleParkedCompaction/applyModelCompaction. Cite it
  PAST-tense only.
- Tests: the plan's singular "compaction test file" resolves to THREE:
  `tests/Chat/CompactModelSummaryTest.php` (21 tests — the /compact model route,
  PRIMARY home for P8.S1 tests), `tests/Chat/AutomaticCompactionModelSummaryTest.php`
  (27 tests, parked route, ships scripted backends `RecordingTurnBackend :1182`,
  `CountingSummaryBackend :1225`, `BillingSummaryBackend :1260`,
  `FailingSummaryBackend :1286`), `tests/Chat/CompactCommandTest.php` (6, heuristic).
  `tests/CompactorTest.php` (13) exists as cited.
- Goldens: system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 B, agent
  `ef0326dd38535aaa2f1d715919bff26e`/1,060 B — COMPACT_SUMMARY_PROMPT rides a
  SEPARATE backend call, never the base system prompt; these MUST NOT move.
- Floor inherited: 11,018 / 169,219 / 0F / 0E / Skipped 2 / EXIT 0 (gated `da178befb`;
  belt `git diff da178befb..master -- sugar-crush/` byte-empty — master above it is
  markdown + foreign ansi/ assets only).
- §5 collision check 2026-09-08: Chat.php / ContextCompactor.php /
  CompactorConfig.php / IdleCompactionPolicy.php all QUIESCENT (last touches
  in-master; crush-lanes stale at 2026-08-25, zero foreign commits).

## Orchestrator rulings (binding)

- **P8.S1-D1**: format = per-exchange structured RECORD (numbered block with six
  facet lines `asked:/did:/files:/decided:/corrected:/error:`, `none` mandatory for
  empty), NOT a whole-conversation document. Rationale: document-level summaries
  would require splicing surgery in ContextCompactor/applyModelCompaction = the
  forbidden restructure; the record form carries everything the plan names as lost
  (paths, decisions, user corrections, exact error strings — plan :2718-2720), keeps
  `array<string,string>` transport + heuristic fallback byte-unchanged, and leaves
  P8.S3's `<prior-summary>` recursion cleanly layered.
- **P8.S1-D2**: `SUMMARY_LINE_MAX_CHARS` 200 → 2,000 with an honest rewritten
  docblock (transport protection, flattening rationale; NO LONGER a "matches the
  prompt ask" claim since the prompt stops stating 200). Both bounds must agree on
  the new reality.
- **P8.S1-D3**: parse must accept a block ONLY if it opens with a bare numbered line
  (`1.` alone); facets absent inside a recognized block fill `none`; a block with
  ZERO recognized facets + zero continuation lines stays UNMAPPED (heuristic).
  Legacy `1. text-on-same-line` output must NOT be half-parsed — model obeying the
  new template is the contract.

## Superseded/forward notes

- Plan :2718 anchor drifted (above). Plan P8.S3/S5 file lists will need the same
  re-anchoring at their staffing (Chat.php-side seams).
- P8.S2 appends anti-forgery + security-preservation RULES to the same const — keep
  the Rules list structured so S2 is a clean append.

## Concurrency

Phase 8 is FULLY SERIAL (plan :2820-2821): S1→S2→S3→S4→S5, every step touches
Chat.php or ContextCompactor.php. Do not staff any sibling.

## Worktree provisioning record (staffing-time, 2026-09-08)

- `/home/sites/prompt-step-P8.S1` created via `git worktree add` @ `be01660a0`,
  branch `prompt/P8.S1`.
- `sugar-crush/vendor` = real hard-linked dir (`cp -al`, NOT a symlink; link count
  18 shared with the main checkout).
- `composer dump-autoload` regenerated; `vendor/composer/autoload_psr4.php` shows
  `$vendorDir = dirname(__DIR__);` / `$baseDir = dirname($vendorDir);` — relative
  form, zero absolute leak to `/home/sites/sugarcraft`.
- Sanity solo (box-quiet probe 0 at run time):
  `vendor/bin/phpunit tests/Chat/CompactModelSummaryTest.php` → `OK (21 tests,
  99 assertions)` — matches the plan's 21-test figure exactly.
- Post-provisioning `git status --porcelain` in the worktree: empty (vendor/ and
  .phpunit.cache/ are gitignored at .gitignore:6 / :17).
