# P7.S5 FORENSIC RE-DERIVATION — ground truth at 4e7f79014 (source: delegation empirical-green-bobolink, complete)
> **SUPERSEDES** the anchors in P7.S5-premise.md sections 3/6/7/8 and the brief P7.S5-step-brief.md decisions R-5 + mandatory-item-1 wherever they conflict. Items 1,2,7,8,9 are CONFIRMED real (with the two corrections below); items 3,4,5,6,10 exposed FABRICATIONS. This file is the P7.S5 anchor of record until a CORRECTED premise replaces the old one.
> Correction to item 9: `resolveGrantedTools` IS PUBLIC (`:704`), not private — and `executeSubAgent` at `:556-557` DOES call it (`:556`) and `refuseCallOutsideGrant` (`:557`); its `CompleteRequest` carries `tools: $grantedTools` at `:572`. `executeAll` (`:589-621`) calls NEITHER — no `tools:` in its `:609` `CompleteRequest`.
> Correction to item 7: line counts are 14/13/15 (`wc -l`) because the final line lacks a trailing newline; the awk body-is-empty check stands for all three.
> Additional orchestrator note (2026-09-06, from the same-forensic delegation's second report): `git show 90f38c97b --stat` — P2.S4 "surface refused agent-preset/skill/workflow search-paths" added 5 FILES: `src/Cli/Bootstrap.php +97`, `src/Skills/SkillManager.php +41`, `src/Workflows/WorkflowRegistry.php +33`, `tests/Skills/SkillManagerRefusedDirectoriesTest.php +72 (NEW)`, `tests/Workflows/WorkflowRegistryProjectTierTest.php +18` — it NEVER created `src/Tui/AgentPresets.php` and NEVER added a `Chat::agentPresetRefusals` param. `git log --all -S` for `class AgentPresets`, `agentPresetRefusals`, `described`, `testBuiltinPresetFilesWin` → ZERO hits across ALL branches. Verdict: FABRICATION in premise A, not drift.

# FORENSIC RE-DERIVATION — /home/sites/prompt-step-P7.S5 @ 4e7f79014

All facts below verified by direct read/grep of /home/sites/prompt-step-P7.S5/sugar-crush @ 4e7f79014, nothing inferred.

## 1. Bootstrap::agentRoster + agentPresetTiers
agentRoster docblock: lines 1529-1604; signature 1605; body 1605-1644. VERBATIM BODY [lines 1607-1643] shows: `$native = self::agentPresets($root);` :1616; `$agents = [];` :1618; foreign foreach :1621-1623; six built-ins TYPE_CODER..TYPE_DEVOPS via AgentDefinition::fromType + Agent::fromDefinition :1625-1637; native foreach :1639-1641; return array_values :1643. NO $described in agentRoster or agentPresetTiers (zero hits file-wide). agentPresetTiers docblock 1861-1882, signature :1883 `private static function agentPresetTiers(string $root): array`, return `array{0: list<string>, 1: array<string, string>}` — RETURNS [searchPaths, anchors] (NOT ['presets','described','tiers']). Construction: AgentPresetRegistry :1791-1794.
## 2. Per-file/per-preset refusal channel
`ls src/Tui/ | grep -i 'agent|preset'` → no AgentPresets.php anywhere in src; tests/Tui/AgentPresetsTest.php ABSENT. The only preset-adjacent refusal in src/Tui is SkillsPane.php:106 (workflow, different feature).
## 3. Transcript surfaces — REAL
`private const PROJECT_TIER_REFUSAL_FORMAT = 'ignoring %s — %s';` :390; `private static array $projectTierRefusals = [];` :1411; `public static function reportProjectTierRefusals(): int` :1425-1434 (foreach → `self::warnPermissionConfigInTranscript(sprintf(...))` :1430, resets `= []` :1431); `private static function warnPermissionConfigInTranscript(string $message): void` :1439-1453 → fwrite(STDERR) :1452 + `$_ENV['SUGARCRUSH_TEST_CAPTURE']` :1446. Called at :1095 in Bootstrap::chat(). Chat construction :1119-1128. ALL warnPermissionConfigInTranscript call sites (8): 1331,1353,1369,1391,1406,1430,2947,3206. NO P7.S2-hook warning routes here: Chat.php:3711 routes hook notices via `Message::system(sprintf('PreToolUse hook blocked the turn: %s', $blockedReason))` inside submit(). `Chat::__construct` real line :485; NO preset/refusal param; nearest = `private array $permissionConfigWarnings = [],` :603.
## 4. P2.S4 + -S history — FABRICATION CONFIRMED
90f38c97b: 5 files: Bootstrap.php +97, SkillManager.php +41, WorkflowRegistry.php +33, tests/Skills/SkillManagerRefusedDirectoriesTest.php +72 NEW, tests/Workflows/WorkflowRegistryProjectTierTest.php +18. NO Tui/AgentPresets.php. `git log --all -S` 'class AgentPresets' EMPTY; 'agentPresetRefusals' EMPTY; 'testBuiltinPresetFilesWin' EMPTY; 'described' EMPTY all-history.
## 5. tests/Cli/AgentManagerWiringTest.php — 21 test methods. NO test loads REAL .sugar-crush/agents (zero occurrences of '.sugar-crush/agents' string). testBuiltinPresetFilesWinOverTheCodeDefinitions — DOES NOT EXIST ANYWHERE. isFlaggedRedIsolation... :614-646 real (synthetic). testARefusedSearchPathIsObservable... :713-741 real (registry-level; no transcript emission). testAgentRosterReturnsTheSixBuiltins... :170-197 real.
## 6. tests/Agents/AgentPresetTest.php — 27 methods. testInitialPromptKeyWinsOverTheBody :294-306; testMissingBodyStaysNullWhenNoInitialPromptKey :308-320; testBodyWinsWhenTheInitialPromptKeyIsAbsent :322-331. NO testBuiltinPresetFilesWin.
## 7. wc -l coder 14 / reviewer 13 / security-auditor 15 (no trailing NL; bodies empty per awk — claim correct).
## 8. Agent::fromPreset :255; prompt assignment :269; docblock claim :245-246 verbatim.
## 9. resolveGrantedTools PUBLIC :704; executeSubAgent :526 calls resolveGrantedTools :556 + refuseCallOutsideGrant :557; CompleteRequest tools :572; executeAll :589 calls neither; agentFor :296; AgentDefinition::all :188-195 six definitions; all() consumers: AppBuilder:128, TeamPermissionBridge:118, Team:601,616; AgentManager::allDefinitions() :117-120, definition() :131.
## 10. SkillManager::refusedDirectories :246; WorkflowRegistry::projectTierRefusal :221; merges :2943-2948 (skills), :1247-1254 + :1275-1282 (workflows), pattern `$refusals[$key] = $message;`.
## 11. Support/Frontmatter.php, parse :16-46, unknown keys ignored (:42 filter).
FABRICATION VERDICT: 4 of 6 fabricated (Tui\AgentPresets, $described, tiers return, agentPresetRefusals) + testBuiltinPresetFilesWin (5th); 3 of 4 real-but-wrong (wc 14/13/15; resolveGrantedTools PUBLIC not private; fromPreset :269 vs :256).
