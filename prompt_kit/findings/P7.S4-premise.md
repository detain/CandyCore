# P7.S4 premise — `SkillRegistry::findForPrompt()` measured before wiring

> Measurement executed 2026-09-06 at base commit `b289eaf44`. EVERY number in this file is MEASURED at b289eaf44 by `/tmp/p7s4-measure-orch/measure.php`; its raw output is `/tmp/p7s4-measure-orch/raw-output.txt`. Both artifacts now live durably in-repo at `/home/sites/sugarcraft/prompt_kit/findings/P7.S4/measure.php` (428 lines, the 52-prompt battery embedded at section 2) and `/home/sites/sugarcraft/prompt_kit/findings/P7.S4/raw-output.txt` (208 lines) — byte-identical, md5-verified copies; nothing durable remains in /tmp.

## THE MANDATE (verbatim, `prompt_plan.md:2612-2630`)

> **P7.S4 — `SkillRegistry::findForPrompt()` — measure before wiring**
>
> **Goal** It is defined but unreachable from the chat loop; its only callers are
> `SkillManager::getSkillsForTask():143` and `App::findSkillsForTask()` (`src/App/App.php:384`),
> neither with a production call site. Its matcher is crude: `Skill::matchesPrompt():90-102` lowercases
> the **description** and treats any token >3 chars appearing as a substring of the prompt as a match.
> **Wiring it as-is may be worse than leaving it dormant.** Measure the false-positive rate on the
> repo's own skill set first.
>
> **Done when** either the matcher is improved (whole-word, per P6.S1's `KeywordTrigger`) and wired
> with a test proving the false-positive case is gone, **or** the worklog carries the measurement
> showing the false-positive rate and the step closes as "deliberately left dormant, here is the
> number". Both are acceptable outcomes; "wired it, seems fine" is not.

Measurement FIRST — done. The plan's caller cites drifted: at b289eaf44 the `findForPrompt` calls sit at `sugar-crush/src/Skills/SkillManager.php:143` (signature :141) and `sugar-crush/src/App/App.php:424` (signature :422; plan's `:384` STALE). Both confirmed by grep to have ZERO production call sites — the matcher is dormant exactly as the premise claimed.

## CORPUS

- MEASURED: 12 built-in skills via `SkillLoader::loadBuiltInSkills()` → `Skill::fromFile` per `sugar-crush/src/Skills/BuiltIn/*/SKILL.md` → `SkillRegistry::register()`. Project `.sugar-crush/skills` and user `~/.sugar-crush/skills` absent on the box.
- All 12 auto-invocable (`disable-model-invocation` false/absent everywhere). `matchups-sync` is `user-invocable: false` but that flag does NOT gate `findForPrompt` (only `disable-model-invocation` does, via `isAutoInvocable`).
- 239 description tokens with `strlen > 3` qualify as needles. Length distribution: `4:48 5:32 6:22 7:33 8:24 9:25 10:27 11:8 12:5 13:6 14:4 15:1 16:3 23:1`. Split: 155 clean vs 84 punctuation-bound.

## THE MATCHER (code as measured)

`sugar-crush/src/Skills/Skill.php:90-102`:

```php
public function matchesPrompt(string $prompt): bool
{
    // Simple keyword matching on description
    $keywords = array_filter(explode(' ', strtolower($this->description)));

    foreach ($keywords as $keyword) {
        if (strlen($keyword) > 3 && stripos($prompt, $keyword) !== false) {
            return true;
        }
    }

    return false;
}
```

The needle side is the lowercased DESCRIPTION split on single spaces; `strlen` is a BYTE length (a multibyte caveat for non-ASCII tokens); the prompt itself is never tokenized — each needle is an unanchored `stripos` substring probe.

`sugar-crush/src/Skills/SkillRegistry.php:251-280` (`findForPrompt`): skips non-auto-invocable skills, keeps each `matchesPrompt` hit, then `usort`s by `substr_count(strtolower($a->description), strtolower($prompt))` desc. That sort key uses the WHOLE PROMPT as the needle — realistically never a substring of any one-line description, so the count is 0 for every skill every time and the "relevance sort" is effectively an input-order no-op. The function has ZERO production callers (dormant, per plan).

## BATTERY (52 prompts, embedded in measure.php section 2)

- 15 NEUTRAL — everyday chat unrelated to any skill (e.g. "what's the weather like today"). Any firing = FP.
- 12 OBVIOUS — one intended-true-positive per skill. Intended skill firing = TP; every OTHER skill firing on the same prompt = FP.
- 25 BOUNDARY — one of the skill's OWN description tokens embedded in an off-domain sentence (e.g. "handle this package with care at the airport" ← `port`). Any firing = FP.
- Variants run against the same corpus: **W1** = same needles, predicate swapped `stripos` → `/\b…\b/iu`; **W2** = punctuation-stripped clean word tokens (`\p{L}\p{N}+`, >3) fed through the real P6.S1 `KeywordTrigger::matchedWords()`.

## HEADLINE RESULTS (MEASURED at b289eaf44)

| metric | CURRENT | W1 (whole-word, same needles) | W2 (clean words → KeywordTrigger) |
|---|---|---|---|
| FP/neutral (prompts) | 4/15 (26.7%) | 4/15 (26.7%) | 4/15 (26.7%) |
| FP/boundary (prompts) | **24/25 (96.0%)** | 20/25 (80.0%) | 24/25 (96.0%) |
| precision | 0.162 | 0.214 | 0.167 |
| recall | 1.000 | 1.000 | 1.000 |
| false (skill,prompt) pairs | 62 | 44 | 60 |

## THE DECISIVE SPLIT

Of the 62 CURRENT off-domain false pairs: **11 (17.7%)** are substring artifacts whole-word deletes (`port` in airport, `work` in working, `over` in cover, `check` in checking, `service` in services); **51 (82.3%)** are genuine whole words that whole-word KEEPS — `'when'` ×10 pairs, `'patterns'` ×4, and singles `that / audit ×2 / services / with ×2 / coverage / blade / authentication / event / working / over / changes / files / file / reviewing / security / container / mock / composer / modern / setting / under / check / form / protocol / designing / structure / responses`. Whole-word anchoring fixes the wrong thing: the dominant failure is not substring bleeding, it is common English words living in descriptions.

## WORST 3 (MEASURED)

1. `'when'` fires 9 skills (api-design, explore-codebase, laravel-best-practices, matchups-sync, mcp-authoring, php-best-practices, symfony-best-practices, testing-strategies, worktree-workflow — 10 false pairs, explore-codebase carries two `when` tokens) on the single prompt "let me know when you are ready for lunch". Whole-word cannot fix this — `when` is really the word.
2. `'audit'` fires security-audit on "can you audit my home heating bill please" and "please audit the financial books for the cafe".
3. The `form`/`event`/`check`/`security` class: symfony-best-practices fires on "the form of the contract is confusing" and "the birthday event was canceled last minute"; security-audit fires on "check please under the sofa cushions" and "the security cameras at the museum were stolen".

## TOKEN-LENGTH INSIGHT

Length-4 needles are 48/239 = 20% of the corpus and are function-ish English (`when/with/that/form/best/type`). Raising the floor 3→5 would drop them — but kills nothing domain-useful either, and the 5-char class (`audit/check/event/blade/port/over`) keeps firing. **The length floor is not the lever; description-as-keyword-bag is the flaw.**

## ORCHESTRATOR DECISION (2026-09-06)

**Outcome: deliberately-left-dormant WITH THE NUMBER.** The plan authorizes exactly this: "the worklog carries the measurement showing the false-positive rate and the step closes as 'deliberately left dormant, here is the number'" (`prompt_plan.md:2628-2630`).

Why improve-and-wire is FALSIFIED by the measurement:

- Whole-word is insufficient. W1 still fires on 20/25 boundary prompts (80%); W2 — the "realistic" KeywordTrigger variant — raises boundary FP back to 24/25 (96%) with precision 0.167, WORSE than W1 and barely above CURRENT. The 51-of-62 genuine-word mass is unreachable by predicate surgery.
- Wiring precision 0.214 is a NET NEGATIVE versus wiring nothing's 0. A dormant matcher's live FP rate is zero by construction; promoting W1 to production would inject ~4-5 spurious skill fires per 25 off-domain prompts into every turn.
- Plan lesson §16: do not half-fix unused machinery. A small, mechanically testable swap that maxes at 0.214 precision and preserves recall 1.000 is exactly the half-fix that buys an FP stream without buying a usable feature.

What the step SHIPS INSTEAD (`/home/sites/sugarcraft/prompt_kit/briefs/P7.S4-step-brief.md`):

- Pin tests for the CURRENT semantics — both `matchesPrompt` polarities, the pathological boundary cases pinned AS FEATURES (documenting the substring rule), and the `findForPrompt` input-order sort no-op pinned so any future change is a visible diff.
- Landmine docblocks on `SkillRegistry::findForPrompt()` and `Skill::matchesPrompt()`: WHY-dormant + here-is-the-number, citing this premise and the artifacts. DOCBLOCK-ONLY — zero behavior change.
- The two dormant callers — `SkillManager::getSkillsForTask()` (`sugar-crush/src/Skills/SkillManager.php:143`) and `App::findSkillsForTask()` (`sugar-crush/src/App/App.php:424`) — stay UNTOUCHED (§1.10: nothing removed, stubbed or deprecated).

Recorded follow-up (NOT in scope): a production-viable matcher needs CURATED frontmatter keywords — an explicit trigger vocabulary fed to `KeywordTrigger` (`sugar-crush/src/Context/Triggers/KeywordTrigger.php`). The anchor primitive is right; the INPUT (raw description as keyword bag) is undisciplined. That is a feature-design step, not this close-out.

## METHOD INTEGRITY

Throwaway detached worktree `/tmp/p7s4-measure-wt` @ b289eaf44; `cp -al` vendor; own-src PSR-4 proven via reflection on `SkillRegistry`/`Skill`/`KeywordTrigger`; every alternative matcher implemented ONLY in the /tmp script — production code untouched; worktree removed, main tree porcelain-empty, HEAD unchanged after measurement.
