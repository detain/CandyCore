# WRITE DENIED — my toolset expo...

WRITE DENIED — my toolset exposes no write/edit capability (read-only researcher config: `read`/`grep`/`glob` + a bash allowlist that excludes any f...

**ID:** basic-moccasin-swift
**Agent:** researcher
**Status:** complete
**Session:** ses_f77b46253ffelEjPYYpQtw63AZ
**Started:** 2026-09-09T22:30:52.077Z
**Completed:** 2026-09-09T22:38:42.304Z

---



WRITE DENIED — my toolset exposes no write/edit capability (read-only researcher config: `read`/`grep`/`glob` + a bash allowlist that excludes any file-writing command; every write path is policy-denied). Per the task's fallback, the complete report follows inline. One tooling constraint to state up front: **`git` is denied by the session bash policy** — my attempt `git -C /home/sites/sugarcraft log --oneline -5` returned the permission-denial output listing the allowlist (`head/tail/grep/rg/wc/sort/uniq/cut/awk/tr/jq/base64/gh api/...`, no `git`). §5's last-touch evidence therefore uses the on-disk git reflog (`read` tool) plus the worklog/resume merge chains, each cited.

---

# P6.S5b premise report — /home/sites/sugarcraft @ master `bc9953c11` (read-only scout)

HEAD verification (no `git` binary access): `/home/sites/sugarcraft/.git/logs/HEAD:1395` — `49d61be45... bc9953c11571bc58a1af22e93f6e23b50489b566 ... commit: P11.S5: resume close — PLAN COMPLETE except P6.S5b, merged 768427809, worktrees reaped`. The reflog tip is `bc9953c11…`, matching the task's stated tip; lines 1376-1395 are the whole tail and every entry is a `merge prompt/P11.S*`, `prompt-*` bookkeeping commit, or the `pull --all` → `6326cdb9d` (= `origin/master` per `prompt_resume.md:17`). No crush-lane worktree was read.

---

## 1. Current state of rules→prompt

**The splice is in `sugar-crush/src/Runtime.php::systemPromptSections()` (private, `:2519`). Rules load ONCE at the one construction site:**

```php
2565:        // P6.S2 (rulings D1 + D2): the three-tier rules surface, framed by
2566:        // provenance. RuleLoader owns the walk (user ~/.sugar-crush/rules,
2567:        // project <root>/.sugar-crush/rules, root <root>/RULES.md); load() is
2568:        // its single deduplicated, filename-ordered, enabled-only entry point
2569:        // (OD3), so the tiers are consulted ONCE here ...
...
2598:        // TRIGGERS ARE NOT YET APPLIED (named so, not hidden): the loader
2599:        // builds each rule's paths/keywords/description triggers into the Rule
2600:        // object and neither splice here reads $rule->triggers, so a rule
2601:        // scoped with `paths: ["src/**"]` renders into EVERY session until the
2602:        // P6.S5 / P7.S4 wiring gates it (measured at P6.S2 review: YES, it
2603:        // renders). Framing and escape are tier-blind, so this is a scoping
2604:        // gap, not a safety gap; recorded as a row in prompt_plan §18.
...
2620:        $rules = (new RuleLoader(
2621:            $app->root ?? (getcwd() ?: ''),
2622:            rulesState: $app->rulesState,
2623:        ))->load();
```
**Source:** `sugar-crush/src/Runtime.php:2565-2623` (observed verbatim).

**User tier renders every rule unconditionally (`:2625-2639`):**

```php
2625:        foreach ($rules as $rule) {
2626:            if ($rule->tier !== 'user' || trim($rule->body) === '') {
2627:                continue;
2628:            }
2629:
2630:            $sections[] = $this->section(
2631:                '<user-rules>',
2632:                Stability::PerSession,
...
2636:                "<user-rules>\n" . self::USER_RULES_AUTHORITY_PREAMBLE . "\n\n"
2637:                . PromptFence::escape($rule->body) . "\n</user-rules>",
2638:            );
2639:        }
```

**Project + root tiers ride the same inline fence as instruction docs (`:2682-2693`):**

```php
2682:        foreach ($rules as $rule) {
2683:            if ($rule->tier === 'user' || trim($rule->body) === '') {
2684:                continue;
2685:            }
2686:
2687:            $sections[] = $this->section(
2688:                '<project-instructions>',
2689:                Stability::PerSession,
2689:                "<project-instructions>\n" . self::INSTRUCTIONS_AUTHORITY_PREAMBLE . "\n\n"
2691:                . PromptFence::escape($rule->body) . "\n</project-instructions>",
2692:            );
2693:        }
```
**Source:** `sugar-crush/src/Runtime.php:2625-2639` and `:2682-2693`. The predicates are **tier + blank-body only** — no path test appears anywhere in the loops.

Rules do carry triggers: `Rule::buildTriggers()` (`sugar-crush/src/Context/Rule.php:225-246`, keywords→`KeywordTrigger` at `:229-231`, `paths:`→`PathTrigger::new($paths)` at `:234-236`, description→`IntentTrigger` at `:243`) and `Rule::$path` is documented as "The resolved absolute path this rule was read from" (`Rule.php:115`, promoted `public readonly string $path` `:126`) — the exact operand a pointer tail needs. `RuleLoader` itself states the gap: "TRIGGERS ARE BUILT, NOT APPLIED … deciding whether a rule fires for a given prompt or path is a later wiring step (P7.S4, P6.S5)" (`sugar-crush/src/Context/RuleLoader.php:116-121`).

**What `### P6.S5b` demands — `prompt_plan.md:2422-2431`:**

> `2422:` **### P6.S5b — Glob-scoped rules reach the model (BLOCKED — user decision)**
> `2424:` **Status 2026-09-05: BLOCKED on one §1.10 escalation. Do not brief or build until the user answers.**
> `2426:` **Shape (ruled)** transient `SkillPathNudge`-shaped channel, NOT in-splice filtering (Q1). Per premise check Q7 the build wants a **new** `src/Context/RulePathNudge.php` sibling (keep `SkillPathNudge` single-subject), `src/Cli/Bootstrap.php` construction + injection at the path-resolving tool site (`:5191-5205`), and appends in `src/Tools/BuiltIn/{Read,Edit,Write,Glob,Grep}.php`. The plan's original list (`RuleLoader.php`, `SkillPathNudge.php` + their two test files) is **withdrawn** — `RuleLoader.php` needs no change and the two named test files are near-irrelevant.

The blocking question the user has now ruled is quoted verbatim at `prompt_plan.md:2438-2444` (identical wording in `prompt_resume.md:765` item **(4)**). The step also carries: fixture landmine `:2433-2436`; the withdrawn 300 B transplant `:2446-2452`; `Rule::withTriggers()` zero-caller deferral `:2454-2456`; P7.S4 coordination `:2458-2459`; serial-after-S4 because it needs `Bootstrap.php`, and "Shape A additionally collides with `EngineBackend.php` … a second reason to prefer B" `:2461-2465`. (The Bootstrap line cite `:5191-5205` has drifted; the live construction site is `src/Cli/Bootstrap.php:5613` `$nudge = SkillPathNudge::new($skills);` with injections at `:5617/:5618/:5619/:5631/:5645` — observed via grep.)

---

## 2. PathTrigger: match API and where a touched-file set would come from

**API** (`sugar-crush/src/Context/Triggers/PathTrigger.php`, 176 lines, read in full):
- `PathTrigger::new(array $globs): self` — `:90-108`; throws `InvalidArgumentException` on an empty list (`:92-94`) or blank/non-string entry (`:97-103`). `withGlobs()` `:115-118`.
- `matches(string $path): bool` — `:134-143`: loops globs, `PathGlob::matchCompiled($this->pattern($glob), $path) === true`.
- `matchingGlobs(string $path): array` — `:151-161`: "The patterns that hit, in declaration order — the loader uses them to attribute which glob admitted the path."
- `private pattern(string $glob): string` — `:172-175`: memoized `PathGlob::compile($glob)`. A glob whose PCRE refuses to execute answers NO (`:59-66`, `:131-132`) — SkillRegistry instead falls back to `legacyPathMatch()`; a rules caller must live with the NO.

**Dialect**: the single compiler `sugar-crush/src/Util/PathGlob.php` (P6.S5a deliverable) — `**` spans `/`, a bare `*` also crosses separators, leading `**/` is zero-or-more dirs, `[…]` is fnmatch-style, `{a,b}` is NOT alternation (`PathGlob.php:68-80`), ruled with four measured reasons at `:33-66`.

**'MATCHER NOT GATEKEEPER' clause** — `PathTrigger.php:18-26`, verbatim:

> A MATCHER, NOT A GATEKEEPER — the decision this step pins. The trigger answers only "does this string match this pattern"; it knows nothing about any repository root and touches no filesystem (pure string in, bool out — it must stay invisible to the read-sink census over `src/`) … An `/etc/passwd`-style absolute path therefore MATCHES `**` like any other string: matching is not authorisation, and the containment gate belongs to the P6.S2 loader that feeds these values

Plus the case/anchoring contract: "Case-SENSITIVE, BYTE-wise … and anchored at both ends" (`:68-71`). Consequence for callers: because matching is anchored, a repo-relative glob like `src/*.php` does **not** match an absolute path string, while `**/*.php` matches anything — so the caller decides the spelling it feeds (see §RULING risk 4). Precedent in production today: tools hand SkillPathNudge **resolved paths** (`SkillPathNudge.php:14-15` "Read/Edit/Glob now hand every path they resolve to this tracker"; `forPaths()` `:215`).

**Turn-time touched-file state: NONE EXISTS.** `grep` over `sugar-crush/src` for `touchedFiles|openedPaths|readPaths|filesTouched|touchedThisSession` → **"No files found"** (observed). The nearest state is per-session *announcement names* for skills (`SkillPathNudge::$announced`, `src/Skills/SkillPathNudge.php:68`) — a name-set, not a path-set, and it lives in the tool layer, not in `Runtime`/`Chat`. There is also the on-touch instruction precedent: `InstructionFileLoader::loadForPath()` "only fires once the agent happens to open a file in that subtree" (`Runtime.php:2471-2476`). **Where the splice lives**: `Runtime::systemPromptSections()` at `Runtime.php:2620-2693`, reached per prompt build through `buildSystemPrompt()` (`:2493-2496`, `return self::assemblePrompt($this->systemPromptSections($app));`) and through the run-path fold (`:1112`, `[$systemPrompt, $systemBlocks] = self::assembleSections($this->systemPromptSections($app));` inside `run()`, `:1102`). A step wanting a turn-time touched set must either (a) put the predicate at the splice (needs session state reaching `Runtime` — none exists, and per-turn variance in the stable prefix region is forbidden by Q1/§7, see §5/§7 below), or (b) put the predicate at the five path-resolving tool sites like the skills channel does (the plan's ruled shape, `prompt_plan.md:2426-2431`). The ruling-compatible answer is (b): the tools ARE the touched-file source.

---

## 3. Precedent shapes to transplant

### 3a. SkillPathNudge (`sugar-crush/src/Skills/SkillPathNudge.php`)

`MAX_ENTRY_BYTES:103-105` (+ the constant at `:107`), verbatim:

```
103:      * 300 bytes is about two sentences, which is what a `description` is FOR:
104:      * it is a trigger phrase the model matches a task against, not the skill's
105:      * body.
106:      */
107:     private const MAX_ENTRY_BYTES = 300;
```

The nudge-line builder, verbatim (`:294-312`):

```php
294:    /**
295:     * One `- name: description` line, held to {@see MAX_ENTRY_BYTES}.
296:     */
297:    private static function entry(Skill $skill): string
298:    {
299:        $line = "- {$skill->name}: {$skill->description}";
300:        if (strlen($line) <= self::MAX_ENTRY_BYTES) {
301:            return $line;
302:        }
303:
304:        // mb_strcut and not substr, for the reason
305:        // {@see \SugarCraft\Crush\Tools\Concerns\TruncatesOutput::clipInstructions()}
306:        // gives: a description is arbitrary repository text, so a plain byte
307:        // cut lands inside a UTF-8 sequence and puts invalid bytes into a tool
308:        // result the model reads. There is no line-boundary fallback behind
309:        // this clip — an entry IS one line — so the byte cut is the only cut.
310:        return mb_strcut($line, 0, self::MAX_ENTRY_BYTES - strlen(self::ENTRY_CLIP_MARKER), 'UTF-8')
311:            . self::ENTRY_CLIP_MARKER;
312:    }
```

Supporting parts of the same machine the ruling re-uses: `HEADER` `:74-75` / `FOOTER` `:78` (`<system-reminder>` envelope), the deferral note `DEFERRED_NOTE = '... [%d further path-scoped skill(s) matched; they announce on a later call.]'` `:172`, the whole budget loop `:243-291` (`$room = min($budget, maxBytes())`, note-reserve `:248`, per-entry cost `:263`, last-entry免note `:264-266`, "A budget too small for even one entry returns null and MARKS NOTHING" `:207-211`, mark-only-on-delivery `:280-284`), `MAX_ENTRIES = 8` `:155`, `CALLER_BUDGET_DIVISOR = 8` `:335`, `maxBytes()` derived pricing `:377-386`, `smallestUnclippedCallerCap()` `:347-350`.

### 3b. HookContextFiles — `sugar-crush/src/Support/HookContextFiles.php` (located, 226 lines, read in full)

- **The cap constant**: not in this class — it's the producer-side `HookResult::MAX_ADDITIONAL_CONTEXT_BYTES = 10000` (`sugar-crush/src/Hooks/HookResult.php:68`), consumed at `ScriptHook.php:650` `HookContextFiles::bound($output, HookResult::MAX_ADDITIONAL_CONTEXT_BYTES)` and `HookRegistry.php:362`/`:783`.
- **Retained-file pattern**: `write()` `:198-225` — file `<tmp>/sc-hook-ctx/ctx-<16hex>.txt` (`:200-202`), `umask(0o077)` around `file_put_contents` to a `.partial` then atomic `rename` (`:204-224`), mode 0600 for life (docblock `:21-25`); directory created 0700 on demand (`dir()` `:86-101`, `DIR_NAME = 'sc-hook-ctx'` `:62`). Sweep-safety proof in the class docblock `:35-52` (glob-prefix disjointness from `ToolIpcFiles::sweep()`). Retention: "A retained file lives until the tool-result that names it leaves the session — it is never auto-deleted" (`:27-33`).
- **What the model-visible line says** — `bound()` `:150-158`:

```php
151:            $marker = sprintf(
152:                ' … [hook additional context truncated: %d of %d bytes shown; the full '
153:                . 'output is retained at %s]',
154:                0,
155:                $total,
156:                $path,
157:            );
```
with the unwritable-`/tmp` fallback marker `:142-147` ("could not be retained"), `MARKER_RESERVE_BYTES = 512` `:77`, `max(1, $cap - self::MARKER_RESERVE_BYTES)` head-room `:164`, re-stamped actual count `:169-174`, and the FINAL CAP GUARANTEE clamp `:178-188`. **This is the user's cited shape: bounded model-visible bytes + a pointer that names where the whole lives.** For rules the "whole" already lives at `Rule::$path` (`Rule.php:115/:126`), so only the pointer half of the transplant is load-bearing.
- **Tests that pin it**: `sugar-crush/tests/Support/HookContextFilesTest.php` — `test…` at `:47-52` (dir spelling = `sys_get_temp_dir().'/sc-hook-ctx'`), write/atomicity `:62-72`, retention-vs-sweep `:104` and `:124-147`, `bound()` below-cap passthrough + write-nothing `:147-157`, overflow returns ≤ cap with retained pointer `:166`, degenerate tiny cap `:187`. Plus `ScriptHookTest.php:253` (spillover integration). **Guard-story caveat for S5b**: `prompt_resume.md:598` declares `sugar-crush/tests/Support/` "Assigned wholesale to an in-flight lane" — yet P7.S1 shipped `HookContextFilesTest.php` into exactly that directory; the conservative placement precedent is P2.S4's workaround, which put `PromptFixture` in its own `tests/Prompt/` "because that directory is assigned wholesale to another plan's in-flight lane" (`sugar-crush/tests/Prompt/PromptFixture.php:35-40`).

### 3c. The per-turn prompt-content test idiom

- `sugar-crush/tests/RuntimeTest.php` renders through the real private assembler and asserts VALUES: `$result = $this->invokePrivateMethod($this->runtime, 'buildSystemPrompt', [$app]);` (`:1340`, `:1366`, `:1421`, …) and the harness form `$result = $fixture->systemPrompt($app);` (`:1813`, `:1833`), then `$this->assertSame(1, substr_count($result, 'DISTINCTIVE AGENTS BODY MARKER'));` (`:1815`), `assertStringNotContainsString('@./AGENTS.md', $result)` (`:1817`), `assertSame(1, substr_count($result, '<project-instructions>'))` (`:1818`), `assertStringNotContainsString('<project-instructions>', $result)` absence polarity (`:1851`) — exactly the "Call the thing, assert the value … both polarities" law of `prompt_plan.md:890-892`.
- The harness itself: `sugar-crush/tests/Prompt/PromptFixture.php` — controllable temp-root repo (`:74-77`), `write()` `:93-103`, `app()` `:174-191`, `systemPrompt()` via `\Closure::bind` onto `Runtime::class` with a deterministic `EnvironmentBlock` (`:203-219`).
- Wiring-level idiom for nudges: `sugar-crush/tests/Integration/SkillPathScopingWiringTest.php` (exists; the end-to-end budget ceiling guard is `testEveryShippedNudgeBudgetClearsTheTrackerCeiling`, named at `SkillPathNudge.php:129-137`, with the figure "re-derived … rather than written into it" per `:343-345`). S5b's analog should be a `RulePathScopingWiringTest` in the same directory and style.

---

## 4. Aggregate-budget follow-up

**Location correction first (paste-or-die):** the measured figures are **NOT in `prompt_resume.md` §8**. `grep` for `193 files|12.06 MiB|12,724,235|20,313,188|10,002,823` hits only `prompt_worklog.md:1509`, `prompt_plan.md:2446-2449` (+ `:2486`, `:3302`). Resume §8's only related content is item **(4)** — the S5b escalation verbatim (`prompt_resume.md:765`) — and the note that P6.S5a's five follow-ups are "recorded in the worklog P6.S5a entry … (the four doc/test-hygiene ones are folded into `Open follow-ups` below; the fifth is the §5 re-check)" (`:765`); the worklog's P6.S5a follow-up list itself (`prompt_worklog.md:1446-1451`) does not contain the aggregate bound either.

**Exact wording** (`prompt_worklog.md:1509`):

> **NEW EXPOSURE FINDING — recorded as its own follow-up, deliberately NOT folded into S5b.** Q4 re-derived the rules surface from **today's** constants instead of reusing the skills figure: 3 walked directories × `MAX_FILES = 64` × `MAX_FILE_BYTES = 65536`, plus an **uncapped** root `RULES.md`, = **193 files / 12.06 MiB raw / 12,724,235 B emitted / 20,313,188 B** after `PromptFence::escape`'s measured **1.6×** blow-up. The rules tier is therefore **already 1.27-2.03× worse than the 10,002,823-byte figure P6.S5's hard constraint was imported to guard against**, and that aggregate exposure exists on master **today**, independent of S5b — the per-file caps bound a *file*, never the *prompt*. Needs its own step: an aggregate bound.

Restated at `prompt_plan.md:2446-2452` ("the rules tier is **already 1.27-2.03× worse** … Recorded as its own follow-up (an aggregate bound on rules bytes), not folded into S5b's scope"). Today's constants confirmed at the caps: `RuleLoader::MAX_FILES = 64` (`src/Context/RuleLoader.php:151`), `MAX_FILE_BYTES = 65536` (`:170`), per-directory counter arithmetic `:102-114`, uncapped `loadRootRules()` (`:429`).

**Where the byte budget belongs given the ruling:** two distinct surfaces, and the ruling maps to only one of them.
- **Transient channel (S5b's own scope):** the budget lives in the new `RulePathNudge` as class constants priced like `SkillPathNudge::maxBytes()` (`:377-386`): a per-nudge `MAX_RULE_BYTES` (whole-body allowance) + `MAX_ENTRIES`-style count ceiling + a derived `maxBytes()` + a shipped-tool-budget guard test mirroring `SkillPathNudge.php:126-137`. The `10,002,823 B` figure (`SkillPathNudge.php:80-105`) is the precedent for WHY the body channel must be bounded at all — but sized for whole rule bodies, and the per-file rule cap `MAX_FILE_BYTES = 65536` is the natural per-body bound: a whole rule that fits the nudge budget is delivered whole (ruling), one that does not is **never partially delivered** — it emits exactly the pointer line `Rule 'x' deferred: budget. Read <path>` (the §1.10 half-instructed-model problem at `RuleLoader.php:155-166` is respected because bodies are indivisible in this design).
- **System-prompt splice (the deferred follow-up's scope):** the 12.06 MiB / 20.3 MB aggregate stays OUT of S5b per `prompt_plan.md:2452` / `prompt_worklog.md:1509` — S5b must not claim to fix it. Note the ruling *shrinks* future aggregate exposure incidentally (path-scoped rules leave the splice for a `paths:`-less world), but the standing `193 files` worst case of un-scoped rules is the separate step's problem.

---

## 5. Collision rows (declared files vs `prompt_resume.md` §5)

§5's live-table status (`prompt_resume.md:560`): "The collision table below stays live ONLY for the still-blocked **P6.S5b** (it wants `src/Backend/EngineBackend.php` + the rule-body transport); re-check §5 before staffing P6.S5b when user item (4) is answered."

- **`EngineBackend.php` row** (`prompt_resume.md:594`): "CONSUMED BY THE P7.S3 MERGE `b289eaf44` … The row STAYS LIVE for P6.S5b … RE-CHECK again before staffing P6.S5b; this clearance is historical, not a standing licence." **Does S5b actually need it?** Per the plan's ceiling list (`:2426-2431`, quoted in §1), NO — the ruled transient Shape's files are `RulePathNudge.php` + `Bootstrap.php` + the five `BuiltIn` tools; the EngineBackend collision is explicitly attributed to the *rejected* shape: "**Shape A additionally collides with `EngineBackend.php`**, recorded elsewhere as held by another lane — a second reason to prefer B" (`prompt_plan.md:2464-2465`). The resume's "it wants EngineBackend.php" (`:119`, `:560`, `:594`, `:595`) and worklog follow-up #5 (`:1451` "it wants `src/Backend/EngineBackend.php` (a live collision row)") carry the pre-split framing. **Recommendation: the step should NOT declare `EngineBackend.php`; the brief must record the plan:2464-2465 cite as the reason, and the row's RE-CHECK is still owed because `Cli/Bootstrap.php` shares that same row (`:594` header includes "+ `sugar-crush/src/Cli/Bootstrap.php`").**
- **Chat.php row** (`prompt_resume.md:595`): "P6.S5b touches neither of THESE two but DOES want `EngineBackend.php` - see its own row." So `src/Chat.php` + `src/Context/ContextCompactor.php` are OFF-limits-confirmed for S5b; the R-1 Chat clearance is SPENT (`:595`) and the standing Phases-5-11 clearance is the route P6.S3/P7.S2/P7.S3 rode (`:587`, `:595`) — S5b shouldn't need Chat.php at all under Shape B.
- **Tree-wide tests census row** (`prompt_resume.md:599`): "`SymbolCitationDriftTest`, `SwallowingCatchCensusTest`, `DuplicatedTestHelperDriftTest`, `ChildWallClockBudgetTest`, `EnvRosterDriftTest` and others walk the whole tree. **Every test file this plan adds can red one of them.**" Plus `prompt_resume.md:99`: "`TreeWideGuardRosterTest` walks the whole test tree (unaccounted 0), so full-suite predictions must name the mover classes first." Mitigations proven in-lane: `AssertionSwallowingCatchTest` costs +3 assertions per `try` under tests/ (`prompt_worklog.md:1659` lesson 4), `EnvRosterDriftTest` is SELF-REGISTERING over src/+bin/ reads (`prompt_worklog.md:488`), `BinSugarcrushWiringTest` per-file data provider enrolls every `src/**` file (a new `src/Context/RulePathNudge.php` is +1t/+6a, `prompt_resume.md:137`). The `BuiltInToolCorpusTest` src-count row is RESOLVED — adding src files reddens nothing (`prompt_resume.md:593`, `CENSUS_RESOLUTION` at `tests/Tools/BuiltInToolCorpusTest.php:285-287`).
- **`tests/Support/` row** (`prompt_resume.md:598`): whole directory "Assigned wholesale to an in-flight lane" — but P7.S1 landed `HookContextFilesTest.php` there anyway; place S5b's new tests in `tests/Context/` + `tests/Integration/` to sidestep the row.

**Last-touch per declared file — method note:** `git log` is denied by session policy (observed denial), so last-touch = worklog/resume merge-chain records cross-checked against the reflog. The reflog tail `.git/logs/HEAD:1376-1395` shows master since `6326cdb9d` (`pull --all`, `:1391`) advanced ONLY through `ec2d9e196` (staffing), `768427809` (P11.S5 merge), `49d61be45`, `bc9953c11` — all prompt-lane commits (identical text in `.git/logs/refs/heads/master:1235-1239`); and "belt `ec2d9e196..HEAD sugar-crush/` = 0 bytes" (`prompt_resume.md:584`) means ZERO sugar-crush files entered the post-origin window. Path verification first: the task's `sugar-crush/src/Context/Runtime*` is wrong — glob `sugar-crush/src/**/Runtime.php` → exactly `/home/sites/sugarcraft/sugar-crush/src/Runtime.php`; no `src/Context/Runtime.php`.

| Declared file                                                 | Last touch (recorded)                                                                                                                                                                                                                                                   | Verdict                                   |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `sugar-crush/src/Runtime.php`                                   | P10.S1 merge `77881b9cb` ("ONE shared `Runtime::assembleSections()` fold", `prompt_resume.md:466`); earlier Phase-9 window traced all Runtime.php edits to "declared ceiling (`Runtime.php` dual S1+S7)", `prompt_resume.md:134`/`:368`                                             | CLEAR — prompt-lane chain                 |
| `sugar-crush/src/Cli/Bootstrap.php`                             | P7.S3 merge `b289eaf44` — "merged its edits to BOTH files" (`prompt_resume.md:594`); P6.S4 touched it at `608914072` (`:1457-1459`); P10.S3 explicitly NOT (`prompt_worklog.md:488` "NO Cli/Bootstrap.php edit")                                                                  | CLEAR                                     |
| `sugar-crush/src/Backend/EngineBackend.php`                     | P7.S3 merge `b289eaf44` (`prompt_resume.md:594`) — row consumed, clearance "historical, not a standing licence"                                                                                                                                                             | CLEAR but row still RE-CHECKs (see above) |
| `sugar-crush/src/Context/Rule.php`                              | P6.S5a merge `505734f9f` — "doc-block only … verified over the merged 7-file delta" (`prompt_worklog.md:1377`/`:1386`, `prompt_resume.md:587`)                                                                                                                                  | CLEAR                                     |
| `sugar-crush/src/Context/Triggers/` (PathTrigger.php)           | P6.S5a merge `505734f9f` (`prompt_worklog.md:1385`)                                                                                                                                                                                                                         | CLEAR                                     |
| `sugar-crush/src/Tools/BuiltIn/{Read,Edit,Write,Glob,Grep}.php` | Phase-9 window: `Write.php` traced to "dual S1+S4" declared ceiling, merges through `d0711b2` (`prompt_resume.md:134`, `:368`); Phase-10/11 touched none of the five (P10.S3 `prompt_worklog.md:488`; P11 batch-A files are docs/MemoryBlock/new test only, `prompt_resume.md:584`) | CLEAR                                     |
| `docs/ENVIRONMENT.md`                                           | P10.S3 merge `5f30fec05` row-add (`prompt_worklog.md:488`); Phase-11 batch-A touched `sugar-crush/docs/*.md` — all prompt-lane (`prompt_resume.md:584`)                                                                                                                         | CLEAR                                     |
| `sugar-crush/src/Context/RulePathNudge.php` + new tests         | Do not exist — glob `sugar-crush/src/Context/RulePathNudge.php` → "No files found"; Phase-6 close review verified it absent: "P6.S5b correctly UNBUILT (`src/Context/RulePathNudge.php` absent)" (`prompt_resume.md:753`)                                                     | N/A (new)                                 |

**Overall: CLEAR** — every declared file's recorded last-touch is a prompt-lane merge in the chain (`505734f9f`, `608914072`, `f77cc7747`, `8e6ab1a5f`, `b289eaf44`, `77881b9cb`…`d44f13a48`-era Phase-9/10 merges, `768427809`), no foreign touch named in resume §5 UPDATE 13 (`:584`) or the close records (`:560-562`).

---

## 6. P7.S4 measured-number context

- **§18 row** (`prompt_plan.md:3914`): "Firing rule triggers (`paths:`/`keywords:`/`description`) at prompt assembly — scoped rules currently render globally … Triggers are BUILT into every `Rule` and consulted by NOBODY at assembly: a `paths: ["src/**"]` project rule rides every session (measured at review). Removing the built triggers would violate §1.10 … wiring them is the trigger-matching step's territory".
- **P7.S4 close**: `prompt_plan.md:2620` — "**Status 2026-09-06: MERGED — merge `5deb98fe8`; floor 10,997/168,995 gated `a906e9c72`.**" Outcome (worklog): "**closed deliberately-dormant WITH THE MEASURED NUMBER** — 52-prompt battery, substring precision **0.162**, improve-and-wire FALSIFIED" (`prompt_worklog.md:286`; detail `:1032`: boundary FP 24/25 = 96%, whole-word `KeywordTrigger` variant maxes 0.214; measurement at `48cf44795` in `prompt_kit/findings/P7.S4-premise.md`). Discharge record: `prompt_resume.md:753` — "The 'triggers-built-not-applied' thread … is discharged for `findForPrompt`/`matchesPrompt` BY THE P7.S4 DECISION ITSELF (dormant-with-number)".
- **Scope of S5b is path-only — verified in plan text**: the heading is "**Glob-scoped** rules reach the model" (`prompt_plan.md:2422`); Q2's gap statement is about `paths:` → `PathTrigger` (`:2382-2385`); the coordination line (`:2458-2459`) says P7.S4 would share "whatever gate S5b introduces … one named method", but P7.S4 has since CLOSED as dormant (`:2620`, worklog `:1022`), and any keyword-revival is owned by deferred follow-up **(49)** ("curated-frontmatter-keywords matcher (P7.S4 revival design) … DEFERRED — feature-design step", `prompt_worklog.md:351`). Also `prompt_worklog.md:1309-1310`: "`Rule::withTriggers()` adoption is owned by S5b/P7.S4; `Skill::matchesPrompt()` rewiring is owned by P7.S4" — and `withTriggers()` itself is explicitly deferred out of S5b to the §16.4 dormancy roster (`prompt_plan.md:2454-2456`). **Conclusion: S5b predicates on PathTrigger over touched files; no keyword/dormancy work belongs in it.**

---

## 7. Golden stability

**The default-launch golden DOES contain a rules fence.** `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt:90-95` carries the live `<user-rules>` opener, the authority preamble, and the fixture rule body `Prefer the shortest answer that is still correct, and plain sentences over nested clauses.` (grep-verified). The move history is pinned in-test: "MEASURED 2026-09-05 at P6.S2: 7,314 → 7,829 … fixture rule body 91 … the user-rules splice … rendered against the committed fixture user home" (`sugar-crush/tests/BaseSystemPromptTest.php:871-880`), and the current pin `self::assertSame(8278, strlen($golden))` at `:900-902` with the md5 `f5de3858c1bc130d1e97a120d3ead485` pinned a second time at `sugar-crush/tests/Chat/SessionStartHookWireTest.php:519-530` (`assertSame(8278, filesize(...))` `:519`, `assertSame('f5de3858c1bc130d1e97a120d3ead485', md5_file(...))` `:521`, re-pinned `:530`).

**Fixture seeding**: the golden test `testSystemPromptMatchesCommittedGolden` (`BaseSystemPromptTest.php:711`) calls `ensureFixtureRepo()` (`:1747`, repo materialized at `vendor/prompt-fixture/system-repo`, doc `:681-687`) and renders inside `renderUnderFixtureUserHome()` (`:728`; method `:1873-1893` — `putenv('HOME='.$home)` + `$_SERVER['HOME']`, restored in `finally`). `ensureFixtureUserHome()` (`:1827-1853`) copies `tests/fixtures/prompt/home` → `vendor/prompt-fixture/system-home` and **early-returns on `is_file($home . '/.sugar-crush/rules/global-style.md')`** (`:1831-1833` — the plan's `:1751` cite at `prompt_plan.md:2435` is drift; the check is alive at `:1831`). The committed fixture user tier holds exactly ONE rule — glob `sugar-crush/tests/fixtures/**/rules/*.md` returns only `home/.sugar-crush/rules/global-style.md`, whose full content is `---\nname: global-style\ndescription: Operator-global tone rule materialised for the golden prompt fixture\n---\n` + the one-line body (`:1-5`) — **no `paths:`, no `keywords:`**. And by design the project/root rule tiers are absent: "The committed tree is a user tier only - the project and root rule tiers deliberately stay out of the golden" (`:1813-1817`).

**Would a paths-scoped rule in the fixture render?** Yes today (splice reads no triggers — `Runtime.php:2598-2604`) — and it must never be committed there, per the warm-vendor landmine (`prompt_plan.md:2433-2436`).

**Verdict:** making the splice path-conditional **keeps the golden byte-identical** under one precise condition, stated with evidence: the deferral predicate must key on *presence of a `PathTrigger`* (e.g. `anyTrigger instanceof PathTrigger`), not on *presence of any trigger*. The fixture rule `global-style.md` carries a `description:`, and `Rule::buildTriggers()` converts description → `IntentTrigger` (`Rule.php:243`), so `count($rule->triggers) === 1` for it ALREADY — a "has triggers → defer" splice predicate would drop this rule from `<user-rules>` and move the frozen golden, violating the golden law (`BaseSystemPromptTest.php:699-709` "NEVER regenerate to silence a failing test") and P6.S5a's own done-when standard "**both goldens byte-identical** (nothing renders triggers — a golden move is a defect)" (`prompt_plan.md:2419-2420`). With the instanceof-PathTrigger predicate: no fixture rule has `paths:`, none is deferred, zero bytes move — the proof test writes itself (`assertSame(8278, …)` stays green + `substr_count === 1` on the user-rules fence, the `:900` idiom). The stability PREFIX contract is likewise untouched: rule deferral computed from the file's frontmatter is per-session-stable (`Stability::PerSession` sections, `Runtime.php:2632`), and the volatile half lives ONLY in tool results — exactly why Q1 ruled per-turn content out of the system prompt (`prompt_plan.md:2376-2381`, pinned at `sugar-crush/tests/Providers/PromptStabilityTest.php:710` `MIN_STABLE_PREFIX_BYTES = 4096` and the `> $envAt` shape assertion `:1128-1136`; note the plan's `tests/Integration/PromptStabilityTest.php:710` cite at `:2377` has drifted to `tests/Providers/`).

---

## RULING-FEASIBILITY

Given the ruling — *byte-bounded whole bodies, never clipped; each rule that doesn't fit emits `Rule '<name>' deferred: budget. Read <path>`; nothing silently vanishes; HookContextFiles' "bounded visible bytes + pointer at the whole" transplanted* — the design is buildable on the already-ruled Shape B and is strictly more §1.10-compliant than the withdrawn 300 B clip (a pointer never half-instructs: the model either holds the whole rule or a whole pointer to it at `Rule::$path`, `Rule.php:115/:126`).

**Ceiling the step should declare:**
- NEW `sugar-crush/src/Context/RulePathNudge.php` — session tracker over the same loaded `Rule` set; budget loop transplanted from `SkillPathNudge::forPaths()` (`:243-291`) with three changes the ruling demands: entries are whole escaped bodies (no `ENTRY_CLIP_MARKER` half-lines — `:164`/`:297-312` NOT transplanted), `MAX_BODY_BYTES`-style cap with `maxBytes()` derived pricing (`:377-386` pattern, required by the shipped-tool-budget guard idiom `:126-137`), and the deferred line as `DEFERRED_RULE_NOTE`-style constant (the `DEFERRED_NOTE` `:172` idiom).
- MODIFY `sugar-crush/src/Runtime.php` — the splice skip: `$rule->triggers` contains a `PathTrigger` → not appended (both loops `:2625-2639`, `:2682-2693`), with the `:2598-2604` comment updated to name S5b as the gate. **This file is ABSENT from the plan's declared list (`:2426-2431`) — the ceiling must be widened (precedent: "Runtime.php dual S1+S7", `prompt_resume.md:134`), or scoped rules get double-presented (splice + nudge), the exact defect P7.S3 closed with a −97 B golden move (`BaseSystemPromptTest.php:881-894`).**
- MODIFY `sugar-crush/src/Cli/Bootstrap.php` — construct `RulePathNudge` beside `SkillPathNudge::new($skills)` (`:5613`) and inject at the same five sites (`:5617/:5618/:5619/:5631/:5645`).
- MODIFY `sugar-crush/src/Tools/BuiltIn/{Read,Edit,Write,Glob,Grep}.php` — append the rule nudge beside the skill nudge (current call geometry: `Read.php:319-331`, `Grep.php:305-320`, `Glob.php:418-423`, `Edit.php:233`, `Write.php:231`).
- NEW tests (value-asserting, red-on-revert per §1.11, `prompt_plan.md:874-896`): `tests/Context/RulePathNudgeTest.php` — e.g. `testAWholeBodyWithinBudgetIsDeliveredUnclipped`, `testAnOverBudgetRuleIsNeverClippedButDeferredWithItsAbsolutePathInThePointerLine`, `testABudgetTooSmallForOneBodyMarksNothingAndTheNextCallDeliversIt` (mirrors `SkillPathNudge.php:207-211`), `testARuleWithNoPathTriggerIsNeverNudgedBecauseItStillSplices`; `tests/Integration/RuleSpliceScopeTest.php` (or fold into the `RulebookPromptRenderTest.php` idiom) — PromptFixture + synthetic-HOME render: `assertSame(0, substr_count($prompt, 'PATHSCOPED CANARY'))` + standing-rule presence + golden-length non-move; `tests/Integration/RulePathScopingWiringTest.php` — end-to-end through real Read/Grep with the budget-clears-ceiling guard, mirroring `SkillPathScopingWiringTest::testEveryShippedNudgeBudgetClearsTheTrackerCeiling` (`SkillPathNudge.php:129-137`).
- Docs ONLY if an env var is added: `docs/ENVIRONMENT.md` — the self-registering `EnvRosterDriftTest` walks `src/`/`bin/` reads both directions (`prompt_worklog.md:488`); recommendation: add **no** env knob, keep the budget in class constants, and drop `docs/ENVIRONMENT.md` from the ceiling. Plus the mandatory worklog/plan/resume stamps (§1.6/§3).
- Do NOT declare: `EngineBackend.php` (plan `:2464-2465`), `Chat.php`/`ContextCompactor.php` (resume `:595`), `RuleLoader.php` (plan `:2430-2431`), `SkillPathNudge.php` (single-subject, `:2427`).

**Risks spotted:**
1. **IntentTrigger trap (§7)** — deferral keyed on any-trigger-present moves the 8,278 B golden; must be `instanceof PathTrigger` (evidence chain above).
2. **Missing Runtime.php in the declared ceiling** → silent double-presentation (risk 1's twin).
3. **Path spelling**: PathTrigger is anchored, root-blind (`PathTrigger.php:18-26`, `:68-71`); tools resolve to absolute paths (`SkillPathNudge.php:14-15`) — matching root-relative rule globs against absolute paths under-delivers firing while `**/…`-style globs over-deliver (`/etc/passwd` matches `**`); the step must rule relative-vs-absolute vs both and pin it with a differential-style test (PathGlob's dialect makes leading-`**` semantics load-bearing, `PathGlob.php:69-75`).
4. **Budget geometry against shipped caps**: whole rule bodies are up to `MAX_FILE_BYTES=65536` while a Read/Grep/Glob nudge share is `intdiv(cap, 8)` (`Grep.php:320`, `Glob.php:423`, `Read.php:331`), i.e. 8,192 B on Grep/Glob — a 64 KiB rule can NEVER fit any shipped budget, so the deferral path is not the edge case, it is the common case for large rules; the tracker's `maxBytes()`/ceiling-guard pair must be re-derived (the `largestEntryCountWithin()`/`smallestUnclippedCallerCap()` discipline, `SkillPathNudge.php:335-367`), and Edit/Write pass no budget at all (`:203-205`) so their unbounded-result channel needs its own sized cap or the ruling's "budget" silently means ∞ there.
5. **Escape/forgery on the nudge channel**: splice bodies route through `PromptFence::escape()` (`Runtime.php:2637`) but the skill-channel precedent emits raw text in a `<system-reminder>` envelope (`SkillPathNudge.php:74-75`, `:297-312`) and the `system-reminder` tag joined the PromptFence roster (`PromptFence.php:62-63`); a whole rule body from an untrusted clone riding a tool result should be escaped or the step must record why not (§18's `<system-reminder>`-spoofability row, `prompt_plan.md:3909`); §16.4: `Rule::withTriggers()` stays dormant, roster item (`prompt_plan.md:2454-2456`).
6. **Fork/session-state**: the announced-marks pattern (`CarriesSessionState.php:11`, `markAnnounced` union `SkillPathNudge.php:403-433`) — deferred rules must not double-announce across forked tool children.
7. **Derived-census movers**: every added file auto-grows `BinSugarcrushWiringTest` (+1t/+6a per src file, `prompt_resume.md:137`), `GlobFigureDriftTest` (docblock paragraphs), `AssertionSwallowingCatchTest` (+3/try, `prompt_worklog.md:1659`), `TreeWideGuardRosterTest` — floor predictions must name them first (`prompt_resume.md:99`).
8. **§5 re-check is owed at staffing** even though everything reads CLEAR today (`prompt_resume.md:587`, `:594`, worklog `:1451` follow-up #5), and the resume-vs-plan contradiction on EngineBackend (`:594` "stays LIVE for P6.S5b" vs plan `:2464` "Shape A … reason to prefer B") should be settled in the brief, with the plan's cite winning and the decision recorded.
9. **Warm `vendor/` fixture landmine**: S5b tests must use synthetic temp HOMEs (S3 pattern; `putenv('HOME` precedent in `tests/Cli/RulesStateWiringTest.php` and the `BaseSystemPromptTest.php:1831` early-return), never new files under `tests/fixtures/prompt/home` (`prompt_plan.md:2433-2436`).
10. **Aggregate splice bound stays a separate step** — S5b shipping a byte-budget in the transient channel must not be logged as discharging the 20,313,188 B splice exposure (`prompt_worklog.md:1509`, `prompt_plan.md:2452`).