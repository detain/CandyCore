# P7.S5 premise — empty agent presets clobber built-in prompts (verified 2026-09-06 @ master 4e7f79014)

Verdict: PROCEED — mechanism confirmed; all drifted cites re-anchored; the fix seam is `Bootstrap::agentRoster()` (NOT AgentPresetRegistry, which has no built-in visibility). C7 still gates prompt text (two blockers live).

## 1. The three presets (CONFIRMED body-less)
`.sugar-crush/agents/{coder,reviewer,security-auditor}.md` — 15 lines each; `awk 'c==2{print}'` after closing `---` returns ZERO lines for all three. No `initialPrompt:` key in any.

## 2. The clobber chain (all current line numbers @ 4e7f79014)
- `Agent::fromPreset()` — src/Agents/Agent.php:248; `prompt: $preset->initialPrompt ?? ''` at **:256**. Docblock :234-247 (P5.S2 comment rewritten).
- `AgentPreset::$initialPrompt` — src/Agents/AgentPreset.php:61, `?string = null`.
- Registry `resolveInitialPrompt()` — AgentPresetRegistry.php:420-427: declared initialPrompt wins; else body; Null when neither ("AgentPreset's own 'no prompt' value is preserved" — :417-418). So body-less ⇒ null ⇒ `?? ''` ⇒ empty prompt.
- `Bootstrap::agentRoster()` — src/Cli/Bootstrap.php:1605-1646. Order: foreign :1621-1623 → built-ins :1625-1637 → native presets :1639-1641 (`$agents[$name] = Agent::fromPreset(...)` ⇒ name-keyed overwrite). Docblock :1551 `foreign < built-in < native preset`.
- Result on this checkout: native `coder`/`reviewer` (body-less) OVERWRITE the built-in `coder`/`reviewer` Agents with prompt `''`; the built-in factories (AgentDefinition.php:38/:54 — prompts :44/:60) still reach `executeSubAgent` (AgentManager.php:335 `definition()->prompt`) but NOT the live delegation path. `Agent::systemPrompt()` (Agent.php:428-433): empty prompt ⇒ environment block only.
- `security-auditor` (agentId: sa-001): no built-in counterpart — resolves to empty prompt regardless.

## 3. Built-in prompts (AgentDefinition.php)
:38 coder `You are a coding assistant focused on implementation...` (1 real instruction) · :54 reviewer `You are a code review specialist...` (:62-63 references `php-best-practices` and `security-audit` skills "you have been granted" — a SKILL-grant assertion, NOT a tool grant) · :91 debugger · :108 architect · :158 tester · :174 devops. NONE asserts a tool grant.

## 4. WorkflowEngine `prompt: ''`
5 sites — src/Workflows/WorkflowEngine.php:1042, :1152, :1252, :1294, :1397 (inline comments :1038, :1151, :1293). Comment `// system prompt is set via CompleteRequest` now at :3570 (drifted from 1039-era). NOT P7.S5 scope (different file; provider-channel issue).

## 5. C7 status — STILL INERT on live paths
AgentDefinition.php:23-28 docblock + :124-152 architect note record PARTIAL fix: `executeSubAgent()` (AgentManager.php:526, :540 `tools:`) does pass resolved tools via `resolveGrantedTools()` (:683-694) + `refuseCallOutsideGrant()` (:816-844) — BUT (1) no production caller hands AgentManager a tool registry (Bootstrap::agentManager() passes none ⇒ null) and (2) `executeSubAgent()` has NO production caller (live: `executeAll()` from Chat/WorkflowEngine ⇒ AgentWorkerPool, neither resolves granted tools). ⇒ HARD CONSTRAINT STANDS: no preset prompt text may assert a tool grant.

## 6. Load/parsing + existing validation seams
`parsePresetFile()` :355-376 throws RuntimeException only for read-fail / missing-frontmatter / invalid-YAML. NO body-empty validation exists. Frontmatter::parse — src/Support/Frontmatter.php (regex parser; unknown keys ignored).
BUT: `agentRoster()` ALREADY accumulates `$described` from `agentPresetTiers()` (:1899-1921) — keyed `.sugar-crush/agents/<name>.md: <message>` — and surfaces refusals via `Tui\AgentPresets` (`public function __construct(array $refused = [])` Tui/AgentPresets.php:26; `reportRefusals()` :34; wired Bootstrap:1102-1106/:1163; tested Tui/AgentPresetsTest.php 5 tests :38-:147). `Chat::__construct` already takes `array $agentPresetRefusals = []` (Chat.php:624-625, :736). ⇒ The recorded-reason surface EXISTS; the fix rides it.

## 7. Test baselines
- tests/Agents/AgentPresetTest.php: 27 tests / 99 assertions — nothing pins body-less→empty-prompt clobbering.
- tests/Agents/AgentDefinitionTest.php: 38 tests / 202 assertions.
- tests/Cli/AgentManagerWiringTest.php: 21 tests / 74 assertions — :315, :339-343, :377, :426-430, :509, :527, :555, :578, :602-605, :661, :678-682 all synthetic fixtures; NO test loads real `.sugar-crush/agents/` builtins.
- tests/Tui/AgentPresetsTest.php: 5 tests — exercises reportRefusals.
⇒ NO existing test asserts current clobber behavior; NO flips expected; suite-wide confirmation still required.

## 8. Collision rows
- `AgentDefinition.php` (:1051 row): NOT in P7.S5 file list; C7 gates PRESET PROMPT TEXT, not the file. No AgentDefinition.php change proposed.
- `AgentPresetRegistry.php` + `.sugar-crush/agents/*` + `tests/Agents/*` + `tests/Cli/*`: not collision rows.
- `src/Cli/Bootstrap.php`: P7.S3 window cleared + CONSUMED (`b289eaf44`). P7.S5 point-in-time re-check at staffing: lanes quiescent at round-60 ancestor `59411203c`; `git log 59411203c..HEAD -- src/Cli/Bootstrap.php` = own chain only. `agentRoster()` edited by `7e7f7695d`/`2c8c1c072` (merged, no foreign overlap).
- crush-lanes: never touched.
