# P7.S5 premise (CORRECTED v2) — empty agent presets clobber built-in prompts

> **SUPERSEDES `P7.S5-premise.md` v1 (committed `e9eca1f51`) and `P7.S5-forensic.md`.** v1 fabricated
> five anchors outright (`Tui\AgentPresets`, the `$described` collector, an `agentPresetTiers` return
> of `['presets','described','tiers']`, the `Chat::agentPresetRefusals` param, and
> `testBuiltinPresetFilesWinOverTheCodeDefinitions`). The forensic re-derivation killed all five but
> re-fabricated seven line numbers and three symbols of its own (verified 2026-09-07 against a tree
> byte-identical to the forensic's own base — same inputs, different answers, so the forensic numbers
> are not evidence). Every anchor below was re-read DIRECTLY on 2026-09-07 from master `9344e7546`;
> `git diff 4e7f79014 9344e7546 -- sugar-crush/` is byte-empty, so this describes the step worktree's
> base exactly. Spot-verified twice (verification delegation + orchestrator direct reads).

## 1. The three presets (CONFIRMED body-less)
`.sugar-crush/agents/{coder,reviewer,security-auditor}.md` — 15 lines each, each ending with the
closing `---` line followed by a newline (od-verified), nothing after it, no `initialPrompt:` key.
Last touched by `7ab653dea` (P0.S8); unchanged since. (v1 and the forensic said 14/13/15 with no
trailing newline — wrong.)

## 2. The clobber chain (CURRENT anchors, direct reads)
- `Agent::fromPreset()` — `src/Agents/Agent.php:260`; `prompt: $preset->initialPrompt ?? ''` at **:269**.
- `AgentPresetRegistry::resolveInitialPrompt()` — `src/Agents/AgentPresetRegistry.php:420-427`
  (private static); `return $body === '' ? null : $body;` at **:426**; its :417-418 docblock states
  the null is AgentPreset's own preserved "no prompt" value. Body-less ⇒ null ⇒ `?? ''` ⇒ empty prompt.
- `Bootstrap::agentRoster()` — `src/Cli/Bootstrap.php:1605-1644`. Native presets resolved FIRST (:1616,
  with the trusted-home throw rationale in the comment :1607-1615); foreign loop :1621-1623; the six
  built-ins :1625-1637 (`TYPE_CODER/REVIEWER/DEBUGGER/ARCHITECT/TESTER/DEVOPS` via
  `AgentDefinition::fromType($type, $type)`, keyed by `$definition->name` — name equals type);
  native loop **:1639-1641** overwrites the name key. Precedence narrative lives in the method
  docblock (ends :1604).
- Result on this checkout: native `coder`/`reviewer` (body-less) overwrite the built-in entries with
  prompt `''`; `security-auditor` ADDS a 7th entry with prompt `''` (no shadowed definition).
  `Agent::systemPrompt()` with empty prompt ⇒ environment block only.

## 3. Built-in prompts (`src/Agents/AgentDefinition.php`)
Six factories with NON-EMPTY prompts: coder :38, reviewer :54, debugger :91, architect :108,
tester :167, devops :184. `fromType()` :200-211. NONE asserts a TOOL grant; the reviewer's "skills
you have been granted" phrasing (:62-63 per v1, re-verify) is a SKILL-grant claim.
**`AgentDefinition::all()` DOES NOT EXIST** (nor the forensic's item-9 consumer rows
AppBuilder:128 / TeamPermissionBridge:118 / Team:601,616, nor `AgentManager::allDefinitions()` /
`definition()`) — enumerate the built-ins through `fromType` exactly as :1625-1637 does.

## 4. The record seam that actually exists (`src/Cli/Bootstrap.php`)
- `PROJECT_TIER_REFUSAL_FORMAT` — **:390**, `public const` = `'ignoring %s — %s'` (PUBLIC, not private).
- `private static array $projectTierRefusals = [];` — **:771**.
- `reportProjectTierRefusals(): void` — **:1412-1433**; dedups via `$reportedProjectTierRefusals`
  (`array_diff_key` :1414); NO `= []` reset (the forensic's "resets at :1431" is wrong); emits per
  path `sprintf(self::PROJECT_TIER_REFUSAL_FORMAT, $path, rtrim($reason, '.'))` into
  `warnPermissionConfigInTranscript()` (:1429-1431).
- `warnPermissionConfigInTranscript(): void` — **:4634-4662** (NOT :1439 — that range is the
  `agentManager()` docblock on this tree; the forensic's `fwrite(STDERR)`/`SUGARCRUSH_TEST_CAPTURE`
  lines do not exist in the body). Behavior: mb-clip to `LAUNCH_NOTICE_MAX_CHARS` (+clip suffix),
  in_array de-dup push to `$launchNotices` until `LAUNCH_NOTICE_LIMIT`, overflow to
  `$launchNoticesDropped`, then `warnPermissionConfigOnce($message)` :4660.
- `public static function launchNotices(): array` — **:4681** (synthesises the overflow row; public
  precisely so tests read the seam without capturing stderr).
- Report call sites: inside `chat()` (sig :849) at **:1168**, and at **:1973**. The comment block
  :1170-1173 carries seam-census PROSE ORDINALS ("TWENTY call sites now routed onto the transcript
  seam", "This said SIXTEEN") — a new `warnPermissionConfigInTranscript` call site makes prose stale;
  reconcile in-step with measured counts (P7.S3 precedent: docblock-only ordinal refresh).
- `Chat::__construct` (`src/Chat.php:485`) has **NO** preset/refusal parameter (forensic true); the
  nearest field `permissionConfigWarnings` :603 is a different feed. **P7.S5 does NOT touch Chat.php.**

## 5. C7 status — RE-DERIVED live 2026-09-07, constraint BINDS
`AgentManager::__construct` :71 with `private ?array $toolRegistry = null` :76; `resolveGrantedTools()`
is PRIVATE :797 and early-returns null without a registry; the only `new AgentManager(` in src/+bin/
is `Bootstrap.php:1482` and passes no registry. `executeSubAgent()` — `AgentManager.php:419` — has no
production caller (only prose hits); the live path is `executeAll()` :1146, which forwards a
caller-built request (:1162) and never resolves grants. ⇒ **no preset or inherited prompt text may
assert a tool grant.** (The forensic's :526/:589/:683-694/:704 numbers were wrong in both directions.)

## 6. WorkflowEngine `prompt: ''` sites — OUT of scope (plan files list excludes the file; v1 §4's
line numbers not re-verified; different subsystem — leave for its own step or the user).

## 7. Test baselines — re-measure solos at YOUR base before citing; v1/forensic numbers were wrong.
- `tests/Agents/AgentPresetTest.php` — measured **15 tests / 31 assertions** (filter AND path runs agree).
  v1 said 27/99, the forensic said 27 methods. Do not cite either.
- `tests/Cli/AgentManagerWiringTest.php` — v1 said 21 methods; re-derive. No test there or anywhere
  loads the REAL `.sugar-crush/agents/` bundle (forensic item 5 TRUE — zero occurrences of that path
  string in the file), and `testBuiltinPresetFilesWinOverTheCodeDefinitions` does not exist — the
  required real-bundle test is authored from scratch, there is nothing to "flip".
- `tests/Tui/AgentPresetsTest.php` **DOES NOT EXIST** (v1's coverage list named it; remove it).
- Nothing existing pins the body-less-to-empty clobber at roster level ⇒ NO existing test flips.
- Nearest HOME-pinning patterns for a real-root test: `tests/Agents/AgentPreset*Test.php` and
  `tests/Integration/ForeignAgentPresetWiringTest.php` (grep how `trustedConfigDirPath` is satisfied).

## 8. Collisions (§5) — QUIESCENT for this step, re-derived 2026-09-07
Last `AgentDefinition.php` touch `ec926e46e` (2026-08-25, in master); last `Bootstrap.php` touches are
this plan's own merged P7.S3 commits (below `4e7f79014`, already in the step base);
`AgentPresetRegistry.php` last `a921b834b` (2026-08-24). All three crush-lane worktrees are detached
clones at `59411203c` (an ancestor of master); their `master..HEAD` file hits are stale-lane-ref
artifacts (`merge-base --is-ancestor` proves both flagged commits ARE in master). Zero unmerged lane
work touches any file in P7.S5's scope. The step edits `src/Cli/Bootstrap.php` — this plan's standing
Phases-5-11 clearance covers it (record in the merge message, P7.S3 precedent); `AgentDefinition.php`
stays untouched, so the C7 row is consumed by READ ONLY.

## 9. Design ruling (orchestrator, 2026-09-07 — supersedes v1 brief R-5 and mandatory item 1)
Fix = MERGE at `agentRoster()`'s native loop (:1639-1641) with an in-prompt attribution sentence, and
for the no-twin case a recorded notice riding the EXISTING `warnPermissionConfigInTranscript()` seam.
NO `AgentPresetRegistry` change (its instances are transient inside `agentPresets()`; the roster sees
body-ness directly via `$preset->initialPrompt === null`; a parse-time static collector there would
duplicate state), NO Chat.php, NO new src files, NO `.sugar-crush/agents/*.md` content authored.
