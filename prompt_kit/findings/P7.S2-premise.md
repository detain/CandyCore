> Provenance: produced 2026-09-06 by premise-check reviewer `screeching-maroon-deer` (read-only delegation, prompt-architecture plan).
> Verdict: PROCEED-WITH-CORRECTIONS. Persisted verbatim by the bookkeeping writer; orchestrator decisions A–E live in `prompt_kit/briefs/P7.S2-step-brief.md`.

# P7.S2 Premise Check — SessionStart / UserPromptSubmit dispatch

**Date:** 2026-09-06 · **Reviewer:** researcher sub-agent (P7.S2 premise check, plan law §1.2)
**Code under review:** worktree `/home/sites/prompt-step-P7.S1` @ tip `78933f958` (clean; == P7.S1 built branch). All relative paths below are under `/home/sites/prompt-step-P7.S1/sugar-crush/`.
**Plan text:** `/home/sites/sugarcraft/prompt_plan.md:2561-2612` (P7.S2), context `:2505-2560` (Phase 7 header, P7.S1).

**Headline:** the step's *goal* is sound and every seam it needs exists, but **three of its factual claims are wrong as written** — most importantly claim 2 (HookDispatcher is constructed by **nothing** in src/, not by TaskList.php:281) and claim 4's implicit assumption that a "start of conversation" discriminator exists in Chat.php (it does not). None of the corrections change the file list or the done-when; all are plan-text fixes plus two design decisions the orchestrator must make before briefing.

---

## Claim 1 — HookManager has no sessionStart()/userPromptSubmit()/stop()/preCompact()

**VERDICT: CONFIRMED**

`src/Hooks/HookManager.php` is **231 lines**. Complete public surface (file:line):

| Method                                                                                       | Line |
| -------------------------------------------------------------------------------------------- | ---- |
| `__construct(HookRegistry $registry, ?string $configFile = null, ?string $projectRoot = null)` | :37  |
| `loadFromFile(string $file): self`                                                             | :64  |
| `loadEntries(array $entries, string $source = 'config'): self`                                 | :85  |
| `registerBuiltIns(): self`                                                                     | :121 |
| `register(string $event, HookInterface $hook, int $timeout = null): self`                      | :131 |
| `hook(string $event): array`                                                                   | :144 |
| `preToolUse(HookContext $context): HookResult`                                                 | :158 |
| `resolveAsk(HookResult $ask, bool $approved, string $feedback = ''): HookResult`               | :180 |
| `postToolUse(HookContext $context): HookResult`                                                | :215 |
| `applyPreHooks(string $command, string $args): array{string,?HookResult}`                      | :223 |

No `sessionStart`, `userPromptSubmit`, `stop`, or `preCompact`. The dispatch methods are one-liners over the registry:

```php
// src/Hooks/HookManager.php:158-161
public function preToolUse(HookContext $context): HookResult
{
    return $this->registry->executeHooks(HookEvent::PreToolUse->value, $context);
}
```

`postToolUse` (:215-218) is identical with `HookEvent::PostToolUse->value`. **A new event method is a 2-line clone** — the plan's "wire the dispatch" work is entirely on the *call-site* side, not the HookManager side.

---

## Claim 2 — HookDispatcher: 586 lines, 11 dispatchX(), "constructed … at TaskList.php:281"

**VERDICT: WRONG** (line count and method count confirmed; the construction claim is false)

- `src/Hooks/HookDispatcher.php` is exactly **586 lines** ✓ and has exactly **11 `dispatchX()` methods** ✓ over a shared core `dispatch()` at :146: `dispatchPreToolUse` :532, `dispatchPostToolUse` :537, `dispatchStop` :542, `dispatchSubagentStop` :547, `dispatchSessionStart` :552, `dispatchSessionEnd` :557, `dispatchUserPromptSubmit` :562, `dispatchPreCompact` :567, `dispatchTeammateIdle` :572, `dispatchTaskCreated` :577, `dispatchTaskCompleted` :582.
- **`new HookDispatcher` appears NOWHERE in `src/`.** Only construction sites in the whole lib are tests: `tests/Hooks/HookDispatcherTest.php:46,55,236,259,317,322` and `tests/Agents/TaskListHooksTest.php:261,296,308`.
- `src/Agents/TaskList.php:281` is real but is a **call site, not a construction**:

```php
// src/Agents/TaskList.php:271-283 (abridged)
$dispatcher = $this->hookDispatcher;
if ($dispatcher === null) {
    return self::hookFallbackNote('no dispatcher');   // ← production always lands here
}
...
$result = $dispatcher->dispatchTeammateIdle($context);
```

  The dispatcher is a **nullable promoted ctor param** — `private readonly ?HookDispatcher $hookDispatcher = null` (TaskList.php:49) — and the **only src/ construction of `TaskList`** is `src/Agents/Team.php:37`: `new TaskList($this->basePath() . '/tasks.sqlite')` — single-arg, **no dispatcher injected**. So in production the guard at :271 always takes the null arm and `HookDispatcher` has **zero live instantiations anywhere**.
- **additionalContext reconciliation:** moot at the dispatcher layer and confirmed-dropped there. `HookDispatcher::dispatch()`/`scan()` (:146-358) contain **zero occurrences of `additionalContext`**, and its return type `HookDispatchResult` (180 lines) has **no such field at all** (props: exitCode, event, message, continueOnBlock, context, modifiedInput). P7.S1's additionalContext plumbing lives exclusively on the `executeHooks(): HookResult` path (claim 9). Even if TaskList did inject a dispatcher, teammate-idle notes could not carry additionalContext.
- The class self-documents its dormancy honestly at `HookDispatcher.php:124-129`: "Nothing routes PreToolUse here today — TaskList is the only wiring and it dispatches TaskCreated/TaskCompleted/TeammateIdle only — dormant seam." Even that docblock overstates: nothing injects TaskList a dispatcher, so not even those three fire in production.

**Consequence for P7.S2:** the plan's framing ("dispatch already exists in a dormant HookDispatcher — wire it up") is misleading. HookDispatcher is a **parallel, dead, lossy implementation** (no ASK arm — it fails closed; no additionalContext). Copying its shape would re-implement the weak path. The strong path is `HookManager::preToolUse` → `HookRegistry::executeHooks(): HookResult` (claims 1, 9).

---

## Claim 3 — SessionStart / UserPromptSubmit exist in the vocabulary; registered-but-never-dispatched = silence

**VERDICT: CONFIRMED**

- Enum `src/Hooks/HookEvent.php` (100 lines) carries all 11 cases; `case SessionStart = 'SessionStart';` at **:42**, `case UserPromptSubmit = 'UserPromptSubmit';` at **:44**. It even ships per-event semantics that P7.S2 can consult: `discardsOnBlock()` includes UserPromptSubmit (:84) and `stderrToUserOnly()` includes PreCompact + SessionStart (:95-96).
- **The event-name allowlist is `HookEvent` itself, enforced in `HookConfig::parse()` at src/Hooks/HookConfig.php:233:**

```php
// src/Hooks/HookConfig.php:233
if (!is_string($event) || HookEvent::tryFrom($event) === null) {
    throw new \InvalidArgumentException("Unknown hook event {$event} ...");
}
```
  Unknown events are hard refusals (exit 2 via `Bootstrap::hooks()`). Both target names pass.
- A user can TODAY write a `SessionStart` hook in hooks.yaml/JSON: it parses, and `HookManager::loadEntries()` (:107-114) registers it into `HookRegistry::$hooks['SessionStart']` (bucket declared at HookRegistry.php:12-25 — note that map also has a dead `'Notification'` bucket with no enum case, an existing curiosity, not P7.S2's problem).
- Nothing ever dispatches it. `executeHooks()` production call sites are **only two** — `HookManager::preToolUse` (:160) and `postToolUse` (:217) — consumed by `Runtime.php:1902/2009` and `Chat.php:3575/3671`. All other nine events are **parse → register → total silence**. That is exactly the seam P7.S2 wires.

---

## Claim 4 — Chat.php insertion seams

**VERDICT: PARTIALLY — (b) UserPromptSubmit seam exists and is threadable; (a) "where a NEW SESSION assembles its first provider request" DOES NOT EXIST as a discriminator — design decision required.**

`src/Chat.php` is **12,760 lines**.

**(b) UserPromptSubmit — seam is `submit()`'s tail, and hook text placed there goes onto the wire, not into a DTO:**

```php
// src/Chat.php:6084-6100 (end of submit(), which starts :5865)
$newTurnMessages = [];
...
$newTurnMessages[] = Message::user($text);

return $this->dispatchTurn($baseHistory, $newTurnMessages, $tokenLimit);
```

`dispatchTurn` (:6497-6602) merges verbatim into model state:

```php
// src/Chat.php:6563-6564
$next = $this->mutate([
    'history' => [...$baseHistory, ...$newTurnMessages],
    ...
```

and `scheduleBackendCompletion` (:7656+) takes `$history = $next->history` (:7659) and feeds it straight into the backend `Cmd::promise` (:7733). So inserting a `Message::system($note)` into `$newTurnMessages` at :6086-6099 is **provably request-bound** — and there is in-codec precedent three lines up: dispatchTurn itself conditionally appends `$this->contextReminderMessage(...)` to `$newTurnMessages` at :6558 (with stale-reminder stripping at :6556). Caveat for the brief: `submit()` has early arms *before* :6084 — mid-turn queue via `enqueuePrompt` (:5888-5898, :6161), custom-command expansion (:5917), `dispatchCommand` (:5934), spend cap (:5944), idle compaction (:5954-5957), 85/95 token tiers (:5974-6082). UserPromptSubmit must fire **after** those (a queued or slash-command input is not a plain submitted prompt — upstream fires the hook on real user prompts; the orchestrator should pin the fire point to the :6084 tail so blocked/mid-turn/queued paths have defined behavior).

**(a) SessionStart — no seam exists.** Findings:
- `init()` (:1193-1196) returns only `runtimeNoticeWake()` and TEA `init()` cannot mutate the model (returns `?Closure`).
- There is **no `count($history) === 0` / first-turn discriminator anywhere near `submit()`** — grep of Chat.php for session-start conditions finds none.
- Chat does know its session: `private ?string $currentSessionId` (:259, ctor :510, assigned :1138) — null for a fresh session until autosave assigns one; and `handlePaletteNewSession()` at :11228 is the mid-run "new session" path (upstream's `source: clear` analogue).
- Practical candidates, in order of least plumbing: **(1)** fire in `submit()` when `count($this->history) === 0`, prepend the hook `Message::system(...)` into `$newTurnMessages` **before** `Message::user($text)` (:6084-6099) — matches the plan's "start of conversation, before the first prompt" literally and reuses the proven request path; **(2)** fire at construction/`withHooks`/`Bootstrap::chat()` (:1034-1098 — the live wiring point; `hooks: self::hooks($permissionGate, $root)` at :1058) — but Chat is immutable and dispatch there needs an async command, and would also have to cover `handlePaletteNewSession`/resume. **The plan text assumes (a) already exists; it does not.** This is a plan correction, not a blocker.

**Pattern to copy — `applyPostToolUse()` sits at Chat.php:3661-3695**, paired with the pre-gate `gateToolCall()` at :3554-3602 (builds `HookContext(sessionId, toolName, toolArgs, toolInput, toolOutput, model:'', provider:'', projectRoot)`). applyPostToolUse's consumption shape is the P7.S2 template:

```php
// src/Chat.php:3676-3686 (abridged)
$note = $hookResult->additionalContext;
if ($note !== '') {
    // append model-visible context to the tool result content
}
// empty note => byte-identical no-op
```

Live `HookManager` on Chat is guaranteed in production: `private readonly ?HookManager $hooks = null` promoted ctor param (Chat.php:615), `withHooks()` :5294, wired by `Bootstrap::chat()` :1034-1098. **Null-safe `$this->hooks?->...` is still required** (tests construct Chat without hooks; ctor param defaults null).

---

## Claim 5 — mid-conversation role:"system" survives to the wire

**VERDICT: CONFIRMED on every production driver. The plan's "PREFER appended role:system message" is executable TODAY with zero new serialization plumbing.**

Full chain, file:line:
1. `EngineBackend::complete()` (src/Backend/EngineBackend.php:585) → `App::new()->withMessages($this->toTypedMessages($history))` (:611).
2. `toTypedMessages()` (:1588-1600): `match ($message->role->value) { 'user' => UserMessage, 'assistant' => AssistantMessage, default => new SystemMessage(...) }` — **unknown/system roles become SystemMessage; nothing dropped, order preserved.**
3. `Runtime::run()` (src/Backend/Runtime.php:1101) → `buildMessages()` (:2447-2458): passes **every** Message object through; **no role filter anywhere**. Result rides in `CompleteRequest(messages, systemPrompt)`.
4. Provider mappers:
   - **OpenAIProvider::formatMessages :168** — `SystemMessage => ['role'=>'system','content'=>...]` **inline at its array position** (top-level systemPrompt separately prepended as `messages[0]` at :129).
   - **CustomProvider** (this IS the anthropic driver — `ProviderFactory::createAnthropic` :625-657 returns `new CustomProvider('anthropic', ...)` with `x-api-key`/`anthropic-version` headers): :312 same inline mapping; empty systemPrompt treated as null at :211.
   - **BedrockProvider** :162/:209 — filters SystemMessages out of `messages` and **hoists them into top-level `system` blocks preserving relative order** (:322-347).
   - **VertexProvider** — same hoist via `systemInstruction()` :577-602.
   - **SglangProvider** :1555 inline, but its own comment :1518 warns the server-side chat template only reliably renders `messages[0]` system content.
   - ClaudeCodeProvider :323 flattens to `System: {content}` prompt text.
   - **No mapper drops a mid-conversation SystemMessage.**
5. **The repo already relies on this:** `contextReminderMessage` (Chat.php:12044) is a mid-conversation `Message::system(...)` appended per due turn and sent every time — plus `SglangProvider.php:1508` cross-references "(Chat.php :7490) sends a lone history SystemMessage with a null prompt", and `VertexProviderTest` pins the hoist.

Residual risks to acknowledge in the brief (not corrections): providers that API-enforce system-first (Sglang caveat); Anthropic's real API accepts `system` only top-level, which the CustomProvider's inline mapping sends inside `messages` — worth one adversarial test but the existing context-reminder feature has shipped this exact shape indefinitely, so the precedent holds.

---

## Claim 6 — E2E "hook text in the request payload" test seam

**VERDICT: CONFIRMED — seams exist; no plan problem. But one trap: the provider-level double + Chat async fork don't mix (pcntl).**

Two ready seams:

1. **Backend-boundary wire recorder (recommended):** `tests/Chat/ContextReminderDedupTest.php:456-496` — `final class ReminderWireRecorder implements Backend, ReportsContextWindow` records every turn's full `$history` (`calls()` :468, `lastHistory()` :474; both `complete`/`completeAsync` return canned assistant messages). The file already contains the exact assertion the done-when demands, for the context-reminder feature (:201-205):

```php
// tests/Chat/ContextReminderDedupTest.php:201-205 (pattern)
$wire = $backend->lastHistory();
$this->assertSame(Role::System, $wire[count($wire) - 1]->role);
// + content assertion on the same message
```

   Drive pattern: keystroke `KeyMsg(KeyType::Char)` ×n then Enter (:420-427); resolve the returned `Cmd::promise` with the local `resolve()` helper (:430-440). This proves the hook text is in the array sent to the provider layer and that it is `role: system` — one boundary short of the literal JSON.
2. **Provider-level CompleteRequest capture:** `tests/Commands/RulesCommandTest.php:553-611` — anonymous `implements ProviderInterface` with `public array $requests` capturing `complete(CompleteRequest)` (:596), used at :484-498 via `(new EngineBackend($provider, ...))->complete([...])` with the docblock philosophy at :460-475: "assert on the bytes the model receives, not an accessor." **Trap:** Chat's turn goes through `completeAsync`, which **forks under pcntl** (EngineBackend.php:941-975 only uses `completeAsyncBlocking` when pcntl is absent) — so a Chat-driven test would write `$requests` in the child and the parent assertions see nothing. A P7.S2 provider-payload test must therefore either **feed `$next->history` into `EngineBackend(recordingProvider)->complete()` synchronously** (two-step: drive Chat → capture history → complete) or run seam (1). `tests/Integration/ContextWindowWiringTest.php:75` proves full `new Chat(backend: new EngineBackend(new FixedWindowProvider(...)))` construction is cheap.

**Second caveat:** `HookGateE2ETest` (the plan's named test file) is **not actually E2E** — see claim 7.

---

## Claim 7 — Test baselines

**VERDICT: CONFIRMED (counts) — with a naming caveat.**

- `tests/Hooks/HookManagerTest.php`: **28 test methods** (639 lines).
- `tests/Hooks/HookGateE2ETest.php`: **4 test methods** (106 lines) — all four unit-drive `HookManager::preToolUse()` with a hand-built `HookContext`; **no Chat, no Backend, no provider anywhere in the file.** Its "E2E" is in the name only; the done-when's request-payload assertion cannot land here without turning it into a genuinely different test — putting a Chat-driven wire test in `tests/Hooks/` vs `tests/Chat/` is an orchestrator call (all real wire precedents live under `tests/Chat/`).
- `tests/Hooks/` totals 18 files incl. HookDispatcherTest, HookDispatchResultTest, ScriptHookTest, HookEventTest, HookContextTest, HookConfigTest, HookRegistryTest, HookResultTest.

---

## Claim 8 — File sizes / line anchors

**VERDICT: PARTIALLY — sizes exact; the one cited line number is arithmetically right but semantically mislabeled.**

| Item                      | Plan says                        | Actual                                                                                                                                              |
| ------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| HookDispatcher.php length | 586                              | **586 ✓**                                                                                                                                               |
| HookManager.php length    | —                                | 231                                                                                                                                                 |
| Chat.php length           | —                                | 12,760                                                                                                                                              |
| `TaskList.php:281`          | HookDispatcher **constructed** there | :281 is the **`dispatchTeammateIdle` call**, dispatcher is a null-defaulted ctor param (:49), never injected in src/ → **no construction anywhere** (claim 2) |
| HookRegistry.php length   | —                                | 843 (grew in P7.S1)                                                                                                                                 |

No other `:NNN` anchors appear in the P7.S2 step text. Drift on :281 itself is 0 lines; the error is what the line *is*, not where it is.

---

## Claim 9 — What P7.S1 changed; the shape P7.S2 builds on

**VERDICT: CONFIRMED — P7.S1's HookManager diff is minimal and the shape P7.S2 consumes is stable.**

`git diff ad1020b2e 78933f958 -- sugar-crush/src/Hooks/HookManager.php` (per prior evidence pass): **only** `resolveAsk()` threads `$ask->additionalContext` through its deny/allow/modify arms (docblock :190-197 + param propagation) — file went 223→231 lines. Nothing else in HookManager moved.

The consumed shape, as it NOW stands (`src/Hooks/HookRegistry.php:325` `public function executeHooks(string $event, HookContext $context): HookResult`):
- Script-hook **stdout on exit 0 → `HookResult->additionalContext`**, byte-capped at `HookResult::MAX_ADDITIONAL_CONTEXT_BYTES = 10000` with retained-overflow file path (R-2), accumulated across up to `MAX_REWRITE_PASSES = 4` rewrite passes (scan 4-tuple at :351, collection :343-368).
- ASK arm (:436) and allow arm (:455-457, `withContextSet($additional)`) both carry it; deny (:385) returns verbatim.
- `HookManager::resolveAsk()` (post-P7.S1, :180-210) preserves additionalContext through the user's approve/deny/modify decision.

**For P7.S2 this means:** `sessionStart()`/`userPromptSubmit()` calling `executeHooks()` get additionalContext capture **for free** — no new plumbing, only the consumer decision (turn it into a wire message).

---

## Additional load-bearing facts the brief must carry

- **`HookContext` is tool-shaped** (src/Hooks/HookContext.php, 82 lines): `sessionId, toolName, toolArgs, toolInput, toolOutput, model, provider, projectRoot` + three `with*`. It has **no prompt/source field**. UserPromptSubmit's prompt text must ride `toolInput` (zero ripple — what ScriptHook already reads) or the field additions ripple through 3 `with*` methods + every construction site (ScriptHook env, HookManager gates, Chat gateToolCall, ~18 test files).
- **ScriptHook has NO stdin JSON payload** — the pipe is opened and `fclose`d empty (ScriptHook.php:576); scripts receive context **only via env**: `CRUSH_SESSION_ID/CRUSH_TOOL_NAME/CRUSH_MODEL/CRUSH_PROVIDER` (:533-538) + `CRUSH_TOOL_INPUT`/`CRUSH_TOOL_OUTPUT` staged file paths (:513-516). A SessionStart script today can't tell startup vs resume vs clear; a UserPromptSubmit script can only see the prompt if it's smuggled through `CRUSH_TOOL_INPUT`. Exit mapping :632-661 (`EXIT_ALLOW(0)` → `HookResult::allow('', HookContextFiles::bound($output, MAX_ADDITIONAL_CONTEXT_BYTES))` — the P7.S1 line that makes stdout→additionalContext work today).
- **Collision facts (task's ALSO):** P7.S2 touches `src/Chat.php` — that row was CLEARED for Phase 7 by P7.S1's R-1 dual wiring (plan :2505-2559). `src/Agents/TaskList.php` is **cited as evidence only; it is not in P7.S2's file list and is not in the §5 live-row vocabulary** — no collision. My findings additionally reference (read-only): HookRegistry.php, HookResult.php, HookEvent.php, HookConfig.php, HookContext.php, HookDispatchResult.php, ScriptHook.php, Runtime.php, EngineBackend.php, Bootstrap.php, Team.php, and the four test files named in claims 6-7.

---

## VERDICT: PROCEED-WITH-CORRECTIONS

The step is buildable exactly once on the P7.S1 branch; the done-when is achievable with existing seams; but the plan text must be fixed or the implementer will chase a dead class.

**Plan-text corrections required:**
1. **(Claim 2 — loud.)** Replace "constructed by nothing in src/ except Agents/TaskList.php:281" with: *"instantiated by NOTHING in src/; TaskList.php:281 is a call site behind a null-defaulted `?HookDispatcher` ctor param (:49) that production never injects (sole `new TaskList` = Team.php:37, single-arg) — HookDispatcher is fully dead code."*
2. **(Claim 2 corollary.)** Drop any framing that P7.S2 should "wire up"/route through HookDispatcher: its `dispatch()` never reads `additionalContext` (zero occurrences) and its `HookDispatchResult` has no such field — routing the new events there would strand P7.S1's output and lose the ASK arm. The live spine is `HookManager::{preToolUse,postToolUse}` → `HookRegistry::executeHooks(): HookResult`.
3. **(Claim 4a.)** Remove the assumption that a session-start seam exists: Chat.php has **no first-turn discriminator**; `init()` (:1193-1196) can't mutate the model. The brief must specify the chosen SessionStart fire point (decision A below).
4. **(Claim 7 caveat.)** Note that `HookGateE2ETest` is a 4-test unit file despite its name — a real request-payload test is new behaviour, not one more method in that file (placement = decision D).
5. **(Minor.)** `HookRegistry::$hooks` contains a `'Notification'` key with no `HookEvent` case (dead bucket, HookRegistry.php:12-25) — harmless, but do not cite the bucket list as the event vocabulary; the enum is.

## Design decisions the orchestrator must make BEFORE briefing

**A. SessionStart fire point (claim 4a).** Recommended: **inside `submit()`, guarded by `count($this->history) === 0`, prepending `Message::system($note)` to `$newTurnMessages` before `Message::user($text)` at :6084-6099** — literal match to the plan's "start of a conversation, before the first prompt", one fire point, rides the proven request path, trivially testable (fresh Chat has empty history). Rejected alternatives: `Bootstrap::chat()`/`withHooks` (immutable-model problem, async-command plumbing); `init()` (cannot mutate). If resume/new-session-mid-run coverage is demanded, add a second fire in `handlePaletteNewSession` (:11228) — but the done-when doesn't require it; ship first-turn-startup only and document the gap.

**B. Message mechanism: role:system vs `<system-reminder>` (claim 5).** role:system is **executable today** on all five wire providers (OpenAI/Anthropic-via-CustomProvider inline; Bedrock/Vertex order-preserving hoist; repo precedent = `contextReminderMessage` Chat.php:12044 sent every due turn). Keep the plan's PREFER. One caveat to fold into the brief: SglangProvider:1518 warns mid-conversation system content may be template-ignored — accept, it's a non-primary driver.

**C. Context payload transport (additional facts).** Prompt text rides `HookContext::toolInput` (decision to make: `toolName = 'UserPromptSubmit'` sentinel vs empty — upstream sends `prompt` in JSON but sugar-crush's script protocol is env-only). For SessionStart: pass `model`/`provider` if cheaply available, else empty as `gateToolCall` already does. Decide NOW whether to extend `HookContext` with `prompt`/`source` fields (cleaner env names, ripple: 3 `with*` + all ctor sites + tests) or document `toolInput`-as-prompt smuggling (zero ripple). Either is defensible; smuggling keeps P7.S2 inside its 5-file list — extending HookContext does NOT (adds src/Hooks/HookContext.php + ScriptHook.php to the blast radius; ScriptHook env names also need `CRUSH_PROMPT`).

**D. Dispatch class + test placement.** Methods go on **HookManager** as `sessionStart()`/`userPromptSubmit()` 2-line `executeHooks` clones (claim 1) — do not build on HookDispatcher. Consumers: null-safe `$this->hooks?->...` in `Chat::submit()` mirroring `applyPostToolUse`'s empty-note-no-op contract (:3676-3686). The plan's file list already fits: `src/Chat.php` gains the two call sites; `HookDispatcher.php` arguably shouldn't be touched at all (plan lists it — orchestrator may want to downgrade it to "docs note about remaining dormant events" or drop it from scope). Test: new file under `tests/Chat/` (e.g. `SessionStartHookWireTest`) using the `ReminderWireRecorder` pattern (assert `Role::System` + text in `lastHistory()`, ContextReminderDedupTest.php:201-205) — and if the done-when's literal "provider receives" wording must hold, the two-step `EngineBackend(recordingProvider)->complete($next->history)` from RulesCommandTest:484-498; **never** a Chat-driven `completeAsync` + provider double in-parent (pcntl fork trap, claim 6).

**E. Semantics edge to pin in the brief:** `HookEvent::discardsOnBlock()` says UserPromptSubmit blocks discard the *prompt* (:84) and `stderrToUserOnly()` covers SessionStart (:95-96) — P7.S2 should honor or explicitly defer these enum-declared semantics; the plan text is silent, and `applyPreHooks`' deny path shows what honoring looks like.