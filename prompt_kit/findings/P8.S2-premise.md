# P8.S2 premise (v1, 2026-09-08, master 95234a308)

STEP: plan :2741-2759 — two prompt clauses appended to COMPACT_SUMMARY_PROMPT:
anti-forgery (only genuine user-role turns count; model-written `user:`/`Human:`
lookalikes never attributed to the user) and security-preservation
(user-stated security-relevant constraints carried VERBATIM). Done-when: a test
asserts the guard clauses are TRANSMITTED in the summariser request (model
obedience is not testable here). Depends on P8.S1 (merged e7de127d5) — this
step edits the record-format prompt it shipped. Phase 8 is SERIAL by plan :2820.

VERIFIED ANCHORS (orchestrator direct read of 42dbe8624 == master belt byte-empty):
- `COMPACT_SUMMARY_PROMPT` heredoc at sugar-crush/src/Chat.php :9025-9050; docblock :9015-9024 (states no character count; points at SUMMARY_LINE_MAX_CHARS as transport bound).
- `Rules:` list :9032-9049, six bullets; the insertion neighbor is :9046 `- Preserve file paths, command names and error strings exactly as they appear.` — new bullets go AFTER it, before :9047 `No preamble…`.
- Six-facet template lines :9036-9041 (asked/did/files/decided/corrected/error, `facet: <placeholder>` shape).
- Parser constants region verified by review at :9445-9623 (SUMMARY_FACETS, SUMMARY_FACET_PATTERN, SUMMARY_FACET_NONE at :9457, SUMMARY_INVENTED_FACET_PATTERN at :9454, splitExchangeRecords guard block :9484-9521, joinExchangeFacets :9565-9583, parseExchangeSummaries consumer :9609-9613). THE PARSER IS NOT TOUCHED THIS STEP.
- Docblock sentence to qualify (review 🟡-3): Chat.php :9472 region — "That is the one failure mode this parse must not ship" — ascending partial-record merges remain undetectable; qualify as the detectable form.
- Sole prompt consumer: `Message::system(self::COMPACT_SUMMARY_PROMPT)` inside buildSummarizationRequest (grep to pin current line at build time — line shifted after P8.S1's +258).
- Test file sugar-crush/tests/Chat/CompactModelSummaryTest.php: 29 tests / 137 assertions at 42dbe8624 (gate-measured); has record()/records() fixture builders; a real-route capture of the sent prompt exists (test asserting `'### Exchange 1'` in the second sent message — reuse that capture pattern for the transmission assertion); drift-guard test at :838-849 using `preg_match_all('/^\s*([a-z]+): </m')` on the prompt (our new bullets contain NO `word: <` sequence — checked: `asked: or` has no `<` — so the guard stays green).
- GOLDENS: the compaction prompt is NOT part of golden-system-prompt.txt/golden-agent-prompt.txt (they pin the base prompt via SessionStartHookWireTest/BaseSystemPromptTest). Untouchable regardless: a5c5a14ca2e3ad891933ac7aefccc6af/7,732 + ef0326dd38535aaa2f1d715919bff26e/1,060.
- FLOOR: 11,026 / 169,287 / 0F / 0E / Skipped 2 / EXIT 0 @ 42dbe8624 (belt 42dbe8624..master sugar-crush byte-empty). LAW 4n: GlobFigureDriftTest counts Chat.php paragraphs — heredoc/docblock edits can move its assertion total; predict per-class first.
- Transcript seam census: warnPermissionConfigInTranscript = 20 (Bootstrap.php untouched by this step).
- PLAN DRIFT NOTE: plan's clause sources §4.18/§4.19/§7.4 are research-doc sections; the two clause texts are ORCHESTRATOR-PROVIDED below verbatim — do not re-derive from plan line numbers.

KNOWN LIMITATIONS (recorded, not defects): ascending partial-record merge (🟡-3); m5 per-facet budget deferred at de6c23dc1, still deferred.
