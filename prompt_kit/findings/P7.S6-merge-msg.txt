Merge branch 'prompt/P7.S6': P7.S6 wires the foreign memory importer

P7.S6 lands the /memory import command in front of the ForeignMemoryImporter that shipped dormant, plus its re-import sentinel. The step shipped once with a headroom clamp that bounded agent-scope imports against MemoryBlock::MAX_ENTRIES; direct reads disproved that premise, since capture() folds the project scope only, so agent-scope imports can never crowd a project note out of the prompt. The user chose Option A: keep the tested importer, delete the clamp, restore the rationale in code and docs. Two review passes then closed the residue: importer and architecture docblocks that still claimed the class unwired, a bootstrap comment overstating the symlink write-gate, and the missing behavioral test for the sentinel refusal path.

Gate at tip da178befb: 11018 tests, 169219 assertions, 0 failures, 0 errors, 2 skipped, exit 0. Goldens unmoved. Transcript seam census 20. The gated sugar-crush tree is byte-identical to this merge after it lands. Independent gate CLEARED, scoped re-review APPROVE; carried forward non-blocking: the sentinel refusal is pinned by discriminating substrings rather than a byte-exact literal.
