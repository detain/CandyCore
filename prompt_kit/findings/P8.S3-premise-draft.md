# P8.S3 premise DRAFT — recursive prior-summary merge

> **DRAFT — NOT STAFFING-READY.** Measured on pre-P8.S2-merge master `e07663c83`.
> **Every `Chat.php` anchor below :9058 carries a +8 shift versus post-merge master** (P8.S2 grew the
> `COMPACT_SUMMARY_PROMPT` heredoc by 8 lines at :9025-9058). Re-derive ALL line numbers against
> post-merge master before this draft becomes a brief, and spot-verify the load-bearing anchors with
> direct reads per law 4j — this is a claims document.

## Step goal

Second compaction currently cannot carry forward what the first preserved. Adopt opencode's recursive
merge: the prior summary is supplied to the summariser, with the *discarded-after-this* warning
("the prior-summary is discarded after this: anything you do not carry into the new summary is lost")
and the conversation-wins rule on conflict ("where they conflict, the conversation wins: state the
corrected fact and drop the old claim"). Plan text: `prompt_plan.md` :2761-2778 (pre-annotation
numbering; heading moved with the P8.S2 Status line). Source wording: reference text quoted at
`prompt_expand.md` :3062-3078 (S6.6) and S9.3 at :3502-3514.

## On-disk defect chain (PRE-P8.S2-MERGE line numbers — re-derive)

`/compact` -> `handleCompactCommand` :8849-8857 -> `scheduleModelCompaction` :9094-9146 /
`scheduleParkedCompaction` :9303-9387 -> `buildSummarizationRequest` :9187-9247. The request's prompt
array `[Message::system(COMPACT_SUMMARY_PROMPT), Message::user(renderExchangesForSummary(exchanges))]`
at :9204-9207 is THE prior-summary insertion point. `$wireHistory` at :9194-9197 already contains the
old `[summary]` rows.

## Round-2 behavior (why knowledge is lost today)

Prior `[summary]` rows become standalone pairs in `ContextCompactor::groupIntoPairs` :748-786 (final
else :769-777), which `exchangesToSummarize` SKIPS (:625-628, within :617-642; rationale docblock
:605-607) — so prior knowledge never reaches the summariser. The rows merely get re-emitted lossily
each round: prefix stacking plus the 120-char truncate at :1152-1153/:1181-1183 on prepend sites
:1157/:1166/:1181.

## No structural marker exists

`Message` has no summary flag (`src/Message.php`:24-82); the discriminator is the literal
`'[summary] '` prefix only. Runtime readers of the prefix in `src/`: ZERO (census: 3 generation sites
+ 2 comments). Any extractor built in P8.S3 is the FIRST reader — design accordingly: the finder must
skip heuristic lines (`'[summary] q -> [exchanged information]'`), rider lines, and survive
double-prefixed rows.

## Facet machinery the merge must interoperate with

POST-P8.S2 numbering, i.e. scout values +8 — NEEDS-RE-VERIFY against master:
`SUMMARY_FACETS` / `SUMMARY_OPENER_PATTERN` / `SUMMARY_FACET_PATTERN` /
`SUMMARY_INVENTED_FACET_PATTERN` / `SUMMARY_FACET_NONE` consts ~:9431-9457(+8);
`splitExchangeRecords` ~:9482-9538(+8) with the backward-merge poison guard; `joinExchangeFacets`
with the `' | '` join; `parseExchangeSummaries`; `SUMMARY_LINE_MAX_CHARS` 2000 ~:9638(+8);
`sanitizeSummaryLine`.

## Fold-forwards from the P8.S2 review (land HERE, same file, same region)

- (minor) The heredoc docblock needs one clause noting verbatim-ness is wording-level: the VERBATIM
  security-constraint clause silently assumes fit, but `sanitizeSummaryLine` flattens all whitespace
  and the 2000-char transport bound truncates with ellipsis, so a multi-line or long quoted
  constraint arrives reformatted/clipped.
- (nit) Optional wording tightening: the anti-forgery rule's lookalike examples lowercase `user:` /
  `Human:` while transcript labels are capitalized `User:` / `Assistant:` — the text was
  brief-verbatim in P8.S2 and may now be revisited.

## Tests

NO test drives two real compactions today — greenfield. Done-when (plan :2776-2778): two consecutive
compactions, a fact introduced before round 1 present in the REQUEST for round 2.
`CompactModelSummaryTest` (32 tests post-P8.S2) has the capture harness `summarizer($reply, $seen)`
at :331-349 — reuse it: the Done-when asserts on the captured `$seen[1]` content.
`ContextCompactorTest` 2271 L / 80 tests has settle/resolve/withDraft helpers (:2161-2188) but no
backend. BEWARE `testASummarizationForASupersededCompactIsDropped` (:428): it double-types `/compact`
but asserts DROP (superseded-id path) — do not confuse it with two sequential landed compactions.

## RULINGS to decide at staffing (open; recommendation given)

- (a) Prior-summary supplied as an EXTRA `Message::user` block vs appended into the
  `renderExchanges` user message — RECOMMEND extra block AFTER the exchanges, labeled as prior
  summary with the discarded-after instruction (matches opencode, keeps the renderer untouched).
- (b) Where to EXTRACT prior summaries: from `$wireHistory` Message content lines carrying the
  prefix, or from the Chat model's own last-applied summaries state if one exists — RECOMMEND
  wireHistory extraction (single source of truth = what the transcript shows), first-reader design.
- (c) Recursion must compose with six-facet records: prior summary text is free-form (it was itself
  a facet line or heuristic) — do NOT re-parse it; quote it to the model verbatim and let the model
  fold it into the new records per the conversation-wins rule.
- (d) E38 riders stay OUT of the carried prior summary (they are regenerated per turn). Rider lines
  starting `'[summary] '` vs records — check the prefix collision at staffing:
  `ContextCompactor.php`:1181 prepend vs the `Chat` `CONTEXT_REMINDER` rider format; quote both
  exactly, then rule.

## Hard constraints to carry

- Goldens frozen. This step touches no system-prompt path — the compaction prompt rides the
  `summaryBackend` request, not the main prompt — VERIFY this claim at staffing before briefing.
- Transcript seam census stays 20.
- Law 4n: any `Chat.php` paragraph edits move the `GlobFigureDriftTest` assertion count — predict
  per-class deltas before the full-suite number.
- Floor to beat: Tests 11,029 / Assertions 169,302 / 0F / 0E / Skipped 2 / EXIT 0 (gated `b695b7be9`).
- Phase 8 is SERIAL by plan (:2820-2823 region); P8.S3 depends on P8.S1 + P8.S2, both merged.
