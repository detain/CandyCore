# Premise v1 — P7.S6: Wire ForeignMemoryImporter behind /memory import

**Dated 2026-09-07.** All anchors measured on master `bfda6c51a`; `sugar-crush/` tree == `322808eed` (gated floor). Sources: verification scout `ses_f82b25df` (unchanged-azure-wildebeest full report) + orchestrator direct reads.

> **NOTE — known false citations.** An earlier scout (intermediate-violet-hawk) mispathed `src/Chat.php` as `src/Cli/Chat.php` and misquoted `MemoryBlock`'s cap constants as 200 at `:80` — both corrected here by direct read; treat those as the known false citations. Every anchor below must still be re-verified in the step worktree before it binds an edit.

## §1 Goal

- Plan text: `prompt_plan.md` :2661-2680 — wire the dormant foreign-memory importer behind a `/memory import` subcommand.
- The importer docblock `src/Memory/ForeignMemoryImporter.php:38-43` says NOT YET WIRED — zero `new ForeignMemoryImporter` in `src/` + `bin/`. All hits are prose-only: `Support/HomeDirectory.php`, `Support/ContainedPath.php`, `Cli/Bootstrap.php:709`, `Context/MemoryBlock.php:357`, `Agents/ForeignAgentPresetRegistry.php:459`.

## §2 TRUE seams (correcting the plan's claims)

- Subcommand dispatch is NOT `CommandRegistry`. The registry holds only the top-level row `CommandSpec::new('memory','Add, list, search, edit, or clear memory entries','Memory')` at `src/Commands/CommandRegistry.php:219` (file is 372 lines).
- Real seam = `src/Chat.php` (file is 12,973 lines — NOT `src/Cli/Chat.php`):
  - dispatch arm `'memory' => $this->handleMemoryCommand($text)` at `:7119`, inside the `dispatchCommand` match at `:7089-7134`;
  - call chain: `update():1242` -> `submit()` (`:1655`; defined `:6054`) -> `dispatchCommand()` (`:6123`; defined `:7052`) — synchronous, returns `[$next,null]` — no Cmd;
  - `handleMemoryCommand` `:10261-10285`; the sub-match at `:10276-10284` has arms `list/add/search/delete/clear/edit`, NO `import` arm; `default => memoryHelpResponse` with "Unknown command" at `:10283`; null-store guard `:10263-10265`; arg split `preg_split` at `:10272`;
  - `memoryHelpResponse` `:10307-10327` (verb lines appended `:10316-10322`, scopes line `:10324`);
  - `memoryAdd` `:10334-10364` shows the persist idiom: regex scope parse `:10344`/`:10347`, `$this->memoryStore->add($content, $scope)` at `:10357`.

## §3 Chat fields (what the seam can already see)

- `?MemoryStore $memoryStore` declared `:253` (ctor `:508`, assigned `:1136`, accessor `:5257-5259`, `withMemoryStore` `:5433-5435` — `Bootstrap.php:2388`/`:2457` set it via `EngineBackend->forwarded`, `App.php:351` also).
- PROJECT ROOT AVAILABLE: promoted `readonly ?string $projectRoot` `:857`, accessor `projectRoot()` `:5286-5288` (returns `?? getcwd() ?: ''`).

## §4 Importer API

- `final class` `:54`; ctor `(private readonly MemoryStore $store)` `:74`.
- `importClaudeCode(string $projectRoot, ?string $claudeHome = null): int` `:134` — user tier via `HomeDirectory::owned()` `:138-139`; the EXPLICIT `$claudeHome` is documented as NOT gated — the test hook (`:126-128`).
- `importOpencode(string $projectRoot): int` `:201` — per-directory containment.
- `refusedDirectories(): array` `:92` — `path => why`, recomputed per call `:87-88`.
- NOT idempotent — the sentinel `.sugar-crush/memory/.imported-claude` is the CALLER's contract per docblock `:31-36`; ZERO sentinel implementations exist (`/usr/bin/grep` for `.imported*` — prose only).

## §5 CAP REALITY (the plan's hard constraint, corrected)

- Caps live ONLY in `src/Context/MemoryBlock.php` — `MAX_ENTRIES=12` `:114`, `MAX_BYTES=4096` `:133`, `MAX_ENTRY_BYTES=512` `:158` — and are prompt-RENDER-time (`render()` breaks at `:243`/`:261`; per-entry clip `:403-409`).
- `MemoryStore.php` (569 lines): `add()` `:85-103` enforces NO caps, never discards, never throws (only I/O `RuntimeException` `:553`); `list()` `:157`; `search()` `:114`.
- Consequence: disk can exceed 12; entries beyond the render cap are silently OMITTED from the block, i.e. newest-imported can crowd older entries out via `list` order — hence the wiring MUST import WITH A LIMIT (headroom), not dump unbounded.
- `MemoryScope` enum `src/Agents/MemoryScope.php:12` — `User|Project|Local` (`:14-16`); `normalizeScope` `Local -> 'agent'` `:439-446`.
- **UNRESOLVED (builder MUST measure first):** which scope(s) `importClaudeCode`/`importOpencode` actually write — read the importer body `:134-361`; the headroom math must match that scope's `list()`.

## §6 DOC-DRIFT LANDMINES (this plan's own law — wiring makes these prose claims FALSE today)

- `ForeignMemoryImporter.php:38-43` — the NOT-YET-WIRED docblock block.
- `Cli/Bootstrap.php:709-714` — DORMANT roster prose; siblings live nearby `:715-744` — edit ONLY the importer entry.
- `tests/Cli/ProjectTierRefusalInventoryTest.php` — tier row `:232`; named-GAP roster `:607` (inside `testTheEightThatFeedTheCollectorAndTheSixThatAreNamedGaps` `:602-639`; `DRAIN_EVIDENCE` const `:659-668`; `collectorDrains` scans BOOTSTRAP SOURCE ONLY `:626-638` — a Chat-side drain does NOT count as feeding the collector); `dormantHolders()` provider `:772-791` with the `'foreign memory import'` entry `:775-779`; its docblock `:745-749` — acquiring a production caller => move the entry `dormantHolders` -> `wiredHolders` (`:807`).
- `docs/MEMORY.md` fence `:50-58` (verb list) + `:134` ("not implemented").
- `docs/TROUBLESHOOTING.md:235` ("does not exist").
- `README.md:685` — the bare `/memory` row.
- Guards that will NOT fire (do not over-edit): `ReadmeRosterDriftTest` roster2 `:200-248` pins the README slash roster to `CommandRegistry::slashCommands()` (fires only for top-level row/description changes); roster3 `:289-306` irrelevant (CLI argv verbs); `MemoryCommandTest::testMemoryHelpShowsAllCommands` `:300-320` is presence-style (adding an import line won't redden it); `SlashDispatchTest` guard `:98-113` is keyed on the top-level name only (a new arm needs no change).

## §7 FLOOR / GOLDENS (must not move)

- Floor: **11,008 tests / 169,071 assertions / 0F / 0E / 2S / EXIT 0**, gated at `322808eed`.
- Goldens: system `a5c5a14ca2e3ad891933ac7aefccc6af` / 7,732 B; agent `ef0326dd38535aaa2f1d715919bff26e` / 1,060 B — MUST NOT MOVE.
- Census neutrality: `Bootstrap.php` `self::warnPermissionConfigInTranscript` count stays 20; surface refusals/limits in the command RESPONSE, not via new seam calls.

## §8 tests/Memory fixture idiom (the existing pattern to follow)

- Both existing tests use a real temp-dir `MemoryStore`, no mocks: `ImporterTest` setUp `:22-42`, exemplar test `:53-69`; `ContainmentTest` setUp `:39-63`, symlink-escape `:119-129`, refusal asserts `:127`-`:276`.
- `tests/Commands/` pattern (17 files): drive `Chat::update(new KeyMsg(KeyType::Enter))` with draft text, NEVER call privates (header `:21-26`).
