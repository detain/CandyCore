# P9.S7 — History sanitisation before every send — step brief

Plan: prompt_plan.md `### P7`-family heading `### P9.S7` (~line 2964+ — re-derive). Phase 9 batch 2, files DISJOINT from S2/S3/S4. Base: the staffing commit of this brief; sugar-crush tree equals the P9.S1-close floor tree `2c6583561` for all paths this step touches (verify with a belt diff before editing, per law 4j). FLOOR 11,062 / 169,786 / 0F / 0E / 2 Skipped / EXIT 0. Goldens FROZEN: system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 B · agent `ef0326dd38535aaa2f1d715919bff26e`/1,060 B — NEVER eligible here (sanitizer touches wire history, not prompt assembly; any golden move = HALT, you wired it wrong).

File ceiling (plan :2970-2973 + this brief): `sugar-crush/src/Messages/HistorySanitizer.php` (NEW), `sugar-crush/src/Runtime.php` (ONE call site), `sugar-crush/tests/Messages/HistorySanitizationTest.php` (NEW). NO other file. `Providers/`, `EngineBackend.php`, `Chat.php`, root `Message.php` are OFF LIMITS (read-only for you).

## Premise (evidence-first re-scout, pasted anchors verified 2026-09-08; law 4j: re-verify each before relying on it)
- TWO message vocabularies: root `src/Message.php` (Chat::$history; `toWire()` :365-381 feeds ONLY the compactor) vs typed `src/Messages/*` (UserMessage; AssistantMessage(string content, ?array toolCalls); ToolResultMessage(string toolCallId, content, isError); SystemMessage) — the typed set is what providers serialize.
- `EngineBackend::toTypedMessages()` src/EngineBackend.php :1588-1600 (single production caller :611 inside complete(); file ends :1601) STRIPS toolCalls/toolResults/pendingToolCallId — Chat-path orphan tool_calls NEVER reach providers structured today. The plan's orphan-tool_call motivation is therefore PARTLY UNREACHABLE-because-lost on the Chat path: record that honestly (in the sanitizer docblock + your report); do NOT change toTypedMessages (ruling R-C).
- WHAT IS LIVE: refusal path Chat.php :2498-2509 commits `Message::assistant('')` + error result; ESC-ESC cancel (:1571-1578) appends only the `_Request cancelled._` system row and leaves empty/placeholder rows; empty-content AssistantMessage serializes to a BARE `{"role":"assistant"}` row because provider `array_filter` drops the empty content key — SglangProviderTest :404 pins the bare-row shape and :427-430's comment records the server-400 family. Dropping empty assistant rows at the send choke is the live-value operation.
- The single send choke every request passes: `Runtime::buildMessages()` src/Runtime.php :2448-2459 (instanceof filter only) — last function before `CompleteRequest` construction at :1104-1113. That is the wire-in point (the plan's own file, the plan's "before every send" literal).
- Engine-loop pairing today (why ops (b)/(c) are defensive): settle echoes original ids Runtime.php :2048-2054; tool-failure path :2402; sequential denied :1566-1574; SIGKILL branch releases via collectChildResult :1823-1838; generator-abandonment finally :1845-1879 discards the App with its history (unreachable to any send). So valid engine histories pass through the sanitizer UNTOUCHED — prove it (R-E).
- Repair precedent to mirror: `Chat::reviveCheckpointMessage` :5709-5728 (single prod site :10880) synthesizes assistant(INTERRUPTED_TOOL_CALL) + error ToolResult same-id; marker const Chat.php :5621-ish (grep `INTERRUPTED_TOOL_CALL`). DeniedToolCallTest.php :67 pins it.
- Vertex precedent: Anthropic rejects two consecutive user messages; Vertex merges them :631-634 and ALREADY drops empty assistant :649-653 — your sanitizer makes that drop universal/pre-provider instead of Vertex-only.
- ClaudeCodeProvider flattens everything to text (:315-330 `Tool Result: {content}`) and Bedrock flattens tool_result into user text (id never emitted) — validity checks for those two are content-shape, not pairing.
- Test harness idioms available: provider-private-method invocation — read SglangProviderTest :388/:415-418 and copy its pattern; real ESC-ESC snapshots exist in ChatTest :343-355 and StreamingWiringTest :410-430; 106 tests construct `new Chat(history: [...])`.

## Rulings (FIXED — deviate = stop-report)
R-A `final class HistorySanitizer` (namespace `SugarCraft\Crush\Messages`), ONE public static `sanitize(array $messages): array` over typed Messages\*; pure: fresh array out, input never mutated, order preserved, anything it doesn't recognize passes through byte-identical. Operations, in one pass: (1) collect ids from every AssistantMessage toolCalls entry; (2) DROP ToolResultMessage whose toolCallId matches no collected call; (3) for every call id with no result anywhere in the list, SYNTHESIZE immediately after its assistant row a ToolResultMessage(id, <marker>, isError: true) with marker text value-EQUAL to Chat's INTERRUPTED_TOOL_CALL content — define the string as HistorySanitizer's own const and TIE it to Chat's by a test that reflection-reads Chat's const and asserts equality (no cross-file coupling in src, drift-guarded in tests); (4) DROP AssistantMessage rows with `content === ''` AND no toolCalls (the live bare-row producer). Docblock: cite the plan step, the refusal/cancel anchors, and state the §1.12 honesty — op (4) is LIVE-value on the Chat path, ops (2)/(3) are disclosed defense-in-depth (dormant-not-dead, E31 doctrine wording, "a send is exactly where they must hold even if every producer today pairs correctly").
R-B Wire = EXACTLY one line change in `Runtime::buildMessages()` : return the sanitizer's result over the filtered list. Nothing else in Runtime.php. buildSystemPrompt/goldens untouched.
R-C `toTypedMessages`, all providers, Chat.php, root Message.php — ZERO edits. The structured-orphan ambition on the Chat path is unreachable because the structure is lost upstream; the placeholder-rows-hoisted-into-system-prompt leak (SglangProvider :1565-1582 hoisting `_Request cancelled._`-era placeholder text forever) is a SEPARATE live defect: do NOT fix it here, your report must recommend it as its own follow-up step (orchestrator files it).
R-D Done-when (plan :2975-2977): `tests/Messages/HistorySanitizationTest.php` builds histories with (a) orphan tool_call no result, (b) orphan tool_result no call, (c) empty assistant message — asserts the sanitized list is valid FOR EVERY PROVIDER CONVERTER: drive each real converter (Sglang/OpenAI-shape pair + Vertex + Bedrock + ClaudeCode + Echo — locate their formatMessages/serialization entries by reading src/Providers/, use the invokePrivateMethod pattern from SglangProviderTest) and assert the invariants that matter per converter: no bare assistant rows, every emitted tool row's id has a preceding call, every call has a following result (providers that emit them at all), and byte-identity of everything else. PLUS one integration test driving the REAL cancel: replicate ChatTest :343-355's ESC-ESC-with-tools recipe to produce a genuinely orphaned history, map it through the SAME conversion Chat uses when it reaches a send (if the real choke is unreachable without EngineBackend changes, construct the typed list the way toTypedMessages would and say so in a comment — plan's 'if not, say so' clause is honored ONLY if you prove which part is unreachable).
R-E Validity-preservation suite: every history shape the existing engine tests produce must pass through UNCHANGED — include a test replaying at least the settle-path and denied-path typed lists (copy fixture shapes from RuntimeTest) asserting sanitize() === identity.
R-F Prediction hazards: NEW src file + NEW test file → law-4n GlobFigureDrift +2 per new src file (readOrFail ×2 censusScope callers, P9.S1 finding) and paragraph counts; ~16 corpus guards enroll the new TEST file (P9.S1 surprise #4 — the +69a wave; expect the same shape); TreeWideGuardRoster/DocumentParagraphs/StderrEmitterCensus/EnvRoster moves are census-derived — solo GlobFigureDrift + TreeWideGuardRoster + SymbolCitationDrift + DuplicatedDocBlockLine before any full-suite talk. Docblock line cap: keep narrative lines within 92 chars (house law). No new public const is expected (the marker const is private); if you make it public, check BuiltInToolCorpus-style rosters first — prefer private.

## Experiments (§1.11 — all in your two in-ceiling test files; record red-sets verbatim, restore+re-green after each)
- X-EMPTY: neuter op (4) → the (c) unit + the bare-row converter assertion + the ESC-ESC integration must all go RED.
- X-SYNTH: neuter op (3) → (a) unit + every-pairing-assertion + integration RED (if your history pairs stay answered end-to-end, the synthesis pin must instead construct an explicit unanswered call — make sure the discriminator actually bites).
- X-DROP: neuter op (2) → (b) unit RED.
- X-CHOKE: comment the R-B wire line → every wire-shape assertion RED while all sanitize() unit tests stay green — this proves the choke is the load-bearing half, not the class in isolation.
- X-NOOP (positive): E-R fixture — valid engine history in, `assertSame` identical list out (identity + order + bytes).

## Protocol (unchanged house law)
Solo set at tip (your two new classes + RuntimeTest + SglangProviderTest + the other converter suites + BSP + SSHW + Glob + SymCite + DupDoc + TreeWide + census suites), prediction-first, box-quiet probe 0 first, serial full suite ONLY if instructed (batch-2 protocol: builders run SOLO ONLY; the orchestrator runs the master-direct full gate after merge), </dev/null --colors=never, cwd worktree ROOT, junit to your scratch /tmp/opencode/P9.S7-build/tip.xml. Commits: TWO preferred (src, tests), ONE acceptable (record why if squashed). Hygiene 3-scan per commit (bracketed-EMAIL-token scan with ESCAPED pattern, %B '<' == 0, identity ×4 == git-config interpolation — NEVER a typed/literal identity). /tmp/opencode/P9.S7-build/ is your ONLY scratch; no git init anywhere else (ORCHESTRATION-RULE-2); no push, no composer, no caliber (zero active hooks — do not reflexively --no-verify; just commit).
Report format: branch tip SHAs; R-A..R-F dispositions; done-when test names + what each converter asserts; the four X-experiments' EXACT red-sets; movers table vs floor (solo classes); goldens; any cite drift found (law 4j — the plan is often wrong: record it); follow-up recommendation verbatim-able for the placeholder-leak step.
