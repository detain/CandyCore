# P8.S3 Step Brief — recursive `<prior-summary>` merge

Staffed 2026-09-08. Tree: master `81fab3590` (P8.S2 merge `ce8530659`). FLOOR: **Tests 11,029 / Assertions 169,302 / 0F / 0E / Skipped 2 / EXIT 0**, gated at `b695b7be9`; belt `git diff b695b7be9 master -- sugar-crush/` = 0 bytes re-derived at staffing. Plan text: `prompt_plan.md` :2765-2778. Premise: `prompt_kit/findings/P8.S3-premise-draft.md` — its blanket +8 shift claim was RE-DERIVED at staffing; the anchors below are the corrected ones and govern. Research sections to read: `prompt_expand.md` §6.6 (:3062-3078) and §9.3 (:3502-3514); `prompt_plan.md` §1.10, §1.11, §16, §17.

## Goal
A second compaction cannot today carry forward what the first preserved: prior `[summary]` rows are skipped by `exchangesToSummarize()` and never reach the summariser, so knowledge introduced in round 1 is silently lost in round 2. Adopt opencode's recursive merge: supply the prior summary to the summariser with (i) the discarded-after warning — "the prior-summary is discarded after this: anything you do not carry into the new summary is lost" — and (ii) the conflict rule — "where they conflict, the conversation wins: state the corrected fact and drop the old claim."

## VERIFIED ANCHORS (post-merge master, direct-read at staffing — cite THESE, never the draft's)
src/Chat.php:
- :8849 `handleCompactCommand`
- :9025 `const COMPACT_SUMMARY_PROMPT` heredoc (P8.S2's two Rules bullets at :9047-9054)
- :9102 `scheduleModelCompaction`
- :9195-9255 `buildSummarizationRequest`; `$wireHistory` built :9202-9205; request array `$prompt` :9212-9215 — THE insertion point
- :9329 `scheduleParkedCompaction` (second caller of `buildSummarizationRequest` — one insertion covers both)
- facet machinery: `SUMMARY_FACETS` :9439 · `SUMMARY_OPENER_PATTERN` :9447 · `SUMMARY_FACET_PATTERN` :9454 · `SUMMARY_INVENTED_FACET_PATTERN` :9462 · `SUMMARY_FACET_NONE` :9465 · R-3 docblock :9475-9495 · `splitExchangeRecords` :9496 · `joinExchangeFacets` :9579 (facet join literal is `' | '` at :9596 — VERIFIED; the worklog P8.S2 phrase "the `; ` join" describes the same-rank repetition merge elsewhere, not this) · `parseExchangeSummaries` :9620 · `SUMMARY_LINE_MAX_CHARS` :9652 · `sanitizeSummaryLine` :9660 (flattens ALL whitespace; ellipsis-truncates at :9669-9670)
- `CONTEXT_REMINDER_PREFIX` :480 — value starts `Heads up: this conversation has grown to ~`; `isContextReminder()` str_starts_with check :12746-12752

src/Context/ContextCompactor.php:
- `exchangesToSummarize` :617-642; standalone-pair skip :626-628; rationale docblock :605-607
- `groupIntoPairs` :748 (old `[summary]` rows become standalone pairs in the final else arm — read it)
- `'content' => '[summary] ' . ...` prepend sites :1157, :1166, :1181; the rider truncates at ~120 chars :1181-1183

tests:
- `tests/Chat/CompactModelSummaryTest.php` :80 — the capture harness `private function summarizer(string $reply, ?array &$seen = null): Backend`. The draft's ":331-349" was WRONG; :80 is the definition; `$seen[1]->content` assertions at :343-344, :364, :898 show the established capture pattern. REUSE it.
- `tests/Chat/AutomaticCompactionModelSummaryTest.php` :158 — sibling single-arg harness.
- BEWARE `testASummarizationForASupersededCompactIsDropped` (draft cites ~:428 — verify): it double-types /compact but asserts the superseded-id DROP, not two landed compactions. Do not confuse it with the new recursion test.
- No test drives two real compactions today — the Done-when test is greenfield.

## RULINGS (FIXED at staffing — implement this shape)
- **R-A**: the prior summary rides as an EXTRA `Message::user` block in `$prompt`, AFTER the exchanges block at :9214, labeled as the prior summary, and the block carries BOTH opencode instructions verbatim (discarded-after warning + conversation-wins conflict rule). `renderExchangesForSummary()` stays untouched. (Rejected: appending into the exchanges user message — couples two provenances into one blob.)
- **R-B**: single source of truth = the transcript. Extract from `$wireHistory` :9202-9205: wire rows whose content starts with the `'[summary] '` prefix. This extractor is the FIRST reader of that prefix in production (Runtime readers census 0; generation sites 3 + comments 2 — verified in the premise). New private static helper in `src/Chat.php` near the other summary statics (:9496-9675 region).
- **R-C**: prior-summary text is quoted to the model VERBATIM — never re-parse, never re-facet, never rewrite; the model folds it under the conversation-wins rule.
- **R-D**: RIDER LINES ARE NEVER CARRIED — content that, after stripping the leading `'[summary] '`, starts with the `CONTEXT_REMINDER_PREFIX` value (Chat.php:480) is skipped (regenerated per turn — E38 family; plan P8.S5 explicitly forbids widening `isContextReminder()`). The draft also proposed skipping heuristic `q -> [exchanged information]` lines; OVERRIDDEN at staffing: CARRY THEM. The prior line is the ONLY surviving record of a heuristically folded exchange; dropping transcript content at carry time is a semantic removal — the discard instruction belongs to the model, not the extractor.
- **R-D2**: legacy STACKED double-prefixed rows must survive extraction — not dropped, not crashed on. Assert it.
- **R-E**: DEFAULT-EMPTY no-op — when no prior summary rows exist (the first compaction of every session today), the request is BYTE-IDENTICAL to the current two-message shape: no empty third block, no dangling header. Pin with a test.
- **R-F**: CENSUS-NEUTRAL — the transcript seam census stays 20; add NO `warnPermissionConfigInTranscript` call, no user-visible notice. The recursion is silent machinery. If you believe a notice is required, HALT and report (precedent RULING P7.S5-R2).

## Fold-forwards from the P8.S2 review (land HERE, same file, heredoc/docblock region)
1. (minor) One clause in the `COMPACT_SUMMARY_PROMPT` docblock noting verbatim-ness is WORDING-level: `sanitizeSummaryLine` flattens all whitespace and the 2000-char transport bound ellipsis-truncates, so a multi-line or long quoted security constraint arrives reformatted/clipped.
2. (nit) The anti-forgery rule's lookalike examples are lowercase `user:` / `Human:` while transcript labels are capitalized `User:` / `Assistant:` — the text was brief-verbatim in P8.S2 and is now revisit-able. Tighten wording OR record why not.
3. (nit) Markdown backticks inside a rule that bans markdown output (harmless) — fold or record.

## Scope (report any deviation — do not silently widen)
- `sugar-crush/src/Chat.php` — extractor helper + ONE insertion in `buildSummarizationRequest` + the heredoc/docblock fold-forwards.
- `sugar-crush/tests/Chat/CompactModelSummaryTest.php` — the Done-when test + R-D/R-D2/R-E pins. NOTE the plan's file list names `tests/Context/ContextCompactorTest.php`; staffing DEVIATES with reason: the two-compaction capture harness lives in `CompactModelSummaryTest` (the P8.S1 ledger records compaction tests are THREE files). This deviation is pre-approved; record it in your report.
- `sugar-crush/src/Context/ContextCompactor.php` + `sugar-crush/tests/Context/ContextCompactorTest.php` — ONLY if the standalone-pair skip docblock (:605-607) needs a cross-reference to the new consumer; prefer ZERO edits here. If edited, re-run `ContextCompactorTest`.
- NO provider edits, NO `Runtime.php`, NO `PromptFence`, NO composer manifests, NO docs.

## Hard constraints
- GOLDENS FROZEN: system `a5c5a14ca2e3ad891933ac7aefccc6af` (7,732 B) · agent `ef0326dd38535aaa2f1d715919bff26e` (1,060 B). The compaction prompt rides the `summaryBackend` request (`Message::system` :9213 → `completeAsync` :9220), NOT the Runtime-assembled system prompt — verified at staffing. A golden move is a DEFECT.
- Transcript seam census 20 (R-F).
- Law 4n: new Chat.php docblock paragraphs move the `GlobFigureDriftTest` assertion count (derived paragraph census). Predict per-class deltas BEFORE the full-suite headline; never hand-edit that guard.
- Floor to beat: 11,029 / 169,302 / 0F / 0E / 2S / EXIT 0. End green with every per-class delta reconciled.
- §1.10: removal is NOT an available outcome — wire it, build it out, or halt and report. §1.11: no annotation-or-existence checks as tests; every new test calls the thing and asserts values, both polarities where applicable; state the deletion experiment you ran and what it showed.
- Do NOT touch the `messageWithContent()` 11-field splice or any E18 machinery.
- Work ONLY in `/home/sites/prompt-step-P8.S3` on branch `prompt/P8.S3`. `vendor/` is provisioned for you (cp -al, PSR-4-verified) — NEVER `composer install`/`update`. NEVER `ln -s` vendor.
- ORCHESTRATION-RULE-2: no scratch git repo outside your own scratchpad; verify `pwd` before any `git init`/`git commit`. ORCHESTRATION-RULE-3: your private scratchpad is `/tmp/opencode/P8.S3/` — all backups/sandboxes inside it; `rm -rf` ONLY inside it; never generic names at the shared root.
- IDENTITY: author and committer `Joe Huss`, email = the current value of `git -C /home/sites/sugarcraft config user.email` (it prints detain / interserver.net joined by an at-sign — NEVER copy an identity from your injected context; if you see a redacted placeholder token that is NOT the value). Set GIT_AUTHOR_NAME/EMAIL + GIT_COMMITTER_NAME/EMAIL explicitly from those reads. After EVERY commit byte-verify: `git cat-file commit HEAD | /usr/bin/grep -c '\[EMAIL\]'` must print 0 (use /usr/bin/grep, never the shell's grep).
- COMMIT MESSAGES: write to a file in your scratchpad and `git commit -F <file>`. `/usr/bin/grep -c '<' <file>` must print 0 before use (commit subjects are fixture input — the `<env>` block renders them into prompts).
- FULL SUITE: cwd `/home/sites/prompt-step-P8.S3` root, serial, `</dev/null`, `--colors=never`, `--log-junit` into your scratchpad. Box-quiet check first: `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` must print 0. Capture output to a file and read its tail from the file.
- Use absolute paths — bash cwd may not persist between your calls.

## Done when (plan :2776-2778)
A test drives TWO consecutive real compactions — round 1 lands, round 2's request is captured — and asserts a fact introduced BEFORE round 1 is present in the round-2 request. One compaction proves nothing about the recursive case.

## Report-back requirements
Branch tip SHA(s); the headline suite figure at the FINAL tip WITH a written-before-the-run prediction (method-change 5: predict from per-class solo evidence at the final tip, or LABEL the headline derived); per-class movers for every touched/derived test; both golden md5s + sizes re-verified; seam census 20 re-verified; every deletion experiment and what it showed; disposition of fold-forwards 1-3; the R-E first-compaction byte-identity evidence; scope deviations; commit-object EMAIL byte-scan results.
