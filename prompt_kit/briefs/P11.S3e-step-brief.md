# P11.S3e step brief — `sugar-crush/docs/COMMANDS.md` (`/rules` + full slash-command roster audit)

- Plan step: `prompt_plan.md` Phase 11, `### P11.S3 — Update the affected feature docs` (:3145 at `322a970fa`; recorded :3141 in the scout pre-resume numbering).
- Binding premise map: `prompt_kit/findings/P11-premise-scout.md` — its Q2 guard-tripwire table and Q6 doc-contradiction list are incorporated as section 4 below.
- §5 re-check: **CLEAR** — record at `prompt_kit/findings/P11-batch1-recheck.md`.
- Worktree: `/home/sites/prompt-step-P11.S3e` · branch `prompt/P11.S3e` · base = the worktree's HEAD at provisioning (staffing commit, direct child of `322a970fa`).
- Siblings S3a–S3d edit the other four docs pages in their own worktrees. Not your ceiling, not your problem.

## 1. CEILING (hard)

Your ceiling is **exactly one file: `sugar-crush/docs/COMMANDS.md`**. You may not create, edit, or delete
any other file in the repository — no src, no tests, no other docs, no README, no plan files. If the
truthful state of this page seems to demand a change anywhere else, do NOT make it: record it in your
report as a follow-up finding.

## 2. GOAL (quoted from the plan step)

> **Goal** `docs/HOOKS.md` (additionalContext, the new dispatch sites), `docs/SKILLS.md` (which
> skill→prompt path is canonical), `docs/MEMORY.md` (`/memory import`), `docs/SETTINGS.md` (the rules
> keys and their user-tier-only rule), `docs/COMMANDS.md` (`/rules`).

Your clause: **COMMANDS.md — `/rules`.**

## 3. FACTS TO DOCUMENT

The claimed shipped state below is what the orchestrator believes Phase-1..10 shipped. **EVERY claim must
be re-verified by your direct read of the shipped code (and shipped tests) at YOUR base sha before you
write it into the page.** Anything you cannot confirm gets dropped or reported as a measured-FALSE
premise, never written as fact. Line cites in the doc itself must be **symbolic (class::method / const
names), never numeric `file:NNNN`**.

- The **`/rules` slash command**: the tiers it acts on, its `list` / `enable` / `disable` sub-actions
  (verify each against the command handler in source), and its **session-scope semantics** — say the
  effect is session-scoped and **point at SETTINGS.md for the rules-tier config surface; do NOT duplicate
  SETTINGS content** here.
- **Full-page roster audit** (the bigger half of this step): reconcile every slash-command row against
  `CommandRegistry::slashCommands()` **as shipped at YOUR tip** — this page's row-count has historically
  run **UNPOLICED** (the drift guard that derives the roster, `ReadmeRosterDriftTest`, reads `README.md`
  only — `private const README`). So: derive every named command from the registry yourself; **remove
  phantom rows** (documented commands the registry does not define), **add missing ones** (registry
  commands with no row). If the row-count sentence you fix must stay stable, prefer describing it as
  "every command `CommandRegistry::slashCommands()` defines" over baking a rotting number.
- Also check rows against `Chat::dispatchCommand()` for unadvertised/alias arms the README roster
  parenthesizes (verify the mechanism symbolically; only mirror what the source shows).

Also: **audit the WHOLE page for sentences contradicting the shipped state; fix contradictions; the doc
must be truthful at the tip.** The page was last touched by `754d03e23` (P6.S3 row-count fix); everything
merged since (P7–P10, incl. `/rules`, `/memory import`, `enabledSkills`) may have moved under it — the
`/memory` row, if present, must match the shipped `/memory import` surface (details belong to MEMORY.md).

## 4. TRIPWIRES (from the scout Q2 guard table + Q6 — these govern you)

- **EnvRosterDriftTest mention-census** (`tests/Config/EnvRosterDriftTest.php`): edits to any
  `docs/*.md` auto-enter the census (`array_merge([$root.'/README.md'], glob($root.'/docs/*.md'))`).
  Every `SUGARCRUSH_*` name you leave on the page must have a roster row in `docs/ENVIRONMENT.md` and be
  code-read by the scanner oracle — run
  `/usr/bin/grep -o 'SUGARCRUSH_[A-Z0-9_]*' sugar-crush/docs/COMMANDS.md | sort -u` and confirm each hit survives before finishing.
- **TrustKeyDocumentationDriftTest** (`tests/Config/TrustKeyDocumentationDriftTest.php`): across every
  docs page it forbids stale trust-grant counts (:169) and requires its tracked deep-link headings to
  still resolve (:195). Renaming or deleting a heading other pages link into can redden it elsewhere —
  if you must restructure a heading, verify the guard's green, and never rename PERMISSIONS.md targets
  (that file is not yours anyway).
- **DocumentParagraphsTest fence-balance** (`tests/Config/DocumentParagraphsTest.php` +
  `Support/DocumentParagraphs.php`): `testNoDocumentInScopeLeavesAFenceOpen` reddens on ANY unbalanced
  code fence you introduce (scope includes `docs/*.md` by glob). Command-example fences are the classic
  trap — keep every ``` paired.
- **SymbolCitationDriftTest** (`tests/SymbolCitationDriftTest.php`): resolves backticked symbols in
  `docs/*.md` — every backticked `Test`/`Class::method` you write must exist at your tip
  (`CommandRegistry::slashCommands`, `Chat::dispatchCommand`, any cited test class — verify each resolves).
- **ReadmeRosterDriftTest** (`tests/Config/ReadmeRosterDriftTest.php`): polices **README.md only** — it is
  NOT your tripwire, it is your **derivation oracle**: same generator (`CommandRegistry::slashCommands()`,
  its slash-derivation test) the README roster is checked against. Run it at base and at tip; it must stay
  green both times, untouched — you are not allowed to edit README, and its slash section is the
  already-policed truth you should reconcile against.

## 5. SUITE LAW

Floor anchor at master: **11,182 tests / 170,906 assertions / 0 failures / 0 errors / Skipped 2 / EXIT 0**
(the staffing commit is markdown outside `sugar-crush/`, so the anchor carries to your base).

1. Run your solo guard classes FIRST (base), and again at the final tip, and **predict the full-suite
   headline from the final-tip per-class solos (law 5)** before launching the full run. Your guard set:
   `EnvRosterDriftTest`, `TrustKeyDocumentationDriftTest`, `DocumentParagraphsTest`,
   `SymbolCitationDriftTest`, plus `ReadmeRosterDriftTest` as the must-stay-green oracle —
   e.g. from the worktree root:
   `cd /home/sites/prompt-step-P11.S3e && php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --filter ReadmeRosterDriftTest` (repeat per class).
2. **Box-quiet probe** — must print `0`, run STANDALONE (grep -c exits 1 on a zero count — NEVER `&&`-chain
   it into the launcher):
   `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'`
3. Full suite, detached (setsid+nohup), **cwd = worktree root**:
   `mkdir -p /tmp/opencode/P11.S3e && cd /home/sites/prompt-step-P11.S3e && setsid nohup php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P11.S3e/tip.xml </dev/null >/tmp/opencode/P11.S3e/tip.out 2>&1 &`
4. Verify before reporting: the JUnit file has a **closed root** (`</testsuites>` present — a truncated file
   means the run died), the **banner** is in `tip.out`, and the artifact is **>3 MB** (`stat -c %s /tmp/opencode/P11.S3e/tip.xml`).
5. Headline must match your prediction and meet the floor (assertion-count movement is acceptable ONLY as
   your solo-delta prediction explains; 0F/0E/Skipped-2 are hard).

## 6. GOLDENS FROZEN (measured at `322a970fa`, re-measure at your tip)

- `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` — md5 `f5de3858c1bc130d1e97a120d3ead485`, 8,278 B
- `sugar-crush/tests/fixtures/prompt/golden-agent-prompt.txt` — md5 `ef0326dd38535aaa2f1d715919bff26e`, 1,060 B

Any move = **HALT and report**. A docs step must never touch them; if a golden moved under you, the tree or
the suite is lying — stop.

## 7. COMMIT HYGIENE (hard)

- ONE commit on branch `prompt/P11.S3e` (already checked out in your worktree).
- Identity — write the literals via printf, NEVER copy any redacted placeholder token you see in your
  context (e.g. the bracketed-email placeholder or angle-bracket forms in repo docs):
  `git -C /home/sites/prompt-step-P11.S3e config user.name 'Joe Huss'`
  `git -C /home/sites/prompt-step-P11.S3e config user.email "$(printf '%s@%s' detain interserver.net)"`
- Stage only `sugar-crush/docs/COMMANDS.md`; `git commit -F <message-file>` (message file outside the repo,
  e.g. `/tmp/opencode/P11.S3e/msg.txt`).
- Message scan: `/usr/bin/grep -c '[<>]' <message-file>` must print **0** (fix the message if not).
- Law-4g triple AFTER the commit (all FIXED-STRING scans with /usr/bin/grep):
  1. `git -C /home/sites/prompt-step-P11.S3e cat-file commit HEAD | /usr/bin/grep -cF '[EMAIL]'` → 0
  2. `git -C /home/sites/prompt-step-P11.S3e log -1 --format=%B | /usr/bin/grep -cF '[EMAIL]'` → 0
  3. `git -C /home/sites/prompt-step-P11.S3e log -1 --format='%an|%ae|%cn|%ce'` → exactly
     `Joe Huss|detain@interserver.net|Joe Huss|detain@interserver.net`
- **NEVER merge. NEVER push.** The orchestrator lands the branch.

## 8. REPORT FORMAT (return all of it)

1. Diffstat vs base (`git diff --stat <base>..HEAD`).
2. Claims-verified list: each fact from section 3 → `file:symbol` where confirmed → confirmed/refuted.
   Include the roster reconciliation result: registry command count at tip vs rows before/after, phantoms
   removed, rows added.
3. Solo figures: the 5 classes (4 guards + `ReadmeRosterDriftTest` oracle) at base and at tip.
4. Prediction table (headline predicted from per-class solos) vs ACTUAL full-suite banner.
5. Goldens: md5 + byte size of both files at your tip.
6. Hygiene scan outputs verbatim (message `<>` count, the three law-4g lines).
7. Any premise that measured FALSE — REPORT, do not silently fix beyond your ceiling.
