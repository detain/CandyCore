# P8.S5 — The compaction circuit breaker, and E31/E32 — step brief

Plan: prompt_plan.md :2802-2836. Phase 8 SERIAL (S1-S4 merged). Base: master at the staffing commit preceding this brief's build; sugar-crush tree == gated `beb8df1b6` (P8.S4). FLOOR 11,042 / 169,428 / 0F / 0E / 2 Skipped / EXIT 0. Goldens FROZEN: system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 B · agent `ef0326dd38535aaa2f1d715919bff26e`/1,060 B.

File ceiling (plan :2814-2818): `sugar-crush/src/Chat.php`, `sugar-crush/src/Context/IdleCompactionPolicy.php`, `sugar-crush/tests/Chat/AutomaticCompactionModelSummaryTest.php` (primary compaction suite — all new Chat-side tests go here; tests/Chat/ roster is the plan's own scope), `sugar-crush/tests/Context/IdleCompactionPolicyTest.php`. NO other file. Do NOT edit ContextCompactor.php or CompactorConfig.php (P8.S4 just closed them; E38 ruling says no).

## Premise (verified by orchestrator direct reads 2026-09-08 @ beb8df1b6 tree)
- `scheduleParkedCompaction` Chat.php:9352; E31 gate :9357-9359 `if ($this->spendCapReached()) { return null; }`; its docblock :9328-9339 ALREADY ADMITS "UNASSERTED DEFENCE … no test drives it and a mutation deleting it survives the suite"; parked-window four-keys docblock :9295-9321 (explicitly: no CancellationToken armed at park — created only by dispatchTurn when the compaction lands, :9319-9321 — this sentence becomes FALSE under D5 and must be rewritten truthfully).
- `buildSummarizationRequest(array $probeHistory, ?string $parkedSubmission): ?array` at :9207; `Cmd::promise` closure :9241-9275; THE CALL :9243 `return $backend->completeAsync($prompt)->then(...)`.
- `Backend::completeAsync(array $history, ?callable $onToken = null, ?CancellationToken $cancellation = null, ?callable $onEvent = null)` ALREADY ACCEPTS the token — src/Backend.php:93; contract :77-84: cancel-aware backends SHOULD poll `isCancelled()` and reject early; a backend that can't act on it MAY ignore it (cancellation is BEST-EFFORT, say so).
- `/compact`'s cap arm = THE NOTICE TO SHARE: `scheduleModelCompaction` :9114, gate :9124-9142 → `compactNow($inputText, $this->history, [], $sprintf(...))` with the notice sprintf at :9133-9140 (its bytes are pinned by CompactModelSummaryTest tests at ~:1043/:1065 — those pinned bytes MUST NOT change).
- Double-Escape cancel arm: update() :1522-1571 — :1535 `$this->inFlightCancellation?->cancel()` (NO-OP today during the parked window: token is null there — D5 makes it real), :1542 `_Request cancelled._` appended, :1571 `pendingCompactionId => null` released UNCONDITIONALLY.
- Supersession: :1373 `$msg->compactionId !== $this->pendingCompactionId` → late parked landings after a cancel are dropped (already correct).
- Landing error path :9266-9272 carries NO usage (rejection hands back Throwable) — so a cancelled-then-rejected summarization bills nothing once the backend honors the token.
- `spendCapReached` :12682-12687 (fail-open arithmetic; cap always positive-finite :12704-12707). Enforcement-site list docblock :12657-12667 names THREE sites (refusal /compact-decline/applyModelCompaction-landing) — D5 changes none of that; the :9357 gate is the site that stays silent-until-now.
- Test-side accessors EXIST in AutomaticCompactionModelSummaryTest.php: `chat(..., ?float $maxCostUsd = null)` :138-155; `latchOf` :192, `generationOf` :197, `cancellationOf` :202 (ReflectionProperty on `pendingCompactionId`/`generation`/`inFlightCancellation`), `resolve(Cmd-closure)` :179-190, `submit()` :174-177, `generousSummarizer()` :164-172, `CountingSummaryBackend`, `BillingSummaryBackend`.
- KEY DISCRIMINATOR FACT: `testASpendCappedSessionIsRefusedBeforeTheTierAndNeverAsksTheModel` :795 proves submit()-driven capped sessions NEVER reach :9357 (refusal runs first at :6135). The E31 test therefore must reach the tier PAST the refusal — use `ReflectionMethod` on `scheduleParkedCompaction` (reflection is already this file's idiom, :192-204) or an equivalent direct-drive; a submit()-driven test CANNOT satisfy the plan's deletion-experiment bar.
- `mutate()` :5772+ builds the FULL constructor-prop list with current values then overlays `$changes`: a new readonly promoted prop needs exactly ONE entry there; every existing mutate() then carries it automatically; only transition sites name it explicitly.
- IdleCompactionPolicy.php = 79 lines PURE static (IDLE_SECONDS :31, shouldPrompt :63-78); class docblock :7-22 calls it "the one place its two numbers live" — its charter is the IDLE tier; D3 widens it and must say so plainly while keeping the idle-tier prose true.
- submit()'s sync 85% continuation + `$compactionNotice` plumbing ~:6150-6195; 95% 'Blocked until' refusal precedent ~:6150-6170; `spendCapTurnRefusal` :12745-12780 (visible-refusal template: keeps the draft, Role::System notice). New notice must obey :9411-9415 "keep new messages short — transcript rows are unwrapped" rule (95% refusal 423 chars, idle advisory 391 — a NEW message must not join them).

## Deliverables
D1 BREAKER STATE (Chat.php): new readonly promoted `private readonly int $consecutiveRefillCompactions = 0` with docblock citing prompt_expand.md §4.23 (the Claude Code changelog quote at :1821-1823) and THIS step; ONE new entry in mutate()'s list; transitions per rulings R-A/R-B below.
D2 BREAKER TRIP (Chat.php): when the counter REACHES IdleCompactionPolicy::REFILL_LIMIT, the automatic 85% tier stops: no model ask, no repeat heuristic churn — refuse the turn with a short actionable Role::System notice (draft kept, mirror spendCapTurnRefusal shape): says compaction ran 3 consecutive times and the context refilled each time; the preserved recent window is what will not fit; exits: /rewind or /clear, trim the largest recent tool outputs, or /model for a larger window. Cite §4.23 in code comments.
D3 BREAKER NUMBERS (IdleCompactionPolicy.php): `public const REFILL_LIMIT = 3` + `public static function thrashTripped(int $consecutiveRefills): bool` (pure; `>=` boundary pinned); widen the class docblock truthfully (the file now carries a second tier's number; keep the idle-tier sentences intact).
D4 E31 SHAPE (Chat.php): KEEP the :9357 gate (wire-don't-remove policy, backlog :1779) but convert silent null → the sibling's told-in-words shape: extract the /compact notice's first sentence bytes VERBATIM ('Spend cap reached ($%.4f of $%.4f), so the model was not asked to summarise — compacted with the local heuristic instead. ') into ONE private helper both cap arms call; the parked arm's variant gets a parked-appropriate tail (the sibling's '/budget … run /compact again' tail assumes a /compact context — for the parked route the prompt is ALREADY being sent on the heuristic, so the tail must not tell the user to run /compact again as if it were optional; builder composes ONE tail, records the wording decision in the docblock). Return shape: scheduleParkedCompaction returning null currently makes submit() do its sync heuristic compaction — the NOTICE must therefore reach the transcript without double-echoing the prompt (prompt is NOT yet echoed at :9357; submit's sync continuation echoes it). Read submit() :6100-6260 CAREFULLY and choose: (a) return [$this-with-notice-appended… no — the notice BEFORE the prompt is load-bearing :9361-9401] or (b) pass the notice into the sync continuation via the existing $compactionNotice plumbing, or (c) return `compactNow(...)` immediately like the /compact arm does — decide by reading which keeps the parked-route ordering properties (notice-before-prompt, no assistant-role after prompt, single echo). RECORD the choice + why in the docblock rewrite of :9328-9339. The rewritten docblock must no longer say "UNASSERTED" once the test exists.
D5 E32 WIRING (Chat.php): in scheduleParkedCompaction create `new CancellationToken()`, pass it as a NEW third param `?CancellationToken $cancellation = null` into buildSummarizationRequest, forward at :9243 → `completeAsync($prompt, null, $cancellation)`, and store `'inFlightCancellation' => $token` in the mutate at :9410-9433. The double-Escape arm needs NO logic change (:1535 flips a now-real token; :1571 already releases the latch) — but its :1549-1560 comment says the generation bump "does NOT cover a summarization" — that stays TRUE (latch-based) and must not be mangled; add ONE sentence noting the parked summarization is now token-cancellable too. Rewrite :9319-9321 truthfully. /compact route (:9149): passes null — document WHY (no inFlight window; one shared seam, two cancel triggers — backlog E32 :1815-1818). NO TIMEOUTS ANYWHERE (prompt_expand.md §9.12 :3650-3651 standing policy; cancellation is the mechanism). State the best-effort nature per Backend.php:77-84 (which real summary backends poll the token is NOT this step's claim — check quickly, record what you saw, do not over-claim).

## Done-when + experiments (plan :2821-2823 — the E31 bar is the MUTATION)
- E31: `tests/Chat/AutomaticCompactionModelSummaryTest.php` gains a test that direct-drives the capped tier (ReflectionMethod idiom OK) and asserts: no summarization request built (summarizer->calls stays 0), no latch, the shared-notice text reaches the transcript exactly once, and the prompt still gets its heuristic compaction. EXPERIMENT REQUIRED: delete the :9357-9359 gate → this test goes RED (before the change the whole suite is green — record that reproduction: deletion-experiment-on-old-code shows the OLD test blindness, then show the NEW test kills it). Record both.
- BREAKER: fixture where each compaction refills (recent-window pairs big enough that post-compact estimate stays >= the 85% tier on the 88_000-token RecordingTurnBackend — reuse compactablePairs()/system-blob shapes at :130-136 if helpful): assert the 1st/2nd refill increments WITHOUT changing today's behavior, the 3rd trips; after trip the next turn is refused with the actionable notice (draft kept, summarizer->calls frozen); after a successful under-tier compaction the counter resets and the tier works again. Pin /compact manual runs are unaffected by the counter (R-C).
- E32: park a turn; double-Escape (two KeyMsg Escape within DOUBLE_ESCAPE_WINDOW_SECONDS via update()); assert `cancellationOf($cancelled)->isCancelled()` is TRUE post-escape — measured against the parked window's token (note cancellationOf reads the CURRENT chat; capture the token object at park time via reflection before the cancel). With a token-polling fake summarizer (write a small one in the test file — poll isCancelled and reject): assert the rejected landing carries no usage, is dropped or error-reported WITHOUT dispatching the parked turn (main->calls 0), and `_Request cancelled._` still appears once. If `testAnAbandonedSummarizationIsStillBilled` breaks because its fake now honors the token, fix by keeping THAT fake token-blind (it pins billing honesty of the already-landed call, not cancellation) — never weaken its assertions (§1.11).
- IdleCompactionPolicyTest: pin REFILL_LIMIT==3, thrashTripped boundary 2->false/3->true, and that shouldPrompt/IDLE_SECONDS are unchanged (public surface widened, not moved).

## Rulings (FIXED — deviate = stop and report)
R-A "immediately refill to the limit" is MEASURED AT COMPACTION COMPLETION: after the rewrite, `estimateTokenCount(newHistory) >= 85% tier of contextTokenLimit()` — the same estimate+limit pair submit()'s 85% block uses (:6150-6195 region; read the exact comparison and MIRROR it, do not re-invent the math; the 85 figure comes from CompactorConfig backgroundThreshold — use the config value, not a literal). Justification recorded in code comments: if the rewrite is still at/over the tier, the very next prompt re-enters the tier with no work in between — that IS "refills immediately" per §4.23's phrase.
R-B increment happens on the PARKED landing completion AND on the sync heuristic 85% compaction (both automatic-tier outputs); reset on any automatic-tier compaction whose post-state is UNDER the tier; /clear resets to 0 (explicit thread at :7696 beside pendingCompactionId). Offline path (summaryBackend null) still counts — R-A measures the heuristic rewrite too; the thrash §4.23 names is the tier, not the provider.
R-C scope: the breaker guards ONLY the automatic 85% tier. Manual /compact NEVER blocked (user-chosen; it keeps its own spend-cap notice); 95% blocking refusal unchanged.
R-D trip is sustained-while-over (not a one-shot): each auto-tier attempt while `thrashTripped(counter)` refuses; a real shrink restores service.
R-E the /compact notice's existing pinned bytes are untouchable; sharing extracts the first-sentence string into the helper such that scheduleModelCompaction composes byte-identical output (verify with its existing tests).
R-F no env var, no README roster entry for REFILL_LIMIT (constant + pure predicate only). If any drift guard (EnvRoster/ReadmeRoster/TreeWideGuardRoster) demands doc surface for the new public const, HALT and report rather than writing docs.

## E38 (plan :2824-2826) — RULED: DO NOT TOUCH. The reminder→`[summary] ` rider fold lives in ContextCompactor.php (this step's file ceiling excludes it; P8.S4 just touched it — churn rule). The 171-byte figure in the plan is ALSO stale (riders truncate at 120 since the P8.S2-era rider-truncate rider; verify the current width if you cite it anywhere, and prefer citing no number at all). Mention in the final report that E38 was skipped-with-reason.

## Hazards
- Law 4n: GlobFigureDriftTest re-derives one assertion per docblock/comment paragraph across src/**.php — predict its delta per new paragraph you add in Chat.php/IdleCompactionPolicy.php (P8.S4 precedent: each blank-line-separated prose unit = +1 assertion). Do NOT write any numeric figure into prose that a census might pin incorrectly (the '3' in comments is fine — THRASH is the const's value; do not restate counts of enforcement sites that already exist in prose).
- Seam census BootstrapTranscriptSeamCallSiteCensusTest (20, frozen): your notices are NOT that seam — expect unchanged; solo-run it before the full suite.
- Goldens UNMOVED or you have wired something into the prompt path wrongly — HALT.
- Do not change update()'s blanket-swallow or the Escape arm's ordering; D5 is token-plumbing only.
- The compaction docblock at :9295-9321 claims which keys are live in the parked window; if anything you add changes that set (it should not — Escape already live), the docblock MUST be updated truthfully.
- Tests: same-file additions; ZERO test-line removals (§1.11); if an existing pin conflicts with the new behavior, STOP and report — do not edit the old assertion.
- Full-suite protocol: cwd /home/sites/prompt-step-P8.S5, serial, </dev/null, --colors=never, box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0 first; prediction written BEFORE the run; junit to your scratch /tmp/opencode/P8.S5-build/tip.xml.
- Commits: two (src, tests), messages in /tmp/opencode/P8.S5-build/, identity from `git config` interpolation (echo sanitized placeholders NEVER), scans: `git cat-file commit HEAD | /usr/bin/grep -c '\[EMAIL\]'` == 0, `%B | /usr/bin/grep -c '<'` == 0, `%an|%ae|%cn|%ce` == config ×4. No push. No composer. No caliber. No `git init` outside your scratch (ORCHESTRATION-RULE-2/3).
- Deletion/§1.11 experiments live in the tree then restore; `git status --porcelain` empty between commits.

## Report format
Branch tip SHAs; D1-D5 disposition each (including the D4 return-shape decision and the D5 best-effort backend observation); done-when numbers; ALL deletion experiments with exact red-sets; movers per class vs floor; GlobFigureDrift delta vs prediction; goldens; census; hygiene scans verbatim; surprises; deviations from this brief (must be zero without a stop-report).
