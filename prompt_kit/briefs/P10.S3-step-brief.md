# P10.S3 step brief — the kill switch and the minimum-prefix guard

PLAN: prompt_plan.md `### P10.S3` (read it; the Goal/Hard-constraint/Done-when bind). Expand sources: §4.15 (prompt_expand.md ~:1269-1376) and §9.5 (~:3536-3551). Worktree: /home/sites/prompt-step-P10.S3, branch prompt/P10.S3. Sibling concurrent step P10.S4 works in its own worktree — NEVER look at or touch /home/sites/prompt-step-P10.S4.

CEILING (edits/creates ONLY these; anything else = HALT and report):
1. sugar-crush/src/Providers/CacheBreakpoints.php (EDIT)
2. sugar-crush/tests/Providers/CacheBreakpointsTest.php (EDIT)
3. sugar-crush/docs/ENVIRONMENT.md (EDIT — required by the plan's hard constraint: the roster test is a SELF-REGISTERING scanner diffing src reads against this page both directions; the getenv literal registers automatically; you MUST ship the page row in the same commit or testEveryVariableTheCodeReadsHasARowOnTheEnvironmentPage reddens. Do NOT edit tests/Config/EnvRosterDriftTest.php — editing it would be §1.10 tampering with a guard.)
NOTE: the plan also lists Cli/Bootstrap.php. ORCHESTRATOR RULING S3-R1: no Bootstrap edit is licensed. A launch-time warning would announce a switch for a capability that has no production caller — the page and docblock must say so honestly instead. If you conclude a live notice is unavoidable, HALT and report; do not improvise (a new Bootstrap warn would move TRANSCRIPT_SEAM_CALL_SITES/StderrEmitterCensus pinned integers — unlicensed).

DESIGN — FIXED by orchestrator adjudication (scout-quoted evidence in prompt_kit/findings notes; implement as stated, do not re-litigate):
A. Kill switch. `public const DISABLE_ENV = 'SUGARCRUSH_DISABLE_PROMPT_CACHE';` + `public static function disabledFromEnvironment(): bool` implementing the repo presence-flag semantics VERBATIM: value !== false && !== '' && !== '0' means disabled (precedent: Chat.php envFlag :5231-5241 and EngineBackend.php parallelToolCallsEnabled :781-791 — copy the three-condition form). Instance toggle: add a constructor with one trailing defaulted param `bool $marksEnabled = true` (class currently has NO constructor — 17 tests call `new CacheBreakpoints()` bare; default-true keeps every existing path byte-identical). When disabled: apply() STILL validates (shape throws), STILL counts automatic marks and STILL throws the F-1 over-cap breach (the input-contract story is input-derived), STILL wipes ephemeral marks, then returns with ZERO ephemeral marks added (wipe-then-no-mark early return). Document the choice: disabling means "no NEW breakpoints"; preserved foreign marks are untouched (§1.10: we never remove what others set). Amend the DETERMINISM docblock paragraph: "no environment" scopes to apply() itself — the static reader is the only getenv and it is called by consumers, not inside apply().
B. Minimum-prefix diagnostic, N=3. `public const CONSECUTIVE_ZERO_REPORTS_THRESHOLD = 3;` (rationale in docblock: PermissionGate.php STRIKE_THRESHOLD = 3 idiom; neither plan nor expand names N — verified absence). `public function observeCacheHealth(?\SugarCraft\Crush\Usage $usage): ?string` — instance counter (precedent PermissionGate consecutiveBlocks :78/:385-396): fires iff usage !== null AND cacheReadTokens === 0 AND cacheCreationTokens === 0 (both REPORTED zero — null means unreported and must NOT count, per Usage.php :103-106 null-vs-0 doctrine; expand §4.15 :1376 "both ... = 0 means no cache"); a reported non-zero in either bucket resets; unreported (null usage or null buckets) resets without firing. On the 3rd consecutive qualifying report returns a non-null diagnostic string naming the threshold, the two buckets, and the 512-to-4096 model-dependent minimum (§4.15); every subsequent qualifying report also returns it (consumer decides surfacing; no internal suppression state beyond the counter). Pure: no I/O, no clock.
C. The live-channel residual — REQUIRED docblock paragraph + worklog-worthy prose: at master there is NO production path that can feed observe() — CompleteResponse carries no cache fields (quote-free prose citing the boundary: providers parse Usage but the unary response path routes only total+cost; Runtime rebuilds from those). Live surfacing ships WITH the wiring/E17 step that carries cache buckets across the response boundary. Record it plainly; do NOT invent a channel.
D. LOW-38 stays DEFERRED (orchestrator ruling): do not add cache_control value-shape validation; add ONE known-defect sentence to the class docblock naming the asymmetry (non-array marks: block/tool levels ride uncounted, message level dropped) so the wiring step inherits it knowingly.

TESTS (all in CacheBreakpointsTest.php, value-pinned, each reddens on revert — §1.11):
1. disabledFromEnvironment truth table: unset, '', '0' => false; '1', 'true' => true. Env hygiene MANDATORY: capture-originals in setUp + ternary restore in tearDown EXACTLY per tests/Backend/EngineBackendParallelConfigTest.php :45-74 (bare putenv($name) restores-to-unset). Never leak a SUGARCRUSH_DISABLE_* across the suite.
2. Disabled instance apply(): same 4-mark-shaped input as testDefaultMarkPlan... => ephemeral census 0, foreign automatic marks byte-intact, marks absent; enabled instance same input => existing pins (prove the toggle is the ONLY difference).
3. Disabled + over-cap input (5 foreign) => STILL throws naming count and cap.
4. observeCacheHealth script: (zero,zero) x2 => null,null; third => non-null (assertSame on message content — not assertIsString); interleaved reported-non-zero resets (z,z,nonzero,z,z => null at call 5? no: fires at third-in-a-row => call 6 fires); unreported-null usage resets + never fires; single-bucket-zero (read=0, creation=5) never counts. Pin the counter boundary BOTH ways.
Deletion experiments for every test above (report what reverts to what red).

GOLDENS: FROZEN. system f5de3858c1bc130d1e97a120d3ead485 / 8,278 B; agent ef0326dd38535aaa2f1d715919bff26e / 1,060 B. This step touches no prompt assembly; any golden movement = HALT, you wired it wrong (P9.S7 rule).

SUITE PREDICTION (law 5/4n/8): floor anchor Tests 11,148 / Assertions 170,600 / 0F / 0E / Skipped 2 / EXIT 0 at master 4ab8c3f98 (sugar-crush tree = 341d71876). NO new src file => NO BinSugarcrushWiringTest +1t/+6a; expect: your new tests, GlobFigureDriftTest +1 per new docblock paragraph in CacheBreakpoints.php (and any in docs? no — src files + the new test file reads: +2 censusScope reads for a new TEST file too per refined law 8), EnvRosterDriftTest growth over the new var in both scans (measure its solo), SymbolCitationDrift +1 per placed {@see}. Predict from per-class solos at your FINAL tip BEFORE the full run; reconcile with python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py vs a baseline junit — remainder 0 or name every mover. Full suite: box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0; cwd /home/sites/prompt-step-P10.S3; `php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P10.S3/tip.xml </dev/null`. Run suites IN-SESSION, wait on your own process, never return "waiting for tests".

COMMIT HYGIENE: git config must be Joe Huss / detain@interserver.net (verify; type the literal address — NEVER copy a redacted placeholder token you can see in context); commit via `git commit -F /tmp/opencode/P10.S3/msg.txt`; message TAG-FREE: `/usr/bin/grep -c '[<>]' msg.txt` == 0 (no <, no > — spell "to" instead of =>); after each commit run the law-4g triple scan (cat-file object [EMAIL]-count 0; %B [EMAIL]-count 0; %an|%ae|%cn|%ce exact). One commit; message subject like "sugar-crush: prompt-cache kill switch and the sub-minimum diagnostic, unwired with its contract" (your wording, tag-free).

PLAN LESSONS you must obey (prompt_plan.md §1.10, §1.11, §16, §17 — read them): removal is never an outcome; no annotation/existence checks as tests; a figure names its tree; every docblock says WHY; cite "Mirrors charmbracelet/crush CacheBreakpoints" where the expand section maps.

REPORT FORMAT (final message): files+diffstat; public-surface quoted; each test -> the value it pins; deletion experiments table; roster proof (EnvRosterDriftTest solo green AT the tip WITH your row — run it); goldens re-verified (md5+size); solo CacheBreakpointsTest N/N; full-suite prediction-before-run figures + actual + cmp movers table + remainder; commit sha(s); worktree porcelain clean. If ANY premise you find contradicts this brief (line numbers, missing symbols), trust the CODE, report the drift — the anchors here were quoted from master 4ab8c3f98 today but law 4j says re-derive.
