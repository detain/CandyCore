# P7.S5 — Step brief: body-less native presets stop clobbering built-in prompts (tier merge at the roster seam)

Worktree `/home/sites/prompt-step-P7.S5` on `prompt/P7.S5` @ `4e7f79014`. Read first: this brief; `prompt_kit/findings/P7.S5-premise.md` (all cites re-anchored at your base); plan `prompt_plan.md` :2632-2659 (step text), §1.10, §1.11, §16, §17. Premise CONFIRMED the clobber at all four links: preset body-less ⇒ `resolveInitialPrompt` null (AgentPresetRegistry.php:420-427) ⇒ `Agent::fromPreset` `prompt: $preset->initialPrompt ?? ''` (Agent.php:256) ⇒ `agentRoster()` native layer `$agents[$name] = ...` overwrite (Bootstrap.php:1639-1641) over built-ins registered :1625-1637. Live proof already in-repo: tests/Cli/AgentManagerWiringTest.php:276-303 (`testBuiltinPresetFilesWinOverTheCodeDefinitions`) asserts the REAL bundle presets resolve to `''`.

## ORCHESTRATOR DECISIONS (fixed; do not relitigate; a falsifying measurement = HALT + report)
- **R-1 MERGE AT LOAD, not refuse.** The fix lands in `Bootstrap::agentRoster()`'s native layer. Registry refuses-at-load is WRONG: `resolveInitialPrompt`'s null is the documented "no prompt" value (its :417-418 docblock), a body-less preset with no built-in twin is legitimate (custom agents), and refusing drops roster entries = regression. Built-ins are invisible to AgentPresetRegistry — the roster is the ONLY seam that sees both tiers.
- **R-2 Tier semantics.** Body-less native preset whose name shadows a built-in with NON-EMPTY prompt: the Agent's `prompt` becomes the BUILT-IN prompt (frontmatter model/effort/permission/skills/color etc. from the preset UNCHANGED — frontmatter-only merge). Tier attribution ("which tier supplied the prompt" per the plan's done-when) rides with it: append exactly ONE sentence to the inherited text, `Your operating instructions were supplied by the built-in agent definition '<name>'; this preset contributed only its frontmatter settings.` — built as ONE constant + ONE helper so the literal cannot drift. IF the append breaks existing byte-pins you did NOT author (grep tests for resolved-prompt equality asserts first — note AgentDefinitionTest:260-280 pins SOURCE lines, not resolved text, so it survives): keep append; if impossible, put attribution in the recorded notice only and say so in the report. Body-less AND NO built-in twin (real case: `security-auditor`): leave prompt null→'' EXACTLY as today (no invented content — see R-3) but RECORD the notice.
- **R-3 DO NOT write prompt bodies into the three `.sugar-crush/agents/*.md`.** Content creation is not this step's authority. coder/reviewer recover their P5.10b prompts via R-2; security-auditor ships with attribution-notice only — record "preset with no prompt and no built-in twin" as a follow-up for the user.
- **R-4 C7 constraint: NEVER author prompt text asserting a tool grant.** Built-ins verified none assert one (premise §3); preserve verbatim. The reviewer built-in's "skills you have been granted" phrasing: inherit byte-verbatim, do not touch, record as observation. `AgentDefinition.php` stays UNTOUCHED (§5 C7 row); no src/Agents/AgentDefinition.php edit in ANY form (not even docblock).
- **R-5 Recorded reason, non-silent.** New refusals/notices ride the EXISTING per-tier collector the roster already builds (`$described` :1612, :1615-1619, keyed `.sugar-crush/agents/<name>.md: <msg>` → `Tui\AgentPresets::reportRefusals()` → `Chat::agentPresetRefusals`). Add a `refusedPresets()` accessor on `AgentPresetRegistry` (registry records at parse — WHY: the registry is where body-ness is known AND `load()` single-file path needs it; roster consumes the record; both views stay consistent) — R-1's "refuse NOT at load" refers to NOT THROWING / NOT dropping; recording-at-load + merging-at-roster is the design. Message distinguishes the two cases (inherited: names BOTH tiers; absent twin: "no prompt in body or frontmatter and no built-in definition of this name to inherit").

## Files in scope
- `src/Cli/Bootstrap.php` — `agentRoster()` native layer (:1639-1641) + roster docblock (:1530-1540, :1551-1556 — update precedence narrative).
- `src/Agents/AgentPresetRegistry.php` — record body-ness at parse + `refusedPresets()` accessor. NO behavior change to load/list/resolve.
- `src/Agents/Agent.php` — docblock ONLY: correct :244-246 ("An empty `initialPrompt` therefore produces an empty prompt" now false for preset-shadowing-built-in — re-state what is true after the fix).
- `src/Agents/AgentDefinition.php` — docblock ONLY IF a statement becomes false (:17-22); touching this file is the live §5 C7 row — any edit needs explicit justification in the report; default LEAVE UNTOUCHED.
- `tests/Agents/AgentPresetTest.php` (27/99 baseline), `tests/Cli/AgentManagerWiringTest.php` (21/74), `tests/Tui/AgentPresetsTest.php` (5) — ADD + the ONE flip below; nothing else removed/softened.
- `.sugar-crush/agents/*.md` — NOT touched (R-3).

## Mandatory test coverage (§1.11, every one red-on-revert)
1. REAL-bundle rewrite (the plan's completion condition): `testBuiltinPresetFilesWinOverTheCodeDefinitions` — rewrite to assert resolved coder/reviewer prompt is NON-EMPTY, EQUALS built-in text + attribution, `isolation`/`color` still preset-sourced (its existing 295-302 teeth kept); record as the single justified polarity flip (§1.11-compliant: step's PURPOSE).
2. Body-less + no-twin → prompt stays '' + `refusedPresets()` names the file, BOTH tiers visible.
3. Non-empty body → no inheritance, no notice, `refusedPresets()` stays empty.
4. `load()` (not `list()`) path records identically.
5. Attribution string byte-pinned via the constant + helper; helper unit test (trim/`"\n\n"` join, no double-append when prompt already non-empty).
6. Full-roster integrity after fix: 13 names (10 + coder/reviewer/security-auditor) still exactly once each; foreign `coder` still wins (precedence :1601-1604 claim — test it).
7. Optional `?AgentDefinition $builtins = null` param BC: null → today's behavior verbatim (pin).
8. `agentPresetRefusals` reaches Chat transcript surface for the inherited case (follow AgentManagerWiringTest.php:735-739 pattern; if the seam fights back, record why + test accessor layer).

## Suite protocol
FLOOR **10,997 / 168,995 / 0F / 0E / 2 Skipped / EXIT 0** gated `a906e9c72`, belt `5deb98fe8`. Prediction-first from final-tip per-class solos; box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0; full suite IN-SESSION: `cd /home/sites/prompt-step-P7.S5 && php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/P7.S5-junit.xml </dev/null`; reconcile vs base junit via `python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py` — zero unexplained remainder. Predict: AgentPresetTest +6..8, AgentManagerWiringTest +4..6, GlobFigureDriftTest ±docblock-paragraph units (Chat.php untouched → no +30 class jump; Bootstrap.php docblock + roster edits → count with the instrument's own parser), AssertionSwallowingCatchTest +1 per NEW try/finally in TESTS (law 4d), TreeWideGuardRosterTest +1..2, BinSugarcrushWiringTest FLAT (no new src/ file). Per-class mover splits are CLAIMS; the final gate cmp is authority (law 4i). Goldens MUST NOT move: system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 + agent `ef0326dd38535aaa2f1d715919bff26e`/1,060 (`git diff --stat 4e7f79014 -- sugar-crush/tests/fixtures/` empty). Law 4h: CLEAN TREE whenever counting `## Skill`/system-prompt substr_count pins; never leave `## Skill` bare prose in any touched file. If a census demands editing a derived figure: HALT, report, never hand-edit.

## Report
Files+diffstat vs base · every new/rewritten test + what it pins + red-on-revert output (R-1 revert: the append line removed from roster → tests 1/5 red; registry-record removed → 2/4 red) · suite headline + prediction + cmp table + remainder · goldens md5/size + fixture diff empty · census absolutes for movers · §5/§17.1 record (no new src/ FILE → no roster growth) · deviations + reason · follow-ups (security-auditor body authoring, reviewer skill-grant phrasing, any §1.10 observations) · final `git status --porcelain` + commit sha + `[EMAIL]` scan (identity Joe Huss/`detain@interserver.net`, msg `-F` after `grep -c '<'` == 0). ONE commit. NEVER push/merge.
