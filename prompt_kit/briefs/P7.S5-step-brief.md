# P7.S5 step brief v2 (re-anchored) — body-less native presets stop clobbering built-in prompts

THIS REPLACES the v1 brief of `e9eca1f51` IN FULL. v1 rode five fabricated anchors and wrong baselines.
Read first: this brief; `prompt_kit/findings/P7.S5-premise.md` **v2** (the anchor of record — where the
forensic file and this brief disagree about a line number, the forensic file is the one that is wrong);
`prompt_plan.md` :2632-2659 (step text), §1.10, §1.11, §16, §17; `prompt_expand.md` §3.1, §2.7, §12.4
items 2 and 5.

## Where you work
- `/home/sites/prompt-step-P7.S5`, branch `prompt/P7.S5` @ `4e7f79014`, PORCELAIN-CLEAN (the previous
  builder halted at zero edits — nothing on disk to continue from; you start fresh). Its `sugar-crush/`
  tree is byte-identical to master `9344e7546`. `vendor/` is a real hard-linked dir; PSR-4 root already
  verified pointing at the worktree's own `src` (2026-09-07).
- Re-verify every `file:line` you rely on with a direct read BEFORE it binds an edit (law 4j cuts both
  ways — a falsified premise anchor = HALT + report at zero edits).
- Scratchpad: your OWN private subdir (create `/tmp/opencode/P7.S5-builder2/`), all backups/experiments
  inside it; never `rm -rf` anything you did not create; never create a scratch git repo outside it;
  verify `pwd` before any `git init`/`git commit` (ORCHESTRATION-RULES-2/3).

## The defect (premise v2 §1-§2, all confirmed)
Three bundled presets are frontmatter-only; `resolveInitialPrompt` returns null (:426); `fromPreset`
maps null to `''` (:269); the native loop at `Bootstrap.php:1639-1641` overwrites the built-in
`coder`/`reviewer` roster entries with the empty prompt, so every sub-agent launched through the
roster arrives carrying only its environment block. `security-auditor` adds a 7th entry, also empty.

## ORCHESTRATOR DECISIONS (fixed; do not relitigate; a falsifying measurement = HALT + report)
- **R-1 MERGE AT THE ROSTER.** The fix lands in `Bootstrap::agentRoster()`'s native loop only. The
  registry stays untouched; built-ins are invisible to `AgentPresetRegistry` and its instances are
  transient — the roster is the only seam that sees both tiers.
- **R-2 Tier semantics.** In the native loop, BEFORE `$agents[$name] = Agent::fromPreset(...)`: if
  `$preset->initialPrompt === null` AND `isset($agents[$name])` AND the existing entry's prompt is
  non-empty — the resolved Agent carries: existing-prompt text + `"\n\n"` + EXACTLY ONE attribution
  sentence; every frontmatter-derived field (model/tools/skills/color/isolation/permissionMode...)
  still comes from the PRESET (frontmatter-only merge, nothing dropped). Attribution sentence, one
  private const rendered with the definition name via a tiny private static helper:
  built-in tier: `Your operating instructions were supplied by the built-in agent definition '<name>'; this preset contributed only its frontmatter settings.`
  imported (foreign) tier: identical sentence with `imported agent definition` in place of `built-in`.
  Determine the tier by membership of `$name` in the six built-in names your loop already enumerates —
  no provenance field needed. Append ONLY when the inherited prompt does not already end with the
  rendered sentence (idempotence guard — cheap, and a second `agentRoster()` call over the same tree
  must return byte-identical prompts, pinned by test). If the append would redden an EXISTING
  byte-equality pin you did not author (grep first — v1 believed `AgentDefinitionTest:260-280` pins
  SOURCE lines, verify): do NOT touch that test — fall back to attribution-in-notice-only and report
  it prominently. Body-less AND no non-empty shadowed entry (real case: `security-auditor`): prompt
  stays `''` EXACTLY as today (no invented content) + the R-5 notice.
- **R-3 DO NOT author prompt bodies into `.sugar-crush/agents/*.md`** — all three files untouchable;
  content creation is not this step's authority (record "security-auditor has no body and no built-in
  twin" as a user follow-up).
- **R-4 C7.** No prompt text you author may assert a tool grant. Inherited built-ins are copied
  byte-verbatim — including the reviewer's skill-grant phrasing (observation to record, not edit).
  `src/Agents/AgentDefinition.php` and `src/Agents/AgentManager.php` are UNTOUCHABLE (not even a
  docblock; C7 re-verified unfixed 2026-09-07 — premise v2 §5).
- **R-5 The recorded reason rides the REAL seam.** The no-twin case calls the existing private static
  `Bootstrap::warnPermissionConfigInTranscript()` (`:4634-4662`, same class — no visibility change)
  directly from `agentRoster()`, one message per merged/absent preset file. Message shape: file path
  first, then the reason — the inherit case needs NO notice (the in-prompt sentence IS its record,
  per ruling P7.S5-R1); the no-twin case wording ends
  `declares no prompt in body or frontmatter and no agent definition to inherit; the agent carries only its environment block`.
  There is NO `Tui\AgentPresets`, NO `$described`, NO `Chat::agentPresetRefusals`, NO
  `AgentPresetRegistry::refusedPresets()` — v1 named all of them; none exists (premise v2 header).
  Seam census: `warnPermissionConfigInTranscript` call sites are counted by derived guards — adding
  sites moves them adaptively (zero literal edits; verify, never hand-edit a census literal);
  PROSE ordinals in files you touch (e.g. the comment at `:1170-1173` "TWENTY call sites ... This said
  SIXTEEN") must be reconciled with your measured counts in the same commit (P7.S3 precedent).
  Static-state hazard: `$launchNotices` is process-global — your tests must assert DELTAS around the
  roster call (snapshot `Bootstrap::launchNotices()` before/after) so they are order-independent and
  leak nothing across tests (§7 no-weaken law; isolation pinned by construction).
- **R-6 `fromPreset` and `resolveInitialPrompt` are UNCHANGED** — the null semantics are documented
  and pinned (`tests/Agents/AgentPresetTest.php` initial-prompt tests stay green; file is 15/31 today,
  NOT v1's 27/99 — re-derive before citing). The merge lives one layer up.

## Files in scope (complete list; anything else = scope violation)
- `src/Cli/Bootstrap.php` — native loop :1639-1641 + attribution const(s) + helper + the no-twin
  notice call; `agentRoster` docblock precedence narrative; prose-ordinal reconciliation (R-5).
- `tests/Cli/AgentManagerWiringTest.php` — ALL new roster coverage goes HERE (extend the file; create
  NO new test files — derived censuses move on file counts and the home already exists).
- `tests/Agents/AgentPresetTest.php` — expected UNCHANGED; touch only if a preset-level pin genuinely
  belongs there and report why.
- `src/Agents/Agent.php` — docblock ONLY if the merge makes a sentence false (R-6 says the fromPreset
  statements remain true — expected UNTOUCHED; if you edit, quote the false sentence in your report).
- NOT touched: `AgentDefinition.php`, `AgentManager.php`, `Chat.php`, `ContextCompactor.php`,
  `EngineBackend.php`, `AgentPresetRegistry.php`, `.sugar-crush/**`, any workflow file. No new src files.

## Mandatory test coverage (every item §1.11 — calls the thing, asserts the VALUE, red-on-revert;
state the deletion experiment)
1. REAL bundle (plan :2658 done-when): `agentRoster()` with `$root` = the repo checkout root (test
   walks up from `__DIR__`, asserts the three preset files exist first) and the user tier pinned to
   an empty temp HOME (reuse the existing `tests/Agents/AgentPreset*Test` /
   `ForeignAgentPresetWiringTest` HOME-pinning patterns — the user tier must contribute zero entries
   on whatever machine runs CI). Assert `assertSame` byte-equality: resolved coder prompt === the
   built-in coder definition prompt + `"\n\n"` + the built-in attribution sentence (read the built-in
   text via `AgentDefinition::fromType('coder','coder')->prompt` at runtime — do NOT retype the
   paragraph); same for reviewer; `security-auditor` prompt === `''` AND the launchNotices delta
   contains a line naming its path. At least one preset-sourced field per shadowed agent (reviewer's
   isolation/color) still equals the PRESET's value.
2. Synthetic inherit: temp root, frontmatter-only `coder.md` with a distinctive `color:` the built-in
   lacks — prompt === built-in text + attribution; color === preset's (the "which tier supplied what" pin).
3. Synthetic inherit-from-foreign: a foreign-tier agent named e.g. `zorg` with a real prompt + native
   body-less `zorg.md` — inherited prompt === foreign text + the IMPORTED attribution sentence.
4. Synthetic no-twin: native body-less `solo.md` — prompt === `''` verbatim + launchNotices delta
   names the file (both polarities of R-5 visible).
5. Non-effects: native preset WITH a body — body verbatim, no attribution suffix, no notice; native
   preset with declared `initialPrompt:` — declared wins verbatim, no suffix.
6. Roster integrity: over the item-2 root — each name exactly once, total count equals the built-in
   run plus distinct preset names; foreign `coder` still overwritten by the built-in `coder`
   (ordering :1621-1637 untouched); two consecutive `agentRoster()` calls on the same root return
   byte-identical lists (R-2 idempotence + global-state non-mutation).
7. Attribution byte-pin: helper renders the two sentences from the consts (fixed-literal assertSame,
   both tier words); no triple newline; empty-inherited short-circuits (helper returns input).

## Suite protocol
- FLOOR **10,997 / 168,995 / 0F / 0E / 2 Skipped / EXIT 0** — gated at `a906e9c72`; it describes your
  base `4e7f79014` and master `9344e7546` by the belt (`git diff a906e9c72 9344e7546 -- sugar-crush/`
  byte-empty — re-derive and state it).
- BEFORE editing: solo-record the per-class base figures for `AgentPresetTest`, `AgentManagerWiringTest`,
  `GlobFigureDriftTest`, `AssertionSwallowingCatchTest`, `TreeWideGuardRosterTest`, `BinSugarcrushWiringTest`,
  the transcript-seam census class(es) (grep for `warnPermissionConfigInTranscript` in tests/), and
  `MouseModalGuardTest` (arm note: 198 vs 201 is viewport, not behavior — never adjudicate the ±3 by
  headline).
- PREDICT the headline from your FINAL-TIP per-class solos BEFORE the full run (process law 5), or
  label it derived-after-the-fact. Box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'`
  must print 0 before the run. Full suite IN-SESSION from the worktree root:
  `cd /home/sites/prompt-step-P7.S5 && php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P7.S5-builder2/junit.xml </dev/null | tail -4`.
  COMMIT your work before the final full-suite run (law 4h — prompt-count pins read the tree; a dirty
  worktree or a `## Skill`-shaped literal in touched files poisons them). No new src file ⇒
  `BinSugarcrushWiringTest` FLAT (law 4e). Every new try/catch in tests feeds
  `AssertionSwallowingCatchTest` (law 4d). Per-class mover splits are CLAIMS; the final `cmp.py` table
  is authority (law 4i) — reconcile your junit vs base where you have both
  (`python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py <a> <b>`).
- GOLDENS MUST NOT MOVE: system `a5c5a14ca2e3ad891933ac7aefccc6af` / 7,732 B and agent
  `ef0326dd38535aaa2f1d715919bff26e` / 1,060 B. The roster merge changes no prompt assembler; a move
  would itself be the defect — if you are forced toward one, HALT and report (P7.S3 pattern: license
  is granted by the orchestrator, never assumed). `git diff --stat 4e7f79014 -- sugar-crush/tests/fixtures/`
  must be EMPTY at finish.
- If any derived census cannot move adaptively (a literal would need editing): HALT + report, never
  hand-edit, never weaken/skip/rename a test (§7).

## Report (measured output pasted, not claims)
files + diffstat vs `4e7f79014` · every new test + what it pins + the red-on-revert runs (revert the
inherit branch ⇒ items 1/2/3/6 expect red; revert the notice ⇒ item 4; restore green; porcelain clean
after each experiment) · headline prediction vs actual + cmp table · goldens md5/size + fixtures-diff
proof · per-class solo table (base and tip) · seam-census before/after + prose-ordinals reconciled ·
§1.10/§17.1 record (nothing removed; no new files) · deviations + reasons · follow-ups
(security-auditor body = user content decision; reviewer skill-grant phrasing observation; foreign-loop
body-less presets stay silent-empty — documented residual) · ONE commit on `prompt/P7.S5` with literal
identity `Joe Huss <detain@interserver.net>` SHELL-ASSEMBLED (the sanitizer rewrites typed addresses —
use `-c user.email=$(printf '%s@%s' detain interserver.net)`) and the law-4g triple scan pasted
(commit-object bracketed-token count 0 — build the grep pattern via printf too; `%B` count 0; identity
line correct) · message via `-F` file after `grep -c` of both angle brackets returns 0 · NEVER push,
NEVER merge, NEVER touch the main checkout.
