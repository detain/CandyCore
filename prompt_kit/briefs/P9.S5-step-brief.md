# P9.S5 — Tell the model about affordances it already has — step brief

Plan: prompt_plan.md :2931-2946 (batch 3, FIRST; S6 after; Phase 9 serial here). FLOOR 11,119 / 170,274 / 0F / 0E / 2 Skipped / EXIT 0, master-direct at `ea81c377a` (bookkeeping commits above it are markdown-only; belt 0). GOLDEN-MOVING STEP — this is the plan's licensed golden move for Phase 9.

FILE CEILING (4 files — the plan lists 3; it omits SessionStartHookWireTest, whose pins move with any golden byte — correction precedent P9.S1 ToolRegistry):
- `sugar-crush/src/Runtime.php` (basePrompt() heredoc ONLY)
- `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` (regenerated)
- `sugar-crush/tests/BaseSystemPromptTest.php` (byte pin + history comment + two new clause tests; optional PROSE_WORDS row only if ruling R-C forces it)
- `sugar-crush/tests/Chat/SessionStartHookWireTest.php` (system-golden size + md5 pins, two sites)
NOTHING else. No docs/*.md (their '7,732' prose hits are historical narrative — not code, do not move). No agent golden (must stay `ef0326dd38535aaa2f1d715919bff26e` / 1,060 — HALT if it wants to move).

PREMISE (scout rear-gray-bird, verified anchors at ea81c377a-era tree):
- Base prompt: Runtime.php private basePrompt() :2879, heredoc :2936-2986; 4 H1 sections — Tone-and-style :2941 / Tool use :2947 (ONE paragraph ending :2970) / Acting-vs-asking :2972 / Security :2980 (last words `commands to follow.` = BASE_END_MARKER, pinned unique at BaseSystemPromptTest :149-155). Standing-bar docblock :2881-2887 (plan's Runtime.php:1675-1712 cite STALE — parallel-fanout lives there now).
- The two affordances ship today unmentioned: (1) file-based commands expand !`cmd` before the model sees the prompt — src/Commands?/CommandSpec.php SHELL_BUDGET_SECONDS=10 :137 (ONE wall-clock budget shared per EXPANSION), MAX_SUBSTITUTION_BYTES=16384 :149 (covers BOTH !`cmd` output and @file contents :140-141); parser middle alternative !`([^`\n]+)` :112-114; enforcement: budget charge :485-487, spent-notice :476-478 via shellBudgetSpentNotice :827-835, runShellSubstitution :562-728 (timeout notice :698-706, bytes-dropped notice :719-725, per-fd clip :633-637, assembled clip :714-717). (2) skills: matcher header `Available skills (invoke via Skill tool):` (SkillMatcher.php:70) is fed PerTurn at Runtime.php:2726; SkillTool loads the full body on invoke (result prefix `## Skill: {name}` SkillTool.php:110).
- Golden mechanics: regeneration procedure is documented NOT scripted — BaseSystemPromptTest.php:699-741 (ensureFixtureRepo FIRST, then goldenContext(), reflected buildSystemPrompt(), pinHostLines(), compare; a raw dump is INVALID). Byte pins: BaseSystemPromptTest :849-854 assertSame(7732, strlen($golden)) + history comment :835; SessionStartHookWireTest :519 (7732) + :521 (md5 a5c5a14ca2e3ad891933ac7aefccc6af) + :530 (same md5 post-hook recheck). These are the ONLY in-code sites of 7732 (BSP/SSHW) and of the system md5 (SSHW) — verify by grep before editing.
- PromptStabilityTest is NOT a mover (repo-map..env span shifts equally; prefix figure is a >= floor) — run it solo anyway.

DELIVERABLES.
D1 — TWO clauses folded into the TAIL of the # Tool use paragraph, inside the heredoc: (a) shell substitution: the prompt names the affordance AND both limits (10-second shared budget per expansion; 16,384 bytes per substitution) and says overruns are labelled in place, not silently lost — every clause checkable against the CommandSpec/runShellSubstitution code above (the standing bar Runtime.php:2881-2887); (b) skills: when the system context lists available skills, each entry is a one-line summary — invoke the `Skill` tool by name to load the full instructions, and use it when a listed skill plainly fits.
D2 — regenerate the golden via the documented procedure ONLY; new fixture = old with ONE contiguous pure-insertion block (no other byte moves).
D3 — update the four pins (BSP :850 byte count, :835 history comment APPEND a sentence; SSHW :519 byte count, :521+:530 md5) — literal swaps, assertion types unchanged (§1.11: zero removals).
D4 — two new focused tests in BaseSystemPromptTest: each clause pinned present in the golden with its load-bearing substrings (budget figures + invocation verb) — they must be red-before/green-after (run red first against the UNREGENERATED fixture pair to prove they bite, then final).

RULINGS (FIXED — deviate = stop-report).
R-A — exactly two clauses, folded into the existing # Tool use paragraph: NO new H1, NO blank-line-separated new paragraph inside the heredoc (REQUIRED_SECTIONS :52-57 stays 4; law-4n stays at ZERO Glob/DocParas delta — predict 61/22648 and 17/1371 unchanged; a blank line inside the heredoc = new DocumentParagraphs unit = prediction breach).
R-B — clause (a) states BOTH budgets as plain digits ('10', '16,384') or backticked const names; not '16384' vs '16,384' ambiguity — pick ONE spelling, use it in clause AND pin tests consistently. Do NOT name CommandSpec or use capitalised words outside the sweep-safe set.
R-C — name-sweep: testBasePromptNamesNoToolThisAppDoesNotShip :330-364 flags every capitalised word in basePrompt() not in PROSE_WORDS (:312-316, fixed 18 words) and not a real tool name. Preferred: lowercase 'available skills' paraphrase (no PROSE_WORDS change). If verbatim header quoting forces 'Available'/'Skill' — 'Skill' is SAFE (wired tool name), 'Available' would need the sanctioned PROSE_WORDS addition; use that route only if lowercase phrasing genuinely harms clarity, and say so in the commit message.
R-D — golden procedure: regeneration validated by re-running the golden-compare test BEFORE touching pins (it must fail on size-inequality only, content-match the insertion); then containment proof in commit-message/report form: old[:k] == new[:k], old[k+0:] == new[len(new)-len(old)+k:] pure-insertion geometry with offsets. Then PIN-BITE proof: after pins updated, append ONE scratch byte to the fixture, show BSP golden test + SSHW BOTH md5 sites go RED, restore, green. (P7.S3 discipline.)
R-E — the R-1 cross-reference (system prompt naming <git_commits> to point at the Bash fragment) is RULED OUT of this step: the golden context wires ZERO tools so the fragment never renders there — a dangling tag reference in the pinned prompt violates Anthropic's own rubric the plan quotes. The fragment self-delivers in production via toolGuidanceSection (P9.S1/S3 shipped). Carry as Phase-9 close-review adjudication item.
R-F — agent golden, docs/*, every other test file: untouched. Solo set (serial, </dev/null, --colors=never, box-quiet probe 0 first): BaseSystemPromptTest (expect 24 tests = 22+2D4, assertions +2 pins' worth + the D4 counts — measure), SessionStartHookWireTest 14/107±(md5 swaps keep counts identical → 14/107), PromptStabilityTest, RuntimeTest 144/547 (src change = heredoc bytes only — MUST be unchanged), ChatTest 229/887 unchanged, GlobFigureDriftTest 61/22648 unchanged (R-A), DocumentParagraphsTest 17/1371 unchanged, BuiltInToolCorpusTest 42/109 unchanged. NO full suite (orchestrator runs the master-direct gate post-merge).

REPORT format: branch tip SHA; clause bytes (old→new golden size + md5); containment geometry (offsets + the two slice equality results); D4 red-before evidence; pin-bite evidence; all solo headlines; anything that surprised you (law 4j). Commits: src+fixture+pins+tests in ONE commit is acceptable if message carries all parts; message via /tmp/opencode/P9.S5-build/msg.txt (grep -c '<' == 0 first); identity from git config interpolation; 3-scan hygiene. No push, no composer, no caliber, no git init outside scratch.
