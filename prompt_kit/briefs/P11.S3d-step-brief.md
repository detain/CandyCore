# P11.S3d step brief — `sugar-crush/docs/SETTINGS.md` (rules-tier config surface, guard-governed counts)

- Plan step: `prompt_plan.md` Phase 11, `### P11.S3 — Update the affected feature docs` (:3145 at `322a970fa`; recorded :3141 in the scout pre-resume numbering).
- Binding premise map: `prompt_kit/findings/P11-premise-scout.md` — its Q2 guard-tripwire table and Q6 doc-contradiction list are incorporated as section 4 below.
- §5 re-check: **CLEAR** — record at `prompt_kit/findings/P11-batch1-recheck.md`.
- Worktree: `/home/sites/prompt-step-P11.S3d` · branch `prompt/P11.S3d` · base = the worktree's HEAD at provisioning (staffing commit, direct child of `322a970fa`).
- Siblings S3a–S3c/S3e edit the other four docs pages in their own worktrees. Not your ceiling, not your problem.

## 1. CEILING (hard)

Your ceiling is **exactly one file: `sugar-crush/docs/SETTINGS.md`**. You may not create, edit, or delete
any other file in the repository — no src, no tests, no other docs, no README, no plan files. If the
truthful state of this page seems to demand a change anywhere else, do NOT make it: record it in your
report as a follow-up finding.

## 2. GOAL (quoted from the plan step)

> **Goal** `docs/HOOKS.md` (additionalContext, the new dispatch sites), `docs/SKILLS.md` (which
> skill→prompt path is canonical), `docs/MEMORY.md` (`/memory import`), `docs/SETTINGS.md` (the rules
> keys and their user-tier-only rule), `docs/COMMANDS.md` (`/rules`).

Your clause: **SETTINGS.md — the rules keys and their user-tier-only rule.**

## 3. FACTS TO DOCUMENT

The claimed shipped state below is what the orchestrator believes Phase-1..10 shipped. **EVERY claim must
be re-verified by your direct read of the shipped code (and shipped tests) at YOUR base sha before you
write it into the page.** Anything you cannot confirm gets dropped or reported as a measured-FALSE
premise, never written as fact. Line cites in the doc itself must be **symbolic (class::method / const
names), never numeric `file:NNNN`**.

- The **rules tier config surface**: rulebooks live in the operator's rulebooks directory under `HOME`
  (user tier).
- Named packs are toggleable via `/rules`, **SESSION-SCOPED**: `config.json` stays **byte-identical on
  toggle** (verify the shipped test that pins this; cite it symbolically). Persistence of a toggle is a
  **DEFERRED step — say so** on the page.
- `disabledRules` is a **LIVE layered key registered USER-TIER-ONLY** — never taken from a project file.
  Rationale to carry (verify wording against source/README): *a rule pack is prompt prose the operator
  wrote*. It is read through the **MERGED view at launch via `Bootstrap`** (verify the read path).
- Nested pack toggle handle = **path relative to the tier dir minus `.md`** (e.g. `style/terse`). Verify
  the handle derivation in source.
- **Root `RULES.md` keeps its literal name and is untoggleable.**
- **Whitespace-padded names are inert** — document as the known residual it is.
- CRITICAL, count discipline: the page's spelled count sentences about layered keys must match
  `LayeredSettings::LAYERED_KEYS` (staff-side sanity measurement at `322a970fa` found **12** members —
  re-derive yourself from the CURRENT source, plus `userTierOnlyKeys()`/`PROJECT_TIER_KEYS` for the
  never-project subset; the orchestrator's stated split is twelve/five — confirm or refute by measurement).
  **But do NOT hand-write counts that no guard derives.** The shipped roster guards decide the exact
  wording: run `TrustKeyDocumentationDriftTest`, `ReadmeSettingsTierClaimTest`,
  `ConfigWriteProducerDocumentationDriftTest`, `GlobFigureDriftTest` and let their green/red verdicts
  govern. Where no guard polices a spelled number, prefer pointing at the symbol (`LAYERED_KEYS`) over
  baking a rotting numeral. The page previously carried a wrong **"of the ten"** claim — re-derive from
  the CURRENT source, not from this brief, and fix any stale figure you find.

Also: **audit the WHOLE page for sentences contradicting the shipped state; fix contradictions; the doc
must be truthful at the tip.**

## 4. TRIPWIRES (from the scout Q2 guard table + Q6 — these govern you; SETTINGS.md is the most-policed page in the batch)

- **TrustKeyDocumentationDriftTest** (`tests/Config/TrustKeyDocumentationDriftTest.php`) polices
  SETTINGS.md directly: the page must exist+gate (:286), **every `LayeredSettings::LAYERED_KEYS` member
  must have a row** (:302), and the never-project statement must agree with `PROJECT_TIER_KEYS` (:320).
  Across all docs it forbids stale trust-grant counts (:169) and keeps deep-link headings resolving (:195).
- **ReadmeSettingsTierClaimTest** (`tests/Config/ReadmeSettingsTierClaimTest.php`): the SETTINGS.md copy
  of the launch-report line must match the README byte-for-byte (:305); `SPELLED_COUNTS` map (:402);
  retracted claims only inside `>` quote lines (:43–49 rule).
- **ConfigWriteProducerDocumentationDriftTest** (`tests/Config/ConfigWriteProducerDocumentationDriftTest.php`):
  derives the "config.json receives exactly two keys / two producers" census; the enumeration lead-in and
  DOORS structures exist **per page** — your edits must keep those paragraph structures intact (:430, :438, :444).
- **GlobFigureDriftTest** (`tests/Config/GlobFigureDriftTest.php`): SETTINGS.md (:82) argues from the
  `COUNTEREXAMPLE_GLOB` with a spelled length word that must equal `strlen` of the shipped glob (:92,:101);
  a stale-figure census spans `src/`, `docs/`, `tests/`, `README.md` (:592) and asserts SETTINGS.md states
  the site-count the census finds (:876). Do not disturb this arithmetic.
- **EnvRosterDriftTest mention-census** (`tests/Config/EnvRosterDriftTest.php`): edits to any `docs/*.md`
  auto-enter the census (`array_merge([$root.'/README.md'], glob($root.'/docs/*.md'))`). Every
  `SUGARCRUSH_*` name you leave on the page must have a roster row in `docs/ENVIRONMENT.md` and be
  code-read — run `/usr/bin/grep -o 'SUGARCRUSH_[A-Z0-9_]*' sugar-crush/docs/SETTINGS.md | sort -u` and
  confirm each hit survives before finishing.
- **DocumentParagraphsTest fence-balance** (`tests/Config/DocumentParagraphsTest.php` +
  `Support/DocumentParagraphs.php`): `testNoDocumentInScopeLeavesAFenceOpen` reddens on ANY unbalanced
  fence; note the guards above read your page THROUGH `DocumentParagraphs` (fence-aware paragraph/table
  split), so fence or table malformation can redden them indirectly, not just the fence test itself.
- **SymbolCitationDriftTest** (`tests/SymbolCitationDriftTest.php`): every backticked
  `Test`/`Class::method` symbol on the page must resolve at your tip — including the key/const symbols
  you introduce (`LAYERED_KEYS`, `PROJECT_TIER_KEYS`, `userTierOnlyKeys`, `disabledRules`, …).

## 5. SUITE LAW

Floor anchor at master: **11,182 tests / 170,906 assertions / 0 failures / 0 errors / Skipped 2 / EXIT 0**
(the staffing commit is markdown outside `sugar-crush/`, so the anchor carries to your base).

1. Run your solo guard classes FIRST (base), and again at the final tip, and **predict the full-suite
   headline from the final-tip per-class solos (law 5)** before launching the full run. Your guard set:
   `TrustKeyDocumentationDriftTest`, `ReadmeSettingsTierClaimTest`, `ConfigWriteProducerDocumentationDriftTest`,
   `GlobFigureDriftTest`, `EnvRosterDriftTest`, `DocumentParagraphsTest`, `SymbolCitationDriftTest` —
   e.g. from the worktree root:
   `cd /home/sites/prompt-step-P11.S3d && php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --filter GlobFigureDriftTest` (repeat per class).
2. **Box-quiet probe** — must print `0`, run STANDALONE (grep -c exits 1 on a zero count — NEVER `&&`-chain
   it into the launcher):
   `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'`
3. Full suite, detached (setsid+nohup), **cwd = worktree root**:
   `mkdir -p /tmp/opencode/P11.S3d && cd /home/sites/prompt-step-P11.S3d && setsid nohup php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P11.S3d/tip.xml </dev/null >/tmp/opencode/P11.S3d/tip.out 2>&1 &`
4. Verify before reporting: the JUnit file has a **closed root** (`</testsuites>` present — a truncated file
   means the run died), the **banner** is in `tip.out`, and the artifact is **>3 MB** (`stat -c %s /tmp/opencode/P11.S3d/tip.xml`).
5. Headline must match your prediction and meet the floor (assertion-count movement is acceptable ONLY as
   your solo-delta prediction explains; 0F/0E/Skipped-2 are hard).

## 6. GOLDENS FROZEN (measured at `322a970fa`, re-measure at your tip)

- `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` — md5 `f5de3858c1bc130d1e97a120d3ead485`, 8,278 B
- `sugar-crush/tests/fixtures/prompt/golden-agent-prompt.txt` — md5 `ef0326dd38535aaa2f1d715919bff26e`, 1,060 B

Any move = **HALT and report**. A docs step must never touch them; if a golden moved under you, the tree or
the suite is lying — stop.

## 7. COMMIT HYGIENE (hard)

- ONE commit on branch `prompt/P11.S3d` (already checked out in your worktree).
- Identity — write the literals via printf, NEVER copy any redacted placeholder token you see in your
  context (e.g. the bracketed-email placeholder or angle-bracket forms in repo docs):
  `git -C /home/sites/prompt-step-P11.S3d config user.name 'Joe Huss'`
  `git -C /home/sites/prompt-step-P11.S3d config user.email "$(printf '%s@%s' detain interserver.net)"`
- Stage only `sugar-crush/docs/SETTINGS.md`; `git commit -F <message-file>` (message file outside the repo,
  e.g. `/tmp/opencode/P11.S3d/msg.txt`).
- Message scan: `/usr/bin/grep -c '[<>]' <message-file>` must print **0** (fix the message if not).
- Law-4g triple AFTER the commit (all FIXED-STRING scans with /usr/bin/grep):
  1. `git -C /home/sites/prompt-step-P11.S3d cat-file commit HEAD | /usr/bin/grep -cF '[EMAIL]'` → 0
  2. `git -C /home/sites/prompt-step-P11.S3d log -1 --format=%B | /usr/bin/grep -cF '[EMAIL]'` → 0
  3. `git -C /home/sites/prompt-step-P11.S3d log -1 --format='%an|%ae|%cn|%ce'` → exactly
     `Joe Huss|detain@interserver.net|Joe Huss|detain@interserver.net`
- **NEVER merge. NEVER push.** The orchestrator lands the branch.

## 8. REPORT FORMAT (return all of it)

1. Diffstat vs base (`git diff --stat <base>..HEAD`).
2. Claims-verified list: each fact from section 3 → `file:symbol` where confirmed → confirmed/refuted
   (include your re-derived `LAYERED_KEYS` count and never-project count, and the exact final spelled
   sentences you settled on because of a guard verdict).
3. Solo figures: the 7 guard classes at base and at tip (counts per class).
4. Prediction table (headline predicted from per-class solos) vs ACTUAL full-suite banner.
5. Goldens: md5 + byte size of both files at your tip.
6. Hygiene scan outputs verbatim (message `<>` count, the three law-4g lines).
7. Any premise that measured FALSE — REPORT, do not silently fix beyond your ceiling.
