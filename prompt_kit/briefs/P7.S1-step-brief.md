# P7.S1 — `HookResult::additionalContext` and the discarded-message bug

**Phase:** 7 (Hooks) — first step · **Parent:** Phase 6 closed · **Prev:** P6.S5a · **Next:** P7.S2 · **Plan:** `prompt_plan.md:2520-2541` · **Premise report:** `prompt_kit/findings/P7.S1-premise.md` · **Base:** master tip `ce816e981` (belt `git diff 714bea7ec ce816e981 -- sugar-crush/` = 0, so the floor below describes master).

**Status: READY TO STAFF** — the two blocking decisions below are RESOLVED by supervisor ruling 2026-09-06; the one open technical risk (overflow-file retention vs the hourly sweep) is a mandatory pre-code verification (see §Hard constraints, item 6).

## What this step builds
A script hook that prints output and exits 0 currently has that output **thrown away**. `ScriptHook::EXIT_ALLOW` returns `HookResult::allow($output)` (`ScriptHook.php:642`) — the stdout is carried in the result's message — but `HookRegistry::executeHooks()` ends `return $modified ?? $inertRewrite ?? HookResult::allow();` (`HookRegistry.php:428`), rebuilding a permitting verdict with an **empty** message. `ScriptHook.php:631-641` documents this verbatim: on the live path it "goes NOWHERE".

This step adds a dedicated **`additionalContext`** field to `HookResult` (distinct from `message`, which is the human/deny reason; distinct from `modifiedInput`, which is JSON **tool arguments**) and makes that context survive all the way to the model on **both** tool-result paths.

## SUPERVISOR RULINGS (2026-09-06) — binding, do not re-litigate
- **R-1 (consumer scope): "Wire BOTH Runtime and Chat."** P7.S1 must make `additionalContext` genuinely READ on the live path — not a producer-only dead field (which §1.10 forbids). Both model-visible tool-result paths (`Runtime::settle()` and `Chat::applyPostToolUse()`) must append the context. **This explicitly clears the `src/Chat.php` collision row for P7.S1** (plan §5 stop-and-ask; the supervisor answered it on 2026-09-06). Record the clearance in the commit message and worklog exactly as P6.S3 recorded its Chat.php touch.
- **R-2 (overflow mechanism): "New retained-file path."** Implement a purpose-built retained-overflow file (0600 via umask-narrowing + atomic `rename`, like `Support\ToolIpcFiles::write()`) that is **NOT** discarded after read and whose lifetime is tied to the session/tool-result, **not** the `STALE_AFTER_SECONDS = 3600` IPC sweep. The plan's "spillover to a file plus a preview" requirement stands.

## ORCHESTRATOR RULING — units
- **R-3 (units): the cap is BYTES, 10,000 bytes.** The plan text says "10,000 characters", but every bound in this subsystem is byte-denominated (`TruncatesOutput` caps 65536/16384/120 bytes; `ScriptHook::clip()` at `ScriptHook.php:863-865` states byte-boundary cutting "deliberately — this is arbitrary program output, not guaranteed UTF-8"). Cut with `mb_strcut(..., 'UTF-8')` per the `TruncatesOutput` idiom, expose the cap as a named `public const` in BYTES (e.g. `MAX_ADDITIONAL_CONTEXT_BYTES = 10000`), and state in the doc-comment that the plan said "characters" and why bytes is used. Use the trait's `max(1, …)` guard idiom, NOT the `$maxBytes <= 0` "no cap" sentinel unguarded (`TruncatesOutput.php:141-142,367-379` record that misusing that sentinel silently disabled the bound twice — Glob returned 1,091,833 bytes against a 65,536 cap).

## Files in scope (LARGER than the plan's original 4 — this is a full vertical slice)
Producer / hooks layer:
- `sugar-crush/src/Hooks/HookResult.php` — add 4th promoted `readonly` ctor param `public string $additionalContext = ''` (defaulted, so all `new self(...)` factory arms + existing call sites stay valid); extend the class doc-comment to name the three payload channels (`message` = deny reason; `modifiedInput` = JSON tool args; `additionalContext` = model-visible note).
- `sugar-crush/src/Hooks/HookRegistry.php` — **stop the discard AND thread the field through every internal rebuild point**, else it dies before any consumer sees it. At minimum: (a) `scan()` (`@return array{0:?HookResult,1:?HookResult,2:?HookResult}` at `:662-665`; a plain permitting result currently exits iteration unstored at `:767-769`) must COLLECT additionalContext across hooks (widen the tuple + its docblock, or add an out-slot); (b) the ASK rebuild at `:418` (`HookResult::ask($blocking->message, $modified?->modifiedInput)`) must carry it; (c) the allow/settled arm at `:428` must carry the collected context instead of `HookResult::allow()` empty; (d) the `executeHooks():342` destructure updated to match.
- `sugar-crush/src/Hooks/HookManager.php` — `resolveAsk()` (`:180-201`) re-materialises the settled decision three ways (`deny`/`allow`/`modify`); thread `additionalContext` through all three so an approved ASK doesn't drop it.
- `sugar-crush/src/Hooks/ScriptHook.php` — the `EXIT_ALLOW` arm already passes `$output`; ensure it is routed into `additionalContext` (NOT `message`) and clipped to the 10,000-byte cap with spillover-per-R-2. Update the `:631-641` "goes NOWHERE" comment to reflect the new live consumer.

New helper:
- a retained-overflow file mechanism under `sugar-crush/src/Support/` (mirror `ToolIpcFiles`'s 0600 + atomic-rename discipline but with retained lifetime — decide whether it is a new class or new methods; justify the choice). Must be covered by tests.

Consumers (R-1):
- `sugar-crush/src/Runtime.php` — `settle()` (`:1992-2024`) must capture the `postToolUse()` result (`:2009`, currently a bare statement discarding it) and append non-empty `additionalContext` to the model-visible `ToolResultMessage` via the EXISTING blessed `self::annotate($result, …)` seam (`:2011`, `:2024`). Also confirm the `gate()`/deny branch (`:1930-1931`) is unaffected.
- `sugar-crush/src/Chat.php` — `applyPostToolUse()` (`:3661-3668`) must do the equivalent append (supervisor-cleared collision-row edit; keep it SURGICAL — this file has a concurrent backlog; change only what wiring the consumer requires).

Tests (≥1 per public method changed; every new behavior gets a red-on-revert pin per §1.11):
- `sugar-crush/tests/Hooks/HookResultTest.php` (baseline 27 tests / 50 asserts) — extend the full-shape assertions (`testAllow` `:19-25` asserts action/message/modifiedInput) to include the new 4th field; extend `testResultIsReadonly` (`:266`).
- `sugar-crush/tests/Hooks/HookRegistryTest.php` (baseline 68 tests / 176 asserts + 3 expectException; file is 2,112 lines, NOT chronologically ordered — a new `executeHooks` test belongs in the executeHooks cluster `:313-437` or the rewrite-rescan cluster `:951-1119`, BEFORE the `// Helper Methods` banner at `:1135`). Add the **200,000-byte reproduction test** (see done-when).
- `sugar-crush/tests/Hooks/ScriptHookTest.php` — assert `additionalContext` set + capped + spillover-file path returned (not `message`).
- `Runtime` and `Chat` tests — assert a non-empty `additionalContext` on a permitted call reaches the model-visible tool result on BOTH paths; and that an EMPTY `additionalContext` changes the result BYTE-IDENTICALLY (no regression / no-op when unused).
- New test for the retained-overflow helper (retention: assert it is NOT swept by the hourly sweep logic; atomicity; 0600 perms).

## Hard constraints
1. `$modifiedInput` is JSON **tool arguments**, not prompt text — do not conflate; `additionalContext` is a SEPARATE field.
2. Cap = **10,000 bytes** with spillover to a retained file plus a preview + path (R-2, R-3).
3. **NEITHER prompt golden moves.** `additionalContext` is a tool-result/turn payload, never system-prompt text. `git diff <base>..HEAD -- sugar-crush/tests/fixtures/` MUST be empty; if a golden moved, that is a DEFECT (context leaked into the prompt assembler). System golden must stay `f09f37366a1925565dcc7725f659ff41`/7,829 B; agent `ef0326dd38535aaa2f1d715919bff26e`/1,060 B.
4. §1.10: do NOT delete/stub/narrow any existing construct. The widened `scan()` tuple and threaded fields are ADDITIVE. Do not repurpose `->message` for the allow path (ScriptHook.php:631-641 warns consumers key on the deny string).
5. Census guards will move because a new `src/` file + a widened `scan()` signature + doc changes feed the derived source-walking censuses (TreeWideGuardRosterTest, GlobFigureDriftTest, SymbolCitationDriftTest, AssertionSwallowingCatchTest, etc.). Record the figure drift in the commit message; do not hand-edit derived figures.
6. **Mandatory pre-code check (R-2 risk):** the overflow file must survive until the model reads it. Verify the hourly sweep (`Cli/Bootstrap.php:2124`, `:2255`, `ToolIpcFiles::STALE_AFTER_SECONDS = 3600`) CANNOT delete a P7.S1 overflow file before its consumer reads it, and that it is not caught by `discard()`/the partial-suffix cleanup. If no clean retained-lifetime boundary exists in-tree that keeps it for the required duration, HALT and report — do NOT ship a mechanism with a silent data-loss window.

## Done when (plan `:2540` + R-1)
- The 200,000-byte `ScriptHook` reproduction test produces a bounded (≤10,000 bytes), non-empty `additionalContext` (preview + retained file path), with the **measured byte counts pasted into the worklog**.
- A test asserts the `executeHooks()` allow path no longer rebuilds an empty verdict (red-on-revert: restore `HookResult::allow()` and watch it go red).
- A test on EACH of `Runtime::settle()` and `Chat::applyPostToolUse()` shows the context reaches the model-visible result; and an empty-context case asserts a byte-identical no-op.

## Methodology (unchanged plan discipline)
- Worktree from master `ce816e981`; `cp -al` the vendor (NEVER `ln -s`); verify the PSR-4 root prints the worktree's OWN src.
- Predict the suite headline from final-tip per-class solos BEFORE running; reconcile any moved total with a per-class junit diff (`prompt_kit/tools/cmp.py`).
- Run the full suite serially, `</dev/null`, `--colors=never`; box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0 first.
- Red-capability: mutate the change, show the tests redden, restore (verify `md5sum` back + `git status --porcelain sugar-crush/` clean).
- Commit as literal `Joe Huss <detain@interserver.net>` (shell-interpolated, NOT the `[EMAIL]` token; byte-scan `git cat-file commit HEAD | /usr/bin/grep -c '\[EMAIL\]'` == 0). Message via `git commit -F`, `grep -c '<'` == 0 first.
- FLOOR to beat: **10,937 tests / 168,399 assertions / 0 failures / 0 errors / 2 skipped / EXIT 0** at `714bea7ec` (belt-proven to describe master). This step ADDS tests (repro + consumer + helper), so expect a HIGHER floor — derive it, don't guess.
- `/usr/bin/grep` (not ugrep) for whole-tree censuses. Bash cwd persists — anchor absolute. Never touch `/home/sites/crush-lane-*`, `docs/plans/crush_code_*.md`, `left_steps.md`. Each agent gets its own scratchpad subdir; never `rm -rf` a path you didn't create. No `composer install/update`, no `--no-verify`, no `pkill`, no `caliber`.
- Blank/aborted return = transport death — RESUME the same agent (work persists on disk/branch); escalate to a fresh agent only after ~5 similar-duration deaths, ceiling ~15.

## Known hazards (from the premise check)
- `TruncatesOutput` (`src/Tools/Concerns/TruncatesOutput.php`, 731 lines) truncates by BYTES, DROPS overflow with an inline `PARTIAL` marker naming counts — NO file, NO preview. It is the wording/consumption analogue, not the file mechanism.
- The ONLY existing file writers are `Support\ToolIpcFiles` fork/IPC handoffs (deleted after read + hourly swept) — every existing caller discards; none returns a path the model reads later. So R-2's retained path is genuinely new; do not shoehorn it into the discard-after-read lifecycle.
- `scan()` returning a bare 3-tuple and only storing blocking/modify/inert-rewrite results is the FIRST place an ALLOW's payload vanishes — fixing only `:428` is insufficient.
- `HookDispatcher` (which DOES carry the message) is constructed by NOTHING in `src/` (tests only) — do not rely on it as the live consumer.
- Chat.php is a live collision row but is SUPERVISOR-CLEARED for this step (R-1) — keep the edit minimal and record the clearance.
