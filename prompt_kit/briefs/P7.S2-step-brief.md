# P7.S2 — Step brief: dispatch sites for SessionStart + UserPromptSubmit

**Plan:** `prompt_plan.md:2561-2590` (read verbatim before starting). Premise: `prompt_kit/findings/P7.S2-premise.md` (DONE — anchors current as of master `ad1020b2e`+P7.S1). Build on branch `prompt/P7.S2` cut from master tip AFTER P7.S1 merged.
**Read first:** plan §1.10 (`:827`), §1.11 (`:874`), §16 (`:3128`), §17 (`:3626`); precedent test seams `tests/Chat/ContextReminderDedupTest.php:456-496` (ReminderWireRecorder) and `tests/Commands/RulesCommandTest.php:553-611` (recording ProviderInterface).

## Goal (one sentence)
Give the two dead events `SessionStart` and `UserPromptSubmit` real dispatch sites on `HookManager` and route their verdicts (`block`/`deny` semantics + P7.S1's `additionalContext`) into the model-visible turn as factual `role:"system"` notes, proven in the PROVIDER PAYLOAD.

## Binding orchestrator decisions (A–E — do not re-litigate)
- **A. Fire points.** Both dispatch from `Chat::submit()` tail (`:6084-6100` region, AFTER all early arms — queue/commands/spend-cap/compaction/token-tiers return before it): `UserPromptSubmit` fires per real prompt submission; `SessionStart` fires exactly once, guarded by `count($this->history) === 0` at the same point (startup-only; the resume/palette-new-session gap is DOCUMENTED in the method docblock, no second fire point). A note that the SessionStart gate must survive an early command-return prompt is NOT required — first model-visible turn fires it.
- **B. Insertion channel = appended `Message::system($note)`, NOT `<system-reminder>` in the user turn** (plan `:2584-2586` — system role is the non-spoofable operator channel; precedent `contextReminderMessage` at `:6558`). Ordering: SessionStart system note goes BEFORE `Message::user($text)`; UserPromptSubmit system note likewise immediately before the user message in the same turn batch.
- **C. HookContext smuggling — NO HookContext change.** Dispatch with the existing tool-shaped context: `toolName` = event-name sentinel (`'SessionStart'` / `'UserPromptSubmit'`), `toolInput` = JSON `{"prompt": "<user text or ''>"}` (+ `"source":"startup"` for SessionStart), model/provider empty per the `gateToolCall` precedent (premise §). Document the smuggling in each new method's docblock (WHY: blast radius; HookContext is shared 82-line value).
- **D. Dispatch as 2-line `executeHooks` clones ON `HookManager`** (`sessionStart(array $hookCtx): ?HookResult` / `userPromptSubmit(array $hookCtx): ?HookResult` — mirror `preToolUse` `:158`/`postToolUse` `:215`). `HookDispatcher.php` STAYS DORMANT and untouched (§1.10 — zero src constructions, premise-proven; its plan file-list entry is WITHDRAWN, record plan correction in worklog). Chat consumes via null-safe `$this->hooks?->` with the applyPostToolUse empty-note-no-op contract: no hooks configured ⇒ byte-identical behavior.
- **E. Verdict semantics (cheap form, both events).** `block`/deny from `UserPromptSubmit` ⇒ the prompt is NOT submitted; reason surfaced as user-visible notice (`discardsOnBlock` precedent, premise). `block` from `SessionStart` ⇒ note discarded + stderr notice (`stderrToUserOnly` precedent). Non-empty `additionalContext` on an ALLOWED verdict ⇒ appended into the same system note (P7.S1 field arrives for FREE via `executeHooks`). If a seam turns out deeper than the premise says, a documented deferral with the reason in the worklog is acceptable — silent half-wiring is not.

## Files in scope
- `sugar-crush/src/Hooks/HookManager.php` (two dispatch clones + docblocks)
- `sugar-crush/src/Chat.php` (fire points at submit() tail; plan-named file, standing Phases-5-11 collision clearance applies — keep surgical, record clearance in commit message)
- `sugar-crush/tests/Hooks/HookManagerTest.php` (unit: both methods dispatch the RIGHT enum case; registered-but-unmatched = null; context carries sentinel + prompt)
- `sugar-crush/tests/Chat/SessionStartHookWireTest.php` (NEW — the done-when tests; ReminderWireRecorder + two-step provider capture, see below)
- `sugar-crush/tests/Hooks/HookGateE2ETest.php` (optional extension)

## Done-when (all must be MEASURED, red-on-revert per §1.11)
1. E2E: a registered `SessionStart` hook's output text is asserted IN THE REQUEST PAYLOAD the provider receives — NOT a DTO. Mechanic (premise-proven, fork trap documented): drive Chat to the turn boundary, capture `$next->history`, then run `EngineBackend(recordingProvider)->complete($next->history)` SYNCHRONOUSLY (Chat's own completeAsync forks under pcntl — a provider double in-parent with a live Chat NEVER works) and assert the note bytes inside what the provider received, `Role::System` position BEFORE the user message.
2. UserPromptSubmit mirror: note lands alongside the submitted prompt in the payload; `block` ⇒ prompt absent from payload + reason reaches the user surface (both assertions).
3. No-op pin: with zero hooks registered, request payload byte-identical to base behavior.
4. SessionStart fires ONCE: second submit in same session ⇒ exactly one note (count substr occurrences).
5. Goldens FROZEN: `md5` system `f09f37366a1925565dcc7725f659ff41` (7,829 B), agent `ef0326dd38535aaa2f1d715919bff26e` (1,060 B); `git diff <base>..HEAD -- sugar-crush/tests/fixtures/` EMPTY. System messages ride the HISTORY channel (ContextReminderDedup precedent) — never the assembler; if a golden moves you have leaked into prompt assembly: fix the leak, NEVER regenerate a golden.
6. Derived census guards may move — record, never hand-edit.

## Hard constraints
- Hook-text wording guidance (docs/comment, not enforcement): factual statements, not imperatives (plan `:2581-2583`).
- `declare(strict_types=1)`, immutable style, no `get*` accessors, doc-comments cite upstream Anthropic hooks doc where apt.
- Tests-first floor: FLOOR 10,957/168,627/2S (P7.S1 tip) — you ADD tests; predict the new headline BEFORE the run, then `cmp.py` the per-class junit delta to zero remainder.
- Suite protocol: box-quiet probe 0, serial, in-session `php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never </dev/null` from worktree root, `--log-junit /tmp/P7.S2-junit.xml`.
- Provisioning: `git worktree add /home/sites/prompt-step-P7.S2 -b prompt/P7.S2 master`, vendor via `cp -al` (NEVER ln -s), PSR-4 root must print the worktree's own src, both goldens present at expected sizes before coding.
- Commit author/committer `Joe Huss <detain@interserver.net>` SHELL-INTERPOLATED; commit message via `-F` file with `grep -c '<'` == 0; byte-scan `git cat-file commit HEAD | /usr/bin/grep -c '\[EMAIL\]'` == 0. Never push. Never merge to master yourself.

## Escalation
- Any §1.10 removal impulse ⇒ STOP and escalate verbatim (that is a completed step, status `blocked (user-escalation)`).
- If the fork trap turns out to block even the two-step capture (premise wrong), STOP with evidence, don't redesign the wire.
