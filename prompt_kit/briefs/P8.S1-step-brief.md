# P8.S1 step brief v1 — Structured summary sections for the compaction prompt

**Step**: P8.S1 — `prompt_plan.md` :2722-2739.
**Worktree**: `/home/sites/prompt-step-P8.S1` · **Branch**: `prompt/P8.S1` ·
**Base**: `be01660a02` (master at staffing time).
**Premise (anchor of record)**: `prompt_kit/findings/P8.S1-premise.md` v1.
**Dated**: 2026-09-08.

Read first: this brief; the premise v1 in full; `prompt_plan.md` :2722-2739
(step text), §1.10, §1.11, §16, §17.

## Where you work

- `/home/sites/prompt-step-P8.S1`, branch `prompt/P8.S1` @ `be01660a0`,
  PORCELAIN-CLEAN (freshly provisioned 2026-09-08). `sugar-crush/vendor` is a real
  hard-linked dir (`cp -al`, NOT a symlink); `composer dump-autoload` regenerated
  and `vendor/composer/autoload_psr4.php` verified in relative form
  (`$vendorDir = dirname(__DIR__);` / `$baseDir = dirname($vendorDir);`) — zero
  absolute leak to `/home/sites/sugarcraft`. Provisioning sanity
  `vendor/bin/phpunit tests/Chat/CompactModelSummaryTest.php` printed
  `OK (21 tests, 99 assertions)` (box-quiet probe was 0 at run time).
- Scratchpad: your OWN private subdir (e.g. `/tmp/opencode/P8.S1-builder/`), all
  backups/experiments inside it; never `rm -rf` anything you did not create; never
  create a scratch git repo outside it; verify `pwd` before any
  `git init`/`git commit` (ORCHESTRATION-RULES-2/3).

## R-0 READ + VERIFY EVERY ANCHOR IN THE STEP WORKTREE

Read the premise in full, then verify EVERY `file:line` anchor there against
`/home/sites/prompt-step-P8.S1/sugar-crush/src/Chat.php` before it binds an edit
(anchors of record: const :9020-9032 + docblock :9015-9019; consumer :9187 inside
`buildSummarizationRequest` :9169-9229; `parseExchangeSummaries` :9426-9445 +
docblock :9407-9425; `SUMMARY_LINE_MAX_CHARS` :9454 + docblock :9447-9453;
`sanitizeSummaryLine` :9462-9476; `renderExchangesForSummary` :9396-9405;
`submit()` :6056-6316 — UNTOUCHABLE). Anchor mismatch = HALT and report at zero
edits — stale citations have already burned this plan; do not become the next one.

## Background (why this step exists)

The compaction prompt today asks for ONE line per exchange under 200 characters
(`src/Chat.php:9022`, `:9030`), and the parse (`parseExchangeSummaries :9426-9445`)
maps numbered single lines positionally onto exchange keys. The plan (:2718-2720)
records what that format loses: file paths, decisions taken, user corrections, exact
error strings. P8.S1 replaces the FORMAT (prompt text + parse contract) with a
six-facet per-exchange RECORD — `asked:/did:/files:/decided:/corrected:/error:` —
while keeping the transport (`array<string,string>` keyed by exchange hash), the
positional keying, the degrade-not-misattribute fallback, and every surrounding
method byte-unchanged. NO method restructure is permitted by this step.

## Files in scope (ONLY these)

- `sugar-crush/src/Chat.php`
- `sugar-crush/tests/Chat/CompactModelSummaryTest.php`
- `sugar-crush/tests/Chat/AutomaticCompactionModelSummaryTest.php` — ONLY where its
  scripted-backend fixtures return old-format summaries and must migrate; prefer NOT
  touching it if it passes unchanged.
- `sugar-crush/tests/CompactorTest.php` — only if it pins the old parse (check).

Any other file needing an edit = HALT and report; do not widen scope.

## Change spec

- **R-1** Replace `COMPACT_SUMMARY_PROMPT` (`src/Chat.php:9020-9032`) text: keep the
  framing sentence; replace the "ONE line per exchange / under 200 characters" rules
  with the six-facet RECORD template per ruling P8.S1-D1 (opencode discipline: every
  facet line always present, `none` when empty, terse, exact paths and error strings
  preserved; crush discipline: "this summary will be the ONLY context available when
  the conversation resumes"; "Losing detail is expected; inventing it is not" MAY
  survive verbatim; no length ask). Update the docblock `:9015-9019` to describe the
  new shape honestly.
- **R-2** Replace `parseExchangeSummaries` (`:9426-9445`) per ruling P8.S1-D3:
  recognized block = bare-number line; facets matched case-sensitive exactly
  `asked|did|files|decided|corrected|error`; continuation lines append; join to
  `asked: X | did: Y | files: Z | decided: D | corrected: C | error: E`
  (missing → `none`); every joined line passes `sanitizeSummaryLine` unchanged in
  ROLE (flatten+clip) before storing; positional keys, dup/out-of-range ignored
  exactly as today. Update the method docblock (`:9407-9424`) to describe record
  parsing.
- **R-3** `SUMMARY_LINE_MAX_CHARS` 200 → 2000 (`:9454`) + docblock rewrite
  (`:9447-9453`) per ruling P8.S1-D2 — transport protection + flattening rationale,
  NO "matches the prompt ask" claim any more.
- **R-4** Grep census BEFORE editing (from the worktree's `sugar-crush/`):
  `grep -rn '200 characters\|200-char\|SUMMARY_LINE_MAX_CHARS\|COMPACT_SUMMARY_PROMPT\|Exchange [0-9]' src tests docs`
  plus any test that feeds the scripted provider a `'1. something'`-style fixture
  (grep `'[0-9]\. ` in the two model-summary test files) — list what you found in
  the commit message and migrate each pin honestly (NEVER weaken: replace with
  facet-equivalent assertions, same intent).
- **R-5** Tests (in `tests/Chat/CompactModelSummaryTest.php`):
  (a) THE Done-when test — drive `/compact` through the real route with a scripted
      backend returning a full six-facet record for 2+ exchanges where at least one
      facet is `none`: assert every facet survives into the compacted history's
      `[summary]` line (all six labels present, `none` present for the empty one);
  (b) a record missing an optional-content facet line still gets the facet filled
      `none`;
  (c) a malformed reply (no bare-number openers) maps NOTHING and the heuristic
      fallback runs (degrades-not-misattributes preserved);
  (d) line-over-2000 clip: 2001+ char line clipped at 1999+`…` via sanitize;
  (e) immutability of positional fallback semantics: mixed numbering out-of-range
      ignored.

## Measurements the builder must take and paste

- The R-4 census output (verbatim).
- Before/after per-file test tails for EVERY touched class.
- FULL suite tail (verbatim, with EXIT code).
- Goldens md5 + size BEFORE and AFTER both runs.
- Box-quiet probe (`ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'`) reading before
  EVERY phpunit invocation — must be 0 to run.

## Laws

- Floor `11,018 tests / 169,219 assertions / 0F / 0E / Skipped 2 / EXIT 0` must not
  weaken. Added tests raise counts — predict per-class BEFORE running, then measure;
  prediction must match or be explained.
- Goldens `a5c5a14ca2e3ad891933ac7aefccc6af` / 7,732 B (system) and
  `ef0326dd38535aaa2f1d715919bff26e` / 1,060 B (agent) are FROZEN — if either moves,
  HALT.
- Commit BEFORE the final full-suite run (the message may then cite measured
  numbers), with exactly ONE commit on `prompt/P8.S1`. Author AND committer
  `Joe Huss <[EMAIL]>` — the email assembled via
  `$(printf '%s@%s' detain interserver.net)`, never written as a literal into any
  file. Message file pre-scanned for `<`, `>`, backtick — each count 0. Post-commit
  identity triple-scan: grep -cF the bracketed-email placeholder artifact in the
  commit object = 0; `%an|%ae|%cn|%ce` exact; `git status --porcelain` empty.
- NEVER push, NEVER merge. Work ONLY in `/home/sites/prompt-step-P8.S1` — never edit
  `/home/sites/sugarcraft` (main checkout) from this seat. No `caliber`, no
  `--no-verify`, no hook suppression.
- Keep lines ≤120 columns. PSR-12/PSR-4; `declare(strict_types=1);` already present.
- Every changed/added docblock must be TRUE of the shipped behavior (the plan's
  honesty doctrine — a comment that overstates is a finding).

## Report

Return in your FINAL report: files changed; census output; per-class solo tails;
prediction-vs-measured for the full suite; full-suite verbatim tail + EXIT; goldens
before/after; commit SHA; porcelain; deviations.
