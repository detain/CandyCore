# P6.S5b step brief — glob-scoped rules reach the model (transient channel, ruled design)

USER RULING ANSWERING §1.10 ESCALATION ITEM (4), verbatim: "byte-bounded whole bodies with deferral + a pointer tail — each deferred rule emits a pointer line (Rule 'x' deferred: budget. Read <path>) so nothing silently vanishes; the pointer serves the long tail while the budget protects the common case. This is literally the HookContextFiles shape transplanted, so it inherits that design's tests-to-write and its guard story."
Plan: prompt_plan.md ### P6.S5b (BLOCKED banner lifts with this brief; read :2422-2465). Premise evidence: prompt_kit/findings/P6.S5b-premise.md (read fully; its file:line cites are from bc9953c11). §5 re-check: CLEAR at staffing (belt 768427809..bc9953c11 sugar-crush/ empty; all declared files prompt-lane last-touch; lanes quiescent per docs/plans/crush_code_RESUME.md round-60 hold).

## CEILING (hard — exceeding it is a failed step)
NEW: sugar-crush/src/Context/RulePathNudge.php; sugar-crush/tests/Context/RulePathNudgeTest.php; sugar-crush/tests/Integration/RulePathScopingWiringTest.php.
MODIFY: sugar-crush/src/Runtime.php (splice skip + the :2598-2604 honesty comment ONLY); sugar-crush/src/Cli/Bootstrap.php (construct + inject at the five existing SkillPathNudge sites, live geometry :5613/:5617/:5618/:5619/:5631/:5645 — re-derive, plan's :5191-5205 cite has drifted); sugar-crush/src/Tools/BuiltIn/Read.php, Edit.php, Write.php, Glob.php, Grep.php (append the rule nudge beside the skill nudge; current call geometry in the premise file §RULING).
DO NOT TOUCH: src/Backend/EngineBackend.php (plan :2464-2465 — Shape B avoids the row; the resume's stale framing is overridden by the plan cite, record this), src/Chat.php, src/Context/ContextCompactor.php, src/Context/RuleLoader.php, src/Context/Rule.php, src/Skills/SkillPathNudge.php, tests/fixtures/** (EVERYTHING under fixtures is forbidden — warm-vendor landmine, plan :2433-2436), docs/** (no env knob — budget lives in class constants).

## FIXED DESIGN — the rulings, do not re-litigate
D1 (GOLDEN LAW): the splice skips ONLY rules whose trigger list contains a PathTrigger (instanceof check over $rule->triggers — Rule.php buildTriggers :225-246; the fixture golden rule global-style.md carries a description so it ALREADY has 1 IntentTrigger — keying on 'has triggers' or on non-empty paths-frontmatter-string instead of instanceof PathTrigger DROPS IT from <user-rules> and MOVES THE FROZEN GOLDEN 8,278 f5de3858c1bc130d1e97a120d3ead485 — that is a step failure, full stop). Both splice loops (Runtime.php :2625-2639 user tier, :2682-2693 project/root) get the skip. Update the :2598-2604 comment truthfully (triggers NOW applied at tool time, naming P6.S5b).
D2: RulePathNudge is a session tracker over the same rules the splice loads (constructor receives RuleLoader output or reuses the App's loader result — decide by what the Bootstrap sites can hand it; NO new filesystem walk per call if avoidable; zero fs at match time — PathTrigger is pure string). For each touched path from the tool, matching rules (PathTrigger::matches / matchingGlobs :134-161) not yet announced are delivered: whole escaped body if it fits the remaining budget; else NOTHING of the body is ever emitted — a pointer line replaces it: Rule '<name>' deferred: budget. Read <path> with <path> = Rule::$path (absolute, Rule.php :115/:126). A budget too small for even one entry returns null and marks NOTHING (mirror SkillPathNudge.php :207-211). Mark only on delivery (:280-284). Announced-once union semantics (CarriesSessionState / SkillPathNudge :403-433 precedent) — deferred AND delivered both count as announced.
D3: budgets are class constants priced honestly against the shipped call sites: Grep/Glob pass intdiv(cap,8)=8,192 B (Grep.php :320, Glob.php :423) while Read passes intdiv(1MiB,8)=131,072 B (Read.php :22 DEFAULT_MAX_BYTES, :331) — verify each at build time and derive the guard-test roster from the live constants; Edit/Write pass NO budget (SkillPathNudge :203-205) — name what budget those two get in YOUR channel and pin it. Include a shipped-tool-budget guard test mirroring testEveryShippedNudgeBudgetClearsTheTrackerCeiling (SkillPathScopingWiringTest :249; figure derived not written). A 65,536 B rule can NEVER fit 8,192 B — so the deferral path is COMMON, not edge; tests must weight it accordingly.
D4: every repository-derived byte entering the <system-reminder> envelope (body, name in the pointer, path) goes through PromptFence::escape() — the skill channel's raw-text precedent does NOT exempt a 64 KiB untrusted body; envelope openers/closers stay literal. Red-on-revert: deleting any escape() call must redden a planted-forgery test.
D5: tests are VALUE-ASSERTING per §1.11 (byte-exact assertSame of the delivered block; substr_count canaries; both polarities; a matching-path call fires and a non-matching path stays silent; second call never repeats; rule without PathTrigger never nudges AND still splices). Unique helper names — DuplicatedTestHelperDriftTest twin trap.
D6: splice-scope render test (put it in RulePathScopingWiringTest): via PromptFixture or a synthetic temp HOME (NEVER tests/fixtures/prompt/home), a paths-scoped rule renders 0 canary bytes into the assembled prompt, a paths-less standing rule still renders, and the real golden test (BaseSystemPromptTest) must pass UNMODIFIED proving the fixture rule was never deferred.

## SUITE LAW
Master-direct floor: 11,185 / 171,308 / 0F / 0E / Skipped 2 / EXIT 0 at bc9953c11 (junit /tmp/opencode/P11.S5/tip.xml — verify exists first; law 4p). Derive per-class solos for your FINAL tip FIRST (law 5): new src file => BinSugarcrushWiringTest +1t/+6a; new test files + src file => GlobFigureDriftTest per-paragraph; every try under tests/ => AssertionSwallowingCatch +3; TreeWideGuardRoster if your test scans src/ (keep it from doing so or account for it); walk-wave across ~18 census guards for a new src+test pair (lesson 8). Predict the headline from those solos, write PREDICTION.txt BEFORE the run, run full suite serially at the worktree root: ps -eo cmd | /usr/bin/grep -c '^php .*phpunit' MUST print 0 (STANDALONE probe, never &&-chained), setsid+nohup detached, </dev/null, --colors=never, poll YOUR OWN PID with bounded sleeps, verify the junit root CLOSES (</testsuites> present, >3 MB, totals line matching the banner) before believing EXIT 0. cmp.py vs /tmp/opencode/P11.S5/tip.xml on ANY miss; unexplained mover = STOP.
Goldens: BOTH must stay byte-identical (f5de3858c1bc130d1e97a120d3ead485 / 8,278 + ef0326dd38535aaa2f1d715919bff26e / 1,060) — md5sum + stat at tip before AND after the suite. Any move = HALT, do not regenerate.
Red-on-revert (mandatory, in-worktree, restore + md5 verify): (R1) splice skip deleted => scoped rule canary reappears in the synthetic render, wiring test reds; (R2) escape call deleted in nudge => forgery plant reds; (R3) mark-on-delivery removed => second-call-silent test reds; (R4) deferral branch forced to clip => pointer-line test reds (proves bodies are indivisible).

## COMMIT HYGIENE (law 4g, four scans)
Identity via NAME="Joe Huss" EMAIL=$(printf '%s@%s' detain interserver.net) git commit ... NEVER a copied placeholder; /usr/bin/grep -cF '[EMAIL]' on BOTH git cat-file commit HEAD AND git log -1 --format=%B = 0; /usr/bin/grep -c 'REDACTED' on BOTH = 0 (this defect form is REAL — it once rode as [REDACTED_EMAIL] in author fields and passed the [EMAIL] scan); message file /usr/bin/grep -c '[<>]' = 0 (arrows written as words, no angle brackets in the message; parentheses fine); commit -F. One commit per file per §1.6 is preferred (src first, then tests, then tools). Record the item-(4) ruling verbatim in the step report so the worklog entry carries it.
## REPORT FORMAT
Paste-verbatim: git log --oneline -3 before/after; per-commit: git show -s --format='%H%n%an <%ae>%n%s' + git show --name-only + the four scans + porcelain; diffstat vs base; solo tables base->tip; PREDICTION.txt content + full-suite banner + junit size + closed-root proof; goldens md5+stat twice; R1-R4 each RED then restored (md5 verify); premises FALSE list (any file:line in your brief that disagreed with disk — the plan's Bootstrap :5191-5205 cite and tests/Integration/PromptStabilityTest path are KNOWN-stale, expect them); one line per risk 1-10 of the premise file: met and how.
