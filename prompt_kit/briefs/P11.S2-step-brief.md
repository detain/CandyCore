# P11.S2 — correct `docs/ARCHITECTURE.md` + `MemoryBlock.php` docblock

Provenance: prompt_plan.md `### P11.S2` (~:3133). Base at staffing: `master = 6aa5dc30e`
(branch from CURRENT master tip; note actual base sha).

## 1. CEILING

Two files, nothing else:
- `sugar-crush/docs/ARCHITECTURE.md`
- `sugar-crush/src/Context/MemoryBlock.php` — **docblock / comment-text lines ONLY. Zero token
  change: no code, no signature, no string literal, no whitespace outside comments.**

Tests are OUT of your ceiling. Do not create or edit any test file.

## 2. GOAL (quoted from plan)

> **Goal** Its prompt-assembly section (`:229-265`) documented the seven layers and matched the
> code exactly. After phases 3 and 5 it does not. Fix it.
> **Done when** a test — or a documented manual check recorded in the worklog — confirms the
> documented order matches the assembled order. Prose that restates a code fact rots; if a cheap
> assertion can pin it, add one.

## 3. FACTS TO DOCUMENT (re-verify each by direct read at your base)

**The correct order of record** = `Runtime::systemPromptSections()` body (same list as P11.S1 §3A):
base → `MaximsSection` → tool-guidance (if non-empty) → repo-map → `<user-rules>` →
`<project-instructions>` docs → project-tier rules → memory → enabled skill bodies → skill listing →
`<env>` LAST. ARCHITECTURE `:229-265` currently lists only SEVEN and has them WRONG.

**Two contradictions to fix in ARCHITECTURE `:229-265`:**
1. **`EnvironmentBlock` is listed SECOND** (item 2 of the seven-layer list). Post-P3.S1 `<env>` is
   **LAST**. Rewrite the numbered list to the order of record (all eleven, not seven), each layer
   named as its code names it.
2. **The cache paragraph at ~`:261-264`** reads:
   > `EnvironmentBlock::render()` … sits **ahead** of everything else, so the first edit of a
   > session voids the cacheable prefix for everything downstream of it anyway.
   This is the pre-P3.S1 rationale and is now backwards: env is LAST precisely so **nothing is
   downstream of it**. Replace with the live invariant (volatile `<env>` last, ordered by mutation
   frequency; a block earlier would void the prefix for every layer after it).

**Mirrored stale sentence in `MemoryBlock.php` class docblock (~`:52-53`):**
> `EnvironmentBlock::render()` shells out to `git status --porcelain` on every call and **sits AHEAD
> of this block**, so the first edit of a session voids everything downstream of it anyway.
ARCHITECTURE cites MemoryBlock as its source, so this must be fixed too — **comment-text only**,
change the claim so it says `<env>` is **LAST / behind this block**. Mind the logic inversion:
with env downstream, memory is UPSTREAM of env, so env churn no longer voids the memory layer's
prefix — re-word the surrounding cache argument to match the real topology, keeping the pinned
test reference (`PromptStabilityTest::testEnvironmentBlockGitSnapshotIsLivePolledNotFrozenAtCapture`)
accurate. Do not touch the block's code.

**"See also" update:** add a link from the ARCHITECTURE prompt-assembly section to
`sugar-crush/docs/PROMPT_ENGINEERING.md` (lands in P11.S1 — reference it as the rationale page;
the file may not exist yet in your worktree base, that is fine, this is a doc cross-link, not a
symbol cite).

## 4. TRIPWIRES

- **`php -l src/Context/MemoryBlock.php`** — MUST pass (proves zero token breakage). Comment-only
  edits cannot change the token stream; anything that does means you touched code.
- **`DocumentParagraphsTest`** — walks `src/**.php` (and `docs/**.md`, `README.md`). Your
  ARCHITECTURE edit is in scope; run it.
- **`GlobFigureDriftTest`** — `censusScope()` feeds `src/` through the paragraph window. ⚠ **Blank
  line paragraph units inside the `MemoryBlock.php` docblock move this census.** PREDICT the shift:
  run the solo BEFORE editing, note its figure, re-run AFTER, and reconcile wording inside the two
  ceiling files only (never a fixture). Token-stream identity is NOT required for comment text, but
  keep the paragraph census honest.
- **`SymbolCitationDriftTest`** — backticked symbols in `docs/*.md` and `{@see}` in `src/` must
  resolve. The new `PROMPT_ENGINEERING.md` cross-link is a relative doc link (not a resolvable
  symbol) — verify it is not parsed as a `{@see}`/backticked-class citation.

## 5. SUITE LAW

- Zero new/edited tests, zero src token change → `BinSugarcrushWiringTest` +0, test count unchanged.
- Both ceiling files are in the docs+src census walk. Run, in your worktree:
  `php -l` on MemoryBlock, then solos `DocumentParagraphsTest`, `GlobFigureDriftTest`,
  `SymbolCitationDriftTest`, and the ARCHITECTURE-touching `TrustKeyDocumentationDriftTest`
  (do NOT restate `trustedProject*` counts).

## 6. GOLDENS

No golden bytes change (comment text + prose only). If a doc-count figure in ARCHITECTURE or the
MemoryBlock docblock is derived by a census guard, adjust the WORDING in the ceiling files to match
what the guard now measures — never touch a golden/fixture.

## 7. COMMIT HYGIENE

- One commit; staged set = `docs/ARCHITECTURE.md` + `src/Context/MemoryBlock.php` only.
- Subject: `prompt/P11.S2: ARCHITECTURE + MemoryBlock docblock - <env> is last, eleven layers`.
- Body: base sha; the two ARCHITECTURE contradictions fixed; the mirrored docblock sentence fixed
  (comment-only); the See-also link added; the GlobFigure paragraph-shift prediction and actual.
- Commit MESSAGE must contain no `<`/`>` (write "`<env>` is last" as `env is last` in the message;
  the files may carry the fences). No `REDACTED`, no placeholder email.

## 8. REPORT FORMAT

Return: base sha; the old-vs-new ARCHITECTURE numbered list (paste both); the exact MemoryBlock
docblock before/after lines; `php -l` output; the four tripwire solos with PASS lines and the
GlobFigure figure before→after; the documented manual check you performed to confirm documented
order == assembled order (read `systemPromptSections()` and diff the names against your new list);
recommendation for the cheap pin (name the assertion you would add and the class list it pins
against, for the worklog) — since tests are out of your ceiling you DELIVER the manual check and
RECOMMEND the pin, exactly as the plan's manual-check path allows; confirm zero tokens changed in
src (php -l + your own `git diff --stat` showing only comment lines).
