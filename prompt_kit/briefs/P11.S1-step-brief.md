# P11.S1 — ship `docs/PROMPT_ENGINEERING.md`

Provenance: prompt_plan.md `### P11.S1` (~:3120). Base at staffing: `master = 6aa5dc30e`
(branch your worktree from CURRENT master tip; note the actual base sha in your report).

## 1. CEILING

`sugar-crush/docs/PROMPT_ENGINEERING.md` — **new file, this path ONLY.** You touch nothing
else: no src, no other doc, no test, no plan/worklog file. If a fact would force an edit
outside this page, STOP and record it as a worklog recommendation instead.

## 2. GOAL (quoted from plan)

> **Goal** Ship the rationale as docs alongside the prompts, so the layering survives
> contributors who did not design it: the section order and why, the stability classes, the
> fence/provenance rules, the cache-breakpoint contract, and the register rules (what goes
> in, what was deliberately left out and why).
> **Done when** it explains all nine of §9.12's "do not do this" items with their reasons,
> because those are the ones a well-meaning contributor will otherwise re-add.

## 3. FACTS TO DOCUMENT

Every claim below MUST be re-verified by direct read at YOUR base before you write it. Cite
SYMBOLICALLY (`Runtime::systemPromptSections()`, `PromptFence::TAGS`, etc.) — do NOT paste
line numbers into the page (they rot; that is the very defect P11.S2 fixes). If a fact
disagrees with your read, TRUST THE CODE and record the divergence in your report.

**A. Section order of record** — `Runtime::systemPromptSections()` (read the method body):
1. base prompt heredoc (Static)
2. `MaximsSection` (Static) — core.maxims voice, directly behind base
3. tool-guidance fragment (Static) — appended ONLY when `toolGuidanceSection($app)` is non-empty
4. repo-map snapshot
5. `<user-rules>` fences — user-tier rules (each rule its own fence)
6. `<project-instructions>` documents — `InstructionFileLoader::loadRoot()` + `loadForced()`
7. `<project-instructions>` project-tier rules (same fence/preamble as the docs above)
8. memory snapshot (`MemoryBlock`)
9. enabled skill full bodies (`PerTurn`)
10. `SkillMatcher::listForPrompt()` listing (`PerTurn`)
11. `<env>` `EnvironmentBlock` — **LAST** (the P3.S1 invariant)

**B. The assemble invariant** — `Runtime::assembleSections()` and `CompleteRequest`:
`assembleSections()` is a ONE-fold producing `[$systemPrompt, $systemBlocks]`; the string arm
and the block arm come from the same accumulator, so `implode('', $systemBlocks) === $systemPrompt`
holds BY CONSTRUCTION (byte-for-byte), not by convention. `Runtime::run()` destructures that fold
into `new CompleteRequest(systemPrompt:…, systemBlocks:…)`.

**C. Fence/provenance rules** — `PromptFence`:
- Roster `PromptFence::TAGS` is EIGHT tags. Read them at your base and enumerate EXACTLY; do not
  trust this list. (Staffing read: `env`, `project-memory`, `repo-map`, `project-instructions`,
  `system-reminder`, `user-rules`, `prior-summary`, `harness-injected`.)
- `PromptFence::escape()` folds `<` → `&lt;` via one `preg_replace_callback`; it is
  **idempotent** (an already-escaped `&lt;` has no live `<` to re-hit) and throws on PCRE failure
  (fail-fast, never ships an unescaped body).
- Provenance preambles ride under their opener, split from the escaped body by a blank line: the
  user-rules voice (`Runtime::USER_RULES_AUTHORITY_PREAMBLE`), the project-instructions voice
  (`Runtime::INSTRUCTIONS_AUTHORITY_PREAMBLE`), plus the repo-map and project-memory framings —
  re-read each block class to get its exact wording/authority claim. One escape authority, applied
  at every dynamic-byte splice.

**D. Stability classes** — `Stability` enum (`Static` / `PerSession` / `PerTurn`): Static = the
cacheable prefix (base, maxims, tool-guidance); PerSession = repo-map, rules, instruction docs,
memory; PerTurn = skill bodies, skill listing, `<env>`. The ordering is by mutation frequency;
`<env>` LAST is load-bearing (P3.S1) precisely because `EnvironmentBlock::render()` live-polls
`git status` and would otherwise void the prefix for every layer after it.

**E. Cache-breakpoint contract** — `CacheBreakpoints`:
- Applied wipe-then-reapply each step; `MAX_BREAKPOINTS = 4` (Anthropic's per-request cap);
  `BUDGET_WITH_AUTOMATIC = MAX_BREAKPOINTS - 1`.
- **UNWIRED / dormant by design**: no `src/` or `bin/` production caller consults it yet
  (`grep -rn CacheBreakpoints src/` returns only the class + docblock cross-refs). The kill switch
  is `SUGARCRUSH_DISABLE_PROMPT_CACHE` (const `DISABLE_ENV`), already tabulated
  **dormant-by-design** in `docs/ENVIRONMENT.md`. State the contract as a not-yet-armed seam, not
  as live behaviour.
- ⚠ ENVIRONMENT.md is the `EnvRosterDriftTest` census source; see §4.

**F. Session-affinity trait dormant-id state** — the session-affinity/uuid trait is in a
dormant-id state (no stable id minted/consulted on the wired path). Read the trait at your base;
describe its dormant state honestly, do not imply it is active.

**G. Register rules** (what goes in / deliberately left out) — the §18 rows of prompt_plan plus
the deferred items named in `systemPromptSections()` doc-comments:
- triggers-not-applied: rules render into EVERY session until the P6.S5/P7.S4 gating (a scoping
  gap, not a safety gap — framed/escaped tier-blind).
- P6.S5b blocked; persistence deferred; harness-injected content is defang-only.
- What is intentionally NOT a layer (see §9.12 below).

**H. The §9.12 "do not do this" items** — READ `prompt_expand.md §9.12` ("What NOT to do") at
your base and enumerate the items EXACTLY as they appear. ⚠ COUNT DISCREPANCY: the plan's
done-when says "nine", but staffing's read of §9.12 finds **SIX** `Do not` bullets:
(1) don't copy "fewer than 4 lines / one-word answers"; (2) don't stack `IMPORTANT:`/`CRITICAL:`;
(3) don't hardcode a thinking-token ladder (4k/10k/31999 gone); (4) don't add a per-tool-result
safety reminder; (5) don't add a user-supplied system-prompt override key (Roo "footgun prompting";
`LayeredSettings` right to omit); (6) don't put a blanket total-request timeout on a completion
(short `connect_timeout` fine; cancellation is the mechanism).
→ Document every item that ACTUALLY exists at your base. If §9.12 still lists six, ship six and
record the plan-says-nine discrepancy in your report; do NOT invent three to hit nine. The
done-when is "explains all of §9.12's items with reasons", which the real set satisfies.

## 4. TRIPWIRES

The new page joins these docs-driven guards (all re-derive from generators; verify by running
each solo BEFORE finalising):
- **`EnvRosterDriftTest`** — census over `docs/**.md`: any `SUGARCRUSH_*` var you name MUST match
  a row in `docs/ENVIRONMENT.md`'s roster, else red. Keep `SUGARCRUSH_*` mentions MINIMAL and
  EXACT (only `SUGARCRUSH_DISABLE_PROMPT_CACHE`, phrased to agree with its dormant row).
- **`TrustKeyDocumentationDriftTest`** — recursive walk incl. deep-link/stale-count census over
  docs + README. Do NOT restate any `trustedProject*` count; if you must reference trust keys,
  link rather than enumerate.
- **`DocumentParagraphsTest`** — fence-balance over `README.md` + `src/**.php` + `docs/**.md`
  (NOT `tests/`). Every fenced code block in the page must be balanced; blank-line paragraph
  units are policed.
- **`SymbolCitationDriftTest`** — every BACKTICKED `*Test` symbol you cite in the page must
  resolve to a real test/class. Prefer symbolic `Class::method` cites that the walker resolves;
  run it to confirm none dangle.

## 5. SUITE LAW

- New file is a doc → adds **0** tests, **0** src symbols. Expect `BinSugarcrushWiringTest` +0.
- `DocumentParagraphsTest` / `GlobFigureDriftTest` DO walk `docs/**.md` → your page is in scope.
  Run those solos + `EnvRoster` + `TrustKey` + `SymbolCitation` before declaring done.
- No keystroke/loop work in this step.

## 6. GOLDENS

Docs-only; no golden/byte fixtures for this page. Do NOT regenerate any golden corpus. If any
guard reports a figure derived from file counts that your new page shifts, run the solo, read its
message, and update only the wording inside `PROMPT_ENGINEERING.md` (never a fixture).

## 7. COMMIT HYGIENE

- One commit, staged set = `sugar-crush/docs/PROMPT_ENGINEERING.md` only.
- Subject: `prompt/P11.S1: ship docs/PROMPT_ENGINEERING.md (layering rationale)`.
- Body: name base sha, the section order shipped, the §9.12 count shipped (six or nine as read).
- No angle brackets / `<` `>` in the commit MESSAGE (the page may contain them; the message may
  not). No `REDACTED`, no placeholder emails.

## 8. REPORT FORMAT

Return: base sha; the enumerated §9.12 count shipped with a one-line reason each; the section
order you documented; the 8-tag roster as read; which tripwire solos you ran and their PASS line;
any fact at your base that diverged from §3 (with the file+symbol); confirmation you touched no
file outside the ceiling.
