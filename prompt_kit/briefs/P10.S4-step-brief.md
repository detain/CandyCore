# P10.S4 step brief — hashed session-affinity header

PLAN: prompt_plan.md `### P10.S4` (Goal/Hard-constraint/Done-when bind) + the concurrency note (:3100-3102 — scope is the Concerns/ builder ONLY; you must NOT touch CacheBreakpoints.php; sibling P10.S3 owns it in a concurrent worktree — never look at or touch /home/sites/prompt-step-P10.S3). Expand sources §5.8 :2530-2531 / §9.5 :3553-3554: the header exists to route a session to a warm cache backend; upstream name/hash form is UNVERIFIED in-repo — the decisions below are FIXED.

CEILING (ONLY these; anything else = HALT and report):
1. sugar-crush/src/Providers/Concerns/SessionAffinity.php (NEW trait — there is NO existing shared header builder; zero hits for extraHeaders|buildHeaders|sessionHeaders in src/ — the plan's directory entry anticipates a new file)
2. sugar-crush/src/Providers/SglangProvider.php (EDIT)
3. sugar-crush/src/Providers/CustomProvider.php (EDIT)
4. sugar-crush/tests/Providers/SessionAffinityHeaderTest.php (NEW)
NOT allowed: CompleteRequest.php (S1-owned, merged 77881b9cb), Cli/Bootstrap.php or ProviderFactory.php (out of declared scope — S3 may be editing Bootstrap docs in parallel and neither file is yours), Chat.php (foreign-lane row), CacheBreakpoints.php (S3).

DESIGN — FIXED by orchestrator adjudication from scout evidence (law 4m: the cross-class premise was verified — session id lives ONLY at Chat::$currentSessionId (mutated by /resume :10494, /branch :10564, Ctrl+Tab) and the App field Runtime already reads :2252; it reaches NO provider today through no declared-file route):
A. The trait: namespace + file style EXACTLY like sibling Concerns traits (read ToolSchema.php and HttpClientDefaults.php headers first — re-derive, do not assume). Public const `SESSION_AFFINITY_HEADER = 'X-SugarCrush-Session'` (first header-name constant in src/ — note that in the docblock). Method `sessionAffinityHeaderValue(?string $sessionId): ?string` returning `hash('sha256', $sessionId)` — full 64-char lowercase hex, NO truncation (in-tree precedent: ContextCompactor exchangeKey :106-109 + three more hash('sha256') users; truncation would need a collision analysis full hex does not) — and NULL for null or '' sessionId (empty-hashing would pin every sessionless caller to one backend — mirror the `if ($apiKey !== null)` conditional-header idiom SglangProvider.php:459-461). Docblock cites Mirrors charmbracelet/crush sessionHeaders(call.SessionID). Also `sessionAffinityHeaders(): array` returning [] or [self::SESSION_AFFINITY_HEADER => hash] from a `?string $sessionAffinityId` property.
B. Providers: each gains a trailing defaulted promoted param `private ?string $sessionAffinityId = null` on the CONSTRUCTOR and a matching trailing `?string $sessionAffinityId = null` on openAiCompatible() passed through. Uses the trait. MERGE PER-REQUEST at ALL SIX post() sites (Sglang :631 complete, :668 completeStream, :921 embeddings; Custom :164, :219, :279 — anchors from master 4ab8c3f98, RE-DERIVE before each edit per law 4j/§7h: they self-shift under your own inserted lines; SglangProvider is 2,473 lines) via `'headers' => $this->sessionAffinityHeaders()` in the request options. WHY per-request: client-level headers alone would miss every injected-client shape (unit tests build bare Clients; ProviderFactory::createAnthropic :625-656 injects a hand-built client). GUARD: Guzzle must MERGE request 'headers' with client-default 'headers' per-name (Content-Type/Authorization must survive on the wire). Pin this with a real captured-request assertion (below) — if on this Guzzle version the per-request 'headers' option REPLACES client defaults, STOP: that breaks auth on every call — HALT and report, do not paper over it.
C. Live wiring is a RECORDED FOLLOW-UP, not this step: no declared file can carry Chat's id into the providers, construction-time id would go stale across /resume (evidence above), and the Done-when "title, summary, main-loop all carry it" is asserted HERE at the provider-construction-shape level (all three backends build Sglang/Custom via ProviderFactory — SglangProvider.php:1324-1332 in-repo proof that title/summary are exactly Runtime turns with tools=null). The trait + both provider docblocks state the consumer contract: pass the current session id at construction (or re-construct) when the wiring step lands. Roster note: remaining providers (OpenAI SDK path :98/:132/:141, Vertex, Bedrock, ClaudeCode CLI — it carries the raw id only to a local `--resume` argument, not a proxy header) are an enumerated follow-up in the trait docblock; do NOT touch them.

TESTS (SessionAffinityHeaderTest.php; capture harness COPIED IN SPIRIT from tests/Providers/SglangProviderRequestBuildingTest.php :32-47 — HandlerStack + MockHandler + Middleware::history, assert getHeaderLine(...) on captured PSR-7 requests; keep helpers file-private, uniquely named — DuplicatedTestHelperDriftTest byte-twin trap claimed in P10.S1: a twin of an existing capture helper differing only by a literal REDDENS the guard; give yours a distinct name AND a why-docblock):
1. Shape: 64-hex regex via preg_match === 1 (value form, not assertIsString).
2. Not-raw: header !== sessionId AND !str_contains(header, sessionId) — the plan's hard-constraint bite.
3. Same-session stability: two calls on one provider => identical header — on BOTH complete() AND completeStream() (stream is the live path — HttpClientDefaults.php :48-52 says so; use an SSE-shaped body fixture per ProviderRequestResponseTest.php :535-536 pattern).
4. Discrimination: two providers, two ids => different headers.
5. Absent: null and '' ids => no header on the wire (assertSame('', getHeaderLine(...))).
6. Co-presence: captured request has Content-Type application/json (and Authorization when apiKey given) TOGETHER with the affinity header — pins Guzzle merge semantics (design B GUARD).
7. Three construction-shape cases: agentic-style provider WITH tools, title-style toolless (tools=null, the one-shot shape), summary-style toolless — header present on the main call each way (the Done-when triad at the honest achievable level).
8. embeddings() site carries it (or if you rule embeddings should NOT, HALT — plan says every LLM call; the cheap honest read is all six sites marked).
Every test reddens when its production line is reverted (deletion experiments reported). CustomProvider mirror-sites need at least one own test (do not assume Sglang coverage transfers — the trait is shared but the six sites are not one code path).

GOLDENS FROZEN: system f5de3858c1bc130d1e97a120d3ead485 / 8,278 B; agent ef0326dd38535aaa2f1d715919bff26e / 1,060 B. The header is transport — a golden move means bytes leaked into prompt assembly = HALT (P9.S7 rule verbatim). Also law 4h: do not add any heading-shaped or tag-bearing literal to src prose (the env-block/name-sweep landmines).

SUITE PREDICTION: floor anchor Tests 11,148 / Assertions 170,600 / 0F / 0E / Skipped 2 / EXIT 0 at master 4ab8c3f98. Your wave: NEW src trait => BinSugarcrushWiringTest +1t/+6a (law 4e); new src + new TEST file => refined law-8 walk wave (~18 classes incl +2 censusScope reads per file); GlobFigureDrift +1 per docblock paragraph (both new/edited src files); SymbolCitationDrift +1 per placed {@see}; edited providers' own docblock changes count too. Predict from per-class solos at your FINAL tip BEFORE the full run; python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py reconciliation, remainder 0 or named movers. Full-suite command + box-quiet + cwd + in-session wait rules identical to the S3 brief (artifact /tmp/opencode/P10.S4/tip.xml).

COMMIT HYGIENE: identical to S3 (literal Joe Huss / detain@interserver.net typed via shell interpolation, -F file, TAG-FREE grep '[<>]' == 0, law-4g triple scan). Suggested subject: "sugar-crush: hashed session-affinity header on the two declared provider transports" (your wording).

REPORT FORMAT: files+diffstat; public surface quoted (trait consts/methods, both ctor+factory signatures); per-test pin table incl the six-site ledger (file:line ACTUAL after your edits); Guzzle-merge proof (assertion 6 output); deletion experiments; goldens re-verified; solo N/N; full-suite prediction-vs-actual + cmp table + remainder; commit sha; porcelain clean. Premise drift -> trust CODE, report drift (law 4j: all line anchors were quoted from master 4ab8c3f98 today and will shift under your edits).
