# P7.S3 — Step brief: skills-into-the-prompt, canonical channel wired at the composition root

## ORCHESTRATOR CANONICAL-PATH DECISION (made pre-brief; restate with reasoning as the FIRST artifact of the worklog entry)

The withSkills -> enabledSkills -> Runtime-bodies channel is CANONICAL for pre-enabled skill bodies in the SYSTEM PROMPT, wired at the composition root (Bootstrap.php:2206 + :2274). SkillTool remains canonical for ON-DEMAND loading (tool-result into history — a different carrier, not the prompt); AgentManager for subagents; neither is rerouted or weakened (§1.10). The Level-1 registry LISTING and enabled BODIES must not both present the same skill: if measurement shows listForPrompt includes enabled skills, exclude at the splice seam — a deviation from the plan file list (Runtime.php not listed) IF needed, recorded like P7.S2's deviations.

Rationale: (1) the plan premise was wrong (picker dead-seam, premise-A proof) so picker-flow cannot be canonical without Chat/App surgery outside this step's file list; (2) the plan's own file list (EngineBackend + Bootstrap) names composition-root wiring; (3) Anthropic progressive disclosure favors default-empty — with no honest config allowlist source found (premise-B: only the disabledSkills denylist), the DEFAULT enabled-set is EMPTY, so behavior is unchanged for every existing user; the channel becomes production-reachable ONLY via explicit configuration. The step answers the premise's real question — "is enabledSkills -> Runtime.php:2675 reachable from production at all? currently NO" — by making it YES, reachable-and-tested. Auto-enable-everything (denylist-inverted semantics) is REJECTED: it would dump every skill body into every prompt, violating progressive disclosure and token economics.

## STEP 0 — MANDATORY PRE-CODE MEASUREMENTS (HALT if falsified)

1. Re-confirm the config vocabulary yourself (grep LayeredSettings + readUserConfig paths for any skill allow-key). If one exists, use it as the source. Else determine whether readUserConfig is a free-form passthrough: adding one documented user-config key enabledSkills mirroring the disabledSkills plumbing is IN SCOPE if it lands in the same few places disabledSkills touches; if it sprawls, fall back to wiring the seam to accept the set (default empty) from a Bootstrap-side parameter plus integration-test injection, and record the config-key deferral. HALT and report if neither path is honest.
2. Verify skillRegistry construction (Bootstrap.php:2907) and where disabledSkills filtering happens upstream (:2917 usage).
3. Read listForPrompt + SkillMatcher to determine listing-inclusion semantics (does the listing already exclude enabled skills?).
4. Re-derive EVERY premise line cite against your branch base — both premise parts warn of drift.

## Files in scope (primary)

- sugar-crush/src/Cli/Bootstrap.php (wiring at the :2206/:2274 sites + the new config key if STEP 0 lands it)
- sugar-crush/src/Backend/EngineBackend.php (verify-only; expected UNTOUCHED — the :600 consumer already exists)
- sugar-crush/tests/Integration/SystemPromptWiringTest.php
- sugar-crush/tests/Integration/FeatWiringReachabilityTest.php
- conditional: sugar-crush/src/Runtime.php splice seam (only if exclusion needed — record the deviation)

NOT in scope: Chat.php, App/App.php, Skills/Skill.php, Tools/BuiltIn/SkillTool.php, Hooks/HookDispatcher.php (leave dormant).

## Done-when (all four)

1. Exactly-once body pin: the real-launch seam drives a populated enabled set; assertSame(1, substr_count($prompt, the skill section marker)) — shape precedent RuntimeTest.php:1652; plus a listing-does-not-also-carry assertion (or documented why acceptable).
2. Launch-REACHABILITY kill-shot vs plan lesson 16.1: an enabled skill body reaches the PROVIDER PAYLOAD through the Bootstrap composition path (not just an App DTO) — reuse the launchedSystemPrompt pattern :1336/:1367. AND a default-empty no-op proof: unchanged config means the launched payload is byte-identical to base behavior.
3. Every new pin red-on-revert: deletion/revert experiments mandatory, paste outputs (§1.11).
4. Goldens UNMOVED unless order changes — re-measure AFTER code, never before. PromptStability byte tables are drift candidates: record ABSOLUTE new figures; never hand-edit derived numbers.

## Suite protocol

FLOOR at master tip after the P7.S2 merge — re-derive at staffing; current expectation 10,980 tests / 168,841 assertions / 2 skipped / EXIT 0. Measure base at YOUR provisioning SHA anyway. Box-quiet probe == 0 before each run. Predict the headline BEFORE running (per-class solos at final tip, or label the figure derived-after-fact). Full suite serial in-session: cd WORKTREE, then php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never with stdin redirected from /dev/null. Reconcile per-class with python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py to zero remainder. MouseModalGuard 198-vs-201 is legal timer wobble. A new src file would auto-bump BinSugarcrushWiringTest (+1 test/+6 asserts) — none expected. Identity: author and committer Joe Huss, email detain@interserver.net shell-interpolated; commit via -F file after a grep count of the less-than char in the message file == 0; after committing, scan cat-file commit HEAD for the bracket-EMAIL literal — must be 0. ONE commit. NEVER push, NEVER merge to master. §1.10: zero deletions of existing behavior — the dormant withSkills method getting its FIRST production consumer is the point of the step. Escalate (HALT, report verbatim) any impulse to remove, rename, or weaken existing code or tests; also HALT if a provider-payload seam turns out to fork (the P7.S2 two-step capture pattern is the fallback proof mechanism).
