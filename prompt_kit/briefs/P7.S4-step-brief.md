# P7.S4 — Step brief: findForPrompt closed deliberately-dormant, with the measured number + semantics pinned

BINDING. The measurement and the orchestrator's outcome decision are FINAL (see `/home/sites/sugarcraft/prompt_kit/findings/P7.S4-premise.md`); this step does not re-open them. Read the premise before touching anything.

## Scope (closed list — anything outside it is an ESCALATION, not a judgement call)

- `/home/sites/sugarcraft/sugar-crush/src/Skills/SkillRegistry.php` — `findForPrompt()` DOCBLOCK ONLY. NO behavior change, NO wiring, NO caller changes.
- `/home/sites/sugarcraft/sugar-crush/src/Skills/Skill.php` — `matchesPrompt()` DOCBLOCK ONLY. Same limits.
- Tests: `/home/sites/sugarcraft/sugar-crush/tests/Skills/SkillRegistryTest.php` and `/home/sites/sugarcraft/sugar-crush/tests/Skills/SkillMatcherTest.php` — BOTH VERIFIED PRESENT at staffing; extend these in place, do not create new test files.
- Already on disk (do not regenerate, do not modify): `/home/sites/sugarcraft/prompt_kit/findings/P7.S4/measure.php` (md5 `01ac5f85c8afecdc095d06ce51698337`) and `/home/sites/sugarcraft/prompt_kit/findings/P7.S4/raw-output.txt` (md5 `8e0f610bfe58989d702287277df47914`) — byte-identical repo copies of the /tmp artifacts.

The dormant callers `SkillManager::getSkillsForTask()` (`src/Skills/SkillManager.php:143`) and `App::findSkillsForTask()` (`src/App/App.php:424`) stay UNTOUCHED — §1.10 (nothing removed, stubbed or deprecated).

## Tasks

1. **Landmine docblocks** on `findForPrompt()` (`SkillRegistry.php:251`) and `matchesPrompt()` (`Skill.php:90`). Required content, verbatim intent: *deliberately unwired — measured FP rate at b289eaf44: precision 0.162, boundary-FP 96%, whole-word variant maxes 0.214; see `prompt_kit/findings/P7.S4-premise.md` (P7.S4 close)* — and cite the artifacts by repo path (`prompt_kit/findings/P7.S4/measure.php`, `prompt_kit/findings/P7.S4/raw-output.txt`). Docblock prose states WHY, per project conventions. LANDMINE: do not paste bare `## Skill`-style section-marker literals into src prose or docblocks — env-embedded-count tests inflate on them (the trap measured in the P7.S3 worklog entry, `/home/sites/sugarcraft/prompt_worklog.md`); phrase any reference generally, not as the literal marker.
2. **Pin tests** (new methods in the two existing test classes) capturing CURRENT semantics as FEATURE:
   - `matchesPrompt` BOTH polarities: a prompt with no >3-char description-token substring returns false; a substring hit returns true.
   - The pathological boundary pins asserting the substring rule ON PURPOSE: `'airport'` fires the `matchups-sync`-style `port` needle — assert `matchesPrompt('handle this package with care at the airport')`-shaped input TRUE for the port-bearing skill; and the multi-fire case — a registry holding the corpus-shaped skills returns MANY skills for "let me know when you are ready for lunch" (the `'when'` ×9 fire). Pin counts/booleans, name the tests so a reader learns the rule is documented-by-assertion, not accidental.
   - `findForPrompt` ORDERING no-op: with two matching skills whose descriptions do not contain the whole prompt (always true for realistic prompts), the sort key is 0-0 and the returned array preserves REGISTRY INPUT ORDER — pin the order, so a future sort change surfaces as a visible test diff.
3. **Red-on-revert each pin**: for every new test, revert the corresponding src side (or a semantic equivalent) in a scratch copy and paste the FAILING output (§1.11 — a pin that passes against changed behavior is not a pin).
4. **Suite protocol.** FLOOR at base: 10,990 tests / 168,977 assertions / 2 skipped @ b289eaf44 (master tip b289eaf44; FLOOR gated at 129587a97). Predict the headline BEFORE running, derived from per-class solos at final tip. Reconcile with `python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py` to zero remainder. Record every census mover as ABSOLUTES: docblock paragraph edits → `GlobFigureDrift` ±; new test methods → their own class totals; `BinSugarcrushWiringTest` must stay FLAT (NO new src files in this step). Goldens MUST NOT MOVE: `a5c5a14ca2e3ad891933ac7aefccc6af` / 7732 and `ef0326dd38535aaa2f1d715919bff26e` / 1060, and the fixtures diff stays empty. Any movement = HALT and escalate, do not re-baseline.
5. **One commit**: `sugar-crush: P7.S4 findForPrompt closed deliberately-dormant with the measured number + semantics pinned`. Author+committer identity shell-interpolated `Joe Huss <[EMAIL]>`. Message via `git commit -F <file>` AFTER asserting a `<`-char scan of the file == 0; after committing, `cat-file` scan of the commit object for the bracket-EMAIL literal == 0. NEVER push, never merge.

## Done-when (all four)

1. Both docblocks landed (WHY-dormant + the number + artifact cites), zero behavior diff in the two src files (`git diff` shows docblock hunks only).
2. All pins green AND each proven red-on-revert with pasted output.
3. Suite ≥ FLOOR with cmp.py zero remainder; goldens unmoved; census movers recorded as absolutes.
4. A worklog-ready numbers paragraph (test/assert/skip deltas, per-mover lines) handed back with the commit SHA.

## ESCALATE instead of improvising

Any impulse to change matcher behavior, wire `findForPrompt`, or touch the dormant callers (§1.10 + the orchestrator decision above). Any golden move. Any census mover you cannot explain as an absolute. Any test that cannot be made red-on-revert (that means the pin is vacuous — report, do not pad).
