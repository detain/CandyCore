# P9.S2 — Capability-aware descriptions (rg/fd) — step brief

Plan: prompt_plan.md `### P9.S2` (re-derive line; ~:2871). Batch 2 CONCURRENT (S2/S3/S4 disjoint by construction). Base: staffing commit. FLOOR 11,062/169,786/0F/0E/2S gated `2c6583561`. Goldens FROZEN system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 · agent `ef0326dd38535aaa2f1d715919bff26e`/1,060 — expected UNMOVED (descriptions ride tools[], never the golden heredoc; goldenContext has NO withTools).

**Goal**: detect `rg` and `fd` on PATH ONCE at boot; Grep's and Glob's description() gain a capability clause only when present. `gh` is OUT OF SCOPE (measured: no non-Bash description references gh; Bash.php is P9.S3's declared file on a live collision row — shipping a gh boolean with no consumer is §1.12 dead weight. Record this in the final report).

**File ceiling (deviation-corrected, mirrors P9.S1's ToolRegistry correction)**: NEW `sugar-crush/src/Tools/Concerns/` probe file (name it — e.g. DetectsCapabilities.php, your call, PSR-4-honest); `src/Cli/Bootstrap.php`; `src/Tools/BuiltIn/Grep.php`; `src/Tools/BuiltIn/Glob.php`; NEW `sugar-crush/tests/Tools/CapabilityAwareDescriptionTest.php`. NO other file. NO docs. NO env var. NO config key. NO change to execute()/runtime behavior of any tool — this step changes RENDERED TEXT only.

**R-A probe**: stat-based PATH walk — split `getenv('PATH')` on ':', is_file+is_executable per candidate for 'rg' and 'fd'. The precedent is the private resolveExecutable at `src/ClaudeCodeMcpClient.php:945-968` — DUPLICATE ~20 lines into your Concerns file; do NOT edit ClaudeCodeMcpClient.php. NO subprocess (the doctrine at ClaudeCodeMcpClient.php:938-943: a missing binary via proc_open warns, and phpunit failOnWarning turns a warning into a suite failure). Memoize the result in a static (house idiom: `src/Tools/BuiltIn/Doctor.php` `self::$mosaic ??=`) so it runs at most once per process regardless. ZERO `SUGARCRUSH_`-prefixed getenv anywhere (EnvRosterDriftTest walks every src file; a roster row would force docs/ENVIRONMENT.md — out of ceiling).

**R-B absent-by-default is the CONSTRUCTOR, not a testing flag** (measured: sugar-crush has no testing-mode flag). New defaulted param (e.g. `private bool $rgAvailable = false` on Grep, `$fdAvailable = false` on Glob). EVERY existing pin renders the absent default: Grep's 'runs `grep -rn`, not PCRE'/'GNU basic' pins (ToolDescriptionGuidanceTest ~:450-473) and Glob's notContains families (BuiltInToolTest ~:162-172, ToolDescriptionGuidanceTest ~:678) MUST survive byte-identically → the ABSENT rendering of both descriptions is byte-identical to today. Verify by running those classes solo before AND after.

**R-C present clauses**: Grep gains one honest sentence (rg is on PATH; when Grep's GNU-BRE dialect bites, Bash `rg` is available to you — Grep itself still runs grep, do NOT imply the tool switched). Glob gains the fd analogue beside its existing find/ls sentence's subject. Truthfulness bar inherited from Bash.php's docblock doctrine (a false claim is worse than terse prose): every clause you add must be checkable against the code that exists.

**R-D threading**: `Bootstrap::tools()` (~:5552-5653; verify anchors by reading) gains a defaulted param (e.g. `bool $probeCapabilities = false`); ONLY when true does it read the probe and pass trues into new Grep/new Glob. The three production call sites (tools() comment self-counts: app() ~:2145, backend() ~:2378, backendFor() ~:2452 — re-derive) pass true. Test-side callers of Bootstrap::tools() (ReadmeRosterDriftTest ~:137, BuiltInToolCorpus ~:587, the name-sweep test, Cli tests) stay UNMODIFIED → they receive the absent defaults → host PATH never leaks into test renderings. If any other production caller exists (grep `self::tools(` / `Bootstrap::tools(`), route it deliberately and say so.

**Done-when** (in CapabilityAwareDescriptionTest): (1) Grep/Glob constructed bare render description() BYTE-IDENTICAL to a literal captured from today's strings (copy the current text into the test as the absent expectation — read the current source bytes yourself); (2) with the boolean true, description() contains the new clause and its sibling facts survive; (3) probe determinism: calling the probe twice returns identical booleans; (4) PATH-absence path: point the probe's PATH scanning at a sandbox PATH (constructor/static-injection seam or a path parameter) containing neither binary → both false. Golden assertion: BaseSystemPromptTest solo stays 22/319 with golden UNMOVED (it must — zero tools involved).

**Census (law 8)**: +2 GlobFigureDrift for the ONE new src file (readOrFail ×2 censusScope callers) plus +1 per blank-line paragraph in your new docblocks (predict exactly; solo GlobFigureDriftTest to confirm — expect ~22,570 base + your delta). New test file auto-enrolls: TreeWideGuardRosterTest +2a (17 tests stay), BuiltInToolCorpus glob walkers +~3t/+~12a (measure; do not guess). SymbolCitationDriftTest +1 per placed {@see}. Solo ONLY (batch-2 protocol): the orchestrator runs the full suite at master after your merge.

**§1.11/§1.10**: zero test-line removals; every existing Grep/Glob description assertion untouched (they test the absent half — R-B makes that free). If one fights R-B, STOP and report.

**Commits**: two (src+probe+Bootstrap, tests), messages via /tmp/opencode/P9.S2-build/, scans ×2. NO push. NO full suite. NO composer. Scratch /tmp/opencode/P9.S2-build/. Report: anchors, clause bytes (before/after per tool), mover solos, done-when outputs, deviations.
