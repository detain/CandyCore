# P6.S4 — Config surface for rules — STEP BRIEF

**Worktree:** `/home/sites/prompt-step-P6.S4` on branch `prompt/P6.S4`. It ALREADY EXISTS with a working
`sugar-crush/vendor` (hard-linked, PSR-4 self-resolving). Do NOT create another worktree.
**Do the sync FIRST:** `git -C /home/sites/prompt-step-P6.S4 merge master` — master is 3 PLAN-ONLY commits
ahead and `git diff prompt/P6.S4 master -- sugar-crush/` is EMPTY, so this merge cannot conflict in code and
makes your final tip's figures describe master by the belt argument.

**Read before coding:** `prompt_plan.md` §1 (especially §1.10, §1.11), §16, §17; `prompt_worklog.md`
conventions + the P6.S2, P6.S3 and HANDOFF entries; this brief in full.

## Goal (RULED — option (i); do not re-open the decision)

Register a read-side `disabledRules` layered setting key, **user-tier only**, and use it to seed the
already-dormant `RulesState::new(array $disabled = [])`. A hand-edited config value is honoured at launch.
There is **NO write door**: the `/rules` command stays session-scoped and P6.S3's pinned contract
`RulesCommandTest::testTogglingAPackLeavesTheConfigFileByteIdentical` must survive untouched. Do not build
refusal-message machinery, do not add a `refusedKeys()` accessor, do not touch the guarded write door —
all three are separately deferred and priced in the plan.

## Why this is not decorative (the §1.10 outcome)

`RulesState::new()`'s `$disabled` parameter (`src/Context/RulesState.php:98-107`) has zero production callers
passing an argument today. Feeding it is the plan's permitted "wire it" outcome — the same adoption pattern
as P6.S3's `Rule::withEnabled()`. Nothing may be removed, stubbed, deprecated or narrowed.

## Files (complete and closed — report rather than silently widening)

1. `sugar-crush/src/Config/LayeredSettings.php` — add `'disabledRules'` to `LAYERED_KEYS` (`:272-284`).
   **Do NOT add it to `PROJECT_TIER_KEYS`** (`:486-494`); `userTierOnlyKeys()` derives it automatically via
   `array_diff` (`:543-546`). Update the prose obligations below.
2. `sugar-crush/src/Cli/Bootstrap.php` — seed at `:1010` (`$rulesState = RulesState::new();`). `$userConfig`
   is already in scope at `:933` and it **is the merged layered view** (`readUserConfig()` returns
   `self::mergedConfig(true)` at `:2540-2543`) — verify this yourself and cite it in the comment.
3. `sugar-crush/tests/Config/LayeredSettingsTest.php` — the exact-list assert at `:52-57`
   (`assertSame(['provider','instructions','allowedTools','statusLine'], userTierOnlyKeys())`) goes hard red
   on a 5th user-tier key. Update it, and add the tier/precedence coverage described below.
4. `sugar-crush/tests/Cli/BootstrapLayeredSettingsTest.php` — 544 lines, 17 tests, `HomeSandboxTrait`,
   helpers `writeUserConfigFile()`/`writeUserSettings()`/`writeProjectSettings()`/`trustTheProject()`
   at `:482-544`. Add the value-plumbing tests here.
5. `sugar-crush/tests/Cli/RulesStateWiringTest.php` — the launch-based seed proof (see HARD CONSTRAINT 2).
6. `sugar-crush/docs/SETTINGS.md` — table row + the unpoliced prose figures (see Obligations).

NOT in scope: `tests/Cli/ProjectTierRefusalInventoryTest.php` (the plan's done-when named the wrong
instrument — it walks `.sugar-crush/*` FILE dot-paths via `dotPathsIn()` `:966-998` regex `:987` and has zero
settings-key assertions; S4 adds no dot-path). **Do not touch its `$ownWords` (37 of 37, zero headroom) or
`$pathWords` (consumed unguarded at `:548`, CRASHES on overflow) maps** — that is a landmine, not work.

## HARD CONSTRAINT 1 — the seed must not be able to throw (the trap in the nearest precedent)

`LayeredSettings::only()` (`:761-771`) preserves absence and validates NOTHING about values. But
`RulesState::new()` runs every entry through `parsePack()` (`:189-200`), which **THROWS
`InvalidArgumentException`** on a non-string OR on `trim($pack) === ''`. `Bootstrap::chat()` is the launch
path and `readUserConfig()`'s own docblock (`:2516-2521`) promises the launch is **tolerant and
non-throwing**.

The seductive mistake is to copy the sibling `disabledSkills` read at `Bootstrap.php:2840-2843` verbatim:

```php
$disabled = self::readUserConfig()['disabledSkills'] ?? [];
if (is_array($disabled)) {
    $manager->disableFromConfig(array_values(array_filter($disabled, 'is_string')));
}
```

That filter admits `""` and `"   "`. `SkillRegistry::disable('')` accepts a blank harmlessly;
`RulesState::new([''])` **crashes the launch**. So the seed must be strictly narrower: keep an entry only if
`is_string($v) && trim($v) !== ''`, wrap the whole thing in an `is_array()` guard so a scalar/`null`/nested
object value degrades to "nothing disabled", and `array_values()` the result.

**Do NOT reach for `try`/`catch` here.** Filter up front. A new `catch` would (a) be the worse design and
(b) feed the derived `AssertionSwallowingCatchTest` census for no reason.

Prove every case with a real test and a real value: `["terse"]`, a non-array string, an int, `null`,
`["", "  "]`, `[0 => 5, 1 => "terse"]`, a nested array entry, and the **key-absent** case (no notice, no
behaviour change).

## HARD CONSTRAINT 2 — exactly one `Bootstrap::chat()` launch, and do not weaken an existing pin

`RulesStateWiringTest`'s class docblock (`:60-67`) records that adding ANY new `chat()`-launching test file
destabilised two load-sensitive forked-completion tests, and the class pays for ONE launch
(`setUpBeforeClass` `:127-149`, seeded pack `focus`, `Bootstrap::chat(self::$repo)` at `:148`). So:

- **Do not create a new `chat()`-launching test file.** Extend `RulesStateWiringTest` instead.
- Recommended route (verify it works, adapt if not): seed a SECOND distinct pack into the same temp HOME in
  `seedRulebook()`, write a `config.json` into that same home carrying
  `{"disabledRules":["<second-pack>"]}`, and add a test asserting at launch, with nothing typed, that
  `$state->disabled()` holds exactly that one name and that its sentinel is absent from the rendered prompt
  while `focus`'s sentinel is still present. That keeps every existing assertion about `focus` byte-identical
  — `testAToggleTypedIntoTheLaunchedShellMovesThePackOutOfThePromptItBuilds` asserts
  `assertSame([self::PACK], $state->disabled())` after one keystroke, so it must still see exactly `focus` —
  and proves the seed through the real boot path rather than by inspection.
- §1.11 applies: an assertion about the source text, `method_exists`, or a comment is NOT a test of the seed.

## Obligations the suite will NOT catch (unpoliced — do them or a reviewer will red you)

1. `docs/SETTINGS.md:158-163` says the table is "COMPLETE — `LayeredSettings::LAYERED_KEYS` is exactly these
   **eleven**". Nothing derives that word (`TrustKeyDocumentationDriftTest:266-299` iterates keys → row and
   row → tier; it never counts rows). Update **eleven → twelve** by hand and add the
   `| \`disabledRules\` | Bootstrap::chat() → \`RulesState::new()\` | **no** |` row in the three-column shape
   `/^\| \`key\` \| [^|]+\| ([^|]+) \|/m` with a "no" in the project column.
2. `docs/SETTINGS.md:516-520` says "only five of the **ten** layered keys have an env override" and then names
   ten keys. **This sentence is already stale** — there are eleven today and it omits `statusLine` from both
   lists. Correct the count to twelve and add `disabledRules` to the no-override list; disclose that you also
   repaired the pre-existing `statusLine` omission rather than quietly "fixing" it.
3. `LayeredSettings.php:188-208` — the reader roster ("Every key a lower layer may contribute, each with the
   reader that makes it real. Nothing is listed here speculatively") needs a `disabledRules` line.
4. `LayeredSettings.php:496-539` — `userTierOnlyKeys()`'s docblock says "Today it is `provider`,
   `instructions`, `allowedTools` and `statusLine`" and claims a test reds when it goes stale. It becomes five.
   Record the tier argument there: a disable-list holds NAMES not contents, so the "file contents become
   prompt text" wording in the plan's hard constraint does NOT decide it on its own. The real drivers are seam
   7's "any new prompt key" wording, `LayeredSettings.php:521-528`, and `RulesState::TOGGLEABLE_TIER = 'user'`
   (`:81`) / "SCOPE OF A TOGGLE" (`:66-73`) — a project must not silence a user pack.
5. **State the `disabledSkills` counter-precedent explicitly**, or a reviewer will ask why `disabledSkills` IS
   project-settable (`PROJECT_TIER_KEYS:490`, rationale at `:296-299`) while `disabledRules` is not. The
   difference is that a rule pack is the operator's own prompt content.

## Corrections to carry-forward claims (measure for yourself; do not repeat these errors)

- **`Rule::$key` is NOT "basename minus `.md`".** `RuleLoader::ruleKeyFor()` (`src/Context/RuleLoader.php:685-691`)
  returns the path **relative to the realpath'd tier directory**, separators normalised to `/`, minus `.md` — so
  nested packs have keys with slashes. `parsePack()` accepts slashes, which is what makes nested packs
  disable-able. Say this correctly in any docblock you write.
- The root tier's `RULES.md` gets the literal key `'RULES.md'` **with** extension (`:449`, bypassing
  `ruleKeyFor`) and is tier `root`, so `TOGGLEABLE_TIER='user'` means a config entry naming it can have no
  effect. State that honestly in the SETTINGS.md row or the docblock.
- `testNoKeyIsLayeredThatNothingReads` (`LayeredSettingsTest:76-88`) is a **named** list of five forbidden keys,
  not a derived consumer scan. Nothing in the suite enforces that `disabledRules` has a reader — your new
  tests are what make that true.
- The layer stack is lowest-first: **user files outrank project files**. Do not invert it.
- `docs/COMMANDS.md` row count is on a stale track by design; no guard catches it, don't chase it.

## Goldens

Both must be **UNMOVED**: system `f09f37366a1925565dcc7725f659ff41` (7,829 B) and agent
`ef0326dd38535aaa2f1d715919bff26e` (1,060 B), at `sugar-crush/tests/fixtures/prompt/`. A golden move here
would be the defect. `BaseSystemPromptTest` builds a `Runtime` directly, not through `Bootstrap::chat()`, and
all new tests use a temp HOME. REGEN LAW (only if a move were legitimate, which it is not here): run
`ensureFixtureRepo` first and re-apply `pinHostLines`.

## Verification you must run and report (serial, from the worktree root, `</dev/null`, `--colors=never`)

- Solo: `LayeredSettingsTest`, `BootstrapLayeredSettingsTest`, `RulesStateWiringTest`,
  `TrustKeyDocumentationDriftTest`, `RulesCommandTest`, `BaseSystemPromptTest`, `TreeWideGuardRosterTest`,
  `ProjectTierRefusalInventoryTest`, `AssertionSwallowingCatchTest`.
- Full suite once at your FINAL tip. **Floor to beat: Tests 10912 / Assertions 167830 / Skipped 2 / EXIT 0**
  (master `e28a99b04`-tip figure). Write your predicted totals DOWN BEFORE running and report the miss if any
  — three consecutive Phase-6 steps missed a hand-predicted headline while the per-class diff reconciled to
  zero; derive the prediction from the final tip's per-class solos, or label the headline
  derived-not-predicted.
- State the deletion experiment you ran (revert the seed → which tests red?) and the revert you performed to
  restore the `LAYERED_KEYS` entry → which guards red?

## Working rules

- `declare(strict_types=1);`, PSR-12, `final`, immutable+fluent where applicable.
- Commit as the literal identity `Joe Huss <detain@interserver.net>` — write that address out, never copy an
  identity token from your injected context (agents have shipped the literal `[EMAIL]` placeholder before).
- Commit messages: write to a file and use `git commit -F`; grep the file for `<` before use (commit subjects
  feed the `<env>` block and tag bytes there redden whole-prompt guards).
- Work ONLY in `/home/sites/prompt-step-P6.S4`. Never `git init` a scratch repo anywhere but your own
  scratchpad subdirectory. Verify `pwd` before any git write. Never `rm -rf` a path you did not create; back
  up files inside a PRIVATE scratchpad subdir named for this step, never at the shared root.
- Never weaken, skip, rename or delete an existing test. Never `git push`. Never run `composer install/update`.
- Never touch `/home/sites/crush-lane-*`, `docs/plans/crush_code_*.md`, or `left_steps.md`.
- Run long suites IN-SESSION and wait on them yourself; do not end your turn "waiting for a notification".
- Report at the end: files changed with +/−, the red-capable tests added, all solo figures, the full-suite
  figure vs prediction, the deletion experiments, and anything you found that contradicts this brief (it was
  written from a static-read premise check — if a line number has moved, say so).
