# P9.S3 — Bash: the git/PR playbook fragment — step brief

Plan: prompt_plan.md `### P9.S3` (re-derive; ~:2892). Batch 2 CONCURRENT; disjoint by construction (S3 = Bash.php + its test ONLY). Base: staffing commit. FLOOR 11,062/169,786 gated `2c6583561`; goldens FROZEN (expected UNMOVED: fragments ride tools-bearing requests; goldenContext has no tools).

**§5 record**: this step rides the LIVE Bash.php collision row (E11/E455/E456 lane). The re-check is DISCHARGED at staffing if `git log -1 --format='%h %ad %s' -- sugar-crush/src/Tools/BuiltIn/Bash.php` shows no lane-side commit since the round-60 quiescence decision (`docs/plans/crush_code_RESUME.md` ~:22-27 'run prompt_plan.md to completion FIRST'), AND `git diff <staffing-parent> -- sugar-crush/src/Tools/BuiltIn/Bash.php` = 0 bytes vs the last prompt-lane merge. Paste both outputs into your report; orchestrator records them in the merge message.

**Goal**: `Bash::promptGuidance()` — the repo's ship-as-you-go cadence as a tool-adjacent playbook, on the P9.S1 seam (`src/Tools/PromptGuidance.php` — `implements Tool` becomes `implements Tool, PromptGuidance` at Bash.php:32, re-verify). The 130x split principle: terse always-on description (UNTOUCHED — R-A) + rich conditional fragment.

**File ceiling**: `src/Tools/BuiltIn/Bash.php`, NEW `tests/Tools/BashPromptGuidanceTest.php` (the plan's 'BashDescriptionTest.php' does not exist — creating the guidance-named file is the recorded deviation), NOTHING else.

**R-A description() bytes FROZEN**: :96 `'Execute a bash command...'` prefix pin (BuiltInToolTest) + stderr-per-branch and bound-figure pins (ToolDescriptionGuidanceTest) untouched — you add a method, change no string.

**R-B seam contract**: non-empty return; no trailing newline; the fragment WRAPS ITSELF in a `<git_commits>...</git_commits>` pair as plain text of its body (renderer adds no fence — P9.S1 R-C). The tag is NOT added to PromptFence::TAGS (roster tests pin 7 — touching them is out of ceiling; precedent: `<prior-summary>` rides unrostered, P8.S3).

**R-C name discipline**: ToolPromptGuidanceTest::testNoFragmentNamesAnotherWiredTool sweeps the LIVE Bootstrap::tools() roster — your fragment becomes part of that sweep automatically. The fragment must NOT name any other wired tool (no 'Read', 'Grep', 'Glob' ... inside the fragment; binaries git/gh/composer and file names AGENTS.md/prompt_worklog.md are fine — it may say 'the dedicated file tools' if a reference is unavoidable). Keep the existing test green — never edit it.

**R-D content (the plan's own list, :2893-2897)**: branch naming `ai/<slug>-<short>` (and `feat/` for humans); title `<lib>: <summary>`; body ends `## Test plan` citing test count; bundle 2-4 items; the cadence chain: commit → push → `unset GITHUB_TOKEN && gh pr create` → `gh pr merge <n> --merge --delete-branch` → `git checkout master && git pull --ff-only`; the author identity — express as 'author the commit as the repo's configured git identity' (NO email literal anywhere: not in the fragment, not in the report, not in command echoes — the harness scrubs addresses); `composer validate --strict` gotcha (sugarcraft @dev constraints make --strict flag everything — drop it). THREE DESIGN MOVES (plan :2904-2908): numbered steps with WHICH ONES BATCH IN PARALLEL annotated; safety rules stated WITH THEIR REASON (the plan's own example: a failed pre-commit hook means the commit did NOT happen, so --amend would modify the PREVIOUS commit); a HEREDOC template for the commit/PR message (the operation is genuinely fragile). NEVER-DO list (plan :2910): never --no-verify; never force-push to master; never git add -A. Keep the whole fragment proportionate — this is a ~30-line method body of prose, not an essay (upstream's 2,469 tokens is the ceiling, not a target).

**Done-when**: test (a) fragment present iff Bash present — build requests with/without Bash via the P9.S1 pattern (App withTools arrays), assert the <git_commits> block appears/disappears and Read/Write fragments are unaffected; (b) never-do list presence: the three never-do phrases assert-present; (c) tag balance: exactly one opener + one closer in the assembled prompt; (d) the P9.S1 tests still pass untouched (ToolPromptGuidanceTest solo 6/59).

**Census (law 8)**: no new src file → no +2; +1 per docblock paragraph you add (fragment method + class-doc sentence if honest); Bash.php Paragraphs delta — compute with DocumentParagraphs before full prediction. New test file enrolls the corpus walkers (+~3t/+~12a expected, measure). Solo ONLY. Report movers + all deletions-experiments: (M1) return '' permanently → done-when (a) red; (M2) name a tool in the fragment → ToolPromptGuidanceTest name-sweep red (this is the §1.12 of the seam — prove the guard bites); (M3) drop the never-do list → (b) red.

**§1.11/§1.10**: zero removals; execute() untouched (live row — do not 'fix' anything you notice while in there; record observations instead). Commits two; protocol as P9.S1's (scratch /tmp/opencode/P9.S3-build/, hygiene scans, no push, no full suite).
