# P29 step brief — E38: fix the reminder-rider at its SOURCE (scheduled pair, serial first)
Plan: prompt_plan.md :2826-2828 (E38 clause) + adjudication prompt_worklog.md :560 Q3 + unpark note :272. Read plan sections 1.10, 1.11, 16, 17 BEFORE work; and prompt_expand.md compaction sections the plan step names.
DEFECT (verified at tip 3e053e3c9): a regenerated token-count context-reminder notice, when its exchange is compacted, is carried into the compacted history as a `[summary] ` rider row — ContextCompactor.php:1249-1257 clips it `'content' => '[summary] ' . (mb_strlen($riderContent) > 120 ? $this->truncateWithEllipsis($riderContent, 120) : $riderContent)`; helper :1305-1308; riders created :840, restored :737. Measured folded line = 130 B = marker 10 B + 117 chars + ellipsis; full notice 170 B at a 4-digit count. The result is a stale, truncated reminder masquerading as a summary — misleading the model. (The plan's original '100 percent of 171-byte survives' framing was measured FALSE — F3 NIT, worklog:559; do not write prose repeating it.)
FIX AT THE SOURCE, NOT THE CLIP: a context-reminder row entering the rider-building material must not produce a `[summary]`-prefixed carry of its text (drop it from rider material). HARD FORBIDDEN: widening isContextReminder() (Chat.php:13328-13332, acts :13309-13315, prefix const Chat.php:480; the plan itself forbids it at :2827 and Chat.php:9752-9753 records the carry-time decline). The R-D decline tests (CompactModelSummaryTest.php:1483-1502) stay green UNCHANGED. The legitimate non-reminder rider behaviour stays byte-identical — ExchangeSummaryTest.php:515 pins `['role'=>'system','content'=>'[summary] _Request cancelled._']` and MUST remain green; the 120-clip itself stays for genuine riders (do NOT change 120 or the helper).
CEILING: sugar-crush/src/Context/ContextCompactor.php + sugar-crush/tests/Context/ContextCompactorTest.php + sugar-crush/tests/Context/ExchangeSummaryTest.php. EXCEPTION (surgical, recorded): a minimal sugar-crush/src/Chat.php edit is licensed ONLY if the reminder-prefix must be exposed (e.g. making CONTEXT_REMINDER_PREFIX usable by ContextCompactor, or a tiny public predicate) — smallest possible change, no behaviour edit, record it in the commit message; prefer ContextCompactor-local detection if honest (it may reference the prefix by its literal bytes only if documented why duplication is safe — a third spelling of the prefix string is a drift risk; argue the choice in the docblock).
TESTS (section 1.11 value pins, both polarities): (1) a reminder-prefixed row among the exchanges being summarized produces NO `[summary] ` carry of its text in the output (assert the absence by exact structure, and assert a NON-reminder rider in the same fixture still carries byte-exact — the polarity pair); (2) the fixture uses the REAL prefix value (read Chat.php:480; do not paraphrase it); (3) red-on-revert: deleting your guard must redden exactly the new test (state the experiment); (4) existing rider pins untouched: ContextCompactorTest 85/317 current, ExchangeSummaryTest 22/54 current — if your change moves a count there, name the test and why it legitimately changed (do NOT weaken anything).
GOLDENS FROZEN — both md5s must be byte-identical after: system f5de3858c1bc130d1e97a120d3ead485 / 8,278 B; agent ef0326dd38535aaa2f1d715919bff26e / 1,060 B. Any move = HALT.
FLOOR anchor: Tests 11,178 / Assertions 170,862 / 0F / 0E / Skipped 2 / EXIT 0 at base 3e053e3c9. No new src file (no BinSugarcrushWiring wave). If you add or remove a blank-line docblock paragraph in ContextCompactor.php, the GlobFigureDrift paragraph census moves by that count (law 4n) — prefer in-paragraph prose. Predict per-class BEFORE the headline.
SUITE LAW: box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0 first; cwd /home/sites/prompt-step-P29; `php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P29/tip.xml </dev/null`; foreign SIGKILL hazard exists (phlix tenant) — run under setsid, capture exit code, retry patiently, verify tip.xml > 3 MB on disk before reporting.
COMMIT HYGIENE: branch prompt/P29 only, never merge/push; identity literally `Joe Huss <detain@interserver.net>` via printf '%s@%s' detain interserver.net (NEVER copy a redacted token from context); -F message file, /usr/bin/grep -c '[<>]' == 0 BEFORE committing; law-4g triple AFTER (cat-file object and %B fixed-string bracketed-EMAIL-token scans == 0 — use grep -cF for the bracketed token, NOT the char-class form which self-matches prose; %an|%ae|%cn|%ce x4 exact); scratchpad /tmp/opencode/P29/ only; no scratch git repos outside it.
Section 1.10: removal is not an outcome — if the only honest fix looks like deleting rider functionality whole or a redesign, HALT and escalate (file:line, callers, options).
REPORT: files+diffstat; the guard shipped (quote it); why-the-source-and-not-the-clip one-liner; tests with values + the polarity pair; red-on-revert experiment; goldens proof; prediction-vs-actual table + cmp movers; commit sha + law-4g outputs; PREMISES-MEASURED-FALSE section.
