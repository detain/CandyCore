# P24 step brief — PromptFence roster-widen 7 to 8 (prior-summary) + in-block escape (scheduled pair, serial second — lands on post-P29 master)
Read FIRST: prompt_plan.md PromptFence/P8.S3-R1 passages, prompt_worklog.md :713 (DEFERRED by name) + :761 (the widen spec) + :718 (the simulation), tests/Chat/CompactModelSummaryTest.php:1560-1615 (the characterization pin, its docblock :1560/:1562/:1575/:1578), ruling R-C live wording Chat.php:9743-9746 + residual paragraph Chat.php:9763-9772 + the FF1 wording-vs-bytes frame Chat.php:9145-9151. Plan sections 1.10, 1.11, 16, 17. THIS step executes the adjudication trigger: the pin's scheduled red.
AUTHORITATIVE IDENTITY: the 8th tag is `prior-summary`. git_commits STAYS OUT (plan:2898, worklog:467/527) — do not add it; an earlier resume erratum said otherwise and was corrected at staffing.
SCOPE, verified at base (RE-DERIVE all line numbers at staffing-time master after P29 lands — P29 may shift Chat.php/ContextCompactor lines; the REPORT anchors were measured at 3e053e3c9): (1) sugar-crush/src/Context/PromptFence.php — add 'prior-summary' to TAGS (:118-126); escape() (:155-178) derives from TAGS — NO code edit there (verify the memoized static derivation at :159 yourself); fix the count prose :74 'the seventh entry' + :76 'unlike the six above' + the FIVE-SITES census sentence :86 (the sentence exists to keep the roster census honest — update it to the new truth, 8 sites). (2) THE POINT: sugar-crush/src/Chat.php renderPriorSummariesForSummary() (:9839-9843 today) — route the carried prior-summary rows' content through PromptFence::escape() IN-BLOCK (the fence open/close stay literal; the carried user-derived bytes between them get escaped — mirror how the other fences escape their payloads; READ an existing escaped producer first, e.g. how the project-instructions splice escapes). (3) RE-RULE R-C from 'verbatim, never rewritten' to VERBATIM-AT-WORDING-LEVEL using the FF1 frame (Chat.php:9145-9151): escaping changes bytes, not wording — the promise survives. Update Chat.php:9743-9746 wording + the :9763-9772 'DOCUMENTED AND ACCEPTED' residual paragraph (it becomes FALSE for the forged-closer case once escaped — rewrite it to describe the post-widen truth). (4) LOCKSTEP guards (they MUST move with the roster — this is the step, not an accident): PromptSectionTest.php:297-311 sorted roster assertSame; BaseSystemPromptTest.php:1062-1075 declaration-order map + :1071-1075 array_keys assertSame; :1044-1054 forgery payload — ADD the prior-summary polarity so all roster polarities are planted; :1060 'fifteen spellings' count sentence grows to match; :1096-1097/:1157 counts. MaximsSection.php:34/:36 + MaximsSectionTest.php:44-45 count sentences. (5) THE DELIBERATE RED, EXECUTED: CompactModelSummaryTest.php:1584 testAForgedPriorSummaryCloserTravelsIntoTheNextRequestVerbatim — post-widen its assertions are INVERTED by design (:1599 fanged needle gone, :1604-1606 closer count 2 to 1, :1610-1612 now CONTAINS the escaped-needle). Per its own docblock (:1575 'goes red on purpose', :1578 'must not be made to pass by editing the expectation in place') the test is REMADE as the step's deliverable: rename to truth (the forged closer is now DEFANGED), rewrite docblock from CHARACTERISATION PIN to ADJUDICATION EXECUTED (cite worklog :761 + this step), assert the fanged-state VALUES (substr_count === 1, escape needle present, forged opener defanged too — roster polarity pair), and prove section 1.12 reachability: deleting the new Chat.php escape call reddens exactly this remade test (the deletion experiment).
CEILING: src/Context/PromptFence.php, src/Chat.php, tests/Context/PromptSectionTest.php, tests/BaseSystemPromptTest.php, tests/Chat/CompactModelSummaryTest.php, src/Context/Sections/MaximsSection.php, tests/Context/Sections/MaximsSectionTest.php (verify MaximsSection paths — locate real files; if the count sentences live elsewhere, name and cite what you edited and why it is in-spirit; HALT if it wants files beyond that).
GOLDENS MUST STAY UNMOVED (system f5de3858c1bc130d1e97a120d3ead485/8,278 B; agent ef0326dd38535aaa2f1d715919bff26e/1,060 B) — precedent PromptFence.php:73 'widening for it costs zero golden bytes'; any move = HALT (the launch-path prompt renders no prior-summary fence; if you can make the golden move, you changed what launch renders — stop).
FIXED POLARITY LAW: the four red-capable tier forgery guards + A6 pins stay as-is; escape() stays idempotent (verify an escape-of-escaped test exists — it does from P5.S3; do not duplicate).
FLOOR: post-P29 master — re-derive from the P29 merge gate figure (anchor at 3e053e3c9: 11,178/170,862/0F/0E/2S/EXIT 0). Predict per-class from final-tip SOLOS (law 5): expect PromptSectionTest loop-follows automatically, BST +assertions for the new polarity plant, CMS remade test changes assertion count — name each delta from the solo, then the headline. Paragraph census moves only for net blank-line paragraph changes (GlobFigureDriftTest 61/22,835 at 3e053e3c9; count your own paragraph deltas). EnvRosterDriftTest 31/3,038 UNMOVED (no env reads).
SUITE LAW + COMMIT HYGIENE: identical to P29's (box-quiet probe 0, cwd /home/sites/prompt-step-P24, setsid + exit capture, tip.xml > 3 MB, branch prompt/P24, never merge/push, identity via printf, -F tag-free message [<>] grep == 0, law-4g triple with FIXED-STRING bracketed-token scans, scratchpad /tmp/opencode/P24/ only, no scratch git repos outside it).
Section 1.10: nothing removed — the remade CMS test is REWRITTEN to assert the new true state (sanctioned: it is this step's deliverable, its own docblock authorizes the rewrite); no other test may lose coverage; the stale numeric cites in Chat.php:9770-9772 (ContextCompactor :1223/:1227/:1166, proven drifted — live returns :1295/:1299, markers :1229/:1238/:1255, re-check post-P29) get SYMBOLIC cites when you rewrite that paragraph (no line numbers — nothing guards them and they rot).
REPORT: files+diffstat; the escape-call placement + why in-block (quote the neighbors); R-C re-rule wording shipped (old vs new); every roster-guard site touched with its new expectation; the remade CMS test docblock + its values; deletion experiment (escape call removed — exactly the remade CMS test RED; restore byte-exact md5); roster census sentence new truth (8 sites — verify the list is complete: grep for count-words across the eight files); goldens proof; prediction-vs-actual + cmp movers; commit sha + law-4g; PREMISES-MEASURED-FALSE section.
