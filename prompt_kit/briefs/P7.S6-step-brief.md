# P7.S6 step brief v1 — Wire ForeignMemoryImporter behind /memory import

Read first: this brief; `prompt_kit/findings/P7.S6-premise.md` v1 (the anchor of record); `prompt_plan.md` :2661-2680 (step text), §1.10, §1.11, §16, §17.

## Where you work

- `/home/sites/prompt-step-P7.S6`, branch `prompt/P7.S6` @ `bfda6c51a`, PORCELAIN-CLEAN (freshly provisioned 2026-09-07). Its `sugar-crush/` tree is byte-identical to `322808eed` (the gated floor). `vendor/` is a real hard-linked dir (`cp -al`, NOT a symlink); `vendor/composer/autoload_psr4.php` verified mapping `$baseDir` to the WORKTREE's own `src` (relative anchors, zero leak to `/home/sites/sugarcraft`); provisioning sanity `vendor/bin/phpunit --filter AgentManagerWiringTest` printed OK (25 tests, 162 assertions).
- Scratchpad: your OWN private subdir (e.g. `/tmp/opencode/P7.S6-builder/`), all backups/experiments inside it; never `rm -rf` anything you did not create; never create a scratch git repo outside it; verify `pwd` before any `git init`/`git commit` (ORCHESTRATION-RULES-2/3).

## R-1 READ + VERIFY EVERY ANCHOR IN THE STEP WORKTREE

Read the premise in full, then verify EVERY `file:line` anchor in the STEP WORKTREE `/home/sites/prompt-step-P7.S6` before it binds an edit. Anchor mismatch = HALT and report at zero edits — three fabrications have already burned this plan (the hawk scout named in the premise header is the fourth; do not become the fifth).

## R-2 MEASURE-FIRST

Determine the scope(s) the importer actually writes (premise §5 UNRESOLVED item — read `ForeignMemoryImporter.php:134-361` line by line), and pin the headroom math to the `list()` of exactly that scope. Do not assume `Project`; measure.

## R-3 DESIGN (orchestrator ruling, binding; a falsifying measurement = HALT + report)

- (a) Add an optional `int $limit = null` parameter to `importClaudeCode` + `importOpencode` — `null` keeps current unbounded behavior so all 26 existing importer-test assertions stay green; when set, stop after writing `$limit` entries and return the actual count.
- (b) New arm `'import' => $this->memoryImport($inputText, $args)` in the `handleMemoryCommand` match at `:10276-10284`; `memoryImport` parses target `claude|opencode` (bare/unknown target => `memoryHelpResponse` with an error).
- (c) Headroom: `$limit = max(0, MemoryBlock::MAX_ENTRIES - count(entries in the matching scope))`; `$limit === 0` => response says store at cap N/12, nothing imported, suggests `/memory list` — import NOT performed, sentinel NOT written.
- (d) SENTINEL per the importer docblock `:31-36` contract: path `{projectRoot}/.sugar-crush/memory/.imported-{target}`; if the file exists => response 'already imported (sentinel ...; delete it to re-import)' and NO import; on `imported > 0` write the sentinel (mkdir -p; create dirs defensively); `projectRoot() === ''` => refuse with an explicit message.
- (e) Response text reports: imported N; refused-directories reasons (pull the `refusedDirectories()` `:92` seam into the light); and when N hit the limit, a hint that entries remain unimported.
- (f) `memoryHelpResponse` gets an import line (`:10316-10321` slot).
- (g) Update ALL premise §6 prose claims that become false: importer `:38-43` block, `CommandRegistry.php:219` description to include import, `docs/MEMORY.md` fence + `:134`, `docs/TROUBLESHOOTING.md:235`, and `README.md:685` if the description change requires it.
- (h) `ProjectTierRefusalInventoryTest`: the import arm does NOT construct anything in `Bootstrap`, so the `Bootstrap.php:709-714` prose must be RECLASSIFIED truthfully (no longer 'nothing constructs it' — Chat does; still nothing drains `refusedDirectories` into the collector, so the named-GAP row `:607` STAYS unless you route the drain through Bootstrap, which is OUT of scope), and the `dormantHolders` entry `:775-779` MOVES to `wiredHolders` per the file's own `:745-749` rule. Run the whole class; reconcile ONLY as the guards' documented logic dictates — never loosen a count/assertion to pass.
- (i) NO new `warnPermissionConfigInTranscript` call sites (census stays 20); NO changes to the AgentDefinition/AgentManager/agentRoster region (`:1540-1800`, P7.S5 territory).

## R-4 TESTS

- NEW `tests/Commands/MemoryImportCommandTest.php` following the `SlashDispatchTest` header idiom (drive `update()`, temp dirs, real `MemoryStore` — construct Chat with memoryStore + projectRoot via existing public seams, WITH the test pattern's shell helper). Cover at minimum:
  1. bare `/memory import` => help/unknown-target response;
  2. claude happy path with EXPLICIT `$claudeHome`-style control if Chat can't inject home — else opencode happy path (`{projectRoot}/.opencode/memory` fixture) as primary;
  3. over-cap import with the limit honored (pre-fill 10 entries, source has 5 files => imported 2, 3 remaining);
  4. store-full refusal (12/12 => imported 0, message, sentinel NOT written);
  5. sentinel blocks re-import;
  6. sentinel written after success;
  7. refusal surfacing (symlink-escape fixture => response contains the refusal);
  8. help text now lists import;
  9. MemoryBlock render-cap interplay (imported entries beyond 12 omitted — assert the ACTUAL measured behavior and pin it honestly per plan §1.11).
- Keep all 10 `ContainmentTest` + 16 `ImporterTest` tests green UNTOUCHED.

## R-5 IDENTITY / PROCESS

- ONE branch `prompt/P7.S6` (fresh fix commits allowed, no history rewrite).
- The floor never weakens; prediction-first; box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` must be 0 before every suite run; commit-before-final-suite law applies.
- Identity via `git -c user.name="Joe Huss" -c user.email=$(printf '%s@%s' detain interserver.net) commit -F msgfile`; NEVER write the email literal into any file; no angle brackets or backticks in commit messages; NEVER push.
- The worktree is the source of truth — never edit `/home/sites/sugarcraft` from this seat.
