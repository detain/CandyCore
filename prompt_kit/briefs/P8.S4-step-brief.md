# P8.S4 — Head/tail split and tool-output truncation — STEP BRIEF

Plan: prompt_plan.md `### P8.S4` (heading ~:2784). Goal (:2786-2787): "Only the head is
summarised; the recent window stays verbatim. Bound tool output in the serialised head (upstream
uses 2,000 chars). Never prune skill outputs." Source (:2788): §6.6, §9.3, §2.10. Done-when
(:2796-2798): two fixtures, one 10x longer in the head; assert the serialised head sent to the
summariser does NOT grow proportionally; both numbers go in the worklog. Depends on P8.S3 (DONE).

## PREMISE (verified against master `d8f5c137c`; the +8 anchors from prior steps do NOT apply — these
## were re-derived by direct read this session):
- The head/tail SPLIT and tail-verbatim are ALREADY SHIPPED. `stagePairs()` splits by PAIR COUNT:
  tail-exclusion at ContextCompactor.php:568-575 (`count($pairs) <= $preserveCount` -> return null
  :569-571; `array_slice($pairs,-$preserveCount)` :574; head = `array_slice($pairs,0,count-$preserve)`
  :575); `$preserveCount = config->recentPreserveCount` :558 (default 10, CompactorConfig.php:53).
  `exchangesToSummarize()` :617-642 iterates ONLY the head pairs (:625). Do NOT re-implement a split.
- THE REAL WORK: there is NO bound on what the summariser is SHOWN. `Chat::buildSummarizationRequest`
  (:9207) calls `exchangesToSummarize` (:9218) then `renderExchangesForSummary` (:9226), which at
  Chat.php:9450 interpolates `### Exchange N\nUser: {full}\nAssistant: {full}` — VERBATIM, unbounded.
  The existing 80/100 caps (`generateExchangeSummary` :1216/:1222) bind the HEURISTIC TRANSCRIPT fold,
  NOT the model-facing head. So a head exchange carrying a large tool blob reaches the model whole.
- TOOL BYTES ARE THE ASSISTANT ROLE. `Message::toWire()` (Message.php:365-381) emits only
  role/content/attachments/tool_calls — NEVER `tool_results`. Finished tool output enters history as
  `Message::assistant($result->result)->withToolResults([...])` (Chat.php:2898-2899), so at the wire it
  is ordinary assistant content. `removeToolResults()` (:947-956, predicate role==system && isset
  tool_results) is therefore effectively INERT on production wire (only SessionStore.php:243 emits that
  key) — it is DORMANT; do NOT delete it or its 5 tests (ContextCompactorTest.php:597/611/640/653/660).
- SKILL MARKER (the never-prune discriminator): a skill body is assistant content whose string begins
  `## Skill: ` — emitted at Tools/BuiltIn/SkillTool.php:102 (`"## Skill: {$skill->name}\n\n{$body}"`,
  VERIFIED by direct read this session; builder MUST re-verify at implementation time). The dormant
  `role==='skill'` machinery (`compactSkills` :448 / `filterSkills` :1049, ZERO production callers, the
  role never reaches the head) is NOT the guard path — do NOT use it and do NOT delete it.

## RULINGS (FIXED — decide nothing outside these; halt+report if one is impossible):
- R-A: the bound applies ONLY to the `assistant` half of each head exchange returned by
  `exchangesToSummarize()` (ContextCompactor.php:634-638), clamped to a new config value
  `toolOutputMaxChars` (default 2000). The `user` half is left untouched. Rationale: tool output is
  assistant content; user turns are short; §6.6 `serialize()` truncates `[Tool result]:` lines, not the
  user question. (Long user pastes unbounded here = a recorded, deferred note, not a defect.)
- R-B (KEY INTEGRITY — CRITICAL): the returned `key` MUST remain `exchangeKey($user,$assistantRaw)`
  computed from the UNCLIPPED text, so the model-summary round-trip join (withExchangeSummaries +
  applyModelCompaction, which recompute exchangeKey from the ORIGINAL pair) stays unbroken. Clip ONLY
  the 'assistant' VALUE, never feed clipped text to exchangeKey, never touch the transcript.
- R-C (SKILL GUARD): a head `assistant` string beginning with the `## Skill: ` marker is EXEMPT —
  emitted byte-for-byte, never clipped. Pin this with a test using a skill body > toolOutputMaxChars.
- R-D (CONFIG): add `public int $toolOutputMaxChars = 2000` to CompactorConfig after
  `summaryAssistantMaxChars` (:57), a docblock @param entry, and `withToolOutputMaxChars()`. MUST add
  the new named arg to the `new self(...)` of ALL EIGHT existing with*() methods (:73-82,88-99,105-116,
  :122-133,139-150,156-167,170-181,184-195) — omitting it silently resets the bound to 2000 on any
  wither call; add a test proving the bound SURVIVES each unrelated wither.
- R-E (DOCS — three-2000s): a docblock note must state that `toolOutputMaxChars=2000` (input-side head
  bound) is UNRELATED to `Chat::SUMMARY_LINE_MAX_CHARS=2000` (output-side record clip) and
  `ContextCompactor::INTRA_EXCHANGE_HEADROOM_TOKENS=2000` (token reserve, 95% rescue path) — three
  numerically equal, semantically distinct 2000s — so the next audit cannot merge them.
- R-F (SYMBOL SAFETY): any `{@see}`/backticked symbol in new docblocks MUST resolve or be a plain-text
  word — `SymbolCitationDriftTest` walks src/**.php + tests/**.php + docs/**.md and reddens on an
  unresolvable citation. The new config property and method names are real, so `{@see
  CompactorConfig::$toolOutputMaxChars}` and `{@see ContextCompactor::exchangesToSummarize()}` resolve;
  do NOT invent method names.
- R-G (CLIP SHAPE): when a non-exempt head assistant string exceeds toolOutputMaxChars, clip to
  toolOutputMaxChars and append a truncation marker. Mirror the existing house idiom (`truncateWithEllipsis`
  :1233 / `[… N characters truncated …]` from truncateMessageHead :372-373). Keep it one deterministic
  helper; byte-assert the marker in a test so a wording change is caught.

## DONE-WHEN (measurement — the load-bearing proof):
Add to ContextCompactorTest.php two fixtures built through `exchangesToSummarize()` DIRECTLY (public,
in-file, no Chat needed): identical head pair count + identical tail; fixture A head assistants ~200
chars, fixture B the same pairs with head assistants ~2000*10 chars (tool-output-shaped). Assert the
summed head bytes do NOT grow ~10x (bound bites). ALSO assert the fixture discriminates: with
toolOutputMaxChars raised huge, B DOES grow ~10x over A (so "no growth" is not an accident of the
fixture). The PLAN wants the SERIALISED head — renderExchangesForSummary just wraps these strings in
`### Exchange N\nUser: {u}\nAssistant: {a}` with constant per-pair labels, so bytes(serialize) =
sum(assistant)+sum(user)+CONST*pairCount; document this in the test comment. Report BOTH numbers in
your final message so the orchestrator puts them in the worklog. Keep tests in ContextCompactorTest +
CompactorConfigTest (the declared files).

## IN-SCOPE FILES (hard ceiling — do NOT touch others):
- sugar-crush/src/Context/ContextCompactor.php
- sugar-crush/src/Context/CompactorConfig.php
- sugar-crush/tests/Context/ContextCompactorTest.php
- sugar-crush/tests/Context/CompactorConfigTest.php
You may READ (not edit) Chat.php/Message.php/SkillTool.php for grounding. If a correct fix needs any
OTHER file, HALT and report the need with evidence — do not silently widen.

## INVARIANTS / HAZARDS:
- Goldens FROZEN: system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 + agent
  `ef0326dd38535aaa2f1d715919bff26e`/1,060 — UNMOVED (head rides summaryBackend, never the golden
  path; but re-verify at the end).
- FLOOR: 11,035 tests / 169,367 assertions / 0F / 0E / 2 Skipped / EXIT 0 (P8.S3). Your new tests can
  only ADD. Predict the per-class deltas BEFORE the full run (law 4h: full suite only at a committed
  clean tree).
- Law 4n: each new docblock/comment PARAGRAPH in ContextCompactor.php OR CompactorConfig.php adds +1
  assertion to GlobFigureDriftTest (it walks every src/**.php; stalePattern→word(8)→assertArrayHasKey
  once per paragraph). "2,000" spelled this way does NOT trip the stale-figure trigger (which needs
  eight/8 + char(s)/byte(s)). Predict the GlobFigureDrift delta from your paragraph count.
- §1.10: NEVER delete/remove a dormant seam (removeToolResults, compactSkills/filterSkills, the skill
  budgets). Additive + documenting only.
- §1.11: prove each new pin is load-bearing — a deletion experiment per new assertion-cluster, in
  /tmp/opencode/P8.S4-build/ only.
- Do NOT change COMPACT_SUMMARY_PROMPT bytes; do NOT touch Chat.php.
- Worktree only, NEVER git init outside /tmp/opencode/P8.S4-* scratch dirs (ORCHESTRATION-RULE-2).
- Suite: cwd = worktree ROOT, serial, </dev/null, --colors=never, box-quiet probe
  `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0 before you trust any count.

## COMMIT PROTOCOL:
- Branch prompt/P8.S4 (already provisioned off master by the staffing commit's worktree). Commit src
  and tests as you go; author via `git config` interpolation. Every commit: `git cat-file commit HEAD |
  /usr/bin/grep -c '\[EMAIL\]'` == 0, `git log -1 --format='%B' | /usr/bin/grep -c '<'` == 0, identity
  == Joe Huss|<config-email> x4. Commit messages via -F in your own /tmp/opencode/P8.S4-build/.
- Report: branch tips + what each carries, prediction-vs-actual full suite, per-class movers, goldens,
  deletion experiments, the TWO done-when numbers, any scope tension you hit.
