# P10.S1 step brief — systemBlocks on CompleteRequest

## Mission
Land a structured `systemBlocks` array alongside the flat `systemPrompt` string on
`CompleteRequest`, so an Anthropic-shaped provider can express a block array (and place
`cache_control` per block later in the phase). Read the step text in
`/home/sites/sugarcraft/prompt_plan.md` at `### P10.S1` (line 3011) BEFORE anything else, then
read `prompt_plan.md` sections 1.10, 1.11, 16, 17 IN FULL - they bind you absolutely.
Source sections in `/home/sites/sugarcraft/prompt_expand.md`: 3.3, 4.15, 9.5, and 10 seam 4.

## Premise corrections (verified by the orchestrator 2026-09-08, direct reads)
- The plan cites `VertexProvider::anthropicSystem()` returning `?string`. That method was
  RENAMED: the live symbol is `sugar-crush/src/Providers/VertexProvider.php:593`
  `private function systemInstruction(CompleteRequest $request): ?string` - its own docblock
  (:582-584) records the rename. Work against the live name; do not reintroduce the old one.
  Both envelopes (Anthropic top-level `system` and Google `instances[0].context`) share this ONE
  joiner, so whatever block shape you add must keep BOTH arms transmitting identically when
  `systemBlocks` is absent.
- `CompleteRequest` is `final readonly` with ALL defaults trailing
  (`sugar-crush/src/Providers/CompleteRequest.php:54-97`; last param `reasoningEffort` :96).
  Add `systemBlocks` as a NEW TRAILING promoted param with a null default so every existing
  construction site compiles untouched: Runtime.php:1108, Workflows/WorkflowEngine.php :1057
  :1168 :1271 :1314 :1416, App/App.php:604, Agents/AgentWorkerPool.php:405, and every test.
- The section seam is pre-designed for this: `Runtime::systemPromptSections()` (Runtime.php:2511)
  returns `list<PromptSection>`; `PromptSection::stability()` and `byteBudget()` docblocks say
  they exist for "the cache-breakpoint ... steps to consume"
  (sugar-crush/src/Context/PromptSection.php:32-35). Build the blocks FROM the section list - do
  not re-derive them separately.
- `BedrockProvider` already has a PRIVATE method named `systemBlocks()` (:338) building its
  blocks array from the flat string. Your field is on `CompleteRequest`; no symbol collision, but
  do not rename or touch Bedrock (outside ceiling).

## File ceiling (declared - edits outside it are a defect)
- `sugar-crush/src/Providers/CompleteRequest.php`
- `sugar-crush/src/Providers/VertexProvider.php`
- `sugar-crush/src/Runtime.php`
- `sugar-crush/tests/Providers/ProviderRequestResponseTest.php`
New test pins go INSIDE ProviderRequestResponseTest.php. You may NOT add files. You may NOT edit
`tests/Providers/SystemPromptTransmissionMatrixTest.php` or any other test file.

## Hard constraints
1. The flat `systemPrompt` stays exactly as today. Every provider that reads it today keeps
   working unchanged. `systemBlocks` is purely additive; a provider that ignores it must still
   transmit (the Done-when gate: `SystemPromptTransmissionMatrixTest` passes UNMODIFIED).
2. NEITHER golden may move. System golden must stay md5
   f5de3858c1bc130d1e97a120d3ead485 / 8,278 B and agent golden
   ef0326dd38535aaa2f1d715919bff26e / 1,060 B. The assembled prompt string gains ZERO bytes.
   Verify by md5sum + wc -c on both fixture files after your change, and by running
   `tests/Providers/SystemPromptTransmissionMatrixTest.php`, `tests/BaseSystemPromptTest.php`
   and `tests/PromptStabilityTest.php` green.
3. The blocks must be byte-faithful: the ordered concatenation of the block texts (under ONE
   stated join rule) must reproduce `buildSystemPrompt()`'s flat string EXACTLY. Pin that with a
   real test (both polarities: with tools registered so the tool-guidance layer exists, and
   without). Note sections' render() may carry a leading separator - choose the block boundary
   rule that makes the reconstruction test pass and document it in the field docblock.
4. Vertex: when `systemBlocks` is present, the Anthropic-arm `system` field is the per-block
   array (each `['type' => 'text', 'text' => ...]`); the Google `context` arm keeps taking the
   joined string. Absent/null `systemBlocks` = byte-identical behavior to today (pin both ways).
5. Do not wire `cache_control` anywhere in this step - that is P10.S2/S3's machinery and a later
   consumer. This step carries the STRUCTURE only. A reviewer finding cache_control here is a
   scope violation.

## Process laws you must apply (plan 4-series, condensed)
- 4n/GlobFigureDrift: each new docblock paragraph in a touched src file grows
  `tests/GlobFigureDriftTest` assertion counts; current census 61 tests / 22,648 asserts.
  Predict, or reconcile by cmp.py after the run.
- BaseSystemPromptTest is currently 24/326. It must NOT change (you touch Runtime.php but move no
  prompt bytes).
- Prediction-before-run: write your expected Tests/Assertions totals BEFORE the full-suite run.
- 4g identity: commit as the LITERAL `Joe Huss <detain@interserver.net>` (write the address by
  shell interpolation - your context may show a redacted placeholder; NEVER copy a placeholder).
- ORCHESTRATION-RULE-2: never git init/commit a scratch repo outside your own scratchpad subdir.
- ORCHESTRATION-RULE-3: your private scratchpad is /tmp/opencode/P10.S1/ - all junit artifacts,
  backups, sandboxes go there.
- Commit messages: TAG-FREE (no < or > characters - the env block renders subjects). Write the
  message to a file and use `git commit -F`.
- Section 1.11: no annotation/existence-check tests. Every new test asserts VALUES, goes red
  when your production change is reverted (state the deletion experiment and its result).
- Section 1.10: removal is not an outcome. If you find yourself wanting to delete/refactor-away
  anything unfinished or unreachable, HALT and report instead.

## Run the suite (mandatory, in this worktree)
cwd = /home/sites/prompt-step-P10.S1 (worktree ROOT), serial, `</dev/null`, `--colors=never`:
`php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P10.S1/tip.xml`
Box-quiet probe first: `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` must print 0.

## Report format (return ALL of it)
Files changed + diffstat; the block-shape decision (one paragraph, quote the join rule); every new
test named with what value it asserts; deletion experiments run (command + red/green); full-suite
headline (predicted vs actual, Tests/Assertions/F/E/Skipped/EXIT); md5+size of both goldens after
the change; matrix/BaseSystemPrompt/PromptStability solo results; any premise that proved FALSE
(the plan's anthropicSystem cite is ALREADY KNOWN-false - confirm the live symbol and move on);
commits made (shas, `git log -1 --format='%an|%ae'` each).
