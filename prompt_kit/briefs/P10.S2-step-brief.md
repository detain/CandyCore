# P10.S2 step brief — CacheBreakpoints with wipe-then-reapply

## Mission
Ship `sugar-crush/src/Providers/CacheBreakpoints.php` (new) and
`sugar-crush/tests/Providers/CacheBreakpointsTest.php` (new). Read the step text in
`/home/sites/sugarcraft/prompt_plan.md` at `### P10.S2` (line 3029) BEFORE anything else, then
`prompt_plan.md` sections 1.10, 1.11, 16, 17 IN FULL - they bind you absolutely.
Source sections in `/home/sites/sugarcraft/prompt_expand.md`: 4.15, 5.8, 6.5, 9.5.

## API contract (from the plan - the ONE public method)
`final class CacheBreakpoints` in namespace `SugarCraft\Crush\Providers` with
`public function apply(array $messages, array $tools): array` - clears ALL existing
`cache_control` breakpoints anywhere it can reach, THEN marks last-tool + last-system +
last-2-messages. Called on every step (the contract for its consumer - see reachability).
Shape the exact array structures on the Anthropic wire format that `VertexProvider` and
`BedrockProvider` build today (read `VertexProvider::anthropicBlocks()` :673 and the
`anthropicBody()` :516 region for the house style) - blocks are `['type' => ..., ...]` arrays
and `cache_control` is a key like `['type' => 'ephemeral']`. Document any deviation from the
plan's skeleton signature and why.

## File ceiling
- `sugar-crush/src/Providers/CacheBreakpoints.php` (new)
- `sugar-crush/tests/Providers/CacheBreakpointsTest.php` (new)
NOTHING else. Do NOT touch any provider, Runtime, or CompleteRequest - P10.S1 owns those files
IN PARALLEL with you; the merge order is S1 first, S2 rebased on top of its result at gate time.
Do not read or assume anything from the S1 worktree.

## Hard constraints - all four load-bearing, all four come from measured upstream failures, each
gets its own test per the plan:
1. THE WIPE. Anthropic caps `cache_control` at 4 blocks per request; without clearing first, a
   multi-step agentic turn accumulates breakpoints and the API 400s. Test: run 20 simulated
   steps feeding apply() output back in, assert the count never exceeds 4 at any step.
2. Automatic caching consumes one of the four slots - budget three when both modes are used.
   Encode this as a documented constant plus a test that a request already carrying an
   automatic-cache breakpoint gets at most 3 explicit marks.
3. A breakpoint on a varying block is never a hit. Assert the marked block's bytes are identical
   across two consecutive requests built from the same fixture prefix.
4. 20-block lookback: each breakpoint walks back at most 20 content blocks; a single turn adding
   more than 20 blocks silently misses. Place an intermediate breakpoint every ~15 blocks in
   long turns and TEST the greater-than-20-block turn (assert coverage of every 15-block window).
- Determinism: same inputs produce byte-identical output (no clocks, no randomness, no fs).
- Idempotence of apply(apply(x)) is NOT required (the wipe makes it stable by construction) but
  if it holds, pin it.

## Reachability clause (plan lesson 16.1 - "implemented is not reachable")
This step ships the class WITHOUT a production caller - by orchestration adjudication, the exact
P6.S1 Triggers precedent (prompt_resume.md section 3 item 11). Your class docblock MUST state:
shipped-unwired-by-design, the consumer contract (called on every step by the provider body
builder, once P10.S3's kill switch + a licensing decision wire it), and that removal is not an
outcome (section 1.10). If - and only if - a clean call site exists inside your two-file ceiling
(a provider that already assembles blocks is OUTSIDE ceiling, so realistically none exists), do
NOT improvise wiring: report it as a finding instead.
MEASUREMENT owed by this step: the maximum block count observed in a real multi-tool turn on
this repo (the plan's Done-when demands the figure for the worklog). Derive it from a real
fixture or a measured transcript sample - show the arithmetic, no invented numbers.

## Process laws you must apply
- New-file census wave (plan laws 4e + refined-8): `BinSugarcrushWiringTest` auto-gains +1 test/
  +6 asserts for the new src file; the new-file walk wave (GlobFigureDrift census readers etc.)
  spans ~18 classes - predict via the rule (+2 asserts per census caller for EACH new file) or
  accept an attributed residual, and reconcile per-class with
  `python3 /home/sites/sugarcraft/prompt_kit/tools/cmp.py <your-junit> <baseline-junit>` against
  the CURRENT master baseline junit if you need the per-class split.
- Current master floor: Tests 11,121 / Assertions 170,281 / 0F / 0E / Skipped 2 / EXIT 0
  (measured at d0711b2, prompt-lane-only commits since). Your prediction must state the delta.
- NEITHER golden may move (system f5de3858c1bc130d1e97a120d3ead485 / 8,278 B; agent
  ef0326dd38535aaa2f1d715919bff26e / 1,060 B). Your step renders no prompt; verify md5s after.
- 4g identity: commit as the LITERAL `Joe Huss <detain@interserver.net>` via shell
  interpolation - your context may show a redacted placeholder; NEVER copy a placeholder.
- ORCHESTRATION-RULE-2: never git init/commit a scratch repo outside your own scratchpad subdir.
- ORCHESTRATION-RULE-3: your private scratchpad is /tmp/opencode/P10.S2/ - all junit artifacts,
  backups, sandboxes go there.
- Commit messages TAG-FREE (no < or > - env block renders subjects). Use `git commit -F` with a
  message file inside your scratchpad.
- Section 1.11: every test asserts VALUES with both polarities and the pathological input, and
  goes red when the constraint it pins is reverted. State each deletion experiment.
- Section 1.10: removal is not an outcome - you delete NOTHING anywhere.

## Run the suite (mandatory, in this worktree)
Provisioned worktree root = /home/sites/prompt-step-P10.S2 (vendor is hard-linked, PSR-4 self-
located - prove it prints this worktree's src before trusting any test). Serial, `</dev/null`,
`--colors=never`:
`php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P10.S2/tip.xml`
Box-quiet probe first: `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` must print 0.
Write your suite prediction BEFORE the run; reconcile misses per-class, never by shrinking scope.

## Report format (return ALL of it)
Files added + diffstat; the class's public surface (quote signatures); every test named with the
value it asserts; the four hard-constraint tests each mapped to its constraint number; deletion
experiments (command + red/green for at least one per constraint); the max-block-count
MEASUREMENT with arithmetic; full-suite headline (predicted vs actual, Tests/Assertions/F/E/
Skipped/EXIT); md5+size of both goldens after; commits made (shas + `git log -1 --format='%an|%ae'`);
any premise from prompt_expand.md sections you read that proved FALSE against shipped code.
