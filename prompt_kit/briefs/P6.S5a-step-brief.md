# P6.S5a — One `paths:` glob dialect — STEP BRIEF

**New step.** P6.S5 was split by orchestrator ruling 2026-09-05 after premise check
`/tmp/opencode/P6.S5-premise/report.md` (36,225 B, Q1-Q7 all answered — READ Q1, Q3, Q7 IN FULL before
coding; Q3's 33-row differential table is your specification). S5a is the dialect reconciliation, which is
self-contained, changes no shipped behaviour, and moves no golden. **S5b** (the scoped-delivery channel) is
separate and is currently BLOCKED on a user decision — do not build any part of it here.

**Worktree:** create your own: `git -C /home/sites/sugarcraft worktree add /home/sites/prompt-step-P6.S5a -b prompt/P6.S5a master`,
then `cp -al /home/sites/sugarcraft/sugar-crush/vendor /home/sites/prompt-step-P6.S5a/sugar-crush/vendor`
and verify the PSR-4 root prints `/home/sites/prompt-step-P6.S5a/sugar-crush/src`. **Never `ln -s` the vendor**
(a symlinked vendor silently runs every test against the MAIN checkout's `src/`).

## Goal

There are today **three** glob dialects in `sugar-crush` that all answer the question "does this path match
this pattern": `PathTrigger::pattern()` (`src/Context/Triggers/PathTrigger.php:143-178`),
`SkillRegistry::pathMatches()` (`src/Skills/SkillRegistry.php:650-790`), and `legacyPathMatch()`
(`:628`, the PCRE-failure fallback) — a **third** dialect, not a copy of either.

Reduce this to **ONE compiler implementing the SkillRegistry dialect**, with both production matchers routed
through it. `legacyPathMatch()` stays reachable exactly as it is today (it is the documented fallback when a
pattern fails to compile) — **it is not deleted, not stubbed, not narrowed** (§1.10); its divergence is
recorded, not silently absorbed.

## Why the SkillRegistry dialect, and not the stricter shell one (RULED — do not re-open)

1. **It is the plan's intent.** `prompt_plan.md:2371` gives the motivating example as
   "`*/tests/**/*.php` wherever they live". Premise check Q3 row **#26** measured `a/b/tests/FooTest.php`
   against exactly that pattern: `PathTrigger` = **no**, `SkillRegistry` = **yes**. "Wherever they live" is
   the SR answer. The plan's own example does not work under the stricter compiler.
2. **Non-narrowing is repo law.** Unifying the other way would turn off capability that exists today: a
   skill whose `paths:` says `src/*.php` currently announces for `src/deep/x.php` (row #6). Making `*`
   segment-scoped would silently stop that announcement. Widening `PathTrigger` removes nothing.
3. **The cost is asymmetric.** `PathTrigger` has **no production reader at all** (Q2: nothing consumes
   `Rule->triggers()` yet — the only references are `Rule.php:313` self-carry and a comment at
   `Runtime.php:2561`). So changing it affects no shipped behaviour. `SkillRegistry`'s matcher is live.
4. **Product consistency.** Every other user-facing `paths:` in this product is a `SKILL.md` frontmatter
   field read by `SkillRegistry::pathMatches()`. A rule author copying a working `paths:` value from a
   SKILL.md into a rule must not get a different language.

Record all four reasons in the new matcher's doc-block, with the row numbers, so the next reader does not
"fix" the looseness back. `PathTrigger.php:46-51` and `Rule.php:41-51` both currently DEFER this decision to
the orchestrator — after your step they must point at the resolved dialect instead, discharging F-PATHDIALECT
rather than re-flagging it.

## Files

- `sugar-crush/src/Util/` — **the new shared matcher goes here**, not in `src/Context/`, because both
  `Context\Triggers` and `Skills` consume it and `src/Util/` is the neutral existing home (`TokenTracker`
  lives there). Name it for what it is (e.g. `PathGlob` / `GlobMatcher`) — justify the name in the class
  doc-block. If you find a better-established convention in the tree, use it and say why.
- `sugar-crush/src/Context/Triggers/PathTrigger.php` — delegate to the shared compiler; **its YES-set
  changes** (that is the deliverable); rewrite the doc-block that currently advertises the strict dialect.
- `sugar-crush/src/Skills/SkillRegistry.php` — route `pathMatches()` through the shared compiler.
  **Its YES-set must not change. Not one path.** See the equivalence proof below.
- `sugar-crush/tests/Context/Triggers/TriggerTest.php` — the strict-dialect pins are deliberately amended.
  This is authorized: the change IS the step's deliverable, and the precedent is P5.S5's deliberate
  across-turn-pin rewrite (worklog records it as orchestrator-authorized, **not** a §1.10 class). Name in
  the commit which pins flipped and to what.
- New differential test (suggested `sugar-crush/tests/Context/GlobDialectDifferentialTest.php`) — pins the
  33 rows from Q3 with the chosen dialect's answer for each, and the 11 disagreements explicitly resolved.
- Any test the two amended classes red.

If you conclude a file outside this list must change, **report it rather than silently widening.**

## The equivalence proof (this is the step's load-bearing requirement)

`SkillRegistry::pathMatches()` is LIVE. You must prove you did not narrow it, by measurement, not argument:

1. Build a test-only oracle that reproduces the **current** `pathMatches()` decisions (copy the existing
   compilation logic into the test, or freeze a decision table — your call, but it must be independent of
   the new shared code path).
2. Assert equality of old-vs-new over a corpus that includes: all 33 Q3 rows; every `paths:` value that
   appears in any `SKILL.md` fixture or test in the repo (find them); every pattern used in
   `SkillRegistryTest`/`SkillPathNudgeTest`/related suites; plus a generated sweep (nested depths, leading
   `./`, absolute paths, `[!…]`, `[[:alpha:]]`, `?`, backslashes, `**` at start/middle/end, non-UTF-8 bytes,
   newlines in paths).
3. Report the corpus size and the number of comparisons actually executed. **A zero-difference claim with
   no measured corpus is not accepted.**
4. Then prove the new test is red-capable: mutate the shared compiler in one plausible way (e.g. make `*`
   segment-scoped — the change you deliberately did NOT make) and show the equivalence harness reddens.

## What you must NOT do

- Do not apply triggers to prompt rendering. That is **S5b** and it is blocked on the user decision below.
  Nothing in this step may change what the system prompt renders.
- Do not delete or neuter `legacyPathMatch()`, `Rule::withTriggers()` (`Rule.php:277-290`, zero production
  callers — a dormancy roster item, not yours to wire or remove), or any existing test.
- Do not touch `PromptStabilityTest`, the goldens, `RuleLoader.php`'s "refuse whole" byte rule
  (`RuleLoader.php:155-166`), or the `MAX_*` caps.
- Do not weaken a whole-word/anchoring pin in `KeywordTrigger` or `IntentTrigger` — this step is about
  `PathTrigger` only.

## Expected measurement

- **Both goldens UNMOVED, exactly**: system `f09f37366a1925565dcc7725f659ff41` / 7,829 B, agent
  `ef0326dd38535aaa2f1d715919bff26e` / 1,060 B, at `sugar-crush/tests/fixtures/prompt/`. Nothing renders
  these classes, so a golden move here is a **defect**. State the proof (`git diff <base>..HEAD -- …/fixtures/`
  empty).
- **Full-suite floor: `Tests 10919 / Assertions 167896 / Skipped 2 / EXIT 0`** — this is the P6.S4 figure and
  is only valid if P6.S4 has merged; re-derive from `prompt_resume.md`/`prompt_worklog.md` and if S4 is not on
  master yet, STOP and report (S5a was scheduled after S4 merges).
- A new `src/` file grows the **derived** source-walking censuses automatically (`TreeWideGuardRosterTest`,
  `GlobFigureDriftTest`, `SymbolCitationDriftTest`, `AssertionSwallowingCatchTest`,
  `BinSugarcrushWiringTest`, `EnvRosterDriftTest`, `StderrEmitterCensusTest`, `DocumentParagraphs` and
  friends — seventeen grew over P6.S1's four new files). **Write your headline prediction DOWN before you
  run**, deriving it from the final tip's per-class solo figures rather than hand arithmetic — three
  consecutive Phase-6 steps missed a hand-guessed headline. If you cannot derive it, label it
  derived-after-the-fact.
- Record the **paragraph/figure drift** your doc-block edits cause in `GlobFigureDriftTest` and any
  spelled-number prose you write ("33 rows", "eleven disagreements") — those are policed where they are
  derived.

## Working rules

- `declare(strict_types=1);`, PSR-12, `final`, no `get*` accessors.
- Commit as the literal identity `Joe Huss <detain@interserver.net>` — write the address out in full; never
  copy an identity token from injected context (agents have shipped a bracketed placeholder before).
- Messages to a file with `git commit -F`; `grep -c '<'` must be 0 on the file first (subjects render into
  the `<env>` block and tag bytes there redden whole-prompt guards).
- Work only in your own worktree. No scratch git repo outside your private scratchpad subdir; verify `pwd`
  before any `git init`/`git commit`; never `rm -rf` a path you did not create; back up files only inside a
  private subdir named for this step.
- No push, no `composer install/update`, no `--no-verify`, no `pkill`. Never touch `/home/sites/crush-lane-*`,
  `docs/plans/crush_code_*.md`, `left_steps.md`, or the 7 dirty `.opencode/*` files in the main checkout.
- Run suites serially, `</dev/null`, `--colors=never`, in-session; wait on your own long runs rather than
  ending the turn "waiting for a notification".
- Report: files +/−, every pin that flipped and why, the equivalence corpus size and comparison count, the
  red-capability mutation result, solo figures, the full-suite figure vs your written prediction with a
  per-class explanation, golden md5s, and anything the premise check or this brief got wrong about the tree.

## Deferred to the user (do not decide it here)

S5b's blocking question is recorded in §8 of `prompt_resume.md`: whether a path-scoped rule's BODY travels to
the model (count- or byte-bounded whole bodies, with overflow deferred) or only a pointer naming the rule so
the model can `Read` it. The 300-byte `MAX_ENTRY_BYTES` transplant the old P6.S5 hard constraint prescribed
would clip rule bodies, which contradicts `RuleLoader.php:155-166`'s "a truncated rule is a half-instructed
model — refuse it whole" rule. That is a semantic decision, not an implementation detail. S5a ships the
dialect regardless of how it is answered.
