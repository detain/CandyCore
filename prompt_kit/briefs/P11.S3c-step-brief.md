# P11.S3c step brief — `sugar-crush/docs/MEMORY.md` (memory-in-the-prompt + `/memory import` audit)

- Plan step: `prompt_plan.md` Phase 11, `### P11.S3 — Update the affected feature docs` (:3145 at `322a970fa`; recorded :3141 in the scout pre-resume numbering).
- Binding premise map: `prompt_kit/findings/P11-premise-scout.md` — its Q2 guard-tripwire table and Q6 doc-contradiction list are incorporated as section 4 below.
- §5 re-check: **CLEAR** — record at `prompt_kit/findings/P11-batch1-recheck.md`.
- Worktree: `/home/sites/prompt-step-P11.S3c` · branch `prompt/P11.S3c` · base = the worktree's HEAD at provisioning (staffing commit, direct child of `322a970fa`).
- Siblings S3a/S3b/S3d/S3e edit the other four docs pages in their own worktrees. Not your ceiling, not your problem.

## 1. CEILING (hard)

Your ceiling is **exactly one file: `sugar-crush/docs/MEMORY.md`**. You may not create, edit, or delete any
other file in the repository — no src, no tests, no other docs, no README, no plan files. If the truthful
state of this page seems to demand a change anywhere else, do NOT make it: record it in your report as a
follow-up finding.

## 2. GOAL (quoted from the plan step)

> **Goal** `docs/HOOKS.md` (additionalContext, the new dispatch sites), `docs/SKILLS.md` (which
> skill→prompt path is canonical), `docs/MEMORY.md` (`/memory import`), `docs/SETTINGS.md` (the rules
> keys and their user-tier-only rule), `docs/COMMANDS.md` (`/rules`).

Your clause: **MEMORY.md — `/memory import`.**

## 3. FACTS TO DOCUMENT

The claimed shipped state below is what the orchestrator believes Phase-1..10 shipped. **EVERY claim must
be re-verified by your direct read of the shipped code (and shipped tests) at YOUR base sha before you
write it into the page.** Anything you cannot confirm gets dropped or reported as a measured-FALSE
premise, never written as fact. Line cites in the doc itself must be **symbolic (class::method / const
names), never numeric `file:NNNN`**.

- Memory reaches the prompt through the **`<project-memory>` fence**, escaped via `PromptFence`
  (verify the tag is in the fence roster and the render path escapes it).
- The **tiers** as shipped (user / project / agent — verify names and which reach the prompt against
  the store + `MemoryBlock`; per the P7.S6 fix-forward record, `MemoryBlock::capture` folds ONLY the
  project scope and user/agent entries never reach the prompt through capture).
- The **`/memory import` command** (P7.S6 `ForeignMemoryImporter` wired behind `/memory import` —
  verify the command surface and importer; the fix-cycle-2 record `da178befb` says importer docblocks
  tell the truth post-wiring — re-derive from source, not from git subjects).
- **Capture folds the Project scope only**, while **imported entries land in agent scope by design**
  (verify: importer writes `MemoryScope::Local`, the store normalizes to agent — check both symbols).
- **Documented bounds — assert-no-crowding**: project capture is unchanged by imports; agent entries are
  excluded from capture. Verify each bound against the shipped tests at tip and cite the tests by
  class::method symbol. There is **no prompt-cap clamp on imports** (the false clamp was removed in
  `f043e2d29` fix-forward — if the page still carries cap/headroom prose for imports, that is a
  contradiction to fix).
- The **user-tier-only rule for rule-related keys** belongs on SETTINGS.md — cross-reference it, do NOT
  duplicate SETTINGS content here.

Also: **audit the WHOLE page for sentences contradicting the shipped state; fix contradictions; the doc
must be truthful at the tip.** Every number on the page (bounds, caps, counts) must be re-verified against
the shipped code/tests at tip — this page was last touched by `da178befb` (P7.S6 fix-cycle-2), and P10
merged after it.

## 4. TRIPWIRES (from the scout Q2 guard table + Q6 — these govern you)

- **EnvRosterDriftTest mention-census** (`tests/Config/EnvRosterDriftTest.php`): edits to any
  `docs/*.md` auto-enter the census (`array_merge([$root.'/README.md'], glob($root.'/docs/*.md'))`).
  Every `SUGARCRUSH_*` name you leave on the page must have a roster row in `docs/ENVIRONMENT.md` and be
  code-read by the scanner oracle — before finishing, run
  `/usr/bin/grep -o 'SUGARCRUSH_[A-Z0-9_]*' sugar-crush/docs/MEMORY.md | sort -u` and confirm each hit survives
  the guard.
- **TrustKeyDocumentationDriftTest** (`tests/Config/TrustKeyDocumentationDriftTest.php`): across every
  docs page it forbids stale trust-grant counts (derived from `src/`) and requires every deep-link heading
  it tracks to still resolve. Do not rename trust headings casually; do not write spelled trust-grant
  counts the guard does not derive.
- **DocumentParagraphsTest fence-balance** (`tests/Config/DocumentParagraphsTest.php` +
  `Support/DocumentParagraphs.php`): `testNoDocumentInScopeLeavesAFenceOpen` reddens on ANY unbalanced
  code fence you introduce (scope includes `docs/*.md` by glob). This page shows prompt fences as content —
  keep every ``` fence paired (inline `<project-memory>` backticked tags are fine; unpaired ``` is not).
- **SymbolCitationDriftTest** (`tests/SymbolCitationDriftTest.php`): resolves backticked symbols in
  `docs/*.md` too — every backticked `Test`/`Class::method` name you write MUST exist in the tree at your
  tip (e.g. `MemoryBlock::capture`, `PromptFence`, `ForeignMemoryImporter`, any test class you cite —
  verify each resolves first).

## 5. SUITE LAW

Floor anchor at master: **11,182 tests / 170,906 assertions / 0 failures / 0 errors / Skipped 2 / EXIT 0**
(the staffing commit is markdown outside `sugar-crush/`, so the anchor carries to your base).

1. Run your solo guard classes FIRST (base), and again at the final tip, and **predict the full-suite
   headline from the final-tip per-class solos (law 5)** before launching the full run:
   `EnvRosterDriftTest`, `TrustKeyDocumentationDriftTest`, `DocumentParagraphsTest`, `SymbolCitationDriftTest` —
   e.g. from the worktree root:
   `cd /home/sites/prompt-step-P11.S3c && php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --filter EnvRosterDriftTest` (repeat per class).
2. **Box-quiet probe** — must print `0`, run STANDALONE (grep -c exits 1 on a zero count — NEVER `&&`-chain
   it into the launcher):
   `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'`
3. Full suite, detached (setsid+nohup), **cwd = worktree root**:
   `mkdir -p /tmp/opencode/P11.S3c && cd /home/sites/prompt-step-P11.S3c && setsid nohup php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P11.S3c/tip.xml </dev/null >/tmp/opencode/P11.S3c/tip.out 2>&1 &`
4. Verify before reporting: the JUnit file has a **closed root** (`</testsuites>` present — a truncated file
   means the run died), the **banner** is in `tip.out`, and the artifact is **>3 MB** (`stat -c %s /tmp/opencode/P11.S3c/tip.xml`).
5. Headline must match your prediction and meet the floor (assertion-count movement is acceptable ONLY as
   your solo-delta prediction explains; 0F/0E/Skipped-2 are hard).

## 6. GOLDENS FROZEN (measured at `322a970fa`, re-measure at your tip)

- `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` — md5 `f5de3858c1bc130d1e97a120d3ead485`, 8,278 B
- `sugar-crush/tests/fixtures/prompt/golden-agent-prompt.txt` — md5 `ef0326dd38535aaa2f1d715919bff26e`, 1,060 B

Any move = **HALT and report**. A docs step must never touch them; if a golden moved under you, the tree or
the suite is lying — stop.

## 7. COMMIT HYGIENE (hard)

- ONE commit on branch `prompt/P11.S3c` (already checked out in your worktree).
- Identity — write the literals via printf, NEVER copy any redacted placeholder token you see in your
  context (e.g. the bracketed-email placeholder or angle-bracket forms in repo docs):
  `git -C /home/sites/prompt-step-P11.S3c config user.name 'Joe Huss'`
  `git -C /home/sites/prompt-step-P11.S3c config user.email "$(printf '%s@%s' detain interserver.net)"`
- Stage only `sugar-crush/docs/MEMORY.md`; `git commit -F <message-file>` (message file outside the repo,
  e.g. `/tmp/opencode/P11.S3c/msg.txt`).
- Message scan: `/usr/bin/grep -c '[<>]' <message-file>` must print **0** (fix the message if not).
- Law-4g triple AFTER the commit (all FIXED-STRING scans with /usr/bin/grep):
  1. `git -C /home/sites/prompt-step-P11.S3c cat-file commit HEAD | /usr/bin/grep -cF '[EMAIL]'` → 0
  2. `git -C /home/sites/prompt-step-P11.S3c log -1 --format=%B | /usr/bin/grep -cF '[EMAIL]'` → 0
  3. `git -C /home/sites/prompt-step-P11.S3c log -1 --format='%an|%ae|%cn|%ce'` → exactly
     `Joe Huss|detain@interserver.net|Joe Huss|detain@interserver.net`
- **NEVER merge. NEVER push.** The orchestrator lands the branch.

## 8. REPORT FORMAT (return all of it)

1. Diffstat vs base (`git diff --stat <base>..HEAD`).
2. Claims-verified list: each fact from section 3 → `file:symbol` where confirmed → confirmed/refuted.
3. Solo figures: the 4 guard classes at base and at tip (counts per class).
4. Prediction table (headline predicted from per-class solos) vs ACTUAL full-suite banner.
5. Goldens: md5 + byte size of both files at your tip.
6. Hygiene scan outputs verbatim (message `<>` count, the three law-4g lines).
7. Any premise that measured FALSE — REPORT, do not silently fix beyond your ceiling.
