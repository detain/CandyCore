# P9.S1 — The promptGuidance() seam — step brief

Plan: prompt_plan.md :2852-2867 (heading `### P9.S1`; read it). Phase 9, batch 1 — ALONE (nothing else of Phase 9 may start before this merges; plan :2980). Base: master staffing tip; sugar-crush tree == floor-gate `b417dbbb7`. FLOOR **11,055 / 169,622 / 0F / 0E / 2 Skipped / EXIT 0** (master-direct gate at `b417dbbb7`; the three P8-close bookkeeping commits after it are markdown-only, so the figure describes master). Goldens FROZEN: system `a5c5a14ca2e3ad891933ac7aefccc6af`/7,732 B (also md5+size-pinned by `tests/Chat/SessionStartHookWireTest.php:519-530`) · agent `ef0326dd38535aaa2f1d715919bff26e`/1,060 B.

## CORRECTIONS to the plan's Files list (verified by orchestrator direct reads 2026-09-08)
- `src/ToolRegistry.php` as named in the plan is the WRONG FILE: it is the slash-command registry and hosts an unrelated `final class Tool` (:33) with no description()/inputSchema(). The model-tool seam is `src/Tools/Tool.php` (:19 `interface Tool` — name/description/inputSchema/execute only). prompt_expand.md §10 seam 9 (:3744) confirms: `src/Tools/Tool.php` + `src/Tools/BuiltIn/*`.
- promptGuidance is NOT part of the provider wire `tools[]` payload (per-Provider formatTools read description()+inputSchema() only, e.g. OpenAIProvider.php:183-191). The injection channel is the SYSTEM PROMPT assembled in `Runtime::buildSystemPrompt(App)` (:2485) → `systemPromptSections(App $app)` (~:2510) — the tool list is reachable there as `$app->tools` (App.php:78, default `[]` :192, set via `withTools()` :231). The done-when's "request" means the App handed to prompt assembly.
- `Tool.php` is an INTERFACE (PHP: no default bodies possible). Adding `promptGuidance()` to it fatals ~51 `implements Tool` sites (12 src incl. `McpToolBridge.php:73`, ~39 test doubles incl. anonymous classes and the corpus heredoc probes). DO NOT ADD IT TO THE INTERFACE.

## RULINGS (FIXED — deviate = stop and report)
- **R-A seam shape:** NEW file `src/Tools/PromptGuidance.php` declaring `interface PromptGuidance` — one method `public function promptGuidance(): string` — mirroring the house capability-interface pattern of `Tools/ParallelSafe.php` and `Tools/CarriesSessionState.php`; `src/Backend/ReportsContextWindow.php:40-41` states the doctrine verbatim ("not implementing it is [the default]"). Docblock cites §4.17/§9.9/§10-seam-9 and P9.S1, and says plainly: a tool that does not implement this contributes ZERO prompt bytes. The return is the fragment body with no envelope markup (R-C) and no trailing newline; `''` also means contribute-nothing.
- **R-B renderer:** ONE new entry in `Runtime::systemPromptSections()` driven by `$app->tools`: filter `instanceof PromptGuidance`, render those with non-empty fragments, **sorted by `name()`** (deterministic — the tool set is fixed per session, so the layer is `Stability::Static`; do not let registration order leak into the prompt). When no qualifying tool exists → `render()` returns `''` → `assemblePrompt` (:2795-2814, skips `''` at :2802-2804) drops the layer entirely. This is what keeps the golden still: `BaseSystemPromptTest::goldenContext()` (:1607) NEVER calls `withTools()` — `$app->tools === []` in every golden path (verified: zero `withTools` occurrences in that file).
- **R-C no fence tag:** fragments render as plain prose in the new layer. Do NOT introduce any XML/HTML-ish envelope tag; do NOT touch `src/Context/PromptFence.php` (TAGS stays 7 — the roster-widen is a SCHEDULED deliberate step after Phase 10 with the CompactModelSummaryTest:1571 characterization pin as its adjudication trigger). `McpToolBridge` stays non-implementing (MCP tool guidance is out of scope; the capability pattern makes non-participation silent).
- **R-D additive-only:** `description()` bytes are HEAVILY pinned (`tests/Tools/BuiltInToolTest.php` exact-string asserts :96-187; `tests/Tools/ToolDescriptionGuidanceTest.php` ~19 methods) — S1 moves NOTHING out of any description(). The plan's "separate from description()" = a NEW SURFACE, not a relocation. Reference implementers for the seam: `BuiltIn/Read.php` and `BuiltIn/Write.php` ONLY (tools no later Phase-9 step claims: S3 takes Bash, S4 takes SkillTool+WebFetch). Each fragment: 2-4 self-contained sentences of behavioral guidance for THAT tool; MUST NOT name any other tool (the done-when's dangling-reference test sweeps exactly this; note `Read::description()` :122 naming cat/head is a SHELL command, not a tool name — your fragment text likewise avoids `Grep`/`Glob`/`Bash`/`Write`/`Edit` tokens — Read's fragment may say "shell listings", Write's may say "the edit tool" only if you can phrase it without the literal name `Edit`... simplest: no sibling references at all).
- **R-E Tool.php:** ZERO edits. (A cross-reference paragraph there would be law-4n churn with no pin behind it; the new interface's docblock carries the link instead.)
- **R-F ordering slot:** place the guidance section immediately AFTER the base identity block (`section('', Stability::Static, $this->basePrompt())` at ~:2513) and BEFORE memory/env/repo-map layers, unless reading the section list shows a more natural adjacent slot; whatever you choose, run `tests/Providers/PromptStabilityTest.php` SOLO green (prefix-stability doctrine — the guidance layer rides the Static region because toolsets never change mid-session).

## Declared files (ceiling — plan deviation RECORDED here, corrects the plan's Files list)
- `sugar-crush/src/Tools/PromptGuidance.php` (NEW)
- `sugar-crush/src/Runtime.php`
- `sugar-crush/src/Tools/BuiltIn/Read.php`
- `sugar-crush/src/Tools/BuiltIn/Write.php`
- `sugar-crush/tests/Tools/ToolPromptGuidanceTest.php` (NEW)
- `sugar-crush/tests/Providers/PromptStabilityTest.php` ONLY if the stability doctrine demands a pin there (prefer testing in the new file; adding cases to existing files allowed, removing/weakening assertions is §1.11-forbidden).
`ToolRegistry.php` explicitly NOT to be touched (it is a different `Tool`). Nothing else; no composer, no docs, no goldens regeneration (the golden must not move — if it moves, your source is wrong, not the golden).

## Done-when (plan :2866-2867) + required tests (all in the NEW test file, reflection harness per BaseSystemPromptTest pattern — `(new ReflectionMethod($runtime,'buildSystemPrompt'))->setAccessible(true)`; build Apps via `App::new(...)->withTools([...])`, "disabled" = omitted from the list)
1. both reference tools present → both fragments appear in the assembled prompt;
2. one omitted → its fragment absent, the other present;
3. name-discipline sweep: for every promptGuidance fragment, for every OTHER wired built-in name, the fragment does not contain that name (guard your own fragments against the future too — assert over `Bootstrap::tools()`-shaped instances or the concrete 11 classes' name() strings);
4. ordering: fragments render sorted by name() regardless of withTools() input order (pass the two tools in both orders, assert identical prompt bytes);
5. empty set: `withTools([])` + a `withTools([...no implementer...])` (e.g. a plain Tool double) → guidance layer contributes zero bytes — assert the assembled prompt EQUALS the no-guidance-tools prompt for the same App in ALL other respects (byte-identity across the two renders is the strongest form);
6. tool implementing the interface but returning `''` → contributes nothing.
MANDATORY SOLO SET after implementation (paste each headline): BaseSystemPromptTest (golden pin), SessionStartHookWireTest, PromptStabilityTest, BuiltInToolCorpusTest (new src/Tools file may hit its secondary-declaration map — growing that roster is ALLOWED as honest bookkeeping, weakening assertions is not), ToolDescriptionGuidanceTest + BuiltInToolTest (proof description() untouched), RuntimeTest, AppTest, plus the new file.
DELETION EXPERIMENTS (§1.11, mutate then restore, porcelain empty between): (a) delete the withTools filter/section line → tests 1-2 red; (b) remove sort-by-name → ordering test red; (c) make `''`-fragment render anyway (drop the non-empty guard) → tests 5-6 red. Record each red-set exactly.

## Predictions & laws
- Law 4n: GlobFigureDriftTest +1 assertion per NEW docblock paragraph anywhere in src (your interface docblock, two method docblocks, the runtime section helper docblock — count blank-line-separated prose units and PREDICT the delta per-class before the full run; do not restate any stale glob figure like "8-char" in prose).
- TreeWideGuardRosterTest (17/1127): the new test must NOT whole-tree-walk src/ or tests/ (that would enroll it) — build Apps + reflect only. BootstrapTranscriptSeamCensus (20): Bootstrap.php untouched → expect unchanged; solo it in the final check anyway.
- §11 constraints honored automatically: `buildSystemPrompt` stays private 1-arg (16+ reflection sites), base keeps exactly four `# ` headings (your layer is NOT base).
- Full-suite protocol: cwd /home/sites/prompt-step-P9.S1, serial, </dev/null, --colors=never, box-quiet probe `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` == 0 first, prediction file written BEFORE measurement, junit to /tmp/opencode/P9.S1-build/tip.xml — the junit artifact MUST exist on disk and its totals line be pasted; "ran the suite" without the artifact is a void claim (orchestrator policy since P8.S5).
- Commits: src+tests may be ONE commit or two (your call, prefer two: seam+implementers+renderer, then tests); messages in /tmp/opencode/P9.S1-build/, `grep -c '<'` == 0 before use; identity exported from `git config` interpolation; post-commit scans: `git cat-file commit HEAD | /usr/bin/grep -c '\[EMAIL\]'` == 0, `%B | /usr/bin/grep -c '<'` == 0, `%an|%ae|%cn|%ce` == Joe Huss|detain@interserver.net ×4. No push, no composer, no caliber, no git init outside /tmp/opencode/P9.S1-build (scratchpad ISOLATION: your own subdir only). Vendor note: `vendor/sugarcraft/*` are Packagist real dirs (published mode) — sugar-crush's own classes self-locate to the worktree; never `ln -s` vendor.
- If anything in the tree disagrees with this brief's anchors (the base drifted): STOP + disk forensics + report; never work around a red you don't understand; never weaken a pin.

## Report format
Branch tip SHA(s); R-A..R-F disposition each (incl. which section slot you chose in R-F and why); done-when test list + the three deletion-experiment red-sets verbatim; per-class movers vs FLOOR 11,055/169,622 incl. GlobFigureDrift delta vs your paragraph count; goldens UNMOVED proof; solo set headlines; prediction-vs-actual full suite EXACT or diff explained; hygiene scans verbatim; surprises (plan/expand errors found, esp. any remaining anchor drift); deviations (must be zero without a stop-report).
