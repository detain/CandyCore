# P11.S4 — end-to-end proof: block arm delivered from a real keystroke turn

Provenance: prompt_plan.md `### P11.S4` (~:3160). Base at staffing: `master = 6aa5dc30e`
(branch from CURRENT master tip; note actual base sha).

## 1. CEILING

Two test files ONLY:
- `sugar-crush/tests/Integration/PromptEndToEndTest.php` — **NEW**.
- `sugar-crush/tests/Integration/SystemPromptWiringTest.php` — **APPEND-ONLY.** Never edit, move,
  reorder, or reword any existing method or assertion. You may add the two PROSE corrections in §7
  below (docblock/paragraph text) — but not one assertion.

No src, no docs, no plan file. Tests are the whole ceiling.

## 2. GOAL (quoted from plan)

> **Goal** One test that starts from a real keystroke turn and asserts the model receives all
> seven layers. Not a stub recording a DTO — the payload.
> **Done when** the new test would have failed on the pre-Phase-1 tree. … A regression test that
> would not have caught the original bug is not a regression test.

> **Hard constraint** `SystemPromptWiringTest::testARealChatKeystrokeTurnDeliversBothHalves` is a
> standing **DO NOT TOUCH** entry — "never skip it, never weaken it." Extend alongside it; do not
> edit it.

("seven layers" in the plan predates the eleven of record; assert the layers that EXIST at your
base — see §3D — and note the count.)

## 3. FACTS (re-verify by direct read at your base)

**A. Conventions to MIRROR from `SystemPromptWiringTest`** (do not reinvent):
- `use SugarCraft\Crush\Tests\Support\HomeSandboxTrait;` — covers BOTH HOME spellings
  (`HOME` and the platform variant) so the loader never reads the developer's own `~/.sugar-crush`.
- Capturing seam: `capturingProvider(bool)` returns an **anonymous class implements
  `ProviderInterface`** with `public array $requests` (each `CompleteRequest` pushed). THIS is the
  object whose captured request you assert on — the real payload, not a re-derived stub DTO.
- `echoingProvider()` for the `pcntl_fork()` survival path (the fork round-trips bytes; a captured
  request from the forked child is the true wire proof).
- `BackendSelectionEnvSandboxTrait` ONLY if your test touches `Bootstrap::backend()`.
- Fixture tmp repo: `mkdtemp` repo with the **deterministic-git trick** (fixed author/committer
  date + identity) so `<env>`/repo-map bytes are stable; register it in `$this->fixtures[]` for the
  existing teardown loop; `file_put_contents($tempDir.'/AGENTS.md', …)` to seed project-instructions.
- Keystroke pattern (copy shape from `testARealChatKeystrokeTurnDeliversBothHalves`):
  `update(new KeyMsg(KeyType::Enter, ''))` → returns `[Model, Closure $cmd]`; `$cmd()` yields an
  `AsyncCmd` on `Loop::get()`; resolve via `$asyncCmd->promise->then(fn => $loop->stop())`.

**B. Loop/timer law (there is NO `tests/bootstrap.php` on this suite path):** mirror EXACTLY how
`SystemPromptWiringTest` avoids timer residue — it arms NO timers except the single
`$loop->addTimer(10.0, … $loop->stop())` safety guard wrapped in `if ($resolved === null)`, then
`$loop->cancelTimer($safety)` after `$loop->run()`. Your test MUST use only that pattern — never a
stand-alone/always-armed timer (that is the residue `SharedLoopResidue`/`LoopPin` concern; do not
introduce it). Confirm no armed timer/stream survives your test.

**C. SPECIFIC GAP TO CLOSE (scout Q4)** — the Anthropic-shape providers
(`BedrockProvider::systemBlocks()`, `VertexProvider` reading `$request->systemBlocks`) now transmit
the BLOCK arm, and the existing block tests
(`tests/Providers/SystemPromptTransmissionMatrixTest.php:1332` etc.) build `new CompleteRequest(...)`
BY HAND with synthetic blocks — there is **NO end-to-end assertion, driven from a real keystroke
turn, that the production-assembled block arm reaches the wire.** Your new test closes exactly that:
assert on the **captured real `CompleteRequest`** that:
  (a) `systemBlocks` is **non-null**;
  (b) `implode('', $request->systemBlocks) === $request->systemPrompt` **byte-exact** (the assemble
      one-fold invariant — `Runtime::assembleSections()`);
  (c) the **FIRST** block starts with the base-prompt opening bytes and the **LAST** block ends with
      `</env>` (env is last — the P3.S1 invariant);
  (d) a **needle set covering all layers** is present across the blocks: base identity,
      `core.maxims`, repo-map, `<user-rules>` (if a user rule is seeded), `<project-instructions>`
      (via the fixture AGENTS.md marker), project-memory, the skills listing, `<env>`.

**D. Layer set** = whatever `Runtime::systemPromptSections()` emits at your base (eleven layers of
record — base, maxims, tool-guidance-if-nonempty, repo-map, user-rules, instruction docs,
project-tier rules, memory, enabled-skill bodies, skill listing, env). Assert needles for the layers
your fixture can actually make non-empty; keep tool-guidance optional (guard: empty unless a
`PromptGuidance` tool is wired, and the golden corpus never wires one).

## 4. WHY IT FAILS PRE-PHASE-1 (encode it, and state it)

The done-when requires the test to have FAILED on the pre-Phase-1 tree. Document which needles
encode that: pre-Phase-1 the block arm did not carry layers 2–7 — maxims, the fence-wrapped
repo-map/rules/memory project-instructions block set, enabled-skill bodies — and `<env>` was **not
last** (it was ahead). So the assertions that make it fail early are:
- (c) FIRST-block base bytes + **LAST-block `</env>`** — pre-P3.S1 env was not last → red.
- (d) presence of maxims + fenced repo-map/project-memory/user-rules/project-instructions + skill
  layer needles — pre-Phase-1 those layers did not exist → red.
- (b) `implode('')===systemPrompt` — the block arm itself did not exist → `systemBlocks` null → red.
Name each needle → each pre-Phase-1 failure in the test's docblock. If you can, demonstrate by
running the new test against the Phase-1 parent commit and paste the failure (optional, plan
allows worklog statement instead).

## 5. TRIPWIRES

- **`SymbolCitationDriftTest`** — reads `{@see}` and backticked symbols in `tests/`. Every
  `*Test`/class you reference in docblocks/comments must resolve.
- **`DocumentParagraphsTest`** — walks `README.md` + `src/**.php` + `docs/**.md`, **NOT `tests/`**
  → your test files are OUT of its paragraph/fence scope. VERIFY this by running its solo; do not
  assume. (Commission says: paragraphs are src-only for tests — confirm empirically.)
- **`GlobFigureDriftTest`** — census over src/docs; a NEW test file under `tests/` should not move
  a doc/src figure — run the solo to confirm before/after are identical.
- **`BinSugarcrushWiringTest`** — your change is a TEST file, not src → expect **+0** (no new bin
  symbol). Confirm.

## 6. SUITE LAW

- New file adds **+1 test**; SystemPromptWiringTest gains prose, **0 new assertions/methods**, so
  net suite delta = +1 test. Run the whole `tests/Integration/` dir to prove no timing/regression.
- Shared ReactPHP loop: the single 10s safety-timer pattern ONLY (see §3B). No `LoopPin` needed on
  this suite path (confirm none is imported).

## 7. TWO STALE PROSE SPOTS in `SystemPromptWiringTest` to fix WITHOUT touching any assertion

APPEND/annotate in place — delete NO existing line:
1. **Class docblock ~`:39`**: "So every assertion here reads the `CompleteRequest::$systemPrompt` a
   real provider is handed …". Add ONE honest addendum sentence naming that block-arm
   (`CompleteRequest::$systemBlocks`) coverage now lives in **`PromptEndToEndTest`** (a new sentence
   — do not alter the existing one).
2. **MEASURED paragraph ~`:150-158`**: quotes `` `$systemPrompt = $this->buildSystemPrompt($app);` ``
   as "in `Runtime::run()`". That line is stale — `Runtime::run()` now does
   `[$systemPrompt, $systemBlocks] = self::assembleSections($this->systemPromptSections($app));`.
   Correct it **append-note style**: keep the original quote, add a following note that the live
   path is the `assembleSections()` fold (no existing line deleted, no assertion touched).

## 8. GOLDENS

- New e2e test must not write/require a golden fixture unless `SystemPromptWiringTest` already uses
  one (`PromptFixture` is imported there — mirror its use if applicable). Do NOT regenerate the
  byte corpus; the tool-guidance-empty golden-identity property must stay intact (your fixture must
  not wire a `PromptGuidance` tool, or you change corpus bytes).

## 9. COMMIT HYGIENE

- One commit; staged set = `tests/Integration/PromptEndToEndTest.php` + `tests/Integration/SystemPromptWiringTest.php` only.
- Subject: `prompt/P11.S4: e2e block-arm proof from a real keystroke turn`.
- Body: base sha; the (a)-(d) assertions; which needles encode the pre-Phase-1 failure; confirmation
  `testARealChatKeystrokeTurnDeliversBothHalves` untouched; suite delta (+1 test).
- Commit MESSAGE: no `<`/`>` (write `env`/`user-rules` etc. without fences, e.g. "env last"); no
  `REDACTED`; no placeholder email.

## 10. REPORT FORMAT

Return: base sha; full text of the new test (or a stable diff); confirmation (git diff proof) that
no existing SystemPromptWiringTest assertion or the DO-NOT-TOUCH method changed; the +1 test count
and `tests/Integration/` run output (PASS line + duration); each §3C assertion (a)-(d) with the
needle used; the §4 pre-Phase-1 failure mapping (and the demonstration run if you did one); tripwire
solos (`SymbolCitationDrift`, `DocumentParagraphs` — with the empirical note that tests/ is out of
scope, `GlobFigure`, `BinSugarcrushWiring` +0) each PASS; confirmation only the 10s safety timer is
used and none survives.
