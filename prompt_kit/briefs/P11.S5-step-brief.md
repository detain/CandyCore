# P11.S5 — whole-plan final audit (the plan's LAST step; runs ALONE)

Provenance: `prompt_plan.md` `### P11.S5 — Final plan audit` at **:3176-3187** (Phase-11 header `## Phase 11 — Docs, sweep, final audit` at :3118; "Batch 3 (one, alone): P11.S5" at :3192).
Base at authoring time: `master = 65d4c882f` ("merge prompt/P11.S2pin"). **Re-derive the CURRENT master tip at staffing** — if it moved, re-anchor the suite figures per §6 below.

## 1. CEILING — THREE FILES, nothing else

- `prompt_worklog.md`
- `prompt_resume.md`
- `prompt_plan.md` — **final status stamps only** (no plan prose rewrites, no goal edits)

NO file under `sugar-crush/` may change. NO file under `/home/sites/crush-lane-*/`, `docs/plans/crush_code_*`, or `left_steps.md` may be touched (standing user-decision boundary — crush-lane is owned elsewhere). Tests are OUT of scope; your edits are markdown under git-visible paths only.

## 2. GOAL (quoted from plan :3178-3187)

> **Goal** A whole-plan review agent walks all 12 phases' commits as one change-set and audits against
> `prompt_expand.md` §9's list: which items landed, which were deliberately declined and why, which
> were silently dropped. **Silently dropped is the failure mode; the audit exists to find it.**
> **Done when** every numbered item in §9.1–§9.15 has a disposition in the worklog: landed (with the
> step id), declined (with the reason), or deferred (with what it is blocked on).

Your output is a NEW worklog entry headed exactly:

```
### P11.S5 — whole-plan final audit · 2026-09-09
```

placed at the TOP of `## ENTRIES` (entries are newest-first: the current newest is the P24 entry, worklog :256). It carries the full disposition table — **zero silently-dropped is the pass condition** — plus a one-line disposition for EVERY item in the §4 carried queue below.

## 3. THE AUTHORITATIVE §9 CHECKLIST (enumerate → disposition; grep plan + worklog for each trace)

`prompt_expand.md` §9 begins at :3464 ("## 9. What to build — Ranked by payoff per unit of risk"). Its 15 subsections number **73 items** as enumerated below (expand line spans cited — READ THEM at your tip; if an item split/merge is warranted, refine granularity, but NO named item may be merged away or skipped). For each: grep `prompt_plan.md` (step headings + `**Source**` lines + `**Status**` stamps) and `prompt_worklog.md` for its landing/decline/defer trace. Candidate step names given are HINTS verified live from the plan's 65 heading list — you must confirm the trace, not assume it.

| § | items | what they are | candidate landing steps (verify) |
|---|---|---|---|
| 9.1 (:3468) | 5 | system block at 4 sites — `SglangProvider::buildParams`, `CustomProvider::complete`, `CustomProvider::completeStream`, `OpenAIProvider::completeStream` (expand lists `:642`/`:131`/`:177`/`:113` at its own baseline — line numbers are stale-by-age, match on symbol); + wire-payload tests; + `PromptStabilityTest` rebuild off the stale `MiniMax-M2.7` id | P1.S1, P1.S2, P1.S3, P1.S7, P1.S6 (P1.audit-fix-3 gave Vertex a real arm) |
| 9.2 (:3493) | 2 | `<env>` LAST; diff emitted only on the step after a write (+ stable-first ordering, "breaks three ordering pins §11.2" is context) | P3.S1, P3.S2 (+ P3.S3 caveat, P3.S5/S6 write-signal) |
| 9.3 (:3502) | 11 | compaction rebuild — CC: analysis scratchpad, bookended no-tool ban, security verbatim preservation, forged-user-turn guard, direct quotes (5); opencode: recursive prior-summary merge, head/tail split, TOOL_OUTPUT_MAX_CHARS 2000, skill outputs never pruned (4); crush: fold live todos, "ONLY context" line (2) | P8.S1, P8.S2, P8.S3, P8.S4 (per-facet traces in the worklog P8 entries) |
| 9.4 (:3516) | 5 | PromptSection interface (4-method shape); migrate the 3 memoized snapshots; keep `buildSystemPrompt()` signature for 18 reflection sites; fence escaping in ONE place (absorbs E25 + closes the `<project-instructions>` hole); the CONSTRAINT "do not unify with `Agent::systemPrompt()`" — verify it held | P5.S1, P5.S2, P5.S3 |
| 9.5 (:3536) | 5 | `systemBlocks` on `CompleteRequest`; `CacheBreakpoints` wipe-then-reapply called every step; `SUGARCRUSH_DISABLE_PROMPT_CACHE` kill switch; Usage input/output split (E17); hashed session-affinity header incl. title+summarize | P10.S1, P10.S2, P10.S3, P4.S1+P4.S2, P10.S4 (P10.S4 shipped the seam DORMANT — dispatch it landed, wiring is the recorded follow-up — say so in the row) |
| 9.6 (:3556) | 1 | the verify-before-done clause (candidate shape quoted at expand :3565-3567) | P5.S4 ("§10.7: the verify-before-done clause") |
| 9.7 (:3569) | 3 | trigger union as a discriminated union (Keyword/Path/Intent); the rules-side `paths:` case; the caveat "AGENTS.md has no spec — do not model precedence on a standard that does not exist" | P6.S1 (union, built), P6.S5a (one glob dialect); the rules-application half ends at P6.S5b — see §4(l) |
| 9.8 (:3586) | 3 | `EngineBackend::withSkills()` first caller + canonical-path decision; `SkillRegistry::findForPrompt()` (measure before wiring); the six hook events: `additionalContext` + stop discarding + real `SessionStart`/`UserPromptSubmit` dispatch + Anthropic wording + 10k cap w/ file spillover | P7.S3, P7.S4 (closed **deliberately-dormant WITH THE MEASURED NUMBER** — worklog :854), P7.S1+P7.S2 |
| 9.9 (:3600) | 2 | ship-as-you-go workflow into `Bash::description()`; cross-reference it from the system prompt by tag name | P9.S3 (worklog :499) |
| 9.10 (:3611) | 4 | injectable clock/platform/cwd; golden system prompt; golden agent prompt; the composition harness | P2.S1, P2.S2, P2.S3, P2.S4 (+ P2.audit-fix-1 cwd repair) |
| 9.11 (:3618) | 11 | capability-aware descriptions; two framings for two authorities; terse redirects for `SkillTool`/`WebFetch`; `activeForm`; history sanitization; `cw == 0` guard; compaction circuit breaker; status widgets to transient pane; escape untrusted interpolated values; anti-escalation clause; exfiltration guard | P9.S2; ? (grep the worklog for the authority-preamble ruling); P9.S4; P9.S6; P9.S7; P8.S5 (E31/E32); P8.S5; grep "transient pane"; P5.S3+P6.S2b; grep "anti-escalation"; grep "exfiltration" |
| 9.12 (:3641) | 6 | the six NOT-to-do prohibitions (expand :3643-3651) | P11.S1 — register SHIPPED with all six at `sugar-crush/docs/PROMPT_ENGINEERING.md:198-211` |
| 9.13 (:3653) | 5 | RuleLoader three sources; rulebooks + `/rules`; the trigger union earning its keep inside rule files; provenance fencing (4 fences); `core.maxims` content (8 maxims, expand :3683-3690) | P6.S2; P6.S3; P6.S5a/S5b (application half BLOCKED — §4(l)); P5.S6+P6.S2b (`harness-injected` roster tag, emitter still unwired — §4(n)); P5.S5 |
| 9.14 (:3694) | 2 | cache hit-rate + age in the status line; pair with the §4.15 token-bucket accounting | P4.S3 (worklog :1825 "STATUS LINE only"); P4.S1 buckets |
| 9.15 (:3704) | 8 | template placeholders; never hardcode a tool name; tell the model about affordances (`!`cmd`` + `Skill`); utility prompts; wire `ForeignMemoryImporter`; memory-consolidation prompt; `docs/PROMPT_ENGINEERING.md`; the `<system-reminder>` channel (prefer appended system message) | grep plan §18 + worklog (placeholders/hardcode/utility/consolidation are the likely DECLINED-or-deferred set — the §18 register `prompt_plan.md:3833`-area "Things deliberately NOT in this plan" (:3881) is a decline ledger, use it); P9.S5; P7.S6; P11.S1; P6.S2b/system-reminder roster tag + `Runtime.php:2588-2604` |

**REQUIRED premise correction — §9.12 "nine" vs six.** The plan's P11.S1 done-when (`prompt_plan.md:3130-3131`) says "it explains **all nine** of §9.12's 'do not do this' items". The section enumerates **SIX** (expand :3643-3651). The shipped page ALREADY records the correction honestly (`PROMPT_ENGINEERING.md:194-196`: "The plan's done-when … says 'nine'; the section at base carries six"). Your audit entry must document the correction — the plan's :3130 prose stays as history (stamps only); the count of record is six.

## 4. THE CARRIED DEFERRED QUEUE — every row gets a one-line disposition in your worklog entry

Items (a)-(g),(m),(o) name fixes that live INSIDE `sugar-crush/` — out of your ceiling — so their honest disposition is **DEFERRED with owner = a follow-up step recorded fresh in resume §8 "Open follow-ups"** (number them continuing the ledger). Each fact below was read live at authoring time (`master 65d4c882f`) — re-verify at tip; report any that have become FALSE.

- **(a)** `sugar-crush/docs/PROMPT_ENGINEERING.md:77-78` claims "The consumers that act on the tiers are the cache-breakpoint seam and per-tier compaction downstream". Overstated: `stability()` has **zero src call sites** today — it is declared at `src/Context/PromptSection.php:54` + 5 implementations (`Runtime.php:2819`, `Context/Sections/MaximsSection.php:111`, `Context/RepoMapBlock.php:534`, `Context/EnvironmentBlock.php:822`, `Context/MemoryBlock.php:328`) and `Stability` is passed at construction (`Runtime.php:2522-2733`), but `CacheBreakpoints.php` never reads it; the seam is unwired. Disposition: wording fix or honest deferral note — DEFERRED with owner.
- **(b)** `PROMPT_ENGINEERING.md:258-260` names only the `Runtime::annotate()` seam as PostToolUse `additionalContext` consumer. Incomplete: the TUI path `Chat::applyPostToolUse()` (`src/Chat.php:3707`, first consumer at `:4015`) is the other live one — `ScriptHook.php:640-642` itself names BOTH (`Runtime::settle()` and `Chat::applyPostToolUse()`). DEFERRED with owner.
- **(c)** `sugar-crush/src/Hooks/ScriptHook.php` class docblock — the table row at `:21-22` ("stdout becomes the result message. ON THIS CLASS ONLY") and the block at `:33-44` ("An ALLOW's message reaches nobody in a shipped run … a hook printing 200,000 bytes and exiting 0 produced a message of 0 bytes") + the `MAX_DENY_REASON_BYTES` bullet at `:193-195` ("`EXIT_ALLOW` carries nothing anywhere in a shipped run") — are now FALSE vs the live arm at `:632-651` (stdout ROUTED to `additionalContext`, bounded by `HookContextFiles::bound`) and vs the post-P7/P11 `docs/HOOKS.md`. DEFERRED with owner (src docblock edit).
- **(d)** `HookManager::applyPreHooks()` (`src/Hooks/HookManager.php:282`) has **no src/ caller** — only `tests/Hooks/HookManagerTest.php:142-192`. Dormant roster line under plan §1.10 (never remove; wire-or-escalate). Disposition: DEFERRED (standing §1.10 dormancy register).
- **(e)** `CommandLoader::loadAll()` (method at `src/Commands/CommandLoader.php:478`) comment at `:521-522`: "`permissions` has no registry row, so it is unset" — STALE: `CommandRegistry.php:183` carries the `permissions` row and `:68` lists it in `CONTROL_PLANE`. DEFERRED (one-line comment fix).
- **(f)** `CommandRegistry.php:219` memory row description "Add, list, search, edit, import, or clear" **omits `delete`** — `/memory delete` exists (`src/Chat.php:10954` dispatch, `:10994` help line, `memoryDelete` at `:11316-11332`). DEFERRED.
- **(g)** `src/Commands/CommandSpec.php` docblock `:50`+`:53` says "all **four** template forms / two of the four" while `TEMPLATE_PATTERN` (`:112-114`) carries five distinct expansions (`$$` escape, `$ARGUMENTS`, `$1`-`$9`, `` !`…` ``, `@path`); same stale "ALL FOUR" phrasing at `CommandLoader.php:41-42`. DEFERRED (prose count).
- **(h)** `sugar-crush/docs/ARCHITECTURE.md` **region :205-228** — RE-CHECKED after the S1+S2+S2pin merges: the eleven-slot section LANDED at `:229-287` (`### The system prompt, in assembly order`, list items 1-11 at :237-258, PROMPT_ENGINEERING link at :286) — the seven-vs-eleven residue at :220 is GONE (grep `seven|eleven` at tip: no seven-layer claim survives). BUT the same region carries **stale code-position citations**: `:212-213` "one doc-comment at line 1433" (`src/Runtime.php` today names `maxSteps` in doc comments only at `:3140`/`:3161`) and "`Runtime::__construct` (lines 101-107)" (`:101-107` are constants); `:216-218` "(line 126)" and "`EngineBackend.php:462`" (live: `private readonly int $maxSteps = 8` at `src/Backend/EngineBackend.php:230`; the `for ($step = 0; $step < $this->maxSteps; $step++)` loop at `:652`; `:462` is `withoutHooks()`). Disposition: seven-vs-eleven LANDED (S2/S2pin); the :212-222 citation staleness is a NEW finding → DEFERRED (docs-sweep; note `ArchitectureAssemblyOrderTest` pins the slot list, not these citations).
- **(i)** `docs/plans/crush_code_hardening_backlog.md:5491` dangling cite + `sugar-crush/tests/Skills/SkillPathPatternTest.php:167` "331 pairs" vs the actual grid count ("378"). At authoring: the backlog round-43 paragraph at `:5492-5495` cites `testAPosixClassIsTranslatedRatherThanRoutedToTheFallback()` — the method EXISTS (`SkillPathPatternTest.php:442`) — and the grid row `[[:alpha:]][!a]` exists (`:163`); the "331" figure sits in the `AFTER` docblock at `:167` (recount both consts if you want the delta, but do NOT edit either file). BOTH are **crush-lane / owned-elsewhere** → DEFERRED with the standing user-decision boundary (never touch crush-lane files; ask the supervisor rather than editing).
- **(j)** Ledger **(44)** (resume §8): PromptFence roster-ordinal prose has **no generator** — will rot on the next widen. DEFERRED (candidate docs-sweep step; keep in ledger).
- **(k)** Ledger **(45)**: `prompt_kit/findings/P24-P29-recheck.md:66` quotes the pre-P24 FIVE-SITES census — historical planning record. **Leave** (audit entry notes it; no edit; the ledger itself says "may note at P11.S5").
- **(l)** **P6.S5b stays BLOCKED** on user item (4) (rule-body transport, resume §8 "Awaiting user decision" — copy that item's text VERBATIM into your disposition from the current resume). It remains **the plan's only unfinished step heading**; verify the census yourself: `grep -c '^### P[0-9]' prompt_plan.md` == **65** (true at authoring time; heading at `:2422` "…(BLOCKED — user decision)"). Final math at close: **64 of 65 steps landed, 1 BLOCKED on user item (4)**. Do NOT unblock, do NOT brief it.
- **(m)** F-GUARD: `SymbolCitationDriftTest` is blind to `{@see self::testFoo()}` citations — `scrape()` accepts the bare form only (`tests/SymbolCitationDriftTest.php:266`), fallback `looksLikeATestSymbol()` (`:335-355`) derives class `self` and silently drops the token (worklog :1518; ~250 self-form refs tree-wide would enter policing at once). Its own step. DEFERRED (guard-widening step).
- **(n)** harness-injected wrap mechanics: the P6.S2b ruling — no layer is wrapped; `harness-injected` is the roster tag defang-only with NO emitter (worklog :1456); carry-forward recorded at worklog :1142; `Runtime.php:2598` "TRIGGERS ARE NOT YET APPLIED (named so, not hidden)"; plan §18 row (`prompt_plan.md:3881` section, trigger row at `:3902`). DEFERRED (rides with P6.S5b + the emitter/wrap-mechanics decision).
- **(o)** curated-frontmatter-keywords matcher — the P7.S4 revival design (worklog :875; documented now at `docs/SKILLS.md:198-199` post-S3b; `src/Skills/Skill.php:97`): production-viable matcher needs curated keywords fed to `KeywordTrigger`. Feature-design step. DEFERRED.
- **(p)** Ledger **(40)**: shared support-fixture for the twin capture stubs. DEFERRED (test-hygiene, cosmetic).
- **Fold in from resume §8 :754 (read it live, these are the (38)(39)(42)(43) ledger items):** **(38)** `CacheBreakpoints` non-array mark classification; **(39)** P10.S1 cosmetic deferrals incl. `Runtime.php:2847` prose; **(42)** P10.S4 OPTIONAL deferrals — (a) `SessionAffinity.php:8` present-tense coverage phrasing (FOLD-2), (b) dash-style separators, (c) optional `assertFalse($hasHeader)` polarity asserts; **(43)** P29 slice-A deferrals — `ContextCompactor.php:1244-1251` indentation nit, the '94 B' prose figure with no fixture row, the pre-masked double-marker re-mint note (R-D carry path owns it). Each gets a disposition line (all are deferred-quality nits; (41) FOLD-1 is CLOSED at `b2936ee5c` — record that closure, don't re-open it).

## 5. STAMPS (plan) + BOOKKEEPING (worklog, resume)

Merge chain verified live in `.git` at authoring time (master reflog, all "Merge made by the 'ort' strategy"): batch 1 `37e61e760` (S3a) ← `64434c0d0` (S3b) ← `60d9ab81f` (S3c) ← `cb7a5fe68` (S3d) ← `8e822c6ed` (S3e); batch 2 `097473127` (S2) ← `bbd8696fc` (S4) ← `de3a580fd` (S1) ← `65d4c882f` (S2-pin, tip). The S2-pin branch commit is `b86df7433` (adds `tests/Integration/ArchitectureAssemblyOrderTest.php`).

- **plan** (`prompt_plan.md`, stamps only): add a `**Status 2026-09-09: MERGED — merge `<sha>`**` line (precedent shape: `:3092` P10.S4, `:2848` Phase-9 close) under each P11 step heading that lacks one — **none has one at authoring time**: S1 `de3a580fd`, S2 `097473127` (+ S2-pin `65d4c882f` closes its follow-up), S3a-S3e `37e61e760`/`64434c0d0`/`60d9ab81f`/`cb7a5fe68`/`8e822c6ed`, S4 `bbd8696fc`, S5 itself at close (this batch's commit shas). Plus the **Phase-11 header close annotation** under `## Phase 11` (:3118).
- **worklog**: the audit entry (§2) + the **missing P11 step entries** it finds — at authoring time the worklog contains **NO P11 entries at all** (newest is P24 at :256; `grep '^### P11'` returns zero): S1, S2 (+S2-pin), S3a..S3e, S4 each need one, derived from `git show --stat` of each merge, in the entry format (:52) with a legal status from the five-value table (:106-118 — all `done`; P6.S5b stays `blocked (user-escalation)` precedent). **CHECK before writing — batch-A bookkeeping may have landed S1/S2-pin while you were briefed; do not duplicate.**
- **resume**: rewrite LAST per §R (surviving sections 0,1,2,6,6a,7,§R verbatim; final banner "PLAN COMPLETE except P6.S5b — 64 of 65 steps, Phase 11 CLOSED"; steps **64 of 65** with the 65-census re-derived; suite figure from YOUR gate; goldens; unpushed count **RE-DERIVED** with `git -C /home/sites/sugarcraft rev-list --count origin/master..master` printed after your last commit lands; carry ALL six user-decision items forward verbatim; the (a)-(p)+(38)(39)(42)(43)(44)(45) dispositions that remain open become the new "Open follow-ups" ledger with owners).

## 6. SUITE LAW

Floor anchor **11,185 / 171,308 / 0F / 0E / Skipped 2 / EXIT 0** at master `65d4c882f` at authoring time — VERIFIED live from `/tmp/opencode/P11.S2pin-merge/master.xml` (`tests="11185" assertions="171308" errors="0" failures="0" skipped="2"`). **RE-DERIVE from that artifact (or re-run) if master moved** — your anchor is the CURRENT tip's gate figure.

- Your edits are markdown-only under git-visible paths; **NO `sugar-crush/` file may change** — belt it at each commit: `git diff <base> HEAD -- sugar-crush/` must print NOTHING.
- Full suite ONCE at your final tip, prediction-first (law 5), detached:
  1. Box-quiet probe STANDALONE (never `&&`-chained — grep -c exits 1 on zero): `ps -eo cmd | /usr/bin/grep -c '^php .*phpunit'` must print `0`.
  2. `mkdir -p /tmp/opencode/P11.S5 && cd /home/sites/sugarcraft && setsid nohup php sugar-crush/vendor/bin/phpunit -c sugar-crush/phpunit.xml --colors=never --log-junit /tmp/opencode/P11.S5/tip.xml </dev/null > /tmp/opencode/P11.S5/tip.out 2>&1 &` — record your pid, poll that pid; run from the **main tree at your tip** (or the worktree — name the cwd).
  3. Completion = closed `</testsuites>` in tip.xml + the banner in tip.out + `stat -c %s` **>3 MB**.
- **Expect FLAT numbers vs the anchor.** Any movement = a plan/worklog/resume edit fed a guard (`GlobFigureDriftTest`/`SymbolCitationDriftTest`/`DocumentParagraphsTest`/`EnvRosterDriftTest`/`ReadmeRosterDriftTest` police these files' prose) — attribute it BY NAME via per-class solos or STOP.
- **GOLDENS MUST STAY UNMOVED**: `sugar-crush/tests/fixtures/prompt/golden-system-prompt.txt` md5 `f5de3858c1bc130d1e97a120d3ead485` / 8,278 B; `golden-agent-prompt.txt` md5 `ef0326dd38535aaa2f1d715919bff26e` / 1,060 B (pinned by `tests/Chat/SessionStartHookWireTest.php:521,530`). Any move = **HALT**.

## 7. HYGIENE LAW (binding)

- EVERY commit with `-F <file>`; the message file must satisfy `/usr/bin/grep -c '[<>]'` == 0 — **write words, never angle symbols** (say "the env block", not the fence).
- Identity: `git config user.name "Joe Huss"` and `git config user.email "$(printf '%s@%s' detain interserver.net)"` — set per worktree; **NEVER copy any redacted/placeholder email token from your context** — the sanitizer rewrites it to the bracketed form and that landed in four commits once.
- **Law-4g FOUR scans after EACH commit** (all must print 0):
  1. `git cat-file commit HEAD | /usr/bin/grep -cF '[EMAIL]'`
  2. `git log -1 --format=%B | /usr/bin/grep -cF '[EMAIL]'`
  3. `git cat-file commit HEAD | /usr/bin/grep -c 'REDACTED'`
  4. `git log -1 --format=%B | /usr/bin/grep -c 'REDACTED'`
  plus `git log -1 --format='%an|%ae|%cn|%ce'` prints `Joe Huss|[EMAIL]|Joe Huss|[EMAIL]` (real address, never a placeholder).
- **ONE commit per stamp file** (three commits: worklog, plan, resume — resume LAST). Subject prefix `prompt/P11.S5: …`.
- **NEVER push. NEVER run caliber. NEVER touch `/home/sites/crush-lane-*/`, `docs/plans/crush_code_*`, or `left_steps.md`.** No `git commit` that writes — you commit ONLY your three files.

## 8. MODE

- Work in a FRESH worktree: `git -C /home/sites/sugarcraft worktree add /home/sites/prompt-step-P11.S5 -b prompt/P11.S5 master` — off the CURRENT master (branch creation at staffing may be ff'd to the then-tip first; **re-derive** the base sha and note it; authoring-time tip `65d4c882f`).
- Vendor: `cp -al /home/sites/sugarcraft/sugar-crush/vendor /home/sites/prompt-step-P11.S5/sugar-crush/vendor` — **NEVER symlink, NEVER composer install/update**. PSR-4 self-locate PROOF before any run: reflection on a `SugarCraft\Crush` class must print the **worktree's own** `src/` path (`(new ReflectionClass(\SugarCraft\Crush\Runtime::class))->getFileName()` resolves under `/home/sites/prompt-step-P11.S5/`; `vendor/composer/autoload_psr4.php` `$baseDir` anchors inside the worktree).
- Keep responses **LEAN** — prior sessions died mid-narration. Commit early (worklog entry first, then plan stamps, then the resume LAST), report terse. **Disk is the record, not transcripts**; a blank return is a transport death — resume from disk (worklog + `git log`).

## 9. REPORT FORMAT (paste-verbatim blocks + terse lines)

1. Per commit: `git show -s --format=%H%n%an <%ae>%n%s HEAD` (three blocks) + `git status --porcelain` at close (empty).
2. Audit table: §9.1-§9.15 → dispositions, with counts **N landed / N declined / N deferred — zero silently-dropped** (73-row check: the table in §2 of your worklog entry covers every item; list any item whose trace you could NOT find as `deferred (no trace found)` rather than dropping it).
3. Queue (a)-(p) + (38)(39)(42)(43)(44)(45): one line each, as dispositioned.
4. Stamps list (step → merge sha) + worklog entries added for the missing P11 steps.
5. Suite figure vs anchor (flat? movers attributed by name?); goldens md5s at tip; belt output (`git diff <base> HEAD -- sugar-crush/` = empty).
6. Hygiene: the four scan counts + identity line per commit.
7. **Premises FALSE list**: anything §4/§3 recorded as verified at authoring time that your live re-verify contradicts (name it, quote the live text, disposition honestly — do not silently repair another step's record; the worklog carries the correction).
